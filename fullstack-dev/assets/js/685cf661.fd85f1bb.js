"use strict";(self.webpackChunkdocs_site=self.webpackChunkdocs_site||[]).push([[64422],{5741:(e,n,r)=>{r.d(n,{R:()=>a,x:()=>o});var s=r(59729);const t={},i=s.createContext(t);function a(e){const n=s.useContext(i);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(t):e.components||t:a(e.components),s.createElement(i.Provider,{value:n},e.children)}},10595:(e,n,r)=>{r.r(n),r.d(n,{assets:()=>l,contentTitle:()=>o,default:()=>g,frontMatter:()=>a,metadata:()=>s,toc:()=>c});const s=JSON.parse('{"id":"backend/dead-letter-queue-poison-message-handling","title":"Dead Letter Queues & Poison Message Handling Guide","description":"Overview","source":"@site/docs/backend/dead-letter-queue-poison-message-handling.md","sourceDirName":"backend","slug":"/backend/dead-letter-queue-poison-message-handling","permalink":"/fullstack-dev/docs/backend/dead-letter-queue-poison-message-handling","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/dead-letter-queue-poison-message-handling.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Kafka Implementation Guide for E-commerce Microservices","permalink":"/fullstack-dev/docs/backend/kafka-implementation"},"next":{"title":"Message Deduplication & Ordering Guide","permalink":"/fullstack-dev/docs/backend/message-deduplication-ordering"}}');var t=r(65813),i=r(5741);const a={},o="Dead Letter Queues & Poison Message Handling Guide",l={},c=[{value:"Overview",id:"overview",level:2},{value:"Table of Contents",id:"table-of-contents",level:2},{value:"Dead Letter Queue Fundamentals",id:"dead-letter-queue-fundamentals",level:2},{value:"What is a Dead Letter Queue?",id:"what-is-a-dead-letter-queue",level:3},{value:"Why Use DLQs?",id:"why-use-dlqs",level:3},{value:"DLQ Architecture Patterns",id:"dlq-architecture-patterns",level:3},{value:"Poison Message Detection",id:"poison-message-detection",level:2},{value:"What is a Poison Message?",id:"what-is-a-poison-message",level:3},{value:"Poison Message Detector",id:"poison-message-detector",level:3},{value:"Retry Strategies",id:"retry-strategies",level:2},{value:"Exponential Backoff",id:"exponential-backoff",level:3},{value:"DLQ Implementation - Kafka",id:"dlq-implementation---kafka",level:2},{value:"Kafka DLQ Producer",id:"kafka-dlq-producer",level:3},{value:"Kafka Consumer with DLQ",id:"kafka-consumer-with-dlq",level:3},{value:"DLQ Implementation - RabbitMQ",id:"dlq-implementation---rabbitmq",level:2},{value:"RabbitMQ DLQ Setup",id:"rabbitmq-dlq-setup",level:3},{value:"RabbitMQ Consumer with DLQ",id:"rabbitmq-consumer-with-dlq",level:3},{value:"Message Recovery Patterns",id:"message-recovery-patterns",level:2},{value:"DLQ Message Recovery Service",id:"dlq-message-recovery-service",level:3},{value:"DLQ Admin Controller",id:"dlq-admin-controller",level:3},{value:"Monitoring and Alerting",id:"monitoring-and-alerting",level:2},{value:"DLQ Metrics Service",id:"dlq-metrics-service",level:3},{value:"DLQ Alerting Service",id:"dlq-alerting-service",level:3},{value:"Error Classification",id:"error-classification",level:2},{value:"Error Classifier",id:"error-classifier",level:3},{value:"Best Practices",id:"best-practices",level:2},{value:"1. DLQ Design",id:"1-dlq-design",level:3},{value:"2. Retry Strategy",id:"2-retry-strategy",level:3},{value:"3. Poison Message Handling",id:"3-poison-message-handling",level:3},{value:"4. Monitoring",id:"4-monitoring",level:3},{value:"5. Recovery",id:"5-recovery",level:3},{value:"Summary",id:"summary",level:2}];function d(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",mermaid:"mermaid",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,i.R)(),...e.components};return(0,t.jsxs)(t.Fragment,{children:[(0,t.jsx)(n.header,{children:(0,t.jsx)(n.h1,{id:"dead-letter-queues--poison-message-handling-guide",children:"Dead Letter Queues & Poison Message Handling Guide"})}),"\n",(0,t.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,t.jsx)(n.p,{children:"Dead Letter Queues (DLQ) and poison message handling are critical patterns for building resilient message-driven microservices. This guide covers DLQ implementation, poison message detection, retry strategies, error handling, and recovery patterns for Kafka, RabbitMQ, and other message brokers."}),"\n",(0,t.jsx)(n.h2,{id:"table-of-contents",children:"Table of Contents"}),"\n",(0,t.jsxs)(n.ol,{children:["\n",(0,t.jsx)(n.li,{children:(0,t.jsx)(n.a,{href:"#dead-letter-queue-fundamentals",children:"Dead Letter Queue Fundamentals"})}),"\n",(0,t.jsx)(n.li,{children:(0,t.jsx)(n.a,{href:"#poison-message-detection",children:"Poison Message Detection"})}),"\n",(0,t.jsx)(n.li,{children:(0,t.jsx)(n.a,{href:"#retry-strategies",children:"Retry Strategies"})}),"\n",(0,t.jsx)(n.li,{children:(0,t.jsx)(n.a,{href:"#dlq-implementation---kafka",children:"DLQ Implementation - Kafka"})}),"\n",(0,t.jsx)(n.li,{children:(0,t.jsx)(n.a,{href:"#dlq-implementation---rabbitmq",children:"DLQ Implementation - RabbitMQ"})}),"\n",(0,t.jsx)(n.li,{children:(0,t.jsx)(n.a,{href:"#message-recovery-patterns",children:"Message Recovery Patterns"})}),"\n",(0,t.jsx)(n.li,{children:(0,t.jsx)(n.a,{href:"#monitoring-and-alerting",children:"Monitoring and Alerting"})}),"\n",(0,t.jsx)(n.li,{children:(0,t.jsx)(n.a,{href:"#error-classification",children:"Error Classification"})}),"\n",(0,t.jsx)(n.li,{children:(0,t.jsx)(n.a,{href:"#best-practices",children:"Best Practices"})}),"\n"]}),"\n",(0,t.jsx)(n.h2,{id:"dead-letter-queue-fundamentals",children:"Dead Letter Queue Fundamentals"}),"\n",(0,t.jsx)(n.h3,{id:"what-is-a-dead-letter-queue",children:"What is a Dead Letter Queue?"}),"\n",(0,t.jsx)(n.mermaid,{value:"graph LR\n    A[Producer] --\x3e B[Main Queue]\n    B --\x3e C{Consumer<br/>Processing}\n    C --\x3e|Success| D[Processed]\n    C --\x3e|Retryable Error| E[Retry Queue]\n    C --\x3e|Fatal Error| F[Dead Letter Queue]\n    E --\x3e C\n    F --\x3e G[Manual Review]\n\n    style B fill:#4CAF50\n    style E fill:#FF9800\n    style F fill:#f44336"}),"\n",(0,t.jsx)(n.p,{children:"A Dead Letter Queue (DLQ) is a separate queue where messages that cannot be processed successfully are moved for later analysis and recovery."}),"\n",(0,t.jsx)(n.h3,{id:"why-use-dlqs",children:"Why Use DLQs?"}),"\n",(0,t.jsx)(n.p,{children:(0,t.jsx)(n.strong,{children:"Benefits:"})}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Prevent Message Loss"})," - Failed messages aren't discarded"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Avoid Blocking"})," - Poison messages don't block queue processing"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Enable Analysis"})," - Failed messages can be inspected"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Support Recovery"})," - Messages can be reprocessed after fixes"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Maintain Throughput"})," - Main queue continues processing"]}),"\n"]}),"\n",(0,t.jsx)(n.h3,{id:"dlq-architecture-patterns",children:"DLQ Architecture Patterns"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// entities/message-metadata.entity.ts\nexport interface MessageMetadata {\n  messageId: string;\n  originalTopic: string;\n  originalPartition?: number;\n  failureCount: number;\n  firstFailureTime: Date;\n  lastFailureTime: Date;\n  errorType: string;\n  errorMessage: string;\n  stackTrace?: string;\n  headers: Record<string, string>;\n  payload: any;\n}\n\n// entities/dlq-message.entity.ts\nimport { Entity, Column, PrimaryGeneratedColumn, CreateDateColumn, Index } from 'typeorm';\n\n@Entity('dlq_messages')\n@Index(['status', 'errorType'])\n@Index(['originalTopic', 'createdAt'])\nexport class DlqMessage {\n  @PrimaryGeneratedColumn('uuid')\n  id: string;\n\n  @Column()\n  messageId: string;\n\n  @Column()\n  originalTopic: string;\n\n  @Column({ nullable: true })\n  originalPartition: number;\n\n  @Column()\n  failureCount: number;\n\n  @Column('timestamp')\n  firstFailureTime: Date;\n\n  @Column('timestamp')\n  lastFailureTime: Date;\n\n  @Column()\n  errorType: string;\n\n  @Column('text')\n  errorMessage: string;\n\n  @Column('text', { nullable: true })\n  stackTrace: string;\n\n  @Column('jsonb')\n  headers: Record<string, string>;\n\n  @Column('jsonb')\n  payload: any;\n\n  @Column({\n    type: 'enum',\n    enum: ['pending', 'investigating', 'resolved', 'discarded'],\n    default: 'pending',\n  })\n  status: string;\n\n  @Column({ nullable: true })\n  resolutionNotes: string;\n\n  @CreateDateColumn()\n  createdAt: Date;\n\n  @Column({ nullable: true })\n  resolvedAt: Date;\n}\n"})}),"\n",(0,t.jsx)(n.h2,{id:"poison-message-detection",children:"Poison Message Detection"}),"\n",(0,t.jsx)(n.h3,{id:"what-is-a-poison-message",children:"What is a Poison Message?"}),"\n",(0,t.jsx)(n.p,{children:"A poison message is a message that repeatedly fails processing and causes the consumer to crash, retry infinitely, or block the queue."}),"\n",(0,t.jsx)(n.p,{children:(0,t.jsx)(n.strong,{children:"Common Causes:"})}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Malformed/corrupted data"}),"\n",(0,t.jsx)(n.li,{children:"Missing required fields"}),"\n",(0,t.jsx)(n.li,{children:"Invalid data types"}),"\n",(0,t.jsx)(n.li,{children:"Incompatible schema versions"}),"\n",(0,t.jsx)(n.li,{children:"Business logic violations"}),"\n",(0,t.jsx)(n.li,{children:"External dependency failures"}),"\n"]}),"\n",(0,t.jsx)(n.h3,{id:"poison-message-detector",children:"Poison Message Detector"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// services/poison-message-detector.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { DlqMessage } from '../entities/dlq-message.entity';\n\nexport interface PoisonMessageCriteria {\n  maxRetries: number;\n  timeWindowMinutes: number;\n  errorPatterns: string[];\n}\n\n@Injectable()\nexport class PoisonMessageDetectorService {\n  private readonly logger = new Logger(PoisonMessageDetectorService.name);\n  private readonly messageFailures = new Map<string, number>();\n  private readonly messageFirstFailure = new Map<string, Date>();\n\n  constructor(\n    @InjectRepository(DlqMessage)\n    private dlqRepository: Repository<DlqMessage>,\n  ) {}\n\n  /**\n   * Check if message is poisoned based on retry count and time window\n   */\n  isPoisonMessage(messageId: string, error: Error, criteria: PoisonMessageCriteria): boolean {\n    const failureCount = this.messageFailures.get(messageId) || 0;\n    const firstFailure = this.messageFirstFailure.get(messageId);\n\n    // Check retry count\n    if (failureCount >= criteria.maxRetries) {\n      this.logger.warn(`Message ${messageId} exceeded max retries (${criteria.maxRetries})`);\n      return true;\n    }\n\n    // Check time window\n    if (firstFailure) {\n      const minutesSinceFirstFailure = (Date.now() - firstFailure.getTime()) / (1000 * 60);\n\n      if (minutesSinceFirstFailure <= criteria.timeWindowMinutes && failureCount >= 3) {\n        this.logger.warn(`Message ${messageId} failed ${failureCount} times in ${minutesSinceFirstFailure} minutes`);\n        return true;\n      }\n    }\n\n    // Check error patterns\n    const errorMessage = error.message.toLowerCase();\n    const matchesFatalPattern = criteria.errorPatterns.some((pattern) => errorMessage.includes(pattern.toLowerCase()));\n\n    if (matchesFatalPattern) {\n      this.logger.warn(`Message ${messageId} matched fatal error pattern: ${error.message}`);\n      return true;\n    }\n\n    return false;\n  }\n\n  /**\n   * Record message failure\n   */\n  recordFailure(messageId: string): void {\n    const currentCount = this.messageFailures.get(messageId) || 0;\n    this.messageFailures.set(messageId, currentCount + 1);\n\n    if (!this.messageFirstFailure.has(messageId)) {\n      this.messageFirstFailure.set(messageId, new Date());\n    }\n  }\n\n  /**\n   * Clear failure tracking for successfully processed message\n   */\n  clearFailure(messageId: string): void {\n    this.messageFailures.delete(messageId);\n    this.messageFirstFailure.delete(messageId);\n  }\n\n  /**\n   * Get poison message statistics\n   */\n  async getPoisonMessageStats(days: number = 7): Promise<any> {\n    const since = new Date();\n    since.setDate(since.getDate() - days);\n\n    const stats = await this.dlqRepository\n      .createQueryBuilder('dlq')\n      .select('dlq.errorType', 'errorType')\n      .addSelect('dlq.originalTopic', 'topic')\n      .addSelect('COUNT(*)', 'count')\n      .addSelect('AVG(dlq.failureCount)', 'avgRetries')\n      .where('dlq.createdAt >= :since', { since })\n      .groupBy('dlq.errorType')\n      .addGroupBy('dlq.originalTopic')\n      .orderBy('count', 'DESC')\n      .getRawMany();\n\n    return stats;\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h2,{id:"retry-strategies",children:"Retry Strategies"}),"\n",(0,t.jsx)(n.h3,{id:"exponential-backoff",children:"Exponential Backoff"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// services/retry-strategy.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\n\nexport interface RetryConfig {\n  maxRetries: number;\n  initialDelayMs: number;\n  maxDelayMs: number;\n  backoffMultiplier: number;\n  jitterEnabled: boolean;\n}\n\n@Injectable()\nexport class RetryStrategyService {\n  private readonly logger = new Logger(RetryStrategyService.name);\n\n  /**\n   * Calculate delay with exponential backoff\n   */\n  calculateDelay(attemptNumber: number, config: RetryConfig): number {\n    const delay = Math.min(\n      config.initialDelayMs * Math.pow(config.backoffMultiplier, attemptNumber),\n      config.maxDelayMs,\n    );\n\n    // Add jitter to prevent thundering herd\n    if (config.jitterEnabled) {\n      const jitter = Math.random() * delay * 0.1; // 10% jitter\n      return Math.floor(delay + jitter);\n    }\n\n    return delay;\n  }\n\n  /**\n   * Determine if error is retryable\n   */\n  isRetryable(error: Error): boolean {\n    // Network errors - retryable\n    if (\n      error.message.includes('ECONNREFUSED') ||\n      error.message.includes('ETIMEDOUT') ||\n      error.message.includes('ENOTFOUND')\n    ) {\n      return true;\n    }\n\n    // Database errors - retryable\n    if (\n      error.message.includes('connection') ||\n      error.message.includes('timeout') ||\n      error.message.includes('deadlock')\n    ) {\n      return true;\n    }\n\n    // Rate limiting - retryable\n    if (\n      error.message.includes('rate limit') ||\n      error.message.includes('429') ||\n      error.message.includes('too many requests')\n    ) {\n      return true;\n    }\n\n    // Validation errors - NOT retryable\n    if (\n      error.message.includes('validation') ||\n      error.message.includes('invalid') ||\n      error.message.includes('malformed')\n    ) {\n      return false;\n    }\n\n    // Business logic errors - NOT retryable\n    if (\n      error.message.includes('not found') ||\n      error.message.includes('already exists') ||\n      error.message.includes('unauthorized')\n    ) {\n      return false;\n    }\n\n    // Default to retryable for unknown errors\n    return true;\n  }\n\n  /**\n   * Execute with retry logic\n   */\n  async executeWithRetry<T>(operation: () => Promise<T>, config: RetryConfig, context: string): Promise<T> {\n    let lastError: Error;\n\n    for (let attempt = 0; attempt <= config.maxRetries; attempt++) {\n      try {\n        return await operation();\n      } catch (error) {\n        lastError = error;\n\n        if (!this.isRetryable(error)) {\n          this.logger.error(`Non-retryable error in ${context}: ${error.message}`);\n          throw error;\n        }\n\n        if (attempt < config.maxRetries) {\n          const delay = this.calculateDelay(attempt, config);\n          this.logger.warn(\n            `Retry ${attempt + 1}/${config.maxRetries} for ${context} after ${delay}ms. Error: ${error.message}`,\n          );\n          await this.sleep(delay);\n        }\n      }\n    }\n\n    this.logger.error(`Max retries (${config.maxRetries}) exceeded for ${context}`);\n    throw lastError;\n  }\n\n  private sleep(ms: number): Promise<void> {\n    return new Promise((resolve) => setTimeout(resolve, ms));\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h2,{id:"dlq-implementation---kafka",children:"DLQ Implementation - Kafka"}),"\n",(0,t.jsx)(n.h3,{id:"kafka-dlq-producer",children:"Kafka DLQ Producer"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// kafka/kafka-dlq-producer.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { ConfigService } from '@nestjs/config';\nimport { Kafka, Producer, CompressionTypes } from 'kafkajs';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { DlqMessage } from '../entities/dlq-message.entity';\n\n@Injectable()\nexport class KafkaDlqProducerService implements OnModuleInit {\n  private readonly logger = new Logger(KafkaDlqProducerService.name);\n  private producer: Producer;\n  private readonly kafka: Kafka;\n\n  constructor(\n    private configService: ConfigService,\n    @InjectRepository(DlqMessage)\n    private dlqRepository: Repository<DlqMessage>,\n  ) {\n    this.kafka = new Kafka({\n      clientId: 'dlq-producer',\n      brokers: this.configService.get('KAFKA_BROKERS').split(','),\n    });\n  }\n\n  async onModuleInit() {\n    this.producer = this.kafka.producer({\n      idempotent: true,\n      maxInFlightRequests: 5,\n      transactionalId: 'dlq-producer-tx',\n    });\n\n    await this.producer.connect();\n    this.logger.log('Kafka DLQ Producer connected');\n  }\n\n  /**\n   * Send message to DLQ topic\n   */\n  async sendToDlq(\n    originalTopic: string,\n    message: any,\n    error: Error,\n    metadata: {\n      partition?: number;\n      headers?: Record<string, string>;\n      failureCount: number;\n      firstFailureTime: Date;\n    },\n  ): Promise<void> {\n    const dlqTopic = `${originalTopic}.dlq`;\n    const messageId = metadata.headers?.messageId || this.generateMessageId();\n\n    try {\n      // Save to database\n      const dlqMessage = this.dlqRepository.create({\n        messageId,\n        originalTopic,\n        originalPartition: metadata.partition,\n        failureCount: metadata.failureCount,\n        firstFailureTime: metadata.firstFailureTime,\n        lastFailureTime: new Date(),\n        errorType: error.constructor.name,\n        errorMessage: error.message,\n        stackTrace: error.stack,\n        headers: metadata.headers || {},\n        payload: message,\n        status: 'pending',\n      });\n\n      await this.dlqRepository.save(dlqMessage);\n\n      // Send to DLQ topic\n      await this.producer.send({\n        topic: dlqTopic,\n        compression: CompressionTypes.GZIP,\n        messages: [\n          {\n            key: messageId,\n            value: JSON.stringify({\n              originalTopic,\n              originalPartition: metadata.partition,\n              failureCount: metadata.failureCount,\n              firstFailureTime: metadata.firstFailureTime,\n              lastFailureTime: new Date(),\n              errorType: error.constructor.name,\n              errorMessage: error.message,\n              stackTrace: error.stack,\n              payload: message,\n            }),\n            headers: {\n              'x-original-topic': originalTopic,\n              'x-failure-count': metadata.failureCount.toString(),\n              'x-error-type': error.constructor.name,\n              ...metadata.headers,\n            },\n          },\n        ],\n      });\n\n      this.logger.warn(`Message ${messageId} sent to DLQ topic ${dlqTopic} after ${metadata.failureCount} failures`);\n    } catch (dlqError) {\n      this.logger.error(`Failed to send message to DLQ: ${dlqError.message}`, dlqError.stack);\n      // Critical: DLQ itself failed, log to external system\n      await this.logToCriticalStorage(messageId, originalTopic, message, error);\n    }\n  }\n\n  /**\n   * Fallback storage when DLQ fails\n   */\n  private async logToCriticalStorage(messageId: string, topic: string, message: any, error: Error): Promise<void> {\n    // Write to file system, S3, or external monitoring system\n    this.logger.error({\n      type: 'DLQ_FAILURE',\n      messageId,\n      topic,\n      message,\n      error: {\n        name: error.name,\n        message: error.message,\n        stack: error.stack,\n      },\n    });\n  }\n\n  private generateMessageId(): string {\n    return `${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n  }\n\n  async onModuleDestroy() {\n    await this.producer.disconnect();\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h3,{id:"kafka-consumer-with-dlq",children:"Kafka Consumer with DLQ"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// kafka/kafka-consumer-with-dlq.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { ConfigService } from '@nestjs/config';\nimport { Kafka, Consumer, EachMessagePayload } from 'kafkajs';\nimport { PoisonMessageDetectorService } from '../services/poison-message-detector.service';\nimport { RetryStrategyService } from '../services/retry-strategy.service';\nimport { KafkaDlqProducerService } from './kafka-dlq-producer.service';\n\n@Injectable()\nexport class KafkaConsumerWithDlqService implements OnModuleInit {\n  private readonly logger = new Logger(KafkaConsumerWithDlqService.name);\n  private consumer: Consumer;\n  private readonly kafka: Kafka;\n  private readonly messageFailures = new Map<string, number>();\n  private readonly messageFirstFailure = new Map<string, Date>();\n\n  constructor(\n    private configService: ConfigService,\n    private poisonDetector: PoisonMessageDetectorService,\n    private retryStrategy: RetryStrategyService,\n    private dlqProducer: KafkaDlqProducerService,\n  ) {\n    this.kafka = new Kafka({\n      clientId: 'order-consumer',\n      brokers: this.configService.get('KAFKA_BROKERS').split(','),\n    });\n  }\n\n  async onModuleInit() {\n    this.consumer = this.kafka.consumer({\n      groupId: 'order-service-group',\n      retry: {\n        retries: 0, // We handle retries ourselves\n      },\n    });\n\n    await this.consumer.connect();\n    await this.consumer.subscribe({\n      topics: ['orders.created', 'orders.updated'],\n      fromBeginning: false,\n    });\n\n    await this.consumer.run({\n      eachMessage: async (payload) => this.handleMessage(payload),\n    });\n\n    this.logger.log('Kafka Consumer with DLQ started');\n  }\n\n  private async handleMessage(payload: EachMessagePayload): Promise<void> {\n    const { topic, partition, message } = payload;\n    const messageId = message.headers?.messageId?.toString() || message.key?.toString();\n\n    try {\n      // Parse message\n      const parsedMessage = JSON.parse(message.value.toString());\n\n      // Process with retry logic\n      await this.retryStrategy.executeWithRetry(\n        () => this.processMessage(parsedMessage),\n        {\n          maxRetries: 3,\n          initialDelayMs: 1000,\n          maxDelayMs: 30000,\n          backoffMultiplier: 2,\n          jitterEnabled: true,\n        },\n        `Message ${messageId}`,\n      );\n\n      // Clear failure tracking on success\n      this.poisonDetector.clearFailure(messageId);\n      this.messageFailures.delete(messageId);\n      this.messageFirstFailure.delete(messageId);\n\n      this.logger.debug(`Successfully processed message ${messageId}`);\n    } catch (error) {\n      await this.handleFailure(topic, partition, message, error);\n    }\n  }\n\n  private async handleFailure(topic: string, partition: number, message: any, error: Error): Promise<void> {\n    const messageId = message.headers?.messageId?.toString() || message.key?.toString();\n\n    // Record failure\n    this.poisonDetector.recordFailure(messageId);\n    const failureCount = (this.messageFailures.get(messageId) || 0) + 1;\n    this.messageFailures.set(messageId, failureCount);\n\n    if (!this.messageFirstFailure.has(messageId)) {\n      this.messageFirstFailure.set(messageId, new Date());\n    }\n\n    const firstFailureTime = this.messageFirstFailure.get(messageId);\n\n    // Check if message is poisoned\n    const isPoisoned = this.poisonDetector.isPoisonMessage(messageId, error, {\n      maxRetries: 5,\n      timeWindowMinutes: 10,\n      errorPatterns: ['validation failed', 'malformed', 'invalid schema', 'deserialization error'],\n    });\n\n    if (isPoisoned) {\n      this.logger.error(`Poison message detected: ${messageId}. Moving to DLQ.`);\n\n      await this.dlqProducer.sendToDlq(topic, JSON.parse(message.value.toString()), error, {\n        partition,\n        headers: this.convertHeaders(message.headers),\n        failureCount,\n        firstFailureTime,\n      });\n\n      // Clean up tracking\n      this.messageFailures.delete(messageId);\n      this.messageFirstFailure.delete(messageId);\n    } else {\n      this.logger.warn(`Message ${messageId} failed (attempt ${failureCount}). Will retry.`);\n    }\n  }\n\n  private async processMessage(message: any): Promise<void> {\n    // Actual business logic here\n    this.logger.log(`Processing message: ${JSON.stringify(message)}`);\n\n    // Simulate processing\n    if (message.orderTotal > 1000000) {\n      throw new Error('Order total exceeds maximum allowed value');\n    }\n\n    // Process order...\n  }\n\n  private convertHeaders(headers: any): Record<string, string> {\n    const result: Record<string, string> = {};\n\n    if (headers) {\n      for (const [key, value] of Object.entries(headers)) {\n        result[key] = value?.toString() || '';\n      }\n    }\n\n    return result;\n  }\n\n  async onModuleDestroy() {\n    await this.consumer.disconnect();\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h2,{id:"dlq-implementation---rabbitmq",children:"DLQ Implementation - RabbitMQ"}),"\n",(0,t.jsx)(n.h3,{id:"rabbitmq-dlq-setup",children:"RabbitMQ DLQ Setup"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// rabbitmq/rabbitmq-dlq-setup.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { ConfigService } from '@nestjs/config';\nimport * as amqp from 'amqplib';\n\n@Injectable()\nexport class RabbitMqDlqSetupService implements OnModuleInit {\n  private readonly logger = new Logger(RabbitMqDlqSetupService.name);\n  private connection: amqp.Connection;\n  private channel: amqp.Channel;\n\n  constructor(private configService: ConfigService) {}\n\n  async onModuleInit() {\n    await this.setupConnection();\n    await this.setupDlqInfrastructure();\n  }\n\n  private async setupConnection(): Promise<void> {\n    const rabbitMqUrl = this.configService.get('RABBITMQ_URL');\n    this.connection = await amqp.connect(rabbitMqUrl);\n    this.channel = await this.connection.createChannel();\n    this.logger.log('RabbitMQ connection established');\n  }\n\n  /**\n   * Setup DLQ infrastructure with retry delays\n   */\n  async setupDlqInfrastructure(): Promise<void> {\n    const queues = ['orders', 'payments', 'notifications'];\n\n    for (const queueName of queues) {\n      // Main queue\n      await this.channel.assertQueue(queueName, {\n        durable: true,\n        deadLetterExchange: `${queueName}.dlx`,\n        deadLetterRoutingKey: `${queueName}.dlq`,\n      });\n\n      // Dead Letter Exchange\n      await this.channel.assertExchange(`${queueName}.dlx`, 'direct', {\n        durable: true,\n      });\n\n      // Dead Letter Queue\n      await this.channel.assertQueue(`${queueName}.dlq`, {\n        durable: true,\n        messageTtl: 7 * 24 * 60 * 60 * 1000, // 7 days\n      });\n\n      // Bind DLQ to DLX\n      await this.channel.bindQueue(`${queueName}.dlq`, `${queueName}.dlx`, `${queueName}.dlq`);\n\n      // Retry queue with delay\n      await this.channel.assertQueue(`${queueName}.retry`, {\n        durable: true,\n        messageTtl: 5000, // 5 seconds\n        deadLetterExchange: '', // Default exchange\n        deadLetterRoutingKey: queueName, // Back to main queue\n      });\n\n      this.logger.log(`DLQ infrastructure setup completed for ${queueName}`);\n    }\n  }\n\n  getChannel(): amqp.Channel {\n    return this.channel;\n  }\n\n  async onModuleDestroy() {\n    await this.channel.close();\n    await this.connection.close();\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h3,{id:"rabbitmq-consumer-with-dlq",children:"RabbitMQ Consumer with DLQ"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// rabbitmq/rabbitmq-consumer-with-dlq.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { RabbitMqDlqSetupService } from './rabbitmq-dlq-setup.service';\nimport { PoisonMessageDetectorService } from '../services/poison-message-detector.service';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { DlqMessage } from '../entities/dlq-message.entity';\nimport * as amqp from 'amqplib';\n\n@Injectable()\nexport class RabbitMqConsumerWithDlqService implements OnModuleInit {\n  private readonly logger = new Logger(RabbitMqConsumerWithDlqService.name);\n  private channel: amqp.Channel;\n\n  constructor(\n    private rabbitMqSetup: RabbitMqDlqSetupService,\n    private poisonDetector: PoisonMessageDetectorService,\n    @InjectRepository(DlqMessage)\n    private dlqRepository: Repository<DlqMessage>,\n  ) {}\n\n  async onModuleInit() {\n    this.channel = this.rabbitMqSetup.getChannel();\n    await this.startConsuming('orders');\n  }\n\n  private async startConsuming(queueName: string): Promise<void> {\n    await this.channel.consume(\n      queueName,\n      async (msg) => {\n        if (!msg) return;\n\n        try {\n          const content = JSON.parse(msg.content.toString());\n          const messageId = msg.properties.messageId || this.generateMessageId();\n\n          await this.processMessage(content);\n\n          // Acknowledge successful processing\n          this.channel.ack(msg);\n          this.poisonDetector.clearFailure(messageId);\n        } catch (error) {\n          await this.handleMessageFailure(queueName, msg, error);\n        }\n      },\n      { noAck: false },\n    );\n\n    this.logger.log(`Started consuming from queue: ${queueName}`);\n  }\n\n  private async handleMessageFailure(queueName: string, msg: amqp.ConsumeMessage, error: Error): Promise<void> {\n    const messageId = msg.properties.messageId || this.generateMessageId();\n    const retryCount = this.getRetryCount(msg);\n\n    this.poisonDetector.recordFailure(messageId);\n\n    // Check if poison message\n    const isPoisoned = this.poisonDetector.isPoisonMessage(messageId, error, {\n      maxRetries: 3,\n      timeWindowMinutes: 5,\n      errorPatterns: ['validation', 'malformed', 'invalid'],\n    });\n\n    if (isPoisoned || retryCount >= 3) {\n      this.logger.error(`Moving message ${messageId} to DLQ after ${retryCount} retries`);\n\n      // Save to database\n      await this.saveToDlqDatabase(queueName, msg, error, retryCount);\n\n      // Send to DLQ\n      this.channel.sendToQueue(`${queueName}.dlq`, msg.content, {\n        headers: {\n          ...msg.properties.headers,\n          'x-original-queue': queueName,\n          'x-retry-count': retryCount,\n          'x-error-type': error.constructor.name,\n          'x-error-message': error.message,\n          'x-failure-time': new Date().toISOString(),\n        },\n        persistent: true,\n      });\n\n      // Acknowledge to remove from main queue\n      this.channel.ack(msg);\n    } else {\n      // Send to retry queue\n      this.logger.warn(`Retrying message ${messageId} (attempt ${retryCount + 1})`);\n\n      this.channel.sendToQueue(`${queueName}.retry`, msg.content, {\n        headers: {\n          ...msg.properties.headers,\n          'x-retry-count': retryCount + 1,\n        },\n        persistent: true,\n      });\n\n      this.channel.ack(msg);\n    }\n  }\n\n  private async saveToDlqDatabase(\n    queueName: string,\n    msg: amqp.ConsumeMessage,\n    error: Error,\n    retryCount: number,\n  ): Promise<void> {\n    const messageId = msg.properties.messageId || this.generateMessageId();\n    const content = JSON.parse(msg.content.toString());\n\n    const dlqMessage = this.dlqRepository.create({\n      messageId,\n      originalTopic: queueName,\n      failureCount: retryCount,\n      firstFailureTime: new Date(msg.properties.headers?.['x-first-failure'] || Date.now()),\n      lastFailureTime: new Date(),\n      errorType: error.constructor.name,\n      errorMessage: error.message,\n      stackTrace: error.stack,\n      headers: msg.properties.headers || {},\n      payload: content,\n      status: 'pending',\n    });\n\n    await this.dlqRepository.save(dlqMessage);\n  }\n\n  private getRetryCount(msg: amqp.ConsumeMessage): number {\n    return parseInt(msg.properties.headers?.['x-retry-count'] || '0');\n  }\n\n  private async processMessage(content: any): Promise<void> {\n    this.logger.log(`Processing message: ${JSON.stringify(content)}`);\n    // Business logic here\n  }\n\n  private generateMessageId(): string {\n    return `${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h2,{id:"message-recovery-patterns",children:"Message Recovery Patterns"}),"\n",(0,t.jsx)(n.h3,{id:"dlq-message-recovery-service",children:"DLQ Message Recovery Service"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// services/dlq-recovery.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { DlqMessage } from '../entities/dlq-message.entity';\nimport { Kafka, Producer } from 'kafkajs';\nimport { ConfigService } from '@nestjs/config';\n\n@Injectable()\nexport class DlqRecoveryService {\n  private readonly logger = new Logger(DlqRecoveryService.name);\n  private producer: Producer;\n\n  constructor(\n    @InjectRepository(DlqMessage)\n    private dlqRepository: Repository<DlqMessage>,\n    private configService: ConfigService,\n  ) {\n    const kafka = new Kafka({\n      clientId: 'dlq-recovery',\n      brokers: this.configService.get('KAFKA_BROKERS').split(','),\n    });\n    this.producer = kafka.producer();\n  }\n\n  async onModuleInit() {\n    await this.producer.connect();\n  }\n\n  /**\n   * Replay a single DLQ message back to original topic\n   */\n  async replayMessage(dlqMessageId: string): Promise<void> {\n    const dlqMessage = await this.dlqRepository.findOne({\n      where: { id: dlqMessageId },\n    });\n\n    if (!dlqMessage) {\n      throw new Error(`DLQ message ${dlqMessageId} not found`);\n    }\n\n    if (dlqMessage.status === 'resolved') {\n      throw new Error(`Message ${dlqMessageId} already resolved`);\n    }\n\n    try {\n      // Send back to original topic\n      await this.producer.send({\n        topic: dlqMessage.originalTopic,\n        messages: [\n          {\n            key: dlqMessage.messageId,\n            value: JSON.stringify(dlqMessage.payload),\n            headers: {\n              ...dlqMessage.headers,\n              'x-replayed': 'true',\n              'x-replay-time': new Date().toISOString(),\n              'x-dlq-id': dlqMessage.id,\n            },\n          },\n        ],\n      });\n\n      // Update status\n      dlqMessage.status = 'resolved';\n      dlqMessage.resolvedAt = new Date();\n      dlqMessage.resolutionNotes = 'Manually replayed to original topic';\n      await this.dlqRepository.save(dlqMessage);\n\n      this.logger.log(`Successfully replayed message ${dlqMessageId} to ${dlqMessage.originalTopic}`);\n    } catch (error) {\n      this.logger.error(`Failed to replay message ${dlqMessageId}: ${error.message}`);\n      throw error;\n    }\n  }\n\n  /**\n   * Bulk replay messages by filter\n   */\n  async replayMessagesByFilter(filter: {\n    errorType?: string;\n    originalTopic?: string;\n    createdAfter?: Date;\n    createdBefore?: Date;\n    limit?: number;\n  }): Promise<{ success: number; failed: number }> {\n    const query = this.dlqRepository.createQueryBuilder('dlq').where('dlq.status = :status', { status: 'pending' });\n\n    if (filter.errorType) {\n      query.andWhere('dlq.errorType = :errorType', { errorType: filter.errorType });\n    }\n\n    if (filter.originalTopic) {\n      query.andWhere('dlq.originalTopic = :topic', { topic: filter.originalTopic });\n    }\n\n    if (filter.createdAfter) {\n      query.andWhere('dlq.createdAt >= :after', { after: filter.createdAfter });\n    }\n\n    if (filter.createdBefore) {\n      query.andWhere('dlq.createdAt <= :before', { before: filter.createdBefore });\n    }\n\n    if (filter.limit) {\n      query.limit(filter.limit);\n    }\n\n    const messages = await query.getMany();\n    let success = 0;\n    let failed = 0;\n\n    for (const message of messages) {\n      try {\n        await this.replayMessage(message.id);\n        success++;\n      } catch (error) {\n        this.logger.error(`Failed to replay message ${message.id}: ${error.message}`);\n        failed++;\n      }\n    }\n\n    this.logger.log(`Bulk replay completed: ${success} successful, ${failed} failed`);\n\n    return { success, failed };\n  }\n\n  /**\n   * Discard a message (mark as permanently failed)\n   */\n  async discardMessage(dlqMessageId: string, reason: string): Promise<void> {\n    const dlqMessage = await this.dlqRepository.findOne({\n      where: { id: dlqMessageId },\n    });\n\n    if (!dlqMessage) {\n      throw new Error(`DLQ message ${dlqMessageId} not found`);\n    }\n\n    dlqMessage.status = 'discarded';\n    dlqMessage.resolvedAt = new Date();\n    dlqMessage.resolutionNotes = reason;\n    await this.dlqRepository.save(dlqMessage);\n\n    this.logger.log(`Message ${dlqMessageId} discarded: ${reason}`);\n  }\n\n  /**\n   * Get DLQ statistics\n   */\n  async getDlqStatistics(): Promise<any> {\n    const stats = await this.dlqRepository\n      .createQueryBuilder('dlq')\n      .select('dlq.status', 'status')\n      .addSelect('dlq.errorType', 'errorType')\n      .addSelect('COUNT(*)', 'count')\n      .groupBy('dlq.status')\n      .addGroupBy('dlq.errorType')\n      .getRawMany();\n\n    const recentMessages = await this.dlqRepository.find({\n      order: { createdAt: 'DESC' },\n      take: 10,\n    });\n\n    return {\n      statistics: stats,\n      recentMessages,\n      totalPending: await this.dlqRepository.count({ where: { status: 'pending' } }),\n      totalResolved: await this.dlqRepository.count({ where: { status: 'resolved' } }),\n      totalDiscarded: await this.dlqRepository.count({ where: { status: 'discarded' } }),\n    };\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h3,{id:"dlq-admin-controller",children:"DLQ Admin Controller"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// controllers/dlq-admin.controller.ts\nimport { Controller, Get, Post, Param, Body, Query, UseGuards } from '@nestjs/common';\nimport { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';\nimport { DlqRecoveryService } from '../services/dlq-recovery.service';\nimport { PoisonMessageDetectorService } from '../services/poison-message-detector.service';\nimport { AdminGuard } from '../guards/admin.guard';\n\n@ApiTags('DLQ Admin')\n@Controller('admin/dlq')\n@UseGuards(AdminGuard)\n@ApiBearerAuth()\nexport class DlqAdminController {\n  constructor(private dlqRecoveryService: DlqRecoveryService, private poisonDetector: PoisonMessageDetectorService) {}\n\n  @Get('statistics')\n  @ApiOperation({ summary: 'Get DLQ statistics' })\n  async getStatistics() {\n    return this.dlqRecoveryService.getDlqStatistics();\n  }\n\n  @Get('poison-stats')\n  @ApiOperation({ summary: 'Get poison message statistics' })\n  async getPoisonStats(@Query('days') days: number = 7) {\n    return this.poisonDetector.getPoisonMessageStats(days);\n  }\n\n  @Post('replay/:id')\n  @ApiOperation({ summary: 'Replay a single DLQ message' })\n  async replayMessage(@Param('id') id: string) {\n    await this.dlqRecoveryService.replayMessage(id);\n    return { success: true, message: `Message ${id} replayed` };\n  }\n\n  @Post('replay-bulk')\n  @ApiOperation({ summary: 'Replay multiple messages by filter' })\n  async replayBulk(\n    @Body()\n    filter: {\n      errorType?: string;\n      originalTopic?: string;\n      createdAfter?: Date;\n      createdBefore?: Date;\n      limit?: number;\n    },\n  ) {\n    const result = await this.dlqRecoveryService.replayMessagesByFilter(filter);\n    return result;\n  }\n\n  @Post('discard/:id')\n  @ApiOperation({ summary: 'Discard a DLQ message' })\n  async discardMessage(@Param('id') id: string, @Body('reason') reason: string) {\n    await this.dlqRecoveryService.discardMessage(id, reason);\n    return { success: true, message: `Message ${id} discarded` };\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h2,{id:"monitoring-and-alerting",children:"Monitoring and Alerting"}),"\n",(0,t.jsx)(n.h3,{id:"dlq-metrics-service",children:"DLQ Metrics Service"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// services/dlq-metrics.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { Counter, Gauge, Histogram } from 'prom-client';\n\n@Injectable()\nexport class DlqMetricsService {\n  private readonly dlqMessageCounter = new Counter({\n    name: 'dlq_messages_total',\n    help: 'Total messages sent to DLQ',\n    labelNames: ['topic', 'error_type'],\n  });\n\n  private readonly dlqMessageGauge = new Gauge({\n    name: 'dlq_messages_pending',\n    help: 'Number of pending messages in DLQ',\n    labelNames: ['topic'],\n  });\n\n  private readonly messageRetryHistogram = new Histogram({\n    name: 'message_retry_attempts',\n    help: 'Distribution of retry attempts before DLQ',\n    labelNames: ['topic'],\n    buckets: [1, 2, 3, 5, 10],\n  });\n\n  private readonly dlqProcessingTime = new Histogram({\n    name: 'dlq_recovery_duration_seconds',\n    help: 'Time taken to recover DLQ messages',\n    labelNames: ['status'],\n    buckets: [0.1, 0.5, 1, 2, 5, 10],\n  });\n\n  recordDlqMessage(topic: string, errorType: string): void {\n    this.dlqMessageCounter.inc({ topic, error_type: errorType });\n  }\n\n  updatePendingCount(topic: string, count: number): void {\n    this.dlqMessageGauge.set({ topic }, count);\n  }\n\n  recordRetryAttempts(topic: string, attempts: number): void {\n    this.messageRetryHistogram.observe({ topic }, attempts);\n  }\n\n  recordRecoveryDuration(status: string, durationSeconds: number): void {\n    this.dlqProcessingTime.observe({ status }, durationSeconds);\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h3,{id:"dlq-alerting-service",children:"DLQ Alerting Service"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// services/dlq-alerting.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { Cron, CronExpression } from '@nestjs/schedule';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { DlqMessage } from '../entities/dlq-message.entity';\n\n@Injectable()\nexport class DlqAlertingService {\n  private readonly logger = new Logger(DlqAlertingService.name);\n\n  constructor(\n    @InjectRepository(DlqMessage)\n    private dlqRepository: Repository<DlqMessage>,\n  ) {}\n\n  /**\n   * Check for DLQ threshold violations every 5 minutes\n   */\n  @Cron(CronExpression.EVERY_5_MINUTES)\n  async checkDlqThresholds(): Promise<void> {\n    const pendingCount = await this.dlqRepository.count({\n      where: { status: 'pending' },\n    });\n\n    // Alert if too many pending messages\n    if (pendingCount > 100) {\n      await this.sendAlert({\n        severity: 'warning',\n        title: 'High DLQ message count',\n        message: `${pendingCount} messages pending in DLQ`,\n        action: 'Review and replay or discard messages',\n      });\n    }\n\n    // Check for old unresolved messages\n    const oldMessages = await this.dlqRepository\n      .createQueryBuilder('dlq')\n      .where('dlq.status = :status', { status: 'pending' })\n      .andWhere('dlq.createdAt < :cutoff', {\n        cutoff: new Date(Date.now() - 7 * 24 * 60 * 60 * 1000), // 7 days\n      })\n      .getCount();\n\n    if (oldMessages > 0) {\n      await this.sendAlert({\n        severity: 'warning',\n        title: 'Old DLQ messages detected',\n        message: `${oldMessages} messages in DLQ for more than 7 days`,\n        action: 'Review and resolve old messages',\n      });\n    }\n  }\n\n  /**\n   * Check for poison message patterns\n   */\n  @Cron(CronExpression.EVERY_HOUR)\n  async checkPoisonMessagePatterns(): Promise<void> {\n    const recentPoisonMessages = await this.dlqRepository\n      .createQueryBuilder('dlq')\n      .select('dlq.errorType', 'errorType')\n      .addSelect('COUNT(*)', 'count')\n      .where('dlq.createdAt >= :cutoff', {\n        cutoff: new Date(Date.now() - 60 * 60 * 1000), // Last hour\n      })\n      .groupBy('dlq.errorType')\n      .having('COUNT(*) > :threshold', { threshold: 10 })\n      .getRawMany();\n\n    for (const pattern of recentPoisonMessages) {\n      await this.sendAlert({\n        severity: 'error',\n        title: 'Poison message pattern detected',\n        message: `${pattern.count} messages with error type '${pattern.errorType}' in the last hour`,\n        action: 'Investigate root cause and fix application code',\n      });\n    }\n  }\n\n  private async sendAlert(alert: { severity: string; title: string; message: string; action: string }): Promise<void> {\n    this.logger.warn({\n      type: 'DLQ_ALERT',\n      ...alert,\n    });\n\n    // Send to alerting system (PagerDuty, Slack, etc.)\n    // await this.alertingService.send(alert);\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h2,{id:"error-classification",children:"Error Classification"}),"\n",(0,t.jsx)(n.h3,{id:"error-classifier",children:"Error Classifier"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-typescript",children:"// services/error-classifier.service.ts\nimport { Injectable } from '@nestjs/common';\n\nexport enum ErrorCategory {\n  TRANSIENT = 'TRANSIENT', // Temporary, retry will likely succeed\n  INFRASTRUCTURE = 'INFRASTRUCTURE', // Network, database, external service issues\n  VALIDATION = 'VALIDATION', // Invalid input, schema errors\n  BUSINESS_LOGIC = 'BUSINESS_LOGIC', // Business rule violations\n  CONFIGURATION = 'CONFIGURATION', // Missing config, invalid setup\n  UNKNOWN = 'UNKNOWN', // Unclassified errors\n}\n\nexport interface ErrorClassification {\n  category: ErrorCategory;\n  retryable: boolean;\n  maxRetries: number;\n  backoffMultiplier: number;\n  severity: 'low' | 'medium' | 'high' | 'critical';\n}\n\n@Injectable()\nexport class ErrorClassifierService {\n  /**\n   * Classify error and determine handling strategy\n   */\n  classifyError(error: Error): ErrorClassification {\n    const message = error.message.toLowerCase();\n    const errorName = error.constructor.name;\n\n    // Transient errors - retry aggressively\n    if (this.isTransientError(message, errorName)) {\n      return {\n        category: ErrorCategory.TRANSIENT,\n        retryable: true,\n        maxRetries: 5,\n        backoffMultiplier: 2,\n        severity: 'medium',\n      };\n    }\n\n    // Infrastructure errors - retry with backoff\n    if (this.isInfrastructureError(message, errorName)) {\n      return {\n        category: ErrorCategory.INFRASTRUCTURE,\n        retryable: true,\n        maxRetries: 3,\n        backoffMultiplier: 3,\n        severity: 'high',\n      };\n    }\n\n    // Validation errors - don't retry\n    if (this.isValidationError(message, errorName)) {\n      return {\n        category: ErrorCategory.VALIDATION,\n        retryable: false,\n        maxRetries: 0,\n        backoffMultiplier: 1,\n        severity: 'low',\n      };\n    }\n\n    // Business logic errors - don't retry\n    if (this.isBusinessLogicError(message, errorName)) {\n      return {\n        category: ErrorCategory.BUSINESS_LOGIC,\n        retryable: false,\n        maxRetries: 0,\n        backoffMultiplier: 1,\n        severity: 'medium',\n      };\n    }\n\n    // Configuration errors - alert but don't retry\n    if (this.isConfigurationError(message, errorName)) {\n      return {\n        category: ErrorCategory.CONFIGURATION,\n        retryable: false,\n        maxRetries: 0,\n        backoffMultiplier: 1,\n        severity: 'critical',\n      };\n    }\n\n    // Unknown errors - retry conservatively\n    return {\n      category: ErrorCategory.UNKNOWN,\n      retryable: true,\n      maxRetries: 2,\n      backoffMultiplier: 2,\n      severity: 'high',\n    };\n  }\n\n  private isTransientError(message: string, errorName: string): boolean {\n    const patterns = [\n      'timeout',\n      'rate limit',\n      'too many requests',\n      '429',\n      '503',\n      'service unavailable',\n      'temporarily unavailable',\n    ];\n\n    return patterns.some((pattern) => message.includes(pattern));\n  }\n\n  private isInfrastructureError(message: string, errorName: string): boolean {\n    const patterns = [\n      'econnrefused',\n      'enotfound',\n      'etimedout',\n      'connection',\n      'network',\n      'socket',\n      'database',\n      'deadlock',\n    ];\n\n    return patterns.some((pattern) => message.includes(pattern));\n  }\n\n  private isValidationError(message: string, errorName: string): boolean {\n    const patterns = [\n      'validation',\n      'invalid',\n      'malformed',\n      'schema',\n      'required field',\n      'missing',\n      '400',\n      'bad request',\n    ];\n\n    return patterns.some((pattern) => message.includes(pattern)) || errorName.includes('Validation');\n  }\n\n  private isBusinessLogicError(message: string, errorName: string): boolean {\n    const patterns = [\n      'not found',\n      '404',\n      'already exists',\n      'duplicate',\n      'unauthorized',\n      '401',\n      'forbidden',\n      '403',\n      'insufficient',\n      'exceeded',\n    ];\n\n    return patterns.some((pattern) => message.includes(pattern));\n  }\n\n  private isConfigurationError(message: string, errorName: string): boolean {\n    const patterns = ['configuration', 'config', 'environment variable', 'missing credential', 'invalid credential'];\n\n    return patterns.some((pattern) => message.includes(pattern));\n  }\n}\n"})}),"\n",(0,t.jsx)(n.h2,{id:"best-practices",children:"Best Practices"}),"\n",(0,t.jsx)(n.h3,{id:"1-dlq-design",children:"1. DLQ Design"}),"\n",(0,t.jsxs)(n.p,{children:["\u2705 ",(0,t.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Create separate DLQ for each main queue"}),"\n",(0,t.jsx)(n.li,{children:"Set retention policies on DLQ messages (7-30 days)"}),"\n",(0,t.jsx)(n.li,{children:"Store DLQ messages in database for analysis"}),"\n",(0,t.jsx)(n.li,{children:"Include comprehensive error metadata"}),"\n",(0,t.jsx)(n.li,{children:"Implement monitoring and alerting"}),"\n",(0,t.jsx)(n.li,{children:"Design for idempotent message processing"}),"\n"]}),"\n",(0,t.jsxs)(n.p,{children:["\u274c ",(0,t.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Use single DLQ for all queues"}),"\n",(0,t.jsx)(n.li,{children:"Keep messages in DLQ indefinitely"}),"\n",(0,t.jsx)(n.li,{children:"Lose error context and stack traces"}),"\n",(0,t.jsx)(n.li,{children:"Auto-replay without investigating root cause"}),"\n",(0,t.jsx)(n.li,{children:"Ignore DLQ metrics"}),"\n",(0,t.jsx)(n.li,{children:"Process messages without idempotency checks"}),"\n"]}),"\n",(0,t.jsx)(n.h3,{id:"2-retry-strategy",children:"2. Retry Strategy"}),"\n",(0,t.jsxs)(n.p,{children:["\u2705 ",(0,t.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Use exponential backoff with jitter"}),"\n",(0,t.jsx)(n.li,{children:"Classify errors (transient vs permanent)"}),"\n",(0,t.jsx)(n.li,{children:"Set maximum retry limits"}),"\n",(0,t.jsx)(n.li,{children:"Add delay between retries"}),"\n",(0,t.jsx)(n.li,{children:"Log each retry attempt"}),"\n",(0,t.jsx)(n.li,{children:"Track retry metrics"}),"\n"]}),"\n",(0,t.jsxs)(n.p,{children:["\u274c ",(0,t.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Retry immediately without delay"}),"\n",(0,t.jsx)(n.li,{children:"Retry all errors equally"}),"\n",(0,t.jsx)(n.li,{children:"Retry indefinitely"}),"\n",(0,t.jsx)(n.li,{children:"Skip error classification"}),"\n",(0,t.jsx)(n.li,{children:"Hide retry failures"}),"\n",(0,t.jsx)(n.li,{children:"Retry non-idempotent operations"}),"\n"]}),"\n",(0,t.jsx)(n.h3,{id:"3-poison-message-handling",children:"3. Poison Message Handling"}),"\n",(0,t.jsxs)(n.p,{children:["\u2705 ",(0,t.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Detect poison messages early"}),"\n",(0,t.jsx)(n.li,{children:"Move to DLQ after threshold"}),"\n",(0,t.jsx)(n.li,{children:"Alert on poison message patterns"}),"\n",(0,t.jsx)(n.li,{children:"Preserve original message data"}),"\n",(0,t.jsx)(n.li,{children:"Document common poison message causes"}),"\n",(0,t.jsx)(n.li,{children:"Test with malformed messages"}),"\n"]}),"\n",(0,t.jsxs)(n.p,{children:["\u274c ",(0,t.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Let poison messages block queue"}),"\n",(0,t.jsx)(n.li,{children:"Discard without analysis"}),"\n",(0,t.jsx)(n.li,{children:"Ignore recurring patterns"}),"\n",(0,t.jsx)(n.li,{children:"Lose message context"}),"\n",(0,t.jsx)(n.li,{children:"Skip root cause analysis"}),"\n",(0,t.jsx)(n.li,{children:"Deploy without poison message tests"}),"\n"]}),"\n",(0,t.jsx)(n.h3,{id:"4-monitoring",children:"4. Monitoring"}),"\n",(0,t.jsxs)(n.p,{children:["\u2705 ",(0,t.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Track DLQ message count"}),"\n",(0,t.jsx)(n.li,{children:"Monitor retry attempts"}),"\n",(0,t.jsx)(n.li,{children:"Alert on threshold violations"}),"\n",(0,t.jsx)(n.li,{children:"Dashboard DLQ metrics"}),"\n",(0,t.jsx)(n.li,{children:"Track error patterns"}),"\n",(0,t.jsx)(n.li,{children:"Set up automated alerts"}),"\n"]}),"\n",(0,t.jsxs)(n.p,{children:["\u274c ",(0,t.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Deploy without monitoring"}),"\n",(0,t.jsx)(n.li,{children:"Ignore DLQ growth"}),"\n",(0,t.jsx)(n.li,{children:"Skip alerting"}),"\n",(0,t.jsx)(n.li,{children:"Hide error details"}),"\n",(0,t.jsx)(n.li,{children:"Forget to track trends"}),"\n",(0,t.jsx)(n.li,{children:"React only to outages"}),"\n"]}),"\n",(0,t.jsx)(n.h3,{id:"5-recovery",children:"5. Recovery"}),"\n",(0,t.jsxs)(n.p,{children:["\u2705 ",(0,t.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Provide admin UI for DLQ management"}),"\n",(0,t.jsx)(n.li,{children:"Support bulk replay operations"}),"\n",(0,t.jsx)(n.li,{children:"Validate before replaying"}),"\n",(0,t.jsx)(n.li,{children:"Track replay success rate"}),"\n",(0,t.jsx)(n.li,{children:"Document recovery procedures"}),"\n",(0,t.jsx)(n.li,{children:"Test recovery process"}),"\n"]}),"\n",(0,t.jsxs)(n.p,{children:["\u274c ",(0,t.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Manual database operations"}),"\n",(0,t.jsx)(n.li,{children:"Replay without investigation"}),"\n",(0,t.jsx)(n.li,{children:"Skip validation"}),"\n",(0,t.jsx)(n.li,{children:"Forget to update message status"}),"\n",(0,t.jsx)(n.li,{children:"Deploy without recovery plan"}),"\n",(0,t.jsx)(n.li,{children:"Skip testing recovery"}),"\n"]}),"\n",(0,t.jsx)(n.h2,{id:"summary",children:"Summary"}),"\n",(0,t.jsx)(n.p,{children:"Effective DLQ and poison message handling provides:"}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Resilience"})," - System continues despite message failures"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Visibility"})," - Failed messages are tracked and analyzed"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Recovery"})," - Messages can be replayed after fixes"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Throughput"})," - Poison messages don't block processing"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Debugging"})," - Comprehensive error information"]}),"\n"]}),"\n",(0,t.jsx)(n.p,{children:"Key implementation points:"}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"Separate DLQ for each queue"}),"\n",(0,t.jsx)(n.li,{children:"Exponential backoff with jitter"}),"\n",(0,t.jsx)(n.li,{children:"Error classification (retryable vs permanent)"}),"\n",(0,t.jsx)(n.li,{children:"Poison message detection (max retries, time window, error patterns)"}),"\n",(0,t.jsx)(n.li,{children:"Database storage for DLQ messages"}),"\n",(0,t.jsx)(n.li,{children:"Admin interface for recovery"}),"\n",(0,t.jsx)(n.li,{children:"Comprehensive monitoring and alerting"}),"\n",(0,t.jsx)(n.li,{children:"Kafka and RabbitMQ implementations"}),"\n",(0,t.jsx)(n.li,{children:"Bulk replay capabilities"}),"\n",(0,t.jsx)(n.li,{children:"Error categorization service"}),"\n"]}),"\n",(0,t.jsx)(n.p,{children:"Remember: Good DLQ implementation is about failing gracefully and recovering intelligently!"})]})}function g(e={}){const{wrapper:n}={...(0,i.R)(),...e.components};return n?(0,t.jsx)(n,{...e,children:(0,t.jsx)(d,{...e})}):d(e)}}}]);