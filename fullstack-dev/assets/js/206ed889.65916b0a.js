"use strict";(self.webpackChunkdocs_site=self.webpackChunkdocs_site||[]).push([[8955],{5741:(e,n,r)=>{r.d(n,{R:()=>a,x:()=>o});var t=r(59729);const i={},s=t.createContext(i);function a(e){const n=t.useContext(s);return t.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:a(e.components),t.createElement(s.Provider,{value:n},e.children)}},45991:(e,n,r)=>{r.r(n),r.d(n,{assets:()=>l,contentTitle:()=>o,default:()=>m,frontMatter:()=>a,metadata:()=>t,toc:()=>c});const t=JSON.parse('{"id":"backend/retry-backoff-idempotency","title":"Retry with Backoff & Idempotency Guide","description":"Overview","source":"@site/docs/backend/retry-backoff-idempotency.md","sourceDirName":"backend","slug":"/backend/retry-backoff-idempotency","permalink":"/fullstack-dev/docs/backend/retry-backoff-idempotency","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/retry-backoff-idempotency.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Message Deduplication & Ordering Guide","permalink":"/fullstack-dev/docs/backend/message-deduplication-ordering"},"next":{"title":"Circuit Breakers & Rate Limiting Guide","permalink":"/fullstack-dev/docs/backend/circuit-breakers-rate-limiting"}}');var i=r(65813),s=r(5741);const a={},o="Retry with Backoff & Idempotency Guide",l={},c=[{value:"Overview",id:"overview",level:2},{value:"Table of Contents",id:"table-of-contents",level:2},{value:"Retry Fundamentals",id:"retry-fundamentals",level:2},{value:"When to Retry",id:"when-to-retry",level:3},{value:"Retry Decision Tree",id:"retry-decision-tree",level:3},{value:"Backoff Algorithms",id:"backoff-algorithms",level:2},{value:"Exponential Backoff",id:"exponential-backoff",level:3},{value:"Linear Backoff",id:"linear-backoff",level:3},{value:"Fibonacci Backoff",id:"fibonacci-backoff",level:3},{value:"Jitter Techniques",id:"jitter-techniques",level:2},{value:"Full Jitter",id:"full-jitter",level:3},{value:"Equal Jitter",id:"equal-jitter",level:3},{value:"Decorrelated Jitter",id:"decorrelated-jitter",level:3},{value:"Retry Policies",id:"retry-policies",level:2},{value:"Retry Service with Policies",id:"retry-service-with-policies",level:3},{value:"Predefined Retry Policies",id:"predefined-retry-policies",level:3},{value:"Idempotent Design Patterns",id:"idempotent-design-patterns",level:2},{value:"Idempotency Key Generator",id:"idempotency-key-generator",level:3},{value:"Idempotent Service Wrapper",id:"idempotent-service-wrapper",level:3},{value:"Idempotent API Controller",id:"idempotent-api-controller",level:3},{value:"HTTP Client with Retry",id:"http-client-with-retry",level:2},{value:"Axios Client with Retry",id:"axios-client-with-retry",level:3},{value:"Database Operations with Retry",id:"database-operations-with-retry",level:2},{value:"Database Service with Retry",id:"database-service-with-retry",level:3},{value:"Message Queue Retry",id:"message-queue-retry",level:2},{value:"Kafka Consumer with Retry",id:"kafka-consumer-with-retry",level:3},{value:"Monitoring and Metrics",id:"monitoring-and-metrics",level:2},{value:"Retry Metrics Service",id:"retry-metrics-service",level:3},{value:"Enhanced Retry Service with Metrics",id:"enhanced-retry-service-with-metrics",level:3},{value:"Best Practices",id:"best-practices",level:2},{value:"1. Retry Strategy",id:"1-retry-strategy",level:3},{value:"2. Backoff Configuration",id:"2-backoff-configuration",level:3},{value:"3. Idempotency Design",id:"3-idempotency-design",level:3},{value:"4. HTTP Retries",id:"4-http-retries",level:3},{value:"5. Monitoring",id:"5-monitoring",level:3},{value:"Summary",id:"summary",level:2}];function d(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",mermaid:"mermaid",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,s.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.header,{children:(0,i.jsx)(n.h1,{id:"retry-with-backoff--idempotency-guide",children:"Retry with Backoff & Idempotency Guide"})}),"\n",(0,i.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,i.jsx)(n.p,{children:"Retry mechanisms with exponential backoff and idempotency are fundamental patterns for building resilient distributed systems. This guide covers retry strategies, backoff algorithms, jitter techniques, idempotent design patterns, retry policies, and monitoring for reliable microservices communication."}),"\n",(0,i.jsx)(n.h2,{id:"table-of-contents",children:"Table of Contents"}),"\n",(0,i.jsxs)(n.ol,{children:["\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#retry-fundamentals",children:"Retry Fundamentals"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#backoff-algorithms",children:"Backoff Algorithms"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#jitter-techniques",children:"Jitter Techniques"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#retry-policies",children:"Retry Policies"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#idempotent-design-patterns",children:"Idempotent Design Patterns"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#http-client-with-retry",children:"HTTP Client with Retry"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#database-operations-with-retry",children:"Database Operations with Retry"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#message-queue-retry",children:"Message Queue Retry"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#monitoring-and-metrics",children:"Monitoring and Metrics"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#best-practices",children:"Best Practices"})}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"retry-fundamentals",children:"Retry Fundamentals"}),"\n",(0,i.jsx)(n.h3,{id:"when-to-retry",children:"When to Retry"}),"\n",(0,i.jsx)(n.mermaid,{value:"graph TB\n    A[Request Failed] --\x3e B{Error Type?}\n    B --\x3e|Network Error| C[Retry]\n    B --\x3e|Timeout| C\n    B --\x3e|503 Service Unavailable| C\n    B --\x3e|429 Rate Limited| C\n    B --\x3e|500 Server Error| C\n    B --\x3e|400 Bad Request| D[Don't Retry]\n    B --\x3e|401 Unauthorized| D\n    B --\x3e|404 Not Found| D\n    B --\x3e|Validation Error| D\n\n    C --\x3e E[Apply Backoff]\n    D --\x3e F[Return Error]\n\n    style C fill:#4CAF50\n    style D fill:#f44336"}),"\n",(0,i.jsx)(n.h3,{id:"retry-decision-tree",children:"Retry Decision Tree"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/retry-decision.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\n\nexport enum ErrorCategory {\n  RETRYABLE = 'RETRYABLE',\n  NON_RETRYABLE = 'NON_RETRYABLE',\n  RATE_LIMITED = 'RATE_LIMITED',\n}\n\nexport interface RetryDecision {\n  shouldRetry: boolean;\n  category: ErrorCategory;\n  suggestedDelayMs?: number;\n  reason: string;\n}\n\n@Injectable()\nexport class RetryDecisionService {\n  private readonly logger = new Logger(RetryDecisionService.name);\n\n  /**\n   * Determine if error is retryable\n   */\n  shouldRetry(error: any, attemptNumber: number, maxRetries: number): RetryDecision {\n    // Max retries exceeded\n    if (attemptNumber >= maxRetries) {\n      return {\n        shouldRetry: false,\n        category: ErrorCategory.NON_RETRYABLE,\n        reason: 'Max retries exceeded',\n      };\n    }\n\n    // HTTP status codes\n    if (error.response?.status) {\n      return this.handleHttpError(error.response.status, error);\n    }\n\n    // Network errors\n    if (this.isNetworkError(error)) {\n      return {\n        shouldRetry: true,\n        category: ErrorCategory.RETRYABLE,\n        reason: 'Network error - temporary issue',\n      };\n    }\n\n    // Database errors\n    if (this.isDatabaseError(error)) {\n      return {\n        shouldRetry: true,\n        category: ErrorCategory.RETRYABLE,\n        reason: 'Database error - connection or deadlock',\n      };\n    }\n\n    // Timeout errors\n    if (this.isTimeoutError(error)) {\n      return {\n        shouldRetry: true,\n        category: ErrorCategory.RETRYABLE,\n        reason: 'Timeout error - service slow or unavailable',\n      };\n    }\n\n    // Default: don't retry unknown errors\n    return {\n      shouldRetry: false,\n      category: ErrorCategory.NON_RETRYABLE,\n      reason: 'Unknown error type',\n    };\n  }\n\n  private handleHttpError(status: number, error: any): RetryDecision {\n    // 5xx errors - server errors (retryable)\n    if (status >= 500 && status < 600) {\n      return {\n        shouldRetry: true,\n        category: ErrorCategory.RETRYABLE,\n        reason: `Server error ${status}`,\n      };\n    }\n\n    // 429 - Rate limited (retryable with delay)\n    if (status === 429) {\n      const retryAfter = this.parseRetryAfter(error.response?.headers);\n      return {\n        shouldRetry: true,\n        category: ErrorCategory.RATE_LIMITED,\n        suggestedDelayMs: retryAfter,\n        reason: 'Rate limited',\n      };\n    }\n\n    // 408 - Request timeout (retryable)\n    if (status === 408) {\n      return {\n        shouldRetry: true,\n        category: ErrorCategory.RETRYABLE,\n        reason: 'Request timeout',\n      };\n    }\n\n    // 4xx errors (except 408, 429) - client errors (not retryable)\n    if (status >= 400 && status < 500) {\n      return {\n        shouldRetry: false,\n        category: ErrorCategory.NON_RETRYABLE,\n        reason: `Client error ${status}`,\n      };\n    }\n\n    return {\n      shouldRetry: false,\n      category: ErrorCategory.NON_RETRYABLE,\n      reason: `HTTP ${status}`,\n    };\n  }\n\n  private isNetworkError(error: any): boolean {\n    const networkErrors = ['ECONNREFUSED', 'ENOTFOUND', 'ETIMEDOUT', 'ECONNRESET', 'ENETUNREACH', 'EHOSTUNREACH'];\n\n    return (\n      networkErrors.some((code) => error.code === code) ||\n      error.message?.toLowerCase().includes('network') ||\n      error.message?.toLowerCase().includes('connection')\n    );\n  }\n\n  private isDatabaseError(error: any): boolean {\n    const dbErrors = ['connection', 'deadlock', 'lock timeout', 'transaction', 'could not connect'];\n\n    return dbErrors.some((pattern) => error.message?.toLowerCase().includes(pattern));\n  }\n\n  private isTimeoutError(error: any): boolean {\n    return (\n      error.code === 'ETIMEDOUT' ||\n      error.message?.toLowerCase().includes('timeout') ||\n      error.message?.toLowerCase().includes('timed out')\n    );\n  }\n\n  private parseRetryAfter(headers: any): number {\n    if (!headers || !headers['retry-after']) {\n      return 5000; // Default 5 seconds\n    }\n\n    const retryAfter = headers['retry-after'];\n\n    // If it's a number (seconds)\n    if (/^\\d+$/.test(retryAfter)) {\n      return parseInt(retryAfter) * 1000;\n    }\n\n    // If it's a date\n    const retryDate = new Date(retryAfter);\n    if (!isNaN(retryDate.getTime())) {\n      return Math.max(0, retryDate.getTime() - Date.now());\n    }\n\n    return 5000;\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"backoff-algorithms",children:"Backoff Algorithms"}),"\n",(0,i.jsx)(n.h3,{id:"exponential-backoff",children:"Exponential Backoff"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/backoff/exponential-backoff.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\n\nexport interface ExponentialBackoffConfig {\n  initialDelayMs: number;\n  maxDelayMs: number;\n  multiplier: number;\n  maxRetries: number;\n}\n\n@Injectable()\nexport class ExponentialBackoffService {\n  private readonly logger = new Logger(ExponentialBackoffService.name);\n\n  /**\n   * Calculate delay for exponential backoff\n   * Formula: min(initialDelay * (multiplier ^ attempt), maxDelay)\n   */\n  calculateDelay(attemptNumber: number, config: ExponentialBackoffConfig): number {\n    const delay = Math.min(config.initialDelayMs * Math.pow(config.multiplier, attemptNumber), config.maxDelayMs);\n\n    return Math.floor(delay);\n  }\n\n  /**\n   * Get delay schedule for all attempts\n   */\n  getDelaySchedule(config: ExponentialBackoffConfig): number[] {\n    const schedule: number[] = [];\n\n    for (let i = 0; i < config.maxRetries; i++) {\n      schedule.push(this.calculateDelay(i, config));\n    }\n\n    return schedule;\n  }\n\n  /**\n   * Visualize backoff schedule\n   */\n  visualizeSchedule(config: ExponentialBackoffConfig): void {\n    const schedule = this.getDelaySchedule(config);\n\n    this.logger.log('Exponential Backoff Schedule:');\n    schedule.forEach((delay, index) => {\n      this.logger.log(`  Attempt ${index + 1}: ${delay}ms (${(delay / 1000).toFixed(2)}s)`);\n    });\n\n    const totalDelay = schedule.reduce((sum, delay) => sum + delay, 0);\n    this.logger.log(`  Total retry time: ${(totalDelay / 1000).toFixed(2)}s`);\n  }\n}\n\n// Example delays with default config:\n// Attempt 1: 1000ms (1.00s)\n// Attempt 2: 2000ms (2.00s)\n// Attempt 3: 4000ms (4.00s)\n// Attempt 4: 8000ms (8.00s)\n// Attempt 5: 16000ms (16.00s)\n// Total: 31 seconds\n"})}),"\n",(0,i.jsx)(n.h3,{id:"linear-backoff",children:"Linear Backoff"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/backoff/linear-backoff.service.ts\nimport { Injectable } from '@nestjs/common';\n\nexport interface LinearBackoffConfig {\n  baseDelayMs: number;\n  incrementMs: number;\n  maxDelayMs: number;\n}\n\n@Injectable()\nexport class LinearBackoffService {\n  /**\n   * Calculate delay for linear backoff\n   * Formula: min(baseDelay + (increment * attempt), maxDelay)\n   */\n  calculateDelay(attemptNumber: number, config: LinearBackoffConfig): number {\n    const delay = Math.min(config.baseDelayMs + config.incrementMs * attemptNumber, config.maxDelayMs);\n\n    return Math.floor(delay);\n  }\n}\n\n// Example: baseDelay=1000ms, increment=2000ms\n// Attempt 0: 1000ms\n// Attempt 1: 3000ms\n// Attempt 2: 5000ms\n// Attempt 3: 7000ms\n"})}),"\n",(0,i.jsx)(n.h3,{id:"fibonacci-backoff",children:"Fibonacci Backoff"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/backoff/fibonacci-backoff.service.ts\nimport { Injectable } from '@nestjs/common';\n\nexport interface FibonacciBackoffConfig {\n  baseDelayMs: number;\n  maxDelayMs: number;\n}\n\n@Injectable()\nexport class FibonacciBackoffService {\n  private fibCache = new Map<number, number>();\n\n  /**\n   * Calculate delay using Fibonacci sequence\n   * Formula: fibonacci(attempt) * baseDelay\n   */\n  calculateDelay(attemptNumber: number, config: FibonacciBackoffConfig): number {\n    const fibNumber = this.fibonacci(attemptNumber);\n    const delay = Math.min(fibNumber * config.baseDelayMs, config.maxDelayMs);\n\n    return Math.floor(delay);\n  }\n\n  private fibonacci(n: number): number {\n    if (n <= 1) return 1;\n\n    if (this.fibCache.has(n)) {\n      return this.fibCache.get(n);\n    }\n\n    const result = this.fibonacci(n - 1) + this.fibonacci(n - 2);\n    this.fibCache.set(n, result);\n    return result;\n  }\n}\n\n// Example: baseDelay=1000ms\n// Attempt 0: 1000ms (1 * 1000)\n// Attempt 1: 1000ms (1 * 1000)\n// Attempt 2: 2000ms (2 * 1000)\n// Attempt 3: 3000ms (3 * 1000)\n// Attempt 4: 5000ms (5 * 1000)\n// Attempt 5: 8000ms (8 * 1000)\n"})}),"\n",(0,i.jsx)(n.h2,{id:"jitter-techniques",children:"Jitter Techniques"}),"\n",(0,i.jsx)(n.h3,{id:"full-jitter",children:"Full Jitter"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/jitter/full-jitter.service.ts\nimport { Injectable } from '@nestjs/common';\n\n@Injectable()\nexport class FullJitterService {\n  /**\n   * Full jitter: random value between 0 and calculated delay\n   * Formula: random(0, delay)\n   * Prevents thundering herd problem\n   */\n  applyJitter(delay: number): number {\n    return Math.floor(Math.random() * delay);\n  }\n}\n\n// Example: delay=8000ms\n// Possible results: 0ms to 8000ms (uniformly distributed)\n"})}),"\n",(0,i.jsx)(n.h3,{id:"equal-jitter",children:"Equal Jitter"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/jitter/equal-jitter.service.ts\nimport { Injectable } from '@nestjs/common';\n\n@Injectable()\nexport class EqualJitterService {\n  /**\n   * Equal jitter: half the delay + random half\n   * Formula: delay/2 + random(0, delay/2)\n   * More predictable than full jitter\n   */\n  applyJitter(delay: number): number {\n    const half = delay / 2;\n    return Math.floor(half + Math.random() * half);\n  }\n}\n\n// Example: delay=8000ms\n// Possible results: 4000ms to 8000ms\n"})}),"\n",(0,i.jsx)(n.h3,{id:"decorrelated-jitter",children:"Decorrelated Jitter"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/jitter/decorrelated-jitter.service.ts\nimport { Injectable } from '@nestjs/common';\n\n@Injectable()\nexport class DecorrelatedJitterService {\n  private lastDelay = 0;\n\n  /**\n   * Decorrelated jitter: based on previous delay\n   * Formula: random(baseDelay, lastDelay * 3)\n   * Smooth increase in delays\n   */\n  applyJitter(baseDelay: number, maxDelay: number): number {\n    if (this.lastDelay === 0) {\n      this.lastDelay = baseDelay;\n      return baseDelay;\n    }\n\n    const upperBound = Math.min(this.lastDelay * 3, maxDelay);\n    this.lastDelay = Math.floor(baseDelay + Math.random() * (upperBound - baseDelay));\n\n    return this.lastDelay;\n  }\n\n  reset(): void {\n    this.lastDelay = 0;\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"retry-policies",children:"Retry Policies"}),"\n",(0,i.jsx)(n.h3,{id:"retry-service-with-policies",children:"Retry Service with Policies"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/retry.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { RetryDecisionService } from './retry-decision.service';\nimport { ExponentialBackoffService, ExponentialBackoffConfig } from './backoff/exponential-backoff.service';\nimport { FullJitterService } from './jitter/full-jitter.service';\n\nexport interface RetryPolicy {\n  maxRetries: number;\n  backoff: ExponentialBackoffConfig;\n  jitter: 'none' | 'full' | 'equal' | 'decorrelated';\n  retryableErrors?: string[];\n  onRetry?: (error: Error, attemptNumber: number, delay: number) => void;\n  onFailure?: (error: Error, totalAttempts: number) => void;\n}\n\n@Injectable()\nexport class RetryService {\n  private readonly logger = new Logger(RetryService.name);\n\n  constructor(\n    private retryDecision: RetryDecisionService,\n    private exponentialBackoff: ExponentialBackoffService,\n    private fullJitter: FullJitterService,\n  ) {}\n\n  /**\n   * Execute operation with retry policy\n   */\n  async executeWithRetry<T>(\n    operation: () => Promise<T>,\n    policy: RetryPolicy,\n    operationName: string = 'operation',\n  ): Promise<T> {\n    let lastError: Error;\n\n    for (let attempt = 0; attempt <= policy.maxRetries; attempt++) {\n      try {\n        const result = await operation();\n\n        if (attempt > 0) {\n          this.logger.log(`${operationName} succeeded after ${attempt} retries`);\n        }\n\n        return result;\n      } catch (error) {\n        lastError = error;\n\n        // Check if should retry\n        const decision = this.retryDecision.shouldRetry(error, attempt, policy.maxRetries);\n\n        if (!decision.shouldRetry) {\n          this.logger.error(`${operationName} failed and will not retry: ${decision.reason}`);\n\n          if (policy.onFailure) {\n            policy.onFailure(error, attempt + 1);\n          }\n\n          throw error;\n        }\n\n        // Calculate delay\n        let delay = decision.suggestedDelayMs || this.exponentialBackoff.calculateDelay(attempt, policy.backoff);\n\n        // Apply jitter\n        if (policy.jitter === 'full') {\n          delay = this.fullJitter.applyJitter(delay);\n        }\n\n        this.logger.warn(\n          `${operationName} failed (attempt ${attempt + 1}/${policy.maxRetries + 1}). Retrying in ${delay}ms. Error: ${\n            error.message\n          }`,\n        );\n\n        // Call retry callback\n        if (policy.onRetry) {\n          policy.onRetry(error, attempt + 1, delay);\n        }\n\n        // Wait before retry\n        await this.sleep(delay);\n      }\n    }\n\n    // All retries exhausted\n    this.logger.error(`${operationName} failed after ${policy.maxRetries + 1} attempts`);\n\n    if (policy.onFailure) {\n      policy.onFailure(lastError, policy.maxRetries + 1);\n    }\n\n    throw lastError;\n  }\n\n  /**\n   * Execute with custom retry condition\n   */\n  async executeWithCustomRetry<T>(\n    operation: () => Promise<T>,\n    shouldRetry: (error: Error, attempt: number) => boolean,\n    maxRetries: number,\n    calculateDelay: (attempt: number) => number,\n  ): Promise<T> {\n    let lastError: Error;\n\n    for (let attempt = 0; attempt <= maxRetries; attempt++) {\n      try {\n        return await operation();\n      } catch (error) {\n        lastError = error;\n\n        if (attempt === maxRetries || !shouldRetry(error, attempt)) {\n          throw error;\n        }\n\n        const delay = calculateDelay(attempt);\n        await this.sleep(delay);\n      }\n    }\n\n    throw lastError;\n  }\n\n  private sleep(ms: number): Promise<void> {\n    return new Promise((resolve) => setTimeout(resolve, ms));\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"predefined-retry-policies",children:"Predefined Retry Policies"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// config/retry-policies.config.ts\nimport { RetryPolicy } from '../services/retry.service';\n\nexport const RETRY_POLICIES: Record<string, RetryPolicy> = {\n  // Aggressive retry for critical operations\n  CRITICAL: {\n    maxRetries: 5,\n    backoff: {\n      initialDelayMs: 1000,\n      maxDelayMs: 30000,\n      multiplier: 2,\n      maxRetries: 5,\n    },\n    jitter: 'full',\n  },\n\n  // Standard retry for normal operations\n  STANDARD: {\n    maxRetries: 3,\n    backoff: {\n      initialDelayMs: 1000,\n      maxDelayMs: 10000,\n      multiplier: 2,\n      maxRetries: 3,\n    },\n    jitter: 'equal',\n  },\n\n  // Conservative retry for expensive operations\n  CONSERVATIVE: {\n    maxRetries: 2,\n    backoff: {\n      initialDelayMs: 2000,\n      maxDelayMs: 20000,\n      multiplier: 3,\n      maxRetries: 2,\n    },\n    jitter: 'full',\n  },\n\n  // No retry - fail fast\n  NO_RETRY: {\n    maxRetries: 0,\n    backoff: {\n      initialDelayMs: 0,\n      maxDelayMs: 0,\n      multiplier: 1,\n      maxRetries: 0,\n    },\n    jitter: 'none',\n  },\n};\n"})}),"\n",(0,i.jsx)(n.h2,{id:"idempotent-design-patterns",children:"Idempotent Design Patterns"}),"\n",(0,i.jsx)(n.h3,{id:"idempotency-key-generator",children:"Idempotency Key Generator"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/idempotency-key.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { createHash } from 'crypto';\n\n@Injectable()\nexport class IdempotencyKeyService {\n  /**\n   * Generate idempotency key from request data\n   */\n  generateKey(userId: string, operation: string, data: any): string {\n    const payload = JSON.stringify({\n      userId,\n      operation,\n      data,\n    });\n\n    return createHash('sha256').update(payload).digest('hex');\n  }\n\n  /**\n   * Generate UUID-based key\n   */\n  generateUuidKey(): string {\n    return `${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n  }\n\n  /**\n   * Generate key from client-provided value and server data\n   */\n  generateHybridKey(clientKey: string, serverData: any): string {\n    const payload = JSON.stringify({\n      clientKey,\n      serverData,\n      timestamp: Date.now(),\n    });\n\n    return createHash('sha256').update(payload).digest('hex');\n  }\n\n  /**\n   * Validate idempotency key format\n   */\n  isValidKey(key: string): boolean {\n    // Check if key is a valid SHA-256 hash (64 hex characters)\n    return /^[a-f0-9]{64}$/i.test(key);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"idempotent-service-wrapper",children:"Idempotent Service Wrapper"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// decorators/idempotent.decorator.ts\nimport { SetMetadata } from '@nestjs/common';\n\nexport const IDEMPOTENT_KEY = 'idempotent';\n\nexport interface IdempotentOptions {\n  keyExtractor?: (args: any[]) => string;\n  ttlSeconds?: number;\n}\n\nexport const Idempotent = (options?: IdempotentOptions) => SetMetadata(IDEMPOTENT_KEY, options || {});\n\n// interceptors/idempotent.interceptor.ts\nimport { Injectable, NestInterceptor, ExecutionContext, CallHandler, ConflictException } from '@nestjs/common';\nimport { Reflector } from '@nestjs/core';\nimport { Observable, of } from 'rxjs';\nimport { tap } from 'rxjs/operators';\nimport { IDEMPOTENT_KEY, IdempotentOptions } from '../decorators/idempotent.decorator';\nimport { Redis } from 'ioredis';\nimport { InjectRedis } from '@nestjs-modules/ioredis';\n\n@Injectable()\nexport class IdempotentInterceptor implements NestInterceptor {\n  constructor(private reflector: Reflector, @InjectRedis() private readonly redis: Redis) {}\n\n  async intercept(context: ExecutionContext, next: CallHandler): Promise<Observable<any>> {\n    const options = this.reflector.get<IdempotentOptions>(IDEMPOTENT_KEY, context.getHandler());\n\n    if (!options) {\n      return next.handle();\n    }\n\n    const request = context.switchToHttp().getRequest();\n    const idempotencyKey = request.headers['idempotency-key'];\n\n    if (!idempotencyKey) {\n      return next.handle();\n    }\n\n    const cacheKey = `idempotent:${idempotencyKey}`;\n\n    // Check if operation already executed\n    const cached = await this.redis.get(cacheKey);\n\n    if (cached) {\n      const result = JSON.parse(cached);\n\n      if (result.status === 'processing') {\n        throw new ConflictException('Operation is currently being processed');\n      }\n\n      if (result.status === 'completed') {\n        return of(result.data);\n      }\n    }\n\n    // Mark as processing\n    await this.redis.setex(cacheKey, options.ttlSeconds || 3600, JSON.stringify({ status: 'processing' }));\n\n    return next.handle().pipe(\n      tap(async (data) => {\n        // Cache result\n        await this.redis.setex(cacheKey, options.ttlSeconds || 3600, JSON.stringify({ status: 'completed', data }));\n      }),\n    );\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"idempotent-api-controller",children:"Idempotent API Controller"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// controllers/idempotent-api.controller.ts\nimport { Controller, Post, Body, Headers, UseInterceptors, BadRequestException } from '@nestjs/common';\nimport { IdempotentInterceptor } from '../interceptors/idempotent.interceptor';\nimport { Idempotent } from '../decorators/idempotent.decorator';\n\n@Controller('api/orders')\n@UseInterceptors(IdempotentInterceptor)\nexport class IdempotentApiController {\n  /**\n   * Create order with idempotency key\n   */\n  @Post()\n  @Idempotent({ ttlSeconds: 3600 })\n  async createOrder(@Body() orderData: any, @Headers('idempotency-key') idempotencyKey: string) {\n    if (!idempotencyKey) {\n      throw new BadRequestException('Idempotency-Key header is required');\n    }\n\n    // Process order (will be cached if successful)\n    return {\n      orderId: '12345',\n      status: 'created',\n      total: orderData.total,\n    };\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"http-client-with-retry",children:"HTTP Client with Retry"}),"\n",(0,i.jsx)(n.h3,{id:"axios-client-with-retry",children:"Axios Client with Retry"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/http-client-with-retry.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { HttpService } from '@nestjs/axios';\nimport { firstValueFrom } from 'rxjs';\nimport { AxiosRequestConfig, AxiosResponse } from 'axios';\nimport { RetryService } from './retry.service';\nimport { RETRY_POLICIES } from '../config/retry-policies.config';\n\n@Injectable()\nexport class HttpClientWithRetryService {\n  private readonly logger = new Logger(HttpClientWithRetryService.name);\n\n  constructor(private httpService: HttpService, private retryService: RetryService) {}\n\n  /**\n   * GET request with retry\n   */\n  async get<T>(url: string, config?: AxiosRequestConfig, retryPolicyName: string = 'STANDARD'): Promise<T> {\n    return this.retryService.executeWithRetry(\n      async () => {\n        const response = await firstValueFrom(this.httpService.get<T>(url, config));\n        return response.data;\n      },\n      {\n        ...RETRY_POLICIES[retryPolicyName],\n        onRetry: (error, attempt, delay) => {\n          this.logger.warn(`GET ${url} failed (attempt ${attempt}). Retrying in ${delay}ms`);\n        },\n      },\n      `GET ${url}`,\n    );\n  }\n\n  /**\n   * POST request with retry and idempotency\n   */\n  async post<T>(\n    url: string,\n    data: any,\n    idempotencyKey: string,\n    config?: AxiosRequestConfig,\n    retryPolicyName: string = 'STANDARD',\n  ): Promise<T> {\n    const headers = {\n      ...config?.headers,\n      'Idempotency-Key': idempotencyKey,\n    };\n\n    return this.retryService.executeWithRetry(\n      async () => {\n        const response = await firstValueFrom(this.httpService.post<T>(url, data, { ...config, headers }));\n        return response.data;\n      },\n      RETRY_POLICIES[retryPolicyName],\n      `POST ${url}`,\n    );\n  }\n\n  /**\n   * PUT request with retry (idempotent by nature)\n   */\n  async put<T>(url: string, data: any, config?: AxiosRequestConfig, retryPolicyName: string = 'STANDARD'): Promise<T> {\n    return this.retryService.executeWithRetry(\n      async () => {\n        const response = await firstValueFrom(this.httpService.put<T>(url, data, config));\n        return response.data;\n      },\n      RETRY_POLICIES[retryPolicyName],\n      `PUT ${url}`,\n    );\n  }\n\n  /**\n   * DELETE request with retry (idempotent by nature)\n   */\n  async delete<T>(url: string, config?: AxiosRequestConfig, retryPolicyName: string = 'CONSERVATIVE'): Promise<T> {\n    return this.retryService.executeWithRetry(\n      async () => {\n        const response = await firstValueFrom(this.httpService.delete<T>(url, config));\n        return response.data;\n      },\n      RETRY_POLICIES[retryPolicyName],\n      `DELETE ${url}`,\n    );\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"database-operations-with-retry",children:"Database Operations with Retry"}),"\n",(0,i.jsx)(n.h3,{id:"database-service-with-retry",children:"Database Service with Retry"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/database-with-retry.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository, QueryFailedError } from 'typeorm';\nimport { RetryService } from './retry.service';\n\n@Injectable()\nexport class DatabaseWithRetryService<T> {\n  private readonly logger = new Logger(DatabaseWithRetryService.name);\n\n  constructor(private retryService: RetryService, private repository: Repository<T>, private entityName: string) {}\n\n  /**\n   * Find with retry on deadlock or connection errors\n   */\n  async find(options?: any): Promise<T[]> {\n    return this.retryService.executeWithRetry(\n      () => this.repository.find(options),\n      {\n        maxRetries: 3,\n        backoff: {\n          initialDelayMs: 500,\n          maxDelayMs: 5000,\n          multiplier: 2,\n          maxRetries: 3,\n        },\n        jitter: 'full',\n        onRetry: (error, attempt) => {\n          this.logger.warn(`Database find failed for ${this.entityName} (attempt ${attempt}): ${error.message}`);\n        },\n      },\n      `DB find ${this.entityName}`,\n    );\n  }\n\n  /**\n   * Save with retry and optimistic locking\n   */\n  async save(entity: T): Promise<T> {\n    return this.retryService.executeWithRetry(\n      () => this.repository.save(entity),\n      {\n        maxRetries: 5,\n        backoff: {\n          initialDelayMs: 100,\n          maxDelayMs: 2000,\n          multiplier: 2,\n          maxRetries: 5,\n        },\n        jitter: 'full',\n        onRetry: (error, attempt) => {\n          if (error instanceof QueryFailedError) {\n            this.logger.warn(`Database save failed for ${this.entityName} - possible deadlock (attempt ${attempt})`);\n          }\n        },\n      },\n      `DB save ${this.entityName}`,\n    );\n  }\n\n  /**\n   * Transaction with retry\n   */\n  async executeInTransaction<R>(operation: (manager: any) => Promise<R>): Promise<R> {\n    return this.retryService.executeWithRetry(\n      () => this.repository.manager.transaction(operation),\n      {\n        maxRetries: 3,\n        backoff: {\n          initialDelayMs: 200,\n          maxDelayMs: 3000,\n          multiplier: 2,\n          maxRetries: 3,\n        },\n        jitter: 'full',\n      },\n      `DB transaction ${this.entityName}`,\n    );\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"message-queue-retry",children:"Message Queue Retry"}),"\n",(0,i.jsx)(n.h3,{id:"kafka-consumer-with-retry",children:"Kafka Consumer with Retry"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// consumers/kafka-retry.consumer.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { Kafka, Consumer, EachMessagePayload } from 'kafkajs';\nimport { ConfigService } from '@nestjs/config';\nimport { RetryService } from '../services/retry.service';\nimport { RETRY_POLICIES } from '../config/retry-policies.config';\n\n@Injectable()\nexport class KafkaRetryConsumer implements OnModuleInit {\n  private readonly logger = new Logger(KafkaRetryConsumer.name);\n  private consumer: Consumer;\n  private readonly kafka: Kafka;\n\n  constructor(private configService: ConfigService, private retryService: RetryService) {\n    this.kafka = new Kafka({\n      clientId: 'retry-consumer',\n      brokers: this.configService.get('KAFKA_BROKERS').split(','),\n    });\n  }\n\n  async onModuleInit() {\n    this.consumer = this.kafka.consumer({\n      groupId: 'retry-consumer-group',\n    });\n\n    await this.consumer.connect();\n    await this.consumer.subscribe({\n      topics: ['orders'],\n      fromBeginning: false,\n    });\n\n    await this.consumer.run({\n      eachMessage: async (payload) => this.handleMessage(payload),\n    });\n\n    this.logger.log('Kafka retry consumer started');\n  }\n\n  private async handleMessage(payload: EachMessagePayload): Promise<void> {\n    const { topic, partition, message } = payload;\n    const messageId = message.key?.toString();\n\n    await this.retryService.executeWithRetry(\n      async () => {\n        const parsedMessage = JSON.parse(message.value.toString());\n        await this.processMessage(parsedMessage);\n      },\n      {\n        ...RETRY_POLICIES.CRITICAL,\n        onRetry: (error, attempt, delay) => {\n          this.logger.warn(\n            `Message ${messageId} processing failed (attempt ${attempt}). Retrying in ${delay}ms. Error: ${error.message}`,\n          );\n        },\n        onFailure: async (error, attempts) => {\n          this.logger.error(`Message ${messageId} failed after ${attempts} attempts. Sending to DLQ.`);\n          await this.sendToDLQ(topic, message, error);\n        },\n      },\n      `Kafka message ${messageId}`,\n    );\n  }\n\n  private async processMessage(message: any): Promise<void> {\n    this.logger.log(`Processing message: ${JSON.stringify(message)}`);\n    // Business logic here\n  }\n\n  private async sendToDLQ(topic: string, message: any, error: Error): Promise<void> {\n    // Send to DLQ implementation\n    this.logger.error(`Sending to DLQ: ${topic}.dlq`);\n  }\n\n  async onModuleDestroy() {\n    await this.consumer.disconnect();\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"monitoring-and-metrics",children:"Monitoring and Metrics"}),"\n",(0,i.jsx)(n.h3,{id:"retry-metrics-service",children:"Retry Metrics Service"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/retry-metrics.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { Counter, Histogram, Gauge } from 'prom-client';\n\n@Injectable()\nexport class RetryMetricsService {\n  private readonly retryCounter = new Counter({\n    name: 'retry_attempts_total',\n    help: 'Total retry attempts',\n    labelNames: ['operation', 'attempt_number', 'result'],\n  });\n\n  private readonly retryDurationHistogram = new Histogram({\n    name: 'retry_duration_seconds',\n    help: 'Time spent in retry loops',\n    labelNames: ['operation', 'result'],\n    buckets: [0.1, 0.5, 1, 2, 5, 10, 30, 60],\n  });\n\n  private readonly activeRetriesGauge = new Gauge({\n    name: 'retry_active_operations',\n    help: 'Number of operations currently retrying',\n    labelNames: ['operation'],\n  });\n\n  private readonly backoffDelayHistogram = new Histogram({\n    name: 'retry_backoff_delay_seconds',\n    help: 'Distribution of backoff delays',\n    labelNames: ['operation'],\n    buckets: [0.1, 0.5, 1, 2, 5, 10, 30],\n  });\n\n  recordRetryAttempt(operation: string, attemptNumber: number, result: 'success' | 'failure'): void {\n    this.retryCounter.inc({\n      operation,\n      attempt_number: attemptNumber.toString(),\n      result,\n    });\n  }\n\n  recordRetryDuration(operation: string, durationSeconds: number, result: 'success' | 'failure'): void {\n    this.retryDurationHistogram.observe({ operation, result }, durationSeconds);\n  }\n\n  incrementActiveRetries(operation: string): void {\n    this.activeRetriesGauge.inc({ operation });\n  }\n\n  decrementActiveRetries(operation: string): void {\n    this.activeRetriesGauge.dec({ operation });\n  }\n\n  recordBackoffDelay(operation: string, delaySeconds: number): void {\n    this.backoffDelayHistogram.observe({ operation }, delaySeconds);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"enhanced-retry-service-with-metrics",children:"Enhanced Retry Service with Metrics"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/retry-with-metrics.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { RetryService, RetryPolicy } from './retry.service';\nimport { RetryMetricsService } from './retry-metrics.service';\n\n@Injectable()\nexport class RetryWithMetricsService extends RetryService {\n  constructor(retryDecision: any, exponentialBackoff: any, fullJitter: any, private retryMetrics: RetryMetricsService) {\n    super(retryDecision, exponentialBackoff, fullJitter);\n  }\n\n  async executeWithRetry<T>(\n    operation: () => Promise<T>,\n    policy: RetryPolicy,\n    operationName: string = 'operation',\n  ): Promise<T> {\n    const startTime = Date.now();\n    this.retryMetrics.incrementActiveRetries(operationName);\n\n    try {\n      const result = await super.executeWithRetry(\n        operation,\n        {\n          ...policy,\n          onRetry: (error, attempt, delay) => {\n            this.retryMetrics.recordRetryAttempt(operationName, attempt, 'failure');\n            this.retryMetrics.recordBackoffDelay(operationName, delay / 1000);\n\n            if (policy.onRetry) {\n              policy.onRetry(error, attempt, delay);\n            }\n          },\n        },\n        operationName,\n      );\n\n      const duration = (Date.now() - startTime) / 1000;\n      this.retryMetrics.recordRetryDuration(operationName, duration, 'success');\n      this.retryMetrics.recordRetryAttempt(operationName, 0, 'success');\n\n      return result;\n    } catch (error) {\n      const duration = (Date.now() - startTime) / 1000;\n      this.retryMetrics.recordRetryDuration(operationName, duration, 'failure');\n      throw error;\n    } finally {\n      this.retryMetrics.decrementActiveRetries(operationName);\n    }\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"best-practices",children:"Best Practices"}),"\n",(0,i.jsx)(n.h3,{id:"1-retry-strategy",children:"1. Retry Strategy"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Use exponential backoff with jitter"}),"\n",(0,i.jsx)(n.li,{children:"Set maximum retry limits"}),"\n",(0,i.jsx)(n.li,{children:"Classify errors (retryable vs non-retryable)"}),"\n",(0,i.jsx)(n.li,{children:"Add timeout for total retry duration"}),"\n",(0,i.jsx)(n.li,{children:"Log all retry attempts"}),"\n",(0,i.jsx)(n.li,{children:"Monitor retry metrics"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["\u274c ",(0,i.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Retry indefinitely"}),"\n",(0,i.jsx)(n.li,{children:"Use constant delays (causes thundering herd)"}),"\n",(0,i.jsx)(n.li,{children:"Retry non-retryable errors (400, 401, 404)"}),"\n",(0,i.jsx)(n.li,{children:"Skip error classification"}),"\n",(0,i.jsx)(n.li,{children:"Hide retry attempts from monitoring"}),"\n",(0,i.jsx)(n.li,{children:"Retry without backoff"}),"\n"]}),"\n",(0,i.jsx)(n.h3,{id:"2-backoff-configuration",children:"2. Backoff Configuration"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Start with reasonable initial delay (1-2 seconds)"}),"\n",(0,i.jsx)(n.li,{children:"Cap maximum delay (30-60 seconds)"}),"\n",(0,i.jsx)(n.li,{children:"Use multiplier of 2-3"}),"\n",(0,i.jsx)(n.li,{children:"Apply jitter to prevent thundering herd"}),"\n",(0,i.jsx)(n.li,{children:"Adjust based on operation criticality"}),"\n",(0,i.jsx)(n.li,{children:"Test backoff schedules"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["\u274c ",(0,i.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Use too short initial delay (<100ms)"}),"\n",(0,i.jsx)(n.li,{children:"Use too long maximum delay (>2 minutes)"}),"\n",(0,i.jsx)(n.li,{children:"Skip jitter implementation"}),"\n",(0,i.jsx)(n.li,{children:"Use same config for all operations"}),"\n",(0,i.jsx)(n.li,{children:"Forget to test retry scenarios"}),"\n",(0,i.jsx)(n.li,{children:"Deploy without testing backoff timing"}),"\n"]}),"\n",(0,i.jsx)(n.h3,{id:"3-idempotency-design",children:"3. Idempotency Design"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Design idempotent operations"}),"\n",(0,i.jsx)(n.li,{children:"Use unique idempotency keys"}),"\n",(0,i.jsx)(n.li,{children:"Store operation results with TTL"}),"\n",(0,i.jsx)(n.li,{children:"Validate idempotency key format"}),"\n",(0,i.jsx)(n.li,{children:"Handle concurrent requests"}),"\n",(0,i.jsx)(n.li,{children:"Return cached results for duplicates"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["\u274c ",(0,i.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Assume operations are idempotent"}),"\n",(0,i.jsx)(n.li,{children:"Generate predictable keys"}),"\n",(0,i.jsx)(n.li,{children:"Store results forever"}),"\n",(0,i.jsx)(n.li,{children:"Skip idempotency checks"}),"\n",(0,i.jsx)(n.li,{children:"Ignore concurrent request handling"}),"\n",(0,i.jsx)(n.li,{children:"Process duplicates multiple times"}),"\n"]}),"\n",(0,i.jsx)(n.h3,{id:"4-http-retries",children:"4. HTTP Retries"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Retry only idempotent methods (GET, PUT, DELETE)"}),"\n",(0,i.jsx)(n.li,{children:"Use idempotency keys for POST"}),"\n",(0,i.jsx)(n.li,{children:"Respect Retry-After headers"}),"\n",(0,i.jsx)(n.li,{children:"Set request timeouts"}),"\n",(0,i.jsx)(n.li,{children:"Use connection pooling"}),"\n",(0,i.jsx)(n.li,{children:"Monitor retry rates"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["\u274c ",(0,i.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Retry POST without idempotency key"}),"\n",(0,i.jsx)(n.li,{children:"Ignore rate limit headers"}),"\n",(0,i.jsx)(n.li,{children:"Skip timeout configuration"}),"\n",(0,i.jsx)(n.li,{children:"Create new connections per retry"}),"\n",(0,i.jsx)(n.li,{children:"Forget to close connections"}),"\n",(0,i.jsx)(n.li,{children:"Deploy without retry monitoring"}),"\n"]}),"\n",(0,i.jsx)(n.h3,{id:"5-monitoring",children:"5. Monitoring"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Track retry attempts per operation"}),"\n",(0,i.jsx)(n.li,{children:"Monitor backoff delays"}),"\n",(0,i.jsx)(n.li,{children:"Alert on high retry rates"}),"\n",(0,i.jsx)(n.li,{children:"Dashboard retry success rates"}),"\n",(0,i.jsx)(n.li,{children:"Track total retry duration"}),"\n",(0,i.jsx)(n.li,{children:"Log retry reasons"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["\u274c ",(0,i.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Deploy without retry metrics"}),"\n",(0,i.jsx)(n.li,{children:"Ignore retry failure patterns"}),"\n",(0,i.jsx)(n.li,{children:"Skip alerting on retry storms"}),"\n",(0,i.jsx)(n.li,{children:"Hide retry attempts"}),"\n",(0,i.jsx)(n.li,{children:"Forget to track retry costs"}),"\n",(0,i.jsx)(n.li,{children:"React only to complete failures"}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"summary",children:"Summary"}),"\n",(0,i.jsx)(n.p,{children:"Effective retry with backoff and idempotency provides:"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Resilience"})," - Automatic recovery from transient failures"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Reliability"})," - Safe retry operations without duplicates"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Performance"})," - Optimal retry timing with backoff"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Stability"})," - Prevent thundering herd with jitter"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Observability"})," - Complete retry monitoring"]}),"\n"]}),"\n",(0,i.jsx)(n.p,{children:"Key implementation points:"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Retry decision service (classify retryable errors)"}),"\n",(0,i.jsx)(n.li,{children:"Exponential backoff with configurable parameters"}),"\n",(0,i.jsx)(n.li,{children:"Full/equal/decorrelated jitter techniques"}),"\n",(0,i.jsx)(n.li,{children:"Retry policies for different operation types"}),"\n",(0,i.jsx)(n.li,{children:"Idempotency key generation and validation"}),"\n",(0,i.jsx)(n.li,{children:"Idempotent interceptor for API endpoints"}),"\n",(0,i.jsx)(n.li,{children:"HTTP client with retry and idempotency"}),"\n",(0,i.jsx)(n.li,{children:"Database operations with retry on deadlocks"}),"\n",(0,i.jsx)(n.li,{children:"Kafka consumer with retry and DLQ fallback"}),"\n",(0,i.jsx)(n.li,{children:"Comprehensive Prometheus metrics"}),"\n",(0,i.jsx)(n.li,{children:"Retry visualizations and schedules"}),"\n"]}),"\n",(0,i.jsx)(n.p,{children:"Remember: Always design for at-least-once delivery with idempotent operations - it's more reliable than trying to achieve exactly-once!"})]})}function m(e={}){const{wrapper:n}={...(0,s.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);