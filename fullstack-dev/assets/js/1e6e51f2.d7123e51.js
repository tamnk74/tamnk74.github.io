"use strict";(self.webpackChunkdocs_site=self.webpackChunkdocs_site||[]).push([[55832],{5741:(e,n,t)=>{t.d(n,{R:()=>a,x:()=>o});var s=t(59729);const i={},r=s.createContext(i);function a(e){const n=s.useContext(r);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:a(e.components),s.createElement(r.Provider,{value:n},e.children)}},12802:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>c,contentTitle:()=>o,default:()=>u,frontMatter:()=>a,metadata:()=>s,toc:()=>l});const s=JSON.parse('{"id":"security-practices/security-implementation/security-awareness/a06-insecure-design","title":"A06: Insecure Design - Real Scenarios","description":"Overview","source":"@site/docs/security-practices/security-implementation/security-awareness/a06-insecure-design.md","sourceDirName":"security-practices/security-implementation/security-awareness","slug":"/security-practices/security-implementation/security-awareness/a06-insecure-design","permalink":"/fullstack-dev/docs/security-practices/security-implementation/security-awareness/a06-insecure-design","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/security-practices/security-implementation/security-awareness/a06-insecure-design.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"A03: Injection - Real Scenarios","permalink":"/fullstack-dev/docs/security-practices/security-implementation/security-awareness/a05-injection"},"next":{"title":"A07: Identification and Authentication Failures - Real Scenarios","permalink":"/fullstack-dev/docs/security-practices/security-implementation/security-awareness/a07-authentication-failures"}}');var i=t(65813),r=t(5741);const a={},o="A06: Insecure Design - Real Scenarios",c={},l=[{value:"Overview",id:"overview",level:2},{value:"Key Concepts",id:"key-concepts",level:2},{value:"Vulnerable Scenario 1: Missing Rate Limiting on Critical Functions",id:"vulnerable-scenario-1-missing-rate-limiting-on-critical-functions",level:2},{value:"Vulnerable Code (Next.js API)",id:"vulnerable-code-nextjs-api",level:3},{value:"Attack Scenario",id:"attack-scenario",level:3},{value:"Secure Solution",id:"secure-solution",level:3},{value:"Vulnerable Scenario 2: Price Manipulation in E-commerce",id:"vulnerable-scenario-2-price-manipulation-in-e-commerce",level:2},{value:"Vulnerable Code (Next.js Checkout)",id:"vulnerable-code-nextjs-checkout",level:3},{value:"Attack Example",id:"attack-example",level:3},{value:"Secure Solution",id:"secure-solution-1",level:3},{value:"Vulnerable Scenario 3: Insufficient Account Lockout",id:"vulnerable-scenario-3-insufficient-account-lockout",level:2},{value:"Vulnerable Code (Authentication)",id:"vulnerable-code-authentication",level:3},{value:"Attack Scenario",id:"attack-scenario-1",level:3},{value:"Secure Solution",id:"secure-solution-2",level:3},{value:"Prevention Checklist",id:"prevention-checklist",level:2},{value:"Testing Methods",id:"testing-methods",level:2},{value:"Real-World Impact Examples",id:"real-world-impact-examples",level:2},{value:"References",id:"references",level:2}];function d(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",input:"input",li:"li",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,r.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.header,{children:(0,i.jsx)(n.h1,{id:"a06-insecure-design---real-scenarios",children:"A06: Insecure Design - Real Scenarios"})}),"\n",(0,i.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,i.jsx)(n.p,{children:"Insecure Design represents flaws in the design and architecture phase before implementation. These are different from insecure implementation - even perfect code cannot fix flawed design. This requires threat modeling, secure design patterns, and reference architectures."}),"\n",(0,i.jsx)(n.h2,{id:"key-concepts",children:"Key Concepts"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Missing security controls"}),": No rate limiting, no account lockout"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Flawed business logic"}),": Price manipulation, quantity bypass"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Insufficient isolation"}),": Shared resources without proper boundaries"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Trust boundary violations"}),": Trusting user input or external systems"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Missing abuse cases"}),": Not considering malicious user behavior"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"vulnerable-scenario-1-missing-rate-limiting-on-critical-functions",children:"Vulnerable Scenario 1: Missing Rate Limiting on Critical Functions"}),"\n",(0,i.jsx)(n.h3,{id:"vulnerable-code-nextjs-api",children:"Vulnerable Code (Next.js API)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// pages/api/auth/forgot-password.ts - VULNERABLE\nimport type { NextApiRequest, NextApiResponse } from 'next';\nimport { sendPasswordResetEmail } from '@/lib/email';\nimport { db } from '@/lib/db';\n\n// \u274c VULNERABLE: No rate limiting on password reset\nexport default async function handler(req: NextApiRequest, res: NextApiResponse) {\n  if (req.method !== 'POST') {\n    return res.status(405).json({ error: 'Method not allowed' });\n  }\n\n  const { email } = req.body;\n\n  // \u274c No rate limiting - attacker can spam requests\n  const user = await db.user.findUnique({ where: { email } });\n\n  if (user) {\n    const resetToken = generateResetToken();\n\n    await db.user.update({\n      where: { id: user.id },\n      data: { resetToken, resetTokenExpiry: new Date(Date.now() + 3600000) },\n    });\n\n    // \u274c Attacker can flood victim's inbox\n    await sendPasswordResetEmail(email, resetToken);\n  }\n\n  // \u274c Timing attack: response differs if email exists\n  return res.status(200).json({\n    message: user ? 'Email sent' : 'Email not found',\n  });\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"attack-scenario",children:"Attack Scenario"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-javascript",children:"// Attacker's script to spam password resets\nasync function spamPasswordResets() {\n  const targetEmails = ['victim1@example.com', 'victim2@example.com', 'admin@company.com'];\n\n  // \u274c No rate limit - send 1000 requests per email\n  for (const email of targetEmails) {\n    for (let i = 0; i < 1000; i++) {\n      fetch('/api/auth/forgot-password', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({ email }),\n      });\n\n      // \u274c Victim gets 1000 password reset emails\n      // \u274c Email service costs spike\n      // \u274c Legitimate resets get buried\n    }\n  }\n}\n\n// Enumerate valid email addresses\nasync function enumerateUsers() {\n  const commonNames = ['admin', 'user', 'test', 'support'];\n  const domain = '@company.com';\n\n  for (const name of commonNames) {\n    const email = name + domain;\n    const start = Date.now();\n\n    await fetch('/api/auth/forgot-password', {\n      method: 'POST',\n      body: JSON.stringify({ email }),\n    });\n\n    const duration = Date.now() - start;\n\n    // \u274c Timing attack reveals valid emails\n    if (duration > 500) {\n      console.log(`Valid email found: ${email}`);\n    }\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"secure-solution",children:"Secure Solution"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// pages/api/auth/forgot-password.ts - SECURE\nimport type { NextApiRequest, NextApiResponse } from 'next';\nimport { sendPasswordResetEmail } from '@/lib/email';\nimport { db } from '@/lib/db';\nimport rateLimit from 'express-rate-limit';\nimport RedisStore from 'rate-limit-redis';\nimport { createClient } from 'redis';\n\n// \u2705 SECURE: Rate limiter with Redis\nconst redisClient = createClient({\n  url: process.env.REDIS_URL,\n});\n\nconst limiter = rateLimit({\n  store: new RedisStore({\n    client: redisClient,\n    prefix: 'rl:forgot-password:',\n  }),\n  // \u2705 Strict limits on sensitive endpoint\n  windowMs: 15 * 60 * 1000, // 15 minutes\n  max: 3, // Max 3 requests per 15 minutes per IP\n  message: 'Too many password reset attempts, please try again later',\n  standardHeaders: true,\n  legacyHeaders: false,\n\n  // \u2705 Key by IP + email (more granular)\n  keyGenerator: (req) => {\n    const ip = req.headers['x-forwarded-for'] || req.socket.remoteAddress;\n    const email = req.body.email || '';\n    return `${ip}:${email}`;\n  },\n\n  // \u2705 Skip successful requests from counting\n  skipSuccessfulRequests: false,\n  skipFailedRequests: false,\n});\n\nexport default async function handler(req: NextApiRequest, res: NextApiResponse) {\n  if (req.method !== 'POST') {\n    return res.status(405).json({ error: 'Method not allowed' });\n  }\n\n  // \u2705 Apply rate limiting\n  await new Promise((resolve, reject) => {\n    limiter(req as any, res as any, (result: any) => {\n      if (result instanceof Error) {\n        return reject(result);\n      }\n      resolve(result);\n    });\n  });\n\n  const { email } = req.body;\n\n  // \u2705 Input validation\n  if (!email || !isValidEmail(email)) {\n    return res.status(400).json({ error: 'Invalid email' });\n  }\n\n  // \u2705 Always do database lookup to maintain constant timing\n  const user = await db.user.findUnique({ where: { email } });\n\n  if (user) {\n    // \u2705 Check if recent reset was already requested\n    const recentReset = await db.passwordReset.findFirst({\n      where: {\n        userId: user.id,\n        createdAt: { gte: new Date(Date.now() - 5 * 60 * 1000) }, // 5 min\n      },\n    });\n\n    if (recentReset) {\n      // \u2705 Don't send another email too soon\n      return res.status(200).json({\n        message: 'If that email exists, a reset link has been sent',\n      });\n    }\n\n    const resetToken = generateSecureToken();\n\n    // \u2705 Store in separate table with expiry\n    await db.passwordReset.create({\n      data: {\n        userId: user.id,\n        token: await hashToken(resetToken),\n        expiresAt: new Date(Date.now() + 3600000), // 1 hour\n        ipAddress: req.headers['x-forwarded-for'] || req.socket.remoteAddress,\n      },\n    });\n\n    // \u2705 Send email (with error handling)\n    try {\n      await sendPasswordResetEmail(email, resetToken);\n    } catch (error) {\n      console.error('Failed to send reset email:', error);\n      // \u2705 Don't expose email sending failure to user\n    }\n  } else {\n    // \u2705 Simulate email sending delay to prevent timing attacks\n    await new Promise((resolve) => setTimeout(resolve, Math.random() * 200 + 100));\n  }\n\n  // \u2705 Generic response prevents user enumeration\n  return res.status(200).json({\n    message: 'If that email exists, a reset link has been sent',\n  });\n}\n\n// \u2705 Secure token generation\nfunction generateSecureToken(): string {\n  return crypto.randomBytes(32).toString('hex');\n}\n\n// \u2705 Hash tokens before storage\nasync function hashToken(token: string): Promise<string> {\n  return crypto.createHash('sha256').update(token).digest('hex');\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"vulnerable-scenario-2-price-manipulation-in-e-commerce",children:"Vulnerable Scenario 2: Price Manipulation in E-commerce"}),"\n",(0,i.jsx)(n.h3,{id:"vulnerable-code-nextjs-checkout",children:"Vulnerable Code (Next.js Checkout)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// pages/api/checkout.ts - VULNERABLE\nimport type { NextApiRequest, NextApiResponse } from 'next';\nimport { db } from '@/lib/db';\nimport { processPayment } from '@/lib/payment';\n\n// \u274c VULNERABLE: Trusts price from client\nexport default async function handler(req: NextApiRequest, res: NextApiResponse) {\n  const { items, totalAmount } = req.body;\n\n  // \u274c No server-side price calculation\n  // \u274c Trusts totalAmount from client\n  const paymentResult = await processPayment({\n    amount: totalAmount, // \u274c Client-controlled!\n    currency: 'USD',\n  });\n\n  if (paymentResult.success) {\n    // Create order with manipulated price\n    const order = await db.order.create({\n      data: {\n        userId: req.user.id,\n        items: items,\n        total: totalAmount, // \u274c Stores manipulated price\n        status: 'completed',\n      },\n    });\n\n    return res.status(200).json({ order });\n  }\n}\n"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// components/Checkout.tsx - VULNERABLE CLIENT\nexport default function Checkout({ cart }) {\n  const handleCheckout = async () => {\n    // \u274c Client calculates total\n    let total = 0;\n    for (const item of cart) {\n      total += item.price * item.quantity; // \u274c Client-side calculation\n    }\n\n    // Attacker can modify this in browser console or intercept request\n    const response = await fetch('/api/checkout', {\n      method: 'POST',\n      body: JSON.stringify({\n        items: cart,\n        totalAmount: total, // \u274c Sends client-calculated price\n      }),\n    });\n  };\n\n  return <button onClick={handleCheckout}>Complete Purchase</button>;\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"attack-example",children:"Attack Example"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-javascript",children:"// Attacker opens browser console\n// Intercept and modify the checkout request\n\n// Original request:\nfetch('/api/checkout', {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({\n    items: [\n      { id: 1, name: 'Laptop', price: 1200, quantity: 1 },\n      { id: 2, name: 'Mouse', price: 50, quantity: 1 },\n    ],\n    totalAmount: 1250, // \u274c Original price\n  }),\n});\n\n// \u274c Attacker modifies to:\nfetch('/api/checkout', {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({\n    items: [\n      { id: 1, name: 'Laptop', price: 1200, quantity: 1 },\n      { id: 2, name: 'Mouse', price: 50, quantity: 1 },\n    ],\n    totalAmount: 1, // \u274c Changed to $1!\n  }),\n});\n\n// Result: Attacker gets $1250 worth of items for $1\n"})}),"\n",(0,i.jsx)(n.h3,{id:"secure-solution-1",children:"Secure Solution"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// pages/api/checkout.ts - SECURE\nimport type { NextApiRequest, NextApiResponse } from 'next';\nimport { db } from '@/lib/db';\nimport { processPayment } from '@/lib/payment';\nimport Decimal from 'decimal.js'; // \u2705 Use decimal library for money\n\nexport default async function handler(req: NextApiRequest, res: NextApiResponse) {\n  const { items } = req.body;\n\n  // \u2705 Input validation\n  if (!Array.isArray(items) || items.length === 0) {\n    return res.status(400).json({ error: 'Invalid items' });\n  }\n\n  if (items.length > 100) {\n    return res.status(400).json({ error: 'Too many items' });\n  }\n\n  // \u2705 SERVER-SIDE: Fetch current prices from database\n  const productIds = items.map((item) => item.id);\n  const products = await db.product.findMany({\n    where: { id: { in: productIds } },\n    select: { id: true, price: true, stock: true, isActive: true },\n  });\n\n  // \u2705 Create price lookup map\n  const priceMap = new Map(products.map((p) => [p.id, { price: p.price, stock: p.stock, isActive: p.isActive }]));\n\n  // \u2705 Validate and calculate total server-side\n  let calculatedTotal = new Decimal(0);\n  const validatedItems = [];\n\n  for (const item of items) {\n    const product = priceMap.get(item.id);\n\n    // \u2705 Validate product exists and is active\n    if (!product || !product.isActive) {\n      return res.status(400).json({\n        error: `Product ${item.id} is not available`,\n      });\n    }\n\n    // \u2705 Validate quantity\n    if (!Number.isInteger(item.quantity) || item.quantity < 1) {\n      return res.status(400).json({ error: 'Invalid quantity' });\n    }\n\n    if (item.quantity > 100) {\n      return res.status(400).json({ error: 'Quantity exceeds limit' });\n    }\n\n    // \u2705 Check stock availability\n    if (item.quantity > product.stock) {\n      return res.status(400).json({\n        error: `Insufficient stock for product ${item.id}`,\n      });\n    }\n\n    // \u2705 Calculate using server-side price (not client price!)\n    const itemTotal = new Decimal(product.price).times(item.quantity);\n    calculatedTotal = calculatedTotal.plus(itemTotal);\n\n    validatedItems.push({\n      productId: item.id,\n      quantity: item.quantity,\n      unitPrice: product.price,\n      totalPrice: itemTotal.toFixed(2),\n    });\n  }\n\n  // \u2705 Apply discounts/coupons server-side\n  const discount = await calculateDiscount(req.user.id, validatedItems);\n  calculatedTotal = calculatedTotal.minus(discount);\n\n  // \u2705 Ensure non-negative\n  if (calculatedTotal.lessThan(0)) {\n    calculatedTotal = new Decimal(0);\n  }\n\n  const finalAmount = calculatedTotal.toFixed(2);\n\n  // \u2705 Start database transaction\n  const result = await db.$transaction(async (tx) => {\n    // \u2705 Lock products to prevent race conditions\n    const lockedProducts = await tx.product.findMany({\n      where: { id: { in: productIds } },\n      select: { id: true, stock: true },\n    });\n\n    // \u2705 Recheck stock within transaction\n    for (const item of validatedItems) {\n      const product = lockedProducts.find((p) => p.id === item.productId);\n      if (!product || product.stock < item.quantity) {\n        throw new Error(`Insufficient stock for product ${item.productId}`);\n      }\n    }\n\n    // \u2705 Process payment with calculated amount\n    const paymentResult = await processPayment({\n      amount: parseFloat(finalAmount),\n      currency: 'USD',\n      userId: req.user.id,\n      idempotencyKey: req.body.idempotencyKey, // \u2705 Prevent double-charging\n    });\n\n    if (!paymentResult.success) {\n      throw new Error('Payment failed');\n    }\n\n    // \u2705 Deduct stock\n    for (const item of validatedItems) {\n      await tx.product.update({\n        where: { id: item.productId },\n        data: { stock: { decrement: item.quantity } },\n      });\n    }\n\n    // \u2705 Create order with validated data\n    const order = await tx.order.create({\n      data: {\n        userId: req.user.id,\n        items: {\n          create: validatedItems,\n        },\n        subtotal: calculatedTotal.plus(discount).toFixed(2),\n        discount: discount.toFixed(2),\n        total: finalAmount,\n        status: 'completed',\n        paymentId: paymentResult.id,\n      },\n    });\n\n    return order;\n  });\n\n  return res.status(200).json({ order: result });\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"vulnerable-scenario-3-insufficient-account-lockout",children:"Vulnerable Scenario 3: Insufficient Account Lockout"}),"\n",(0,i.jsx)(n.h3,{id:"vulnerable-code-authentication",children:"Vulnerable Code (Authentication)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// auth.service.ts - VULNERABLE\nimport { Injectable } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { User } from './entities/user.entity';\nimport * as bcrypt from 'bcrypt';\n\n@Injectable()\nexport class AuthService {\n  constructor(\n    @InjectRepository(User)\n    private userRepository: Repository<User>,\n  ) {}\n\n  // \u274c VULNERABLE: No account lockout\n  async login(email: string, password: string) {\n    const user = await this.userRepository.findOne({ where: { email } });\n\n    if (!user) {\n      throw new UnauthorizedException('Invalid credentials');\n    }\n\n    const isValid = await bcrypt.compare(password, user.password);\n\n    if (!isValid) {\n      // \u274c No tracking of failed attempts\n      throw new UnauthorizedException('Invalid credentials');\n    }\n\n    return this.generateToken(user);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"attack-scenario-1",children:"Attack Scenario"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",children:'# Brute force attack script\nimport requests\nimport itertools\nimport string\n\ntarget_email = "admin@example.com"\nurl = "https://api.example.com/auth/login"\n\n# Generate password candidates\npasswords = []\n\n# Try common passwords\nwith open(\'common_passwords.txt\', \'r\') as f:\n    passwords = f.readlines()\n\n# \u274c No rate limit or lockout - try all passwords\nfor password in passwords:\n    response = requests.post(url, json={\n        "email": target_email,\n        "password": password.strip()\n    })\n\n    if response.status_code == 200:\n        print(f"Password found: {password}")\n        break\n\n    # \u274c No delay needed - server accepts requests\n\n# Can try millions of passwords until success\n'})}),"\n",(0,i.jsx)(n.h3,{id:"secure-solution-2",children:"Secure Solution"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// auth.service.ts - SECURE\nimport { Injectable, UnauthorizedException } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { User } from './entities/user.entity';\nimport { LoginAttempt } from './entities/login-attempt.entity';\nimport * as bcrypt from 'bcrypt';\nimport { Cache } from 'cache-manager';\nimport { Inject } from '@nestjs/common';\nimport { CACHE_MANAGER } from '@nestjs/cache-manager';\n\n@Injectable()\nexport class AuthService {\n  // \u2705 Account lockout configuration\n  private readonly MAX_ATTEMPTS = 5;\n  private readonly LOCKOUT_DURATION_MINUTES = 15;\n  private readonly ATTEMPT_WINDOW_MINUTES = 15;\n\n  constructor(\n    @InjectRepository(User)\n    private userRepository: Repository<User>,\n    @InjectRepository(LoginAttempt)\n    private loginAttemptRepository: Repository<LoginAttempt>,\n    @Inject(CACHE_MANAGER) private cacheManager: Cache,\n  ) {}\n\n  async login(email: string, password: string, ipAddress: string) {\n    // \u2705 Check if account is locked\n    const isLocked = await this.isAccountLocked(email);\n    if (isLocked) {\n      const lockoutRemaining = await this.getLockoutTimeRemaining(email);\n      throw new UnauthorizedException(`Account is locked. Try again in ${lockoutRemaining} minutes.`);\n    }\n\n    // \u2705 Check failed attempts from this IP\n    const ipAttempts = await this.getRecentAttempts(ipAddress);\n    if (ipAttempts >= this.MAX_ATTEMPTS) {\n      throw new UnauthorizedException('Too many failed attempts from this IP address');\n    }\n\n    const user = await this.userRepository.findOne({\n      where: { email },\n      select: ['id', 'email', 'password', 'isActive', 'failedLoginAttempts', 'lockedUntil'],\n    });\n\n    if (!user || !user.isActive) {\n      // \u2705 Record failed attempt even if user doesn't exist\n      await this.recordFailedAttempt(email, ipAddress);\n\n      // \u2705 Add delay to slow down brute force\n      await new Promise((resolve) => setTimeout(resolve, 1000));\n\n      throw new UnauthorizedException('Invalid credentials');\n    }\n\n    const isValid = await bcrypt.compare(password, user.password);\n\n    if (!isValid) {\n      // \u2705 Increment failed attempts\n      await this.handleFailedLogin(user, ipAddress);\n\n      const remainingAttempts = this.MAX_ATTEMPTS - (user.failedLoginAttempts + 1);\n\n      if (remainingAttempts > 0) {\n        throw new UnauthorizedException(`Invalid credentials. ${remainingAttempts} attempts remaining.`);\n      } else {\n        throw new UnauthorizedException(\n          `Account locked due to too many failed attempts. Try again in ${this.LOCKOUT_DURATION_MINUTES} minutes.`,\n        );\n      }\n    }\n\n    // \u2705 Successful login - reset failed attempts\n    await this.handleSuccessfulLogin(user, ipAddress);\n\n    delete user.password;\n    return this.generateToken(user);\n  }\n\n  // \u2705 Track failed login attempts\n  private async handleFailedLogin(user: User, ipAddress: string) {\n    const attempts = user.failedLoginAttempts + 1;\n\n    if (attempts >= this.MAX_ATTEMPTS) {\n      // \u2705 Lock the account\n      const lockedUntil = new Date(Date.now() + this.LOCKOUT_DURATION_MINUTES * 60 * 1000);\n\n      await this.userRepository.update(user.id, {\n        failedLoginAttempts: attempts,\n        lockedUntil: lockedUntil,\n      });\n\n      // \u2705 Send alert email\n      await this.sendAccountLockedEmail(user.email, lockedUntil);\n\n      // \u2705 Log security event\n      await this.logSecurityEvent({\n        type: 'ACCOUNT_LOCKED',\n        userId: user.id,\n        ipAddress,\n        reason: `${this.MAX_ATTEMPTS} failed login attempts`,\n      });\n    } else {\n      await this.userRepository.update(user.id, {\n        failedLoginAttempts: attempts,\n      });\n    }\n\n    // \u2705 Record attempt in separate table\n    await this.recordFailedAttempt(user.email, ipAddress);\n  }\n\n  private async handleSuccessfulLogin(user: User, ipAddress: string) {\n    // \u2705 Reset failed attempts counter\n    await this.userRepository.update(user.id, {\n      failedLoginAttempts: 0,\n      lockedUntil: null,\n      lastLoginAt: new Date(),\n      lastLoginIp: ipAddress,\n    });\n\n    // \u2705 Clear IP-based attempts from cache\n    await this.cacheManager.del(`login_attempts:${ipAddress}`);\n\n    // \u2705 Log successful login\n    await this.logSecurityEvent({\n      type: 'LOGIN_SUCCESS',\n      userId: user.id,\n      ipAddress,\n    });\n  }\n\n  private async isAccountLocked(email: string): Promise<boolean> {\n    const user = await this.userRepository.findOne({\n      where: { email },\n      select: ['lockedUntil'],\n    });\n\n    if (!user || !user.lockedUntil) {\n      return false;\n    }\n\n    if (new Date() < user.lockedUntil) {\n      return true;\n    }\n\n    // \u2705 Lockout period expired, unlock account\n    await this.userRepository.update(user.id, {\n      lockedUntil: null,\n      failedLoginAttempts: 0,\n    });\n\n    return false;\n  }\n\n  private async getLockoutTimeRemaining(email: string): Promise<number> {\n    const user = await this.userRepository.findOne({\n      where: { email },\n      select: ['lockedUntil'],\n    });\n\n    if (!user?.lockedUntil) {\n      return 0;\n    }\n\n    const remaining = user.lockedUntil.getTime() - Date.now();\n    return Math.ceil(remaining / 60000); // Convert to minutes\n  }\n\n  // \u2705 Track attempts by IP address (prevents distributed attacks)\n  private async getRecentAttempts(ipAddress: string): Promise<number> {\n    const cacheKey = `login_attempts:${ipAddress}`;\n    const attempts = await this.cacheManager.get<number>(cacheKey);\n    return attempts || 0;\n  }\n\n  private async recordFailedAttempt(email: string, ipAddress: string) {\n    // \u2705 Persist to database\n    await this.loginAttemptRepository.create({\n      email,\n      ipAddress,\n      success: false,\n      timestamp: new Date(),\n    });\n\n    // \u2705 Increment cache counter\n    const cacheKey = `login_attempts:${ipAddress}`;\n    const currentAttempts = await this.getRecentAttempts(ipAddress);\n    await this.cacheManager.set(\n      cacheKey,\n      currentAttempts + 1,\n      this.ATTEMPT_WINDOW_MINUTES * 60 * 1000, // TTL in milliseconds\n    );\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"prevention-checklist",children:"Prevention Checklist"}),"\n",(0,i.jsxs)(n.ul,{className:"contains-task-list",children:["\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Threat modeling"}),": Identify threats in design phase"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Rate limiting"}),": Apply to all sensitive endpoints"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Account lockout"}),": Implement progressive delays"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Server-side validation"}),": Never trust client calculations"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Secure defaults"}),": Fail securely, deny by default"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Business logic review"}),": Consider abuse cases"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Transaction integrity"}),": Use database transactions"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Idempotency"}),": Prevent duplicate operations"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Resource limits"}),": Limit quantities, file sizes, etc."]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Audit logging"}),": Log all security-relevant events"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"testing-methods",children:"Testing Methods"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"describe('Security Design Tests', () => {\n  it('should enforce rate limiting on password reset', async () => {\n    const email = 'test@example.com';\n\n    // Try 4 requests (within limit)\n    for (let i = 0; i < 4; i++) {\n      await request(app).post('/auth/forgot-password').send({ email });\n    }\n\n    // 5th request should be rate limited\n    const response = await request(app).post('/auth/forgot-password').send({ email });\n\n    expect(response.status).toBe(429);\n  });\n\n  it('should lock account after failed attempts', async () => {\n    const credentials = { email: 'test@example.com', password: 'wrong' };\n\n    // Try 5 times with wrong password\n    for (let i = 0; i < 5; i++) {\n      await request(app).post('/auth/login').send(credentials);\n    }\n\n    // Account should be locked\n    const response = await request(app).post('/auth/login').send({ email: credentials.email, password: 'correct' });\n\n    expect(response.status).toBe(401);\n    expect(response.body.error).toContain('locked');\n  });\n\n  it('should prevent price manipulation', async () => {\n    // Try to checkout with manipulated price\n    const response = await request(app)\n      .post('/api/checkout')\n      .send({\n        items: [{ id: 1, quantity: 1 }],\n        totalAmount: 1, // Fake price\n      });\n\n    // Server should calculate correct price\n    expect(response.body.order.total).not.toBe('1');\n  });\n});\n"})}),"\n",(0,i.jsx)(n.h2,{id:"real-world-impact-examples",children:"Real-World Impact Examples"}),"\n",(0,i.jsxs)(n.ol,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Robinhood (2021)"}),": Missing controls allowed account takeovers via social engineering"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Gitlab (2017)"}),": Insufficient rate limiting on password reset enabled account enumeration"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Ticketmaster (2021)"}),": Flawed bot protection allowed scalpers to buy bulk tickets"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"United Airlines (2015)"}),": Price manipulation bug let users buy tickets for $5"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"references",children:"References"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"https://owasp.org/Top10/A04_2021-Insecure_Design/",children:"OWASP Top 10 2021 - A04"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"https://owasp.org/www-project-secure-design-principles/",children:"OWASP Secure Design Principles"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"https://owasp.org/www-community/Threat_Modeling",children:"OWASP Threat Modeling"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"https://cheatsheetseries.owasp.org/cheatsheets/Abuse_Case_Cheat_Sheet.html",children:"OWASP Abuse Case Cheat Sheet"})}),"\n"]})]})}function u(e={}){const{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);