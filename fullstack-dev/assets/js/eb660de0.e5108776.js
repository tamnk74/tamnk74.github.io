"use strict";(self.webpackChunkdocs_site=self.webpackChunkdocs_site||[]).push([[51489],{5741:(e,n,t)=>{t.d(n,{R:()=>i,x:()=>o});var r=t(59729);const s={},a=r.createContext(s);function i(e){const n=r.useContext(a);return r.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:i(e.components),r.createElement(a.Provider,{value:n},e.children)}},56585:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>c,contentTitle:()=>o,default:()=>u,frontMatter:()=>i,metadata:()=>r,toc:()=>l});const r=JSON.parse('{"id":"backend/elasticsearch-stack-guide","title":"Elasticsearch Stack Implementation Guide","description":"Overview","source":"@site/docs/backend/elasticsearch-stack-guide.md","sourceDirName":"backend","slug":"/backend/elasticsearch-stack-guide","permalink":"/fullstack-dev/docs/backend/elasticsearch-stack-guide","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/elasticsearch-stack-guide.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Redis with Microsoft Entra ID Authentication","permalink":"/fullstack-dev/docs/backend/redis-entraid-guide"},"next":{"title":"Dead Letter Queues & Poison Message Handling Guide","permalink":"/fullstack-dev/docs/backend/dead-letter-queue-poison-message-handling"}}');var s=t(65813),a=t(5741);const i={},o="Elasticsearch Stack Implementation Guide",c={},l=[{value:"Overview",id:"overview",level:2},{value:"Key Components",id:"key-components",level:2},{value:"Prerequisites",id:"prerequisites",level:2},{value:"Use Cases",id:"use-cases",level:2},{value:"1. Product Search with Elasticsearch",id:"1-product-search-with-elasticsearch",level:3},{value:"2. Log Aggregation with Logstash",id:"2-log-aggregation-with-logstash",level:3},{value:"3. Real-Time Analytics Dashboard",id:"3-real-time-analytics-dashboard",level:3},{value:"4. Application Performance Monitoring (APM)",id:"4-application-performance-monitoring-apm",level:3},{value:"Docker Compose Setup",id:"docker-compose-setup",level:2},{value:"Kibana Dashboard Configuration",id:"kibana-dashboard-configuration",level:2},{value:"Best Practices",id:"best-practices",level:2},{value:"1. Index Lifecycle Management",id:"1-index-lifecycle-management",level:3},{value:"2. Query Optimization",id:"2-query-optimization",level:3},{value:"3. Monitoring and Alerts",id:"3-monitoring-and-alerts",level:3},{value:"Environment Configuration",id:"environment-configuration",level:2},{value:"Summary",id:"summary",level:2}];function d(e){const n={code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,a.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(n.header,{children:(0,s.jsx)(n.h1,{id:"elasticsearch-stack-implementation-guide",children:"Elasticsearch Stack Implementation Guide"})}),"\n",(0,s.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,s.jsx)(n.p,{children:"The Elasticsearch Stack (ELK Stack) combines Elasticsearch, Logstash, and Kibana to provide powerful search, analytics, and visualization capabilities. This guide demonstrates production-ready implementations for e-commerce platforms including product search, log aggregation, analytics, and monitoring."}),"\n",(0,s.jsx)(n.h2,{id:"key-components",children:"Key Components"}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Elasticsearch"}),": Distributed search and analytics engine"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Logstash"}),": Data processing pipeline for ingesting and transforming data"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Kibana"}),": Visualization and exploration platform"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Beats"}),": Lightweight data shippers (Filebeat, Metricbeat, APM)"]}),"\n"]}),"\n",(0,s.jsx)(n.h2,{id:"prerequisites",children:"Prerequisites"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"# Install Elasticsearch client\nnpm install @elastic/elasticsearch\n\n# Install additional dependencies\nnpm install @nestjs/elasticsearch @nestjs/config\n\n# For development\nnpm install @types/node --save-dev\n"})}),"\n",(0,s.jsx)(n.h2,{id:"use-cases",children:"Use Cases"}),"\n",(0,s.jsx)(n.h3,{id:"1-product-search-with-elasticsearch",children:"1. Product Search with Elasticsearch"}),"\n",(0,s.jsx)(n.p,{children:"Implement advanced product search with full-text search, filters, facets, and autocomplete."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// elasticsearch.config.ts\nimport { registerAs } from '@nestjs/config';\n\nexport default registerAs('elasticsearch', () => ({\n  node: process.env.ELASTICSEARCH_NODE || 'http://localhost:9200',\n  auth: {\n    username: process.env.ELASTICSEARCH_USERNAME || 'elastic',\n    password: process.env.ELASTICSEARCH_PASSWORD || 'changeme',\n  },\n  maxRetries: 5,\n  requestTimeout: 60000,\n  sniffOnStart: true,\n  ssl: {\n    rejectUnauthorized: process.env.NODE_ENV === 'production',\n  },\n}));\n"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// elasticsearch.module.ts\nimport { Module, Global } from '@nestjs/common';\nimport { ElasticsearchModule as ESModule } from '@nestjs/elasticsearch';\nimport { ConfigModule, ConfigService } from '@nestjs/config';\nimport elasticsearchConfig from './elasticsearch.config';\nimport { ProductSearchService } from './product-search.service';\nimport { SearchIndexService } from './search-index.service';\n\n@Global()\n@Module({\n  imports: [\n    ConfigModule.forFeature(elasticsearchConfig),\n    ESModule.registerAsync({\n      imports: [ConfigModule],\n      useFactory: async (configService: ConfigService) => ({\n        node: configService.get('elasticsearch.node'),\n        auth: configService.get('elasticsearch.auth'),\n        maxRetries: configService.get('elasticsearch.maxRetries'),\n        requestTimeout: configService.get('elasticsearch.requestTimeout'),\n        sniffOnStart: configService.get('elasticsearch.sniffOnStart'),\n        ssl: configService.get('elasticsearch.ssl'),\n      }),\n      inject: [ConfigService],\n    }),\n  ],\n  providers: [ProductSearchService, SearchIndexService],\n  exports: [ProductSearchService, SearchIndexService, ESModule],\n})\nexport class ElasticsearchModule {}\n"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// search-index.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { ElasticsearchService } from '@nestjs/elasticsearch';\n\nexport interface ProductDocument {\n  id: string;\n  name: string;\n  description: string;\n  category: string;\n  brand: string;\n  price: number;\n  salePrice?: number;\n  stock: number;\n  rating: number;\n  reviewCount: number;\n  tags: string[];\n  attributes: Record<string, any>;\n  images: string[];\n  createdAt: Date;\n  updatedAt: Date;\n}\n\n@Injectable()\nexport class SearchIndexService implements OnModuleInit {\n  private readonly logger = new Logger(SearchIndexService.name);\n  private readonly PRODUCT_INDEX = 'products';\n\n  constructor(private readonly elasticsearchService: ElasticsearchService) {}\n\n  async onModuleInit() {\n    await this.createProductIndex();\n  }\n\n  async createProductIndex() {\n    const indexExists = await this.elasticsearchService.indices.exists({\n      index: this.PRODUCT_INDEX,\n    });\n\n    if (indexExists) {\n      this.logger.log(`Index ${this.PRODUCT_INDEX} already exists`);\n      return;\n    }\n\n    try {\n      await this.elasticsearchService.indices.create({\n        index: this.PRODUCT_INDEX,\n        body: {\n          settings: {\n            number_of_shards: 3,\n            number_of_replicas: 2,\n            analysis: {\n              analyzer: {\n                // Custom analyzer for product names\n                product_name_analyzer: {\n                  type: 'custom',\n                  tokenizer: 'standard',\n                  filter: ['lowercase', 'asciifolding', 'product_synonym', 'edge_ngram_filter'],\n                },\n                // Autocomplete analyzer\n                autocomplete_analyzer: {\n                  type: 'custom',\n                  tokenizer: 'standard',\n                  filter: ['lowercase', 'autocomplete_filter'],\n                },\n                autocomplete_search_analyzer: {\n                  type: 'custom',\n                  tokenizer: 'standard',\n                  filter: ['lowercase'],\n                },\n              },\n              filter: {\n                edge_ngram_filter: {\n                  type: 'edge_ngram',\n                  min_gram: 2,\n                  max_gram: 20,\n                },\n                autocomplete_filter: {\n                  type: 'edge_ngram',\n                  min_gram: 1,\n                  max_gram: 20,\n                },\n                product_synonym: {\n                  type: 'synonym',\n                  synonyms: ['phone, smartphone, mobile', 'laptop, notebook, computer', 'tv, television'],\n                },\n              },\n            },\n          },\n          mappings: {\n            properties: {\n              id: { type: 'keyword' },\n              name: {\n                type: 'text',\n                analyzer: 'product_name_analyzer',\n                fields: {\n                  keyword: { type: 'keyword' },\n                  autocomplete: {\n                    type: 'text',\n                    analyzer: 'autocomplete_analyzer',\n                    search_analyzer: 'autocomplete_search_analyzer',\n                  },\n                },\n              },\n              description: {\n                type: 'text',\n                analyzer: 'standard',\n              },\n              category: {\n                type: 'keyword',\n                fields: {\n                  text: { type: 'text' },\n                },\n              },\n              brand: {\n                type: 'keyword',\n                fields: {\n                  text: { type: 'text' },\n                },\n              },\n              price: { type: 'float' },\n              salePrice: { type: 'float' },\n              stock: { type: 'integer' },\n              rating: { type: 'float' },\n              reviewCount: { type: 'integer' },\n              tags: { type: 'keyword' },\n              attributes: { type: 'object', enabled: true },\n              images: { type: 'keyword' },\n              createdAt: { type: 'date' },\n              updatedAt: { type: 'date' },\n            },\n          },\n        },\n      });\n\n      this.logger.log(`Created index: ${this.PRODUCT_INDEX}`);\n    } catch (error) {\n      this.logger.error(`Failed to create index: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async indexProduct(product: ProductDocument): Promise<void> {\n    try {\n      await this.elasticsearchService.index({\n        index: this.PRODUCT_INDEX,\n        id: product.id,\n        document: product,\n      });\n\n      this.logger.log(`Indexed product: ${product.id}`);\n    } catch (error) {\n      this.logger.error(`Failed to index product ${product.id}: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async bulkIndexProducts(products: ProductDocument[]): Promise<void> {\n    if (products.length === 0) {\n      return;\n    }\n\n    try {\n      const operations = products.flatMap((product) => [\n        { index: { _index: this.PRODUCT_INDEX, _id: product.id } },\n        product,\n      ]);\n\n      const bulkResponse = await this.elasticsearchService.bulk({\n        refresh: true,\n        operations,\n      });\n\n      if (bulkResponse.errors) {\n        const erroredDocuments = [];\n        bulkResponse.items.forEach((action, i) => {\n          const operation = Object.keys(action)[0];\n          if (action[operation].error) {\n            erroredDocuments.push({\n              status: action[operation].status,\n              error: action[operation].error,\n              document: products[i],\n            });\n          }\n        });\n\n        this.logger.error(`Bulk indexing errors: ${JSON.stringify(erroredDocuments)}`);\n      }\n\n      this.logger.log(`Bulk indexed ${products.length} products`);\n    } catch (error) {\n      this.logger.error(`Bulk indexing failed: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async deleteProduct(productId: string): Promise<void> {\n    try {\n      await this.elasticsearchService.delete({\n        index: this.PRODUCT_INDEX,\n        id: productId,\n      });\n\n      this.logger.log(`Deleted product: ${productId}`);\n    } catch (error) {\n      if (error.meta?.statusCode === 404) {\n        this.logger.warn(`Product not found: ${productId}`);\n        return;\n      }\n      this.logger.error(`Failed to delete product ${productId}: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async updateProduct(productId: string, updates: Partial<ProductDocument>): Promise<void> {\n    try {\n      await this.elasticsearchService.update({\n        index: this.PRODUCT_INDEX,\n        id: productId,\n        doc: updates,\n      });\n\n      this.logger.log(`Updated product: ${productId}`);\n    } catch (error) {\n      this.logger.error(`Failed to update product ${productId}: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async reindexAll(products: ProductDocument[]): Promise<void> {\n    this.logger.log('Starting full reindex...');\n\n    try {\n      // Delete existing index\n      const indexExists = await this.elasticsearchService.indices.exists({\n        index: this.PRODUCT_INDEX,\n      });\n\n      if (indexExists) {\n        await this.elasticsearchService.indices.delete({\n          index: this.PRODUCT_INDEX,\n        });\n      }\n\n      // Create new index\n      await this.createProductIndex();\n\n      // Bulk index all products\n      await this.bulkIndexProducts(products);\n\n      this.logger.log('Full reindex completed');\n    } catch (error) {\n      this.logger.error(`Full reindex failed: ${error.message}`);\n      throw error;\n    }\n  }\n}\n"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// product-search.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { ElasticsearchService } from '@nestjs/elasticsearch';\n\nexport interface SearchQuery {\n  query?: string;\n  category?: string;\n  brand?: string;\n  minPrice?: number;\n  maxPrice?: number;\n  minRating?: number;\n  tags?: string[];\n  inStock?: boolean;\n  sortBy?: 'relevance' | 'price_asc' | 'price_desc' | 'rating' | 'newest';\n  page?: number;\n  limit?: number;\n}\n\nexport interface SearchResult {\n  products: any[];\n  total: number;\n  page: number;\n  limit: number;\n  aggregations?: {\n    categories: Array<{ key: string; count: number }>;\n    brands: Array<{ key: string; count: number }>;\n    priceRanges: Array<{ key: string; count: number; from?: number; to?: number }>;\n    tags: Array<{ key: string; count: number }>;\n  };\n}\n\n@Injectable()\nexport class ProductSearchService {\n  private readonly logger = new Logger(ProductSearchService.name);\n  private readonly PRODUCT_INDEX = 'products';\n\n  constructor(private readonly elasticsearchService: ElasticsearchService) {}\n\n  async search(searchQuery: SearchQuery): Promise<SearchResult> {\n    const {\n      query,\n      category,\n      brand,\n      minPrice,\n      maxPrice,\n      minRating,\n      tags,\n      inStock,\n      sortBy = 'relevance',\n      page = 1,\n      limit = 20,\n    } = searchQuery;\n\n    const from = (page - 1) * limit;\n\n    try {\n      // Build query\n      const must: any[] = [];\n      const filter: any[] = [];\n\n      // Text search\n      if (query) {\n        must.push({\n          multi_match: {\n            query,\n            fields: ['name^3', 'description', 'brand^2', 'category^2', 'tags'],\n            type: 'best_fields',\n            fuzziness: 'AUTO',\n          },\n        });\n      } else {\n        must.push({ match_all: {} });\n      }\n\n      // Category filter\n      if (category) {\n        filter.push({ term: { category } });\n      }\n\n      // Brand filter\n      if (brand) {\n        filter.push({ term: { brand } });\n      }\n\n      // Price range filter\n      if (minPrice !== undefined || maxPrice !== undefined) {\n        const priceRange: any = {};\n        if (minPrice !== undefined) priceRange.gte = minPrice;\n        if (maxPrice !== undefined) priceRange.lte = maxPrice;\n        filter.push({ range: { price: priceRange } });\n      }\n\n      // Rating filter\n      if (minRating !== undefined) {\n        filter.push({ range: { rating: { gte: minRating } } });\n      }\n\n      // Tags filter\n      if (tags && tags.length > 0) {\n        filter.push({ terms: { tags } });\n      }\n\n      // Stock filter\n      if (inStock) {\n        filter.push({ range: { stock: { gt: 0 } } });\n      }\n\n      // Build sort\n      const sort: any[] = [];\n      switch (sortBy) {\n        case 'price_asc':\n          sort.push({ price: 'asc' });\n          break;\n        case 'price_desc':\n          sort.push({ price: 'desc' });\n          break;\n        case 'rating':\n          sort.push({ rating: 'desc' }, { reviewCount: 'desc' });\n          break;\n        case 'newest':\n          sort.push({ createdAt: 'desc' });\n          break;\n        default:\n          sort.push('_score');\n      }\n\n      const response = await this.elasticsearchService.search({\n        index: this.PRODUCT_INDEX,\n        body: {\n          query: {\n            bool: {\n              must,\n              filter,\n            },\n          },\n          sort,\n          from,\n          size: limit,\n          aggs: {\n            categories: {\n              terms: { field: 'category', size: 20 },\n            },\n            brands: {\n              terms: { field: 'brand', size: 20 },\n            },\n            priceRanges: {\n              range: {\n                field: 'price',\n                ranges: [\n                  { key: 'Under $25', to: 25 },\n                  { key: '$25 - $50', from: 25, to: 50 },\n                  { key: '$50 - $100', from: 50, to: 100 },\n                  { key: '$100 - $200', from: 100, to: 200 },\n                  { key: 'Over $200', from: 200 },\n                ],\n              },\n            },\n            tags: {\n              terms: { field: 'tags', size: 30 },\n            },\n          },\n        },\n      });\n\n      const hits = response.hits.hits;\n      const total = typeof response.hits.total === 'number' ? response.hits.total : response.hits.total.value;\n\n      const products = hits.map((hit) => ({\n        id: hit._id,\n        score: hit._score,\n        ...hit._source,\n      }));\n\n      const aggregations = {\n        categories: response.aggregations.categories.buckets.map((b: any) => ({\n          key: b.key,\n          count: b.doc_count,\n        })),\n        brands: response.aggregations.brands.buckets.map((b: any) => ({\n          key: b.key,\n          count: b.doc_count,\n        })),\n        priceRanges: response.aggregations.priceRanges.buckets.map((b: any) => ({\n          key: b.key,\n          count: b.doc_count,\n          from: b.from,\n          to: b.to,\n        })),\n        tags: response.aggregations.tags.buckets.map((b: any) => ({\n          key: b.key,\n          count: b.doc_count,\n        })),\n      };\n\n      return {\n        products,\n        total,\n        page,\n        limit,\n        aggregations,\n      };\n    } catch (error) {\n      this.logger.error(`Search failed: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async autocomplete(query: string, limit = 10): Promise<string[]> {\n    if (!query || query.length < 2) {\n      return [];\n    }\n\n    try {\n      const response = await this.elasticsearchService.search({\n        index: this.PRODUCT_INDEX,\n        body: {\n          query: {\n            match: {\n              'name.autocomplete': query,\n            },\n          },\n          _source: ['name'],\n          size: limit,\n        },\n      });\n\n      return response.hits.hits.map((hit: any) => hit._source.name);\n    } catch (error) {\n      this.logger.error(`Autocomplete failed: ${error.message}`);\n      return [];\n    }\n  }\n\n  async getSimilarProducts(productId: string, limit = 6): Promise<any[]> {\n    try {\n      // Get the product\n      const product = await this.elasticsearchService.get({\n        index: this.PRODUCT_INDEX,\n        id: productId,\n      });\n\n      const productData: any = product._source;\n\n      // Find similar products\n      const response = await this.elasticsearchService.search({\n        index: this.PRODUCT_INDEX,\n        body: {\n          query: {\n            bool: {\n              must: [\n                {\n                  more_like_this: {\n                    fields: ['name', 'description', 'tags'],\n                    like: [\n                      {\n                        _index: this.PRODUCT_INDEX,\n                        _id: productId,\n                      },\n                    ],\n                    min_term_freq: 1,\n                    max_query_terms: 12,\n                  },\n                },\n              ],\n              filter: [{ term: { category: productData.category } }],\n              must_not: [{ term: { id: productId } }],\n            },\n          },\n          size: limit,\n        },\n      });\n\n      return response.hits.hits.map((hit: any) => ({\n        id: hit._id,\n        score: hit._score,\n        ...hit._source,\n      }));\n    } catch (error) {\n      this.logger.error(`Similar products search failed: ${error.message}`);\n      return [];\n    }\n  }\n}\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Controller implementation:"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// product-search.controller.ts\nimport { Controller, Get, Query, Param } from '@nestjs/common';\nimport { ProductSearchService, SearchQuery } from './product-search.service';\n\n@Controller('api/search')\nexport class ProductSearchController {\n  constructor(private readonly searchService: ProductSearchService) {}\n\n  @Get('products')\n  async searchProducts(\n    @Query('q') query?: string,\n    @Query('category') category?: string,\n    @Query('brand') brand?: string,\n    @Query('minPrice') minPrice?: string,\n    @Query('maxPrice') maxPrice?: string,\n    @Query('minRating') minRating?: string,\n    @Query('tags') tags?: string,\n    @Query('inStock') inStock?: string,\n    @Query('sortBy') sortBy?: string,\n    @Query('page') page?: string,\n    @Query('limit') limit?: string,\n  ) {\n    const searchQuery: SearchQuery = {\n      query,\n      category,\n      brand,\n      minPrice: minPrice ? parseFloat(minPrice) : undefined,\n      maxPrice: maxPrice ? parseFloat(maxPrice) : undefined,\n      minRating: minRating ? parseFloat(minRating) : undefined,\n      tags: tags ? tags.split(',') : undefined,\n      inStock: inStock === 'true',\n      sortBy: sortBy as any,\n      page: page ? parseInt(page, 10) : 1,\n      limit: limit ? parseInt(limit, 10) : 20,\n    };\n\n    return this.searchService.search(searchQuery);\n  }\n\n  @Get('autocomplete')\n  async autocomplete(@Query('q') query: string) {\n    return this.searchService.autocomplete(query);\n  }\n\n  @Get('products/:id/similar')\n  async getSimilarProducts(@Param('id') productId: string) {\n    return this.searchService.getSimilarProducts(productId);\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"2-log-aggregation-with-logstash",children:"2. Log Aggregation with Logstash"}),"\n",(0,s.jsx)(n.p,{children:"Centralize application logs from multiple microservices for monitoring and debugging."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// winston-elasticsearch.config.ts\nimport { WinstonModule } from 'nest-winston';\nimport * as winston from 'winston';\nimport { ElasticsearchTransport } from 'winston-elasticsearch';\n\nexport const createLogger = () => {\n  const esTransportOpts = {\n    level: 'info',\n    clientOpts: {\n      node: process.env.ELASTICSEARCH_NODE || 'http://localhost:9200',\n      auth: {\n        username: process.env.ELASTICSEARCH_USERNAME || 'elastic',\n        password: process.env.ELASTICSEARCH_PASSWORD || 'changeme',\n      },\n    },\n    index: 'logs-ecommerce',\n    transformer: (logData: any) => {\n      return {\n        '@timestamp': new Date().toISOString(),\n        message: logData.message,\n        level: logData.level,\n        service: process.env.SERVICE_NAME || 'unknown',\n        environment: process.env.NODE_ENV || 'development',\n        ...logData.meta,\n      };\n    },\n  };\n\n  return WinstonModule.createLogger({\n    transports: [\n      // Console transport for development\n      new winston.transports.Console({\n        format: winston.format.combine(\n          winston.format.timestamp(),\n          winston.format.colorize(),\n          winston.format.printf(({ timestamp, level, message, ...meta }) => {\n            return `${timestamp} [${level}]: ${message} ${\n              Object.keys(meta).length ? JSON.stringify(meta, null, 2) : ''\n            }`;\n          }),\n        ),\n      }),\n      // Elasticsearch transport for centralized logging\n      new ElasticsearchTransport(esTransportOpts),\n    ],\n  });\n};\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Logstash configuration:"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-ruby",children:'# logstash.conf\ninput {\n  # Collect logs from Filebeat\n  beats {\n    port => 5044\n  }\n\n  # Collect logs from HTTP endpoint\n  http {\n    port => 8080\n    codec => json\n  }\n\n  # Collect logs from TCP\n  tcp {\n    port => 5000\n    codec => json_lines\n  }\n}\n\nfilter {\n  # Parse JSON logs\n  json {\n    source => "message"\n    target => "parsed"\n  }\n\n  # Add service name\n  if [parsed][service] {\n    mutate {\n      add_field => { "service" => "%{[parsed][service]}" }\n    }\n  }\n\n  # Parse error stack traces\n  if [parsed][level] == "error" {\n    mutate {\n      add_tag => ["error"]\n    }\n  }\n\n  # Extract HTTP request information\n  if [parsed][req] {\n    mutate {\n      add_field => {\n        "http_method" => "%{[parsed][req][method]}"\n        "http_url" => "%{[parsed][req][url]}"\n        "http_status" => "%{[parsed][res][statusCode]}"\n      }\n    }\n  }\n\n  # Add geolocation for IP addresses\n  if [parsed][clientIp] {\n    geoip {\n      source => "[parsed][clientIp]"\n      target => "geoip"\n    }\n  }\n\n  # Parse timestamps\n  date {\n    match => [ "[parsed][@timestamp]", "ISO8601" ]\n    target => "@timestamp"\n  }\n}\n\noutput {\n  # Send to Elasticsearch\n  elasticsearch {\n    hosts => ["http://elasticsearch:9200"]\n    index => "logs-ecommerce-%{+YYYY.MM.dd}"\n    user => "elastic"\n    password => "${ELASTICSEARCH_PASSWORD}"\n  }\n\n  # Debug output to console\n  stdout {\n    codec => rubydebug\n  }\n}\n'})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// logging.interceptor.ts\nimport { Injectable, NestInterceptor, ExecutionContext, CallHandler, Logger } from '@nestjs/common';\nimport { Observable } from 'rxjs';\nimport { tap, catchError } from 'rxjs/operators';\n\n@Injectable()\nexport class LoggingInterceptor implements NestInterceptor {\n  private readonly logger = new Logger(LoggingInterceptor.name);\n\n  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {\n    const request = context.switchToHttp().getRequest();\n    const { method, url, body, headers, ip } = request;\n    const userAgent = headers['user-agent'] || '';\n    const startTime = Date.now();\n\n    // Log request\n    this.logger.log({\n      message: 'Incoming request',\n      req: {\n        method,\n        url,\n        body,\n        userAgent,\n        clientIp: ip,\n      },\n    });\n\n    return next.handle().pipe(\n      tap((data) => {\n        const response = context.switchToHttp().getResponse();\n        const { statusCode } = response;\n        const duration = Date.now() - startTime;\n\n        // Log successful response\n        this.logger.log({\n          message: 'Request completed',\n          req: { method, url },\n          res: { statusCode, duration },\n        });\n      }),\n      catchError((error) => {\n        const duration = Date.now() - startTime;\n\n        // Log error\n        this.logger.error({\n          message: 'Request failed',\n          req: { method, url, body },\n          error: {\n            message: error.message,\n            stack: error.stack,\n            statusCode: error.status,\n          },\n          duration,\n        });\n\n        throw error;\n      }),\n    );\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"3-real-time-analytics-dashboard",children:"3. Real-Time Analytics Dashboard"}),"\n",(0,s.jsx)(n.p,{children:"Track user behavior, sales metrics, and system performance in real-time."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// analytics.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { ElasticsearchService } from '@nestjs/elasticsearch';\n\nexport interface AnalyticsEvent {\n  eventType: 'page_view' | 'product_view' | 'add_to_cart' | 'purchase' | 'search';\n  userId?: string;\n  sessionId: string;\n  timestamp: Date;\n  data: Record<string, any>;\n}\n\nexport interface AnalyticsQuery {\n  startDate: Date;\n  endDate: Date;\n  eventType?: string;\n  interval?: 'hour' | 'day' | 'week' | 'month';\n}\n\n@Injectable()\nexport class AnalyticsService {\n  private readonly logger = new Logger(AnalyticsService.name);\n  private readonly ANALYTICS_INDEX = 'analytics-events';\n\n  constructor(private readonly elasticsearchService: ElasticsearchService) {}\n\n  async trackEvent(event: AnalyticsEvent): Promise<void> {\n    try {\n      await this.elasticsearchService.index({\n        index: this.ANALYTICS_INDEX,\n        document: {\n          ...event,\n          '@timestamp': event.timestamp.toISOString(),\n        },\n      });\n\n      this.logger.debug(`Tracked event: ${event.eventType}`);\n    } catch (error) {\n      this.logger.error(`Failed to track event: ${error.message}`);\n    }\n  }\n\n  async getEventCounts(query: AnalyticsQuery): Promise<any> {\n    const { startDate, endDate, eventType, interval = 'day' } = query;\n\n    try {\n      const must: any[] = [\n        {\n          range: {\n            '@timestamp': {\n              gte: startDate.toISOString(),\n              lte: endDate.toISOString(),\n            },\n          },\n        },\n      ];\n\n      if (eventType) {\n        must.push({ term: { eventType } });\n      }\n\n      const response = await this.elasticsearchService.search({\n        index: this.ANALYTICS_INDEX,\n        body: {\n          size: 0,\n          query: { bool: { must } },\n          aggs: {\n            events_over_time: {\n              date_histogram: {\n                field: '@timestamp',\n                calendar_interval: interval,\n                min_doc_count: 0,\n              },\n              aggs: {\n                by_type: {\n                  terms: { field: 'eventType' },\n                },\n              },\n            },\n            total_by_type: {\n              terms: { field: 'eventType' },\n            },\n          },\n        },\n      });\n\n      return {\n        timeline: response.aggregations.events_over_time.buckets.map((bucket: any) => ({\n          timestamp: bucket.key_as_string,\n          total: bucket.doc_count,\n          byType: bucket.by_type.buckets.reduce((acc: any, b: any) => {\n            acc[b.key] = b.doc_count;\n            return acc;\n          }, {}),\n        })),\n        totals: response.aggregations.total_by_type.buckets.reduce((acc: any, b: any) => {\n          acc[b.key] = b.doc_count;\n          return acc;\n        }, {}),\n      };\n    } catch (error) {\n      this.logger.error(`Failed to get event counts: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async getSalesMetrics(query: AnalyticsQuery): Promise<any> {\n    const { startDate, endDate, interval = 'day' } = query;\n\n    try {\n      const response = await this.elasticsearchService.search({\n        index: this.ANALYTICS_INDEX,\n        body: {\n          size: 0,\n          query: {\n            bool: {\n              must: [\n                { term: { eventType: 'purchase' } },\n                {\n                  range: {\n                    '@timestamp': {\n                      gte: startDate.toISOString(),\n                      lte: endDate.toISOString(),\n                    },\n                  },\n                },\n              ],\n            },\n          },\n          aggs: {\n            sales_over_time: {\n              date_histogram: {\n                field: '@timestamp',\n                calendar_interval: interval,\n                min_doc_count: 0,\n              },\n              aggs: {\n                revenue: {\n                  sum: { field: 'data.amount' },\n                },\n                average_order_value: {\n                  avg: { field: 'data.amount' },\n                },\n              },\n            },\n            total_revenue: {\n              sum: { field: 'data.amount' },\n            },\n            average_order_value: {\n              avg: { field: 'data.amount' },\n            },\n            top_products: {\n              terms: {\n                field: 'data.productId',\n                size: 10,\n              },\n              aggs: {\n                revenue: {\n                  sum: { field: 'data.amount' },\n                },\n              },\n            },\n          },\n        },\n      });\n\n      return {\n        timeline: response.aggregations.sales_over_time.buckets.map((bucket: any) => ({\n          timestamp: bucket.key_as_string,\n          orders: bucket.doc_count,\n          revenue: bucket.revenue.value,\n          averageOrderValue: bucket.average_order_value.value,\n        })),\n        summary: {\n          totalOrders: response.hits.total.value,\n          totalRevenue: response.aggregations.total_revenue.value,\n          averageOrderValue: response.aggregations.average_order_value.value,\n        },\n        topProducts: response.aggregations.top_products.buckets.map((bucket: any) => ({\n          productId: bucket.key,\n          orders: bucket.doc_count,\n          revenue: bucket.revenue.value,\n        })),\n      };\n    } catch (error) {\n      this.logger.error(`Failed to get sales metrics: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async getUserBehaviorAnalytics(userId: string, days = 30): Promise<any> {\n    const endDate = new Date();\n    const startDate = new Date();\n    startDate.setDate(startDate.getDate() - days);\n\n    try {\n      const response = await this.elasticsearchService.search({\n        index: this.ANALYTICS_INDEX,\n        body: {\n          size: 100,\n          query: {\n            bool: {\n              must: [\n                { term: { userId } },\n                {\n                  range: {\n                    '@timestamp': {\n                      gte: startDate.toISOString(),\n                      lte: endDate.toISOString(),\n                    },\n                  },\n                },\n              ],\n            },\n          },\n          sort: [{ '@timestamp': 'desc' }],\n          aggs: {\n            event_types: {\n              terms: { field: 'eventType' },\n            },\n            daily_activity: {\n              date_histogram: {\n                field: '@timestamp',\n                calendar_interval: 'day',\n              },\n            },\n          },\n        },\n      });\n\n      return {\n        recentEvents: response.hits.hits.map((hit: any) => hit._source),\n        eventTypeCounts: response.aggregations.event_types.buckets.reduce((acc: any, b: any) => {\n          acc[b.key] = b.doc_count;\n          return acc;\n        }, {}),\n        dailyActivity: response.aggregations.daily_activity.buckets.map((bucket: any) => ({\n          date: bucket.key_as_string,\n          count: bucket.doc_count,\n        })),\n      };\n    } catch (error) {\n      this.logger.error(`Failed to get user behavior analytics: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async getSearchAnalytics(query: AnalyticsQuery): Promise<any> {\n    const { startDate, endDate } = query;\n\n    try {\n      const response = await this.elasticsearchService.search({\n        index: this.ANALYTICS_INDEX,\n        body: {\n          size: 0,\n          query: {\n            bool: {\n              must: [\n                { term: { eventType: 'search' } },\n                {\n                  range: {\n                    '@timestamp': {\n                      gte: startDate.toISOString(),\n                      lte: endDate.toISOString(),\n                    },\n                  },\n                },\n              ],\n            },\n          },\n          aggs: {\n            top_searches: {\n              terms: {\n                field: 'data.query.keyword',\n                size: 20,\n              },\n            },\n            searches_with_no_results: {\n              filter: {\n                term: { 'data.resultsCount': 0 },\n              },\n              aggs: {\n                queries: {\n                  terms: {\n                    field: 'data.query.keyword',\n                    size: 20,\n                  },\n                },\n              },\n            },\n          },\n        },\n      });\n\n      return {\n        topSearches: response.aggregations.top_searches.buckets.map((bucket: any) => ({\n          query: bucket.key,\n          count: bucket.doc_count,\n        })),\n        searchesWithNoResults: response.aggregations.searches_with_no_results.queries.buckets.map((bucket: any) => ({\n          query: bucket.key,\n          count: bucket.doc_count,\n        })),\n        totalSearches: response.hits.total.value,\n        noResultsRate: (response.aggregations.searches_with_no_results.doc_count / response.hits.total.value) * 100,\n      };\n    } catch (error) {\n      this.logger.error(`Failed to get search analytics: ${error.message}`);\n      throw error;\n    }\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"4-application-performance-monitoring-apm",children:"4. Application Performance Monitoring (APM)"}),"\n",(0,s.jsx)(n.p,{children:"Monitor application performance, track errors, and identify bottlenecks."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// apm.config.ts\nimport apm from 'elastic-apm-node';\n\nexport const initializeAPM = () => {\n  return apm.start({\n    serviceName: process.env.SERVICE_NAME || 'ecommerce-service',\n    serverUrl: process.env.ELASTIC_APM_SERVER_URL || 'http://localhost:8200',\n    secretToken: process.env.ELASTIC_APM_SECRET_TOKEN,\n    environment: process.env.NODE_ENV || 'development',\n    active: process.env.ELASTIC_APM_ACTIVE === 'true',\n    captureBody: 'all',\n    captureHeaders: true,\n    logLevel: 'info',\n    transactionSampleRate: process.env.NODE_ENV === 'production' ? 0.1 : 1.0,\n    metricsInterval: '30s',\n    centralConfig: false,\n  });\n};\n"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// main.ts with APM\nimport { NestFactory } from '@nestjs/core';\nimport { AppModule } from './app.module';\nimport { initializeAPM } from './apm.config';\n\n// Initialize APM first, before any other imports\nconst apm = initializeAPM();\n\nasync function bootstrap() {\n  const app = await NestFactory.create(AppModule);\n\n  // Enable CORS\n  app.enableCors();\n\n  await app.listen(3000);\n\n  // Track application startup\n  apm.setCustomContext({\n    startupTime: Date.now(),\n    environment: process.env.NODE_ENV,\n  });\n}\n\nbootstrap();\n"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// apm.interceptor.ts\nimport { Injectable, NestInterceptor, ExecutionContext, CallHandler } from '@nestjs/common';\nimport { Observable } from 'rxjs';\nimport { tap, catchError } from 'rxjs/operators';\nimport apm from 'elastic-apm-node';\n\n@Injectable()\nexport class ApmInterceptor implements NestInterceptor {\n  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {\n    const request = context.switchToHttp().getRequest();\n    const { method, url } = request;\n\n    // Start a custom transaction\n    const transaction = apm.startTransaction(`${method} ${url}`, 'request');\n\n    // Add custom context\n    if (transaction) {\n      transaction.setCustomContext({\n        userId: request.user?.id,\n        sessionId: request.sessionID,\n      });\n    }\n\n    return next.handle().pipe(\n      tap(() => {\n        // Mark transaction as successful\n        if (transaction) {\n          transaction.result = 'success';\n          transaction.end();\n        }\n      }),\n      catchError((error) => {\n        // Capture error in APM\n        if (transaction) {\n          apm.captureError(error, {\n            request,\n            custom: {\n              transactionId: transaction.id,\n            },\n          });\n          transaction.result = 'error';\n          transaction.end();\n        }\n        throw error;\n      }),\n    );\n  }\n}\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Custom span tracking:"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// database.service.ts with APM\nimport { Injectable } from '@nestjs/common';\nimport apm from 'elastic-apm-node';\n\n@Injectable()\nexport class DatabaseService {\n  async queryProducts(filters: any): Promise<any[]> {\n    // Create a custom span for database query\n    const span = apm.startSpan('Database Query: Products');\n\n    try {\n      // Add custom labels\n      span?.addLabels({\n        queryType: 'select',\n        table: 'products',\n        filters: JSON.stringify(filters),\n      });\n\n      // Simulate database query\n      const results = await this.executeQuery(filters);\n\n      // Mark span as successful\n      span?.setOutcome('success');\n\n      return results;\n    } catch (error) {\n      // Mark span as failed\n      span?.setOutcome('failure');\n      apm.captureError(error);\n      throw error;\n    } finally {\n      // End the span\n      span?.end();\n    }\n  }\n\n  private async executeQuery(filters: any): Promise<any[]> {\n    // Actual database query implementation\n    return [];\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"docker-compose-setup",children:"Docker Compose Setup"}),"\n",(0,s.jsx)(n.p,{children:"Complete Elasticsearch stack setup for local development:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-yaml",children:"# docker-compose.yml\nversion: '3.8'\n\nservices:\n  elasticsearch:\n    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0\n    container_name: elasticsearch\n    environment:\n      - discovery.type=single-node\n      - 'ES_JAVA_OPTS=-Xms512m -Xmx512m'\n      - xpack.security.enabled=true\n      - ELASTIC_PASSWORD=changeme\n    ports:\n      - '9200:9200'\n      - '9300:9300'\n    volumes:\n      - elasticsearch-data:/usr/share/elasticsearch/data\n    networks:\n      - elastic\n\n  kibana:\n    image: docker.elastic.co/kibana/kibana:8.11.0\n    container_name: kibana\n    environment:\n      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200\n      - ELASTICSEARCH_USERNAME=elastic\n      - ELASTICSEARCH_PASSWORD=changeme\n    ports:\n      - '5601:5601'\n    depends_on:\n      - elasticsearch\n    networks:\n      - elastic\n\n  logstash:\n    image: docker.elastic.co/logstash/logstash:8.11.0\n    container_name: logstash\n    volumes:\n      - ./logstash/pipeline:/usr/share/logstash/pipeline\n      - ./logstash/config/logstash.yml:/usr/share/logstash/config/logstash.yml\n    ports:\n      - '5044:5044' # Beats input\n      - '5000:5000' # TCP input\n      - '9600:9600' # Monitoring\n    environment:\n      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200\n      - ELASTICSEARCH_USERNAME=elastic\n      - ELASTICSEARCH_PASSWORD=changeme\n    depends_on:\n      - elasticsearch\n    networks:\n      - elastic\n\n  filebeat:\n    image: docker.elastic.co/beats/filebeat:8.11.0\n    container_name: filebeat\n    user: root\n    volumes:\n      - ./filebeat/filebeat.yml:/usr/share/filebeat/filebeat.yml:ro\n      - /var/lib/docker/containers:/var/lib/docker/containers:ro\n      - /var/run/docker.sock:/var/run/docker.sock:ro\n      - filebeat-data:/usr/share/filebeat/data\n    command: filebeat -e -strict.perms=false\n    depends_on:\n      - elasticsearch\n      - logstash\n    networks:\n      - elastic\n\n  apm-server:\n    image: docker.elastic.co/apm/apm-server:8.11.0\n    container_name: apm-server\n    ports:\n      - '8200:8200'\n    environment:\n      - output.elasticsearch.hosts=[\"http://elasticsearch:9200\"]\n      - output.elasticsearch.username=elastic\n      - output.elasticsearch.password=changeme\n      - apm-server.rum.enabled=true\n      - apm-server.kibana.enabled=true\n      - apm-server.kibana.host=kibana:5601\n    depends_on:\n      - elasticsearch\n      - kibana\n    networks:\n      - elastic\n\nvolumes:\n  elasticsearch-data:\n  filebeat-data:\n\nnetworks:\n  elastic:\n    driver: bridge\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Filebeat configuration:"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-yaml",children:"# filebeat/filebeat.yml\nfilebeat.inputs:\n  - type: container\n    paths:\n      - '/var/lib/docker/containers/*/*.log'\n    processors:\n      - add_docker_metadata:\n          host: 'unix:///var/run/docker.sock'\n\n  - type: log\n    enabled: true\n    paths:\n      - /var/log/app/*.log\n    json.keys_under_root: true\n    json.add_error_key: true\n\nprocessors:\n  - add_host_metadata:\n      when.not.contains.tags: forwarded\n  - add_cloud_metadata: ~\n  - add_docker_metadata: ~\n\noutput.logstash:\n  hosts: ['logstash:5044']\n\nlogging.level: info\n"})}),"\n",(0,s.jsx)(n.h2,{id:"kibana-dashboard-configuration",children:"Kibana Dashboard Configuration"}),"\n",(0,s.jsx)(n.p,{children:"Create custom dashboards for monitoring:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// kibana-dashboard.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { ElasticsearchService } from '@nestjs/elasticsearch';\n\n@Injectable()\nexport class KibanaDashboardService {\n  private readonly logger = new Logger(KibanaDashboardService.name);\n\n  constructor(private readonly elasticsearchService: ElasticsearchService) {}\n\n  async createProductSearchDashboard() {\n    const dashboard = {\n      attributes: {\n        title: 'Product Search Analytics',\n        description: 'Monitor product search performance and user behavior',\n        panelsJSON: JSON.stringify([\n          {\n            id: 'search-volume',\n            type: 'line',\n            title: 'Search Volume Over Time',\n          },\n          {\n            id: 'top-searches',\n            type: 'table',\n            title: 'Top Search Queries',\n          },\n          {\n            id: 'no-results-searches',\n            type: 'table',\n            title: 'Searches with No Results',\n          },\n          {\n            id: 'search-conversion',\n            type: 'metric',\n            title: 'Search to Purchase Conversion',\n          },\n        ]),\n        optionsJSON: JSON.stringify({\n          darkTheme: false,\n        }),\n        version: 1,\n        timeRestore: false,\n        kibanaSavedObjectMeta: {\n          searchSourceJSON: JSON.stringify({\n            query: { language: 'kuery', query: '' },\n            filter: [],\n          }),\n        },\n      },\n    };\n\n    this.logger.log('Product search dashboard configuration created');\n    return dashboard;\n  }\n\n  async createSalesDashboard() {\n    const dashboard = {\n      attributes: {\n        title: 'Sales Analytics',\n        description: 'Real-time sales metrics and revenue tracking',\n        panelsJSON: JSON.stringify([\n          {\n            id: 'revenue-timeline',\n            type: 'area',\n            title: 'Revenue Over Time',\n          },\n          {\n            id: 'order-count',\n            type: 'metric',\n            title: 'Total Orders',\n          },\n          {\n            id: 'average-order-value',\n            type: 'gauge',\n            title: 'Average Order Value',\n          },\n          {\n            id: 'top-products',\n            type: 'pie',\n            title: 'Top Selling Products',\n          },\n          {\n            id: 'conversion-funnel',\n            type: 'funnel',\n            title: 'Purchase Funnel',\n          },\n        ]),\n        version: 1,\n      },\n    };\n\n    this.logger.log('Sales dashboard configuration created');\n    return dashboard;\n  }\n\n  async createPerformanceDashboard() {\n    const dashboard = {\n      attributes: {\n        title: 'Application Performance',\n        description: 'Monitor application health, errors, and response times',\n        panelsJSON: JSON.stringify([\n          {\n            id: 'response-time',\n            type: 'line',\n            title: 'Average Response Time',\n          },\n          {\n            id: 'error-rate',\n            type: 'line',\n            title: 'Error Rate',\n          },\n          {\n            id: 'throughput',\n            type: 'metric',\n            title: 'Requests per Second',\n          },\n          {\n            id: 'slow-transactions',\n            type: 'table',\n            title: 'Slowest Transactions',\n          },\n          {\n            id: 'error-types',\n            type: 'pie',\n            title: 'Error Distribution',\n          },\n        ]),\n        version: 1,\n      },\n    };\n\n    this.logger.log('Performance dashboard configuration created');\n    return dashboard;\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"best-practices",children:"Best Practices"}),"\n",(0,s.jsx)(n.h3,{id:"1-index-lifecycle-management",children:"1. Index Lifecycle Management"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// index-lifecycle.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { ElasticsearchService } from '@nestjs/elasticsearch';\nimport { Cron } from '@nestjs/schedule';\n\n@Injectable()\nexport class IndexLifecycleService {\n  private readonly logger = new Logger(IndexLifecycleService.name);\n\n  constructor(private readonly elasticsearchService: ElasticsearchService) {}\n\n  async setupILMPolicy() {\n    const policyName = 'logs-lifecycle-policy';\n\n    try {\n      await this.elasticsearchService.ilm.putLifecycle({\n        name: policyName,\n        body: {\n          policy: {\n            phases: {\n              hot: {\n                actions: {\n                  rollover: {\n                    max_size: '50GB',\n                    max_age: '7d',\n                  },\n                },\n              },\n              warm: {\n                min_age: '7d',\n                actions: {\n                  shrink: {\n                    number_of_shards: 1,\n                  },\n                  forcemerge: {\n                    max_num_segments: 1,\n                  },\n                },\n              },\n              cold: {\n                min_age: '30d',\n                actions: {\n                  freeze: {},\n                },\n              },\n              delete: {\n                min_age: '90d',\n                actions: {\n                  delete: {},\n                },\n              },\n            },\n          },\n        },\n      });\n\n      this.logger.log(`ILM policy created: ${policyName}`);\n    } catch (error) {\n      this.logger.error(`Failed to create ILM policy: ${error.message}`);\n      throw error;\n    }\n  }\n\n  @Cron('0 0 * * *') // Run daily at midnight\n  async cleanupOldIndices() {\n    const cutoffDate = new Date();\n    cutoffDate.setDate(cutoffDate.getDate() - 90); // Keep last 90 days\n\n    try {\n      const indices = await this.elasticsearchService.cat.indices({\n        format: 'json',\n        h: ['index', 'creation.date'],\n      });\n\n      for (const index of indices as any[]) {\n        const creationDate = new Date(parseInt(index['creation.date'], 10));\n\n        if (creationDate < cutoffDate && index.index.startsWith('logs-')) {\n          await this.elasticsearchService.indices.delete({\n            index: index.index,\n          });\n          this.logger.log(`Deleted old index: ${index.index}`);\n        }\n      }\n    } catch (error) {\n      this.logger.error(`Failed to cleanup old indices: ${error.message}`);\n    }\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"2-query-optimization",children:"2. Query Optimization"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// optimized-search.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { ElasticsearchService } from '@nestjs/elasticsearch';\n\n@Injectable()\nexport class OptimizedSearchService {\n  private readonly logger = new Logger(OptimizedSearchService.name);\n\n  constructor(private readonly elasticsearchService: ElasticsearchService) {}\n\n  async searchWithCache(query: any, cacheKey: string) {\n    try {\n      const response = await this.elasticsearchService.search({\n        index: 'products',\n        body: query,\n        // Use request cache for frequently accessed queries\n        request_cache: true,\n        // Limit the number of fields returned\n        _source: ['id', 'name', 'price', 'image'],\n        // Use search_after for pagination instead of from/size\n        // This is more efficient for deep pagination\n        track_total_hits: 10000, // Limit total hits tracking\n      });\n\n      return response;\n    } catch (error) {\n      this.logger.error(`Optimized search failed: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async bulkSearch(queries: any[]) {\n    // Use msearch for multiple searches in one request\n    const searches = queries.flatMap((query) => [{ index: 'products' }, query]);\n\n    try {\n      const response = await this.elasticsearchService.msearch({\n        body: searches,\n      });\n\n      return response.responses;\n    } catch (error) {\n      this.logger.error(`Bulk search failed: ${error.message}`);\n      throw error;\n    }\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"3-monitoring-and-alerts",children:"3. Monitoring and Alerts"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// elasticsearch-health.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { ElasticsearchService } from '@nestjs/elasticsearch';\nimport { Cron } from '@nestjs/schedule';\n\n@Injectable()\nexport class ElasticsearchHealthService {\n  private readonly logger = new Logger(ElasticsearchHealthService.name);\n\n  constructor(private readonly elasticsearchService: ElasticsearchService) {}\n\n  @Cron('*/5 * * * *') // Every 5 minutes\n  async checkClusterHealth() {\n    try {\n      const health = await this.elasticsearchService.cluster.health();\n\n      if (health.status === 'red') {\n        this.logger.error('Elasticsearch cluster is RED!');\n        // Send alert\n      } else if (health.status === 'yellow') {\n        this.logger.warn('Elasticsearch cluster is YELLOW');\n      } else {\n        this.logger.debug('Elasticsearch cluster is GREEN');\n      }\n\n      return {\n        status: health.status,\n        numberOfNodes: health.number_of_nodes,\n        numberOfDataNodes: health.number_of_data_nodes,\n        activePrimaryShards: health.active_primary_shards,\n        activeShards: health.active_shards,\n        relocatingShards: health.relocating_shards,\n        initializingShards: health.initializing_shards,\n        unassignedShards: health.unassigned_shards,\n      };\n    } catch (error) {\n      this.logger.error(`Health check failed: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async getNodeStats() {\n    try {\n      const stats = await this.elasticsearchService.nodes.stats();\n      return stats;\n    } catch (error) {\n      this.logger.error(`Failed to get node stats: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async getIndexStats(index: string) {\n    try {\n      const stats = await this.elasticsearchService.indices.stats({\n        index,\n      });\n      return stats;\n    } catch (error) {\n      this.logger.error(`Failed to get index stats: ${error.message}`);\n      throw error;\n    }\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"environment-configuration",children:"Environment Configuration"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"# .env\n# Elasticsearch Configuration\nELASTICSEARCH_NODE=http://localhost:9200\nELASTICSEARCH_USERNAME=elastic\nELASTICSEARCH_PASSWORD=changeme\n\n# APM Configuration\nELASTIC_APM_SERVER_URL=http://localhost:8200\nELASTIC_APM_SECRET_TOKEN=your-secret-token\nELASTIC_APM_ACTIVE=true\n\n# Service Configuration\nSERVICE_NAME=ecommerce-api\nNODE_ENV=production\n\n# Logstash Configuration\nLOGSTASH_HOST=localhost\nLOGSTASH_PORT=5000\n"})}),"\n",(0,s.jsx)(n.h2,{id:"summary",children:"Summary"}),"\n",(0,s.jsx)(n.p,{children:"The Elasticsearch Stack provides comprehensive capabilities for:"}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Advanced Search"}),": Full-text search with facets, filters, and autocomplete"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Log Aggregation"}),": Centralized logging from multiple microservices"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Real-time Analytics"}),": Track user behavior, sales, and performance metrics"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"APM"}),": Monitor application performance and identify bottlenecks"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Visualization"}),": Create custom dashboards in Kibana"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Alerting"}),": Automated health checks and performance monitoring"]}),"\n"]}),"\n",(0,s.jsx)(n.p,{children:"This guide covered production-ready implementations including index lifecycle management, query optimization, and comprehensive monitoring solutions for e-commerce platforms."})]})}function u(e={}){const{wrapper:n}={...(0,a.R)(),...e.components};return n?(0,s.jsx)(n,{...e,children:(0,s.jsx)(d,{...e})}):d(e)}}}]);