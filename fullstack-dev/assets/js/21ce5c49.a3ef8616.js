"use strict";(globalThis.webpackChunkdocs_site=globalThis.webpackChunkdocs_site||[]).push([[17673],{28453(e,n,t){t.d(n,{R:()=>a,x:()=>o});var r=t(96540);const s={},i=r.createContext(s);function a(e){const n=r.useContext(i);return r.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:a(e.components),r.createElement(i.Provider,{value:n},e.children)}},81688(e,n,t){t.r(n),t.d(n,{assets:()=>c,contentTitle:()=>o,default:()=>h,frontMatter:()=>a,metadata:()=>r,toc:()=>d});const r=JSON.parse('{"id":"backend/redis-entraid-guide","title":"Redis with Microsoft Entra ID Authentication","description":"Overview","source":"@site/docs/backend/redis-entraid-guide.md","sourceDirName":"backend","slug":"/backend/redis-entraid-guide","permalink":"/fullstack-dev/docs/backend/redis-entraid-guide","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/redis-entraid-guide.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Redis Search Guide - Full-Text Search & Aggregations","permalink":"/fullstack-dev/docs/backend/redis-search-guide"},"next":{"title":"Elasticsearch Stack Implementation Guide","permalink":"/fullstack-dev/docs/backend/elasticsearch-stack-guide"}}');var s=t(74848),i=t(28453);const a={},o="Redis with Microsoft Entra ID Authentication",c={},d=[{value:"Overview",id:"overview",level:2},{value:"Key Benefits",id:"key-benefits",level:2},{value:"Prerequisites",id:"prerequisites",level:2},{value:"Use Cases",id:"use-cases",level:2},{value:"1. NestJS Service with Managed Identity",id:"1-nestjs-service-with-managed-identity",level:3},{value:"2. Multi-Environment Configuration with Service Principals",id:"2-multi-environment-configuration-with-service-principals",level:3},{value:"3. Session Store with Entra ID Authentication",id:"3-session-store-with-entra-id-authentication",level:3},{value:"4. Distributed Cache with RBAC",id:"4-distributed-cache-with-rbac",level:3},{value:"Azure Setup Guide",id:"azure-setup-guide",level:2},{value:"1. Enable Microsoft Entra ID Authentication on Azure Cache for Redis",id:"1-enable-microsoft-entra-id-authentication-on-azure-cache-for-redis",level:3},{value:"2. Assign Redis Data Access Roles",id:"2-assign-redis-data-access-roles",level:3},{value:"3. Configure Managed Identity",id:"3-configure-managed-identity",level:3},{value:"Best Practices",id:"best-practices",level:2},{value:"1. Token Management",id:"1-token-management",level:3},{value:"2. Health Checks",id:"2-health-checks",level:3},{value:"3. Monitoring and Logging",id:"3-monitoring-and-logging",level:3},{value:"Troubleshooting",id:"troubleshooting",level:2},{value:"Common Issues",id:"common-issues",level:3},{value:"Security Considerations",id:"security-considerations",level:2},{value:"Summary",id:"summary",level:2}];function l(e){const n={code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,i.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(n.header,{children:(0,s.jsx)(n.h1,{id:"redis-with-microsoft-entra-id-authentication",children:"Redis with Microsoft Entra ID Authentication"})}),"\n",(0,s.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,s.jsx)(n.p,{children:"Microsoft Entra ID (formerly Azure Active Directory) integration with Azure Cache for Redis provides enterprise-grade authentication and authorization using managed identities and service principals. This eliminates the need for password-based authentication and provides centralized access management."}),"\n",(0,s.jsx)(n.h2,{id:"key-benefits",children:"Key Benefits"}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Password-less Authentication"}),": Use managed identities instead of connection strings"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Centralized Access Control"}),": Manage permissions through Azure RBAC"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Audit Trail"}),": Track all Redis access through Entra ID logs"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Token-based Security"}),": Automatic token refresh and rotation"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Compliance"}),": Meet enterprise security requirements"]}),"\n"]}),"\n",(0,s.jsx)(n.h2,{id:"prerequisites",children:"Prerequisites"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"# Install required packages\nnpm install @azure/identity redis\n\n# For development/testing\nnpm install @azure/identity-cache-persistence --save-dev\n"})}),"\n",(0,s.jsx)(n.h2,{id:"use-cases",children:"Use Cases"}),"\n",(0,s.jsx)(n.h3,{id:"1-nestjs-service-with-managed-identity",children:"1. NestJS Service with Managed Identity"}),"\n",(0,s.jsx)(n.p,{children:"Use Azure Managed Identity for production deployments on Azure App Service, AKS, or Azure Functions."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// redis-entraid.config.ts\nimport { registerAs } from '@nestjs/config';\n\nexport default registerAs('redis-entraid', () => ({\n  host: process.env.REDIS_HOST || 'your-cache.redis.cache.windows.net',\n  port: parseInt(process.env.REDIS_PORT, 10) || 6380,\n  tls: process.env.REDIS_TLS !== 'false',\n  // Entra ID configuration\n  entraId: {\n    enabled: process.env.REDIS_ENTRA_ENABLED === 'true',\n    scope: process.env.REDIS_ENTRA_SCOPE || 'https://redis.azure.com/.default',\n    // For development: use Azure CLI credentials\n    // For production: use Managed Identity (automatically detected)\n    credentialType: process.env.NODE_ENV === 'production' ? 'managed-identity' : 'azure-cli',\n  },\n}));\n"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// redis-entraid.service.ts\nimport { Injectable, OnModuleInit, OnModuleDestroy, Logger } from '@nestjs/common';\nimport { ConfigService } from '@nestjs/config';\nimport { createClient, RedisClientType } from 'redis';\nimport {\n  DefaultAzureCredential,\n  ManagedIdentityCredential,\n  AzureCliCredential,\n  ChainedTokenCredential,\n} from '@azure/identity';\n\n@Injectable()\nexport class RedisEntraIdService implements OnModuleInit, OnModuleDestroy {\n  private readonly logger = new Logger(RedisEntraIdService.name);\n  private client: RedisClientType;\n  private tokenRefreshInterval: NodeJS.Timeout;\n  private currentToken: string;\n\n  constructor(private configService: ConfigService) {}\n\n  async onModuleInit() {\n    await this.connect();\n  }\n\n  async onModuleDestroy() {\n    if (this.tokenRefreshInterval) {\n      clearInterval(this.tokenRefreshInterval);\n    }\n    await this.disconnect();\n  }\n\n  private async getCredential() {\n    const credentialType = this.configService.get('redis-entraid.entraId.credentialType');\n\n    if (credentialType === 'managed-identity') {\n      // Production: Use Managed Identity\n      this.logger.log('Using Managed Identity credential');\n      return new ManagedIdentityCredential();\n    }\n\n    if (credentialType === 'azure-cli') {\n      // Development: Use Azure CLI\n      this.logger.log('Using Azure CLI credential');\n      return new AzureCliCredential();\n    }\n\n    // Default: Try multiple credential types\n    this.logger.log('Using DefaultAzureCredential');\n    return new DefaultAzureCredential();\n  }\n\n  private async getAccessToken(): Promise<string> {\n    const credential = await this.getCredential();\n    const scope = this.configService.get('redis-entraid.entraId.scope');\n\n    try {\n      const tokenResponse = await credential.getToken(scope);\n\n      if (!tokenResponse?.token) {\n        throw new Error('Failed to get access token from Entra ID');\n      }\n\n      this.logger.log('Successfully retrieved Entra ID access token');\n      return tokenResponse.token;\n    } catch (error) {\n      this.logger.error(`Failed to get Entra ID token: ${error.message}`);\n      throw error;\n    }\n  }\n\n  private async refreshToken() {\n    try {\n      this.currentToken = await this.getAccessToken();\n\n      // Update the client's password (token)\n      if (this.client?.isOpen) {\n        // Re-authenticate with new token\n        await this.client.auth({\n          username: 'default',\n          password: this.currentToken,\n        });\n        this.logger.log('Token refreshed successfully');\n      }\n    } catch (error) {\n      this.logger.error(`Token refresh failed: ${error.message}`);\n      // Attempt to reconnect\n      await this.reconnect();\n    }\n  }\n\n  private async connect() {\n    const host = this.configService.get('redis-entraid.host');\n    const port = this.configService.get('redis-entraid.port');\n    const useTls = this.configService.get('redis-entraid.tls');\n    const entraEnabled = this.configService.get('redis-entraid.entraId.enabled');\n\n    try {\n      if (!entraEnabled) {\n        throw new Error('Entra ID authentication is not enabled');\n      }\n\n      // Get initial access token\n      this.currentToken = await this.getAccessToken();\n\n      // Create Redis client\n      this.client = createClient({\n        socket: {\n          host,\n          port,\n          tls: useTls,\n        },\n        // Use token as password\n        username: 'default',\n        password: this.currentToken,\n      });\n\n      // Error handling\n      this.client.on('error', (err) => {\n        this.logger.error(`Redis client error: ${err.message}`);\n      });\n\n      this.client.on('ready', () => {\n        this.logger.log('Redis client is ready');\n      });\n\n      this.client.on('reconnecting', () => {\n        this.logger.warn('Redis client is reconnecting...');\n      });\n\n      await this.client.connect();\n      this.logger.log(`Connected to Redis at ${host}:${port} using Entra ID`);\n\n      // Set up token refresh (refresh every 30 minutes)\n      this.tokenRefreshInterval = setInterval(() => this.refreshToken(), 30 * 60 * 1000);\n    } catch (error) {\n      this.logger.error(`Failed to connect to Redis: ${error.message}`);\n      throw error;\n    }\n  }\n\n  private async reconnect() {\n    this.logger.warn('Attempting to reconnect to Redis...');\n\n    try {\n      await this.disconnect();\n      await this.connect();\n      this.logger.log('Reconnection successful');\n    } catch (error) {\n      this.logger.error(`Reconnection failed: ${error.message}`);\n      throw error;\n    }\n  }\n\n  private async disconnect() {\n    if (this.client?.isOpen) {\n      await this.client.quit();\n      this.logger.log('Disconnected from Redis');\n    }\n  }\n\n  // Public methods for Redis operations\n  getClient(): RedisClientType {\n    if (!this.client?.isOpen) {\n      throw new Error('Redis client is not connected');\n    }\n    return this.client;\n  }\n\n  async get(key: string): Promise<string | null> {\n    return this.getClient().get(key);\n  }\n\n  async set(key: string, value: string, ttl?: number): Promise<void> {\n    const client = this.getClient();\n    if (ttl) {\n      await client.setEx(key, ttl, value);\n    } else {\n      await client.set(key, value);\n    }\n  }\n\n  async del(key: string): Promise<void> {\n    await this.getClient().del(key);\n  }\n\n  async exists(key: string): Promise<boolean> {\n    const result = await this.getClient().exists(key);\n    return result === 1;\n  }\n\n  async hGetAll(key: string): Promise<Record<string, string>> {\n    return this.getClient().hGetAll(key);\n  }\n\n  async hSet(key: string, field: string, value: string): Promise<void> {\n    await this.getClient().hSet(key, field, value);\n  }\n}\n"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// redis-entraid.module.ts\nimport { Module, Global } from '@nestjs/common';\nimport { ConfigModule } from '@nestjs/config';\nimport { RedisEntraIdService } from './redis-entraid.service';\nimport redisEntraIdConfig from './redis-entraid.config';\n\n@Global()\n@Module({\n  imports: [ConfigModule.forFeature(redisEntraIdConfig)],\n  providers: [RedisEntraIdService],\n  exports: [RedisEntraIdService],\n})\nexport class RedisEntraIdModule {}\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Usage in your services:"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// user-cache.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { RedisEntraIdService } from './redis-entraid.service';\n\n@Injectable()\nexport class UserCacheService {\n  constructor(private redisEntraId: RedisEntraIdService) {}\n\n  async cacheUser(userId: string, userData: any): Promise<void> {\n    const key = `user:${userId}`;\n    await this.redisEntraId.set(\n      key,\n      JSON.stringify(userData),\n      3600, // 1 hour TTL\n    );\n  }\n\n  async getUser(userId: string): Promise<any | null> {\n    const key = `user:${userId}`;\n    const data = await this.redisEntraId.get(key);\n    return data ? JSON.parse(data) : null;\n  }\n\n  async invalidateUser(userId: string): Promise<void> {\n    const key = `user:${userId}`;\n    await this.redisEntraId.del(key);\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"2-multi-environment-configuration-with-service-principals",children:"2. Multi-Environment Configuration with Service Principals"}),"\n",(0,s.jsx)(n.p,{children:"Handle different authentication methods for local development, staging, and production."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// redis-auth.factory.ts\nimport {\n  TokenCredential,\n  DefaultAzureCredential,\n  ClientSecretCredential,\n  ManagedIdentityCredential,\n  AzureCliCredential,\n} from '@azure/identity';\nimport { Logger } from '@nestjs/common';\n\nexport enum CredentialType {\n  DEFAULT = 'default',\n  MANAGED_IDENTITY = 'managed-identity',\n  SERVICE_PRINCIPAL = 'service-principal',\n  AZURE_CLI = 'azure-cli',\n}\n\nexport interface EntraIdConfig {\n  credentialType: CredentialType;\n  tenantId?: string;\n  clientId?: string;\n  clientSecret?: string;\n  managedIdentityClientId?: string;\n}\n\nexport class RedisAuthFactory {\n  private static readonly logger = new Logger(RedisAuthFactory.name);\n\n  static createCredential(config: EntraIdConfig): TokenCredential {\n    switch (config.credentialType) {\n      case CredentialType.MANAGED_IDENTITY:\n        return this.createManagedIdentityCredential(config);\n\n      case CredentialType.SERVICE_PRINCIPAL:\n        return this.createServicePrincipalCredential(config);\n\n      case CredentialType.AZURE_CLI:\n        return this.createAzureCliCredential();\n\n      case CredentialType.DEFAULT:\n      default:\n        return this.createDefaultCredential();\n    }\n  }\n\n  private static createManagedIdentityCredential(config: EntraIdConfig): ManagedIdentityCredential {\n    this.logger.log('Creating Managed Identity credential');\n\n    if (config.managedIdentityClientId) {\n      return new ManagedIdentityCredential({\n        clientId: config.managedIdentityClientId,\n      });\n    }\n\n    return new ManagedIdentityCredential();\n  }\n\n  private static createServicePrincipalCredential(config: EntraIdConfig): ClientSecretCredential {\n    this.logger.log('Creating Service Principal credential');\n\n    const { tenantId, clientId, clientSecret } = config;\n\n    if (!tenantId || !clientId || !clientSecret) {\n      throw new Error('Service Principal requires tenantId, clientId, and clientSecret');\n    }\n\n    return new ClientSecretCredential(tenantId, clientId, clientSecret);\n  }\n\n  private static createAzureCliCredential(): AzureCliCredential {\n    this.logger.log('Creating Azure CLI credential');\n    return new AzureCliCredential();\n  }\n\n  private static createDefaultCredential(): DefaultAzureCredential {\n    this.logger.log('Creating DefaultAzureCredential (tries multiple auth methods)');\n    return new DefaultAzureCredential();\n  }\n}\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Environment-specific configuration:"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// .env.local (Development)\nREDIS_HOST=localhost\nREDIS_PORT=6379\nREDIS_TLS=false\nREDIS_ENTRA_ENABLED=false\n\n# .env.staging (Staging with Service Principal)\nREDIS_HOST=staging-cache.redis.cache.windows.net\nREDIS_PORT=6380\nREDIS_TLS=true\nREDIS_ENTRA_ENABLED=true\nREDIS_ENTRA_CREDENTIAL_TYPE=service-principal\nREDIS_ENTRA_TENANT_ID=your-tenant-id\nREDIS_ENTRA_CLIENT_ID=your-client-id\nREDIS_ENTRA_CLIENT_SECRET=your-client-secret\n\n# .env.production (Production with Managed Identity)\nREDIS_HOST=prod-cache.redis.cache.windows.net\nREDIS_PORT=6380\nREDIS_TLS=true\nREDIS_ENTRA_ENABLED=true\nREDIS_ENTRA_CREDENTIAL_TYPE=managed-identity\nREDIS_ENTRA_MANAGED_IDENTITY_CLIENT_ID=your-managed-identity-client-id\n"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// enhanced-redis-entraid.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { ConfigService } from '@nestjs/config';\nimport { createClient, RedisClientType } from 'redis';\nimport { RedisAuthFactory, CredentialType, EntraIdConfig } from './redis-auth.factory';\n\n@Injectable()\nexport class EnhancedRedisEntraIdService {\n  private readonly logger = new Logger(EnhancedRedisEntraIdService.name);\n  private client: RedisClientType;\n  private tokenRefreshInterval: NodeJS.Timeout;\n\n  constructor(private configService: ConfigService) {}\n\n  async connect() {\n    const host = this.configService.get<string>('REDIS_HOST');\n    const port = this.configService.get<number>('REDIS_PORT');\n    const useTls = this.configService.get<boolean>('REDIS_TLS');\n    const entraEnabled = this.configService.get<boolean>('REDIS_ENTRA_ENABLED');\n\n    if (!entraEnabled) {\n      throw new Error('Entra ID authentication is required');\n    }\n\n    // Build credential configuration\n    const credentialConfig: EntraIdConfig = {\n      credentialType: this.configService.get<CredentialType>('REDIS_ENTRA_CREDENTIAL_TYPE', CredentialType.DEFAULT),\n      tenantId: this.configService.get<string>('REDIS_ENTRA_TENANT_ID'),\n      clientId: this.configService.get<string>('REDIS_ENTRA_CLIENT_ID'),\n      clientSecret: this.configService.get<string>('REDIS_ENTRA_CLIENT_SECRET'),\n      managedIdentityClientId: this.configService.get<string>('REDIS_ENTRA_MANAGED_IDENTITY_CLIENT_ID'),\n    };\n\n    // Create credential using factory\n    const credential = RedisAuthFactory.createCredential(credentialConfig);\n\n    // Get access token\n    const scope = 'https://redis.azure.com/.default';\n    const tokenResponse = await credential.getToken(scope);\n\n    if (!tokenResponse?.token) {\n      throw new Error('Failed to obtain Entra ID token');\n    }\n\n    // Create Redis client\n    this.client = createClient({\n      socket: {\n        host,\n        port,\n        tls: useTls,\n        // Add connection timeout\n        connectTimeout: 10000,\n      },\n      username: 'default',\n      password: tokenResponse.token,\n      // Add command timeout\n      commandsQueueMaxLength: 1000,\n    });\n\n    await this.client.connect();\n    this.logger.log(`Connected to Redis using ${credentialConfig.credentialType} authentication`);\n\n    // Setup token refresh\n    this.setupTokenRefresh(credential, scope);\n  }\n\n  private setupTokenRefresh(credential: any, scope: string) {\n    // Refresh token every 30 minutes (tokens typically valid for 1 hour)\n    this.tokenRefreshInterval = setInterval(async () => {\n      try {\n        const tokenResponse = await credential.getToken(scope);\n\n        if (tokenResponse?.token && this.client?.isOpen) {\n          await this.client.auth({\n            username: 'default',\n            password: tokenResponse.token,\n          });\n          this.logger.log('Token refreshed successfully');\n        }\n      } catch (error) {\n        this.logger.error(`Token refresh failed: ${error.message}`);\n      }\n    }, 30 * 60 * 1000);\n  }\n\n  getClient(): RedisClientType {\n    return this.client;\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"3-session-store-with-entra-id-authentication",children:"3. Session Store with Entra ID Authentication"}),"\n",(0,s.jsx)(n.p,{children:"Secure session management using Redis with Entra ID."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// session-store.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { RedisEntraIdService } from './redis-entraid.service';\n\nexport interface Session {\n  userId: string;\n  email: string;\n  roles: string[];\n  createdAt: number;\n  lastActivity: number;\n  metadata?: Record<string, any>;\n}\n\n@Injectable()\nexport class SessionStoreService {\n  private readonly logger = new Logger(SessionStoreService.name);\n  private readonly SESSION_PREFIX = 'session:';\n  private readonly USER_SESSIONS_PREFIX = 'user:sessions:';\n  private readonly SESSION_TTL = 3600; // 1 hour\n\n  constructor(private redisEntraId: RedisEntraIdService) {}\n\n  private getSessionKey(sessionId: string): string {\n    return `${this.SESSION_PREFIX}${sessionId}`;\n  }\n\n  private getUserSessionsKey(userId: string): string {\n    return `${this.USER_SESSIONS_PREFIX}${userId}`;\n  }\n\n  async createSession(sessionId: string, session: Session): Promise<void> {\n    const client = this.redisEntraId.getClient();\n    const sessionKey = this.getSessionKey(sessionId);\n    const userSessionsKey = this.getUserSessionsKey(session.userId);\n\n    try {\n      // Use pipeline for atomic operations\n      const pipeline = client.multi();\n\n      // Store session data\n      pipeline.hSet(sessionKey, {\n        userId: session.userId,\n        email: session.email,\n        roles: JSON.stringify(session.roles),\n        createdAt: session.createdAt.toString(),\n        lastActivity: session.lastActivity.toString(),\n        metadata: session.metadata ? JSON.stringify(session.metadata) : '{}',\n      });\n\n      // Set TTL on session\n      pipeline.expire(sessionKey, this.SESSION_TTL);\n\n      // Add to user's session set\n      pipeline.sAdd(userSessionsKey, sessionId);\n      pipeline.expire(userSessionsKey, this.SESSION_TTL);\n\n      await pipeline.exec();\n\n      this.logger.log(`Session created for user ${session.userId}`);\n    } catch (error) {\n      this.logger.error(`Failed to create session: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async getSession(sessionId: string): Promise<Session | null> {\n    try {\n      const sessionKey = this.getSessionKey(sessionId);\n      const data = await this.redisEntraId.hGetAll(sessionKey);\n\n      if (!data || Object.keys(data).length === 0) {\n        return null;\n      }\n\n      // Update last activity\n      await this.updateSessionActivity(sessionId);\n\n      return {\n        userId: data.userId,\n        email: data.email,\n        roles: JSON.parse(data.roles),\n        createdAt: parseInt(data.createdAt, 10),\n        lastActivity: parseInt(data.lastActivity, 10),\n        metadata: data.metadata ? JSON.parse(data.metadata) : {},\n      };\n    } catch (error) {\n      this.logger.error(`Failed to get session: ${error.message}`);\n      return null;\n    }\n  }\n\n  async updateSessionActivity(sessionId: string): Promise<void> {\n    const client = this.redisEntraId.getClient();\n    const sessionKey = this.getSessionKey(sessionId);\n    const now = Date.now();\n\n    try {\n      await client.hSet(sessionKey, 'lastActivity', now.toString());\n      await client.expire(sessionKey, this.SESSION_TTL);\n    } catch (error) {\n      this.logger.error(`Failed to update session activity: ${error.message}`);\n    }\n  }\n\n  async deleteSession(sessionId: string): Promise<void> {\n    const client = this.redisEntraId.getClient();\n\n    try {\n      // Get session to find userId\n      const session = await this.getSession(sessionId);\n\n      if (session) {\n        const sessionKey = this.getSessionKey(sessionId);\n        const userSessionsKey = this.getUserSessionsKey(session.userId);\n\n        const pipeline = client.multi();\n        pipeline.del(sessionKey);\n        pipeline.sRem(userSessionsKey, sessionId);\n        await pipeline.exec();\n\n        this.logger.log(`Session ${sessionId} deleted`);\n      }\n    } catch (error) {\n      this.logger.error(`Failed to delete session: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async getUserSessions(userId: string): Promise<string[]> {\n    const client = this.redisEntraId.getClient();\n    const userSessionsKey = this.getUserSessionsKey(userId);\n\n    try {\n      return await client.sMembers(userSessionsKey);\n    } catch (error) {\n      this.logger.error(`Failed to get user sessions: ${error.message}`);\n      return [];\n    }\n  }\n\n  async deleteAllUserSessions(userId: string): Promise<void> {\n    const client = this.redisEntraId.getClient();\n\n    try {\n      const sessionIds = await this.getUserSessions(userId);\n\n      if (sessionIds.length === 0) {\n        return;\n      }\n\n      const pipeline = client.multi();\n\n      // Delete all session keys\n      sessionIds.forEach((sessionId) => {\n        pipeline.del(this.getSessionKey(sessionId));\n      });\n\n      // Delete user sessions set\n      pipeline.del(this.getUserSessionsKey(userId));\n\n      await pipeline.exec();\n\n      this.logger.log(`Deleted ${sessionIds.length} sessions for user ${userId}`);\n    } catch (error) {\n      this.logger.error(`Failed to delete user sessions: ${error.message}`);\n      throw error;\n    }\n  }\n}\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Express middleware integration:"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// session.middleware.ts\nimport { Injectable, NestMiddleware, UnauthorizedException } from '@nestjs/common';\nimport { Request, Response, NextFunction } from 'express';\nimport { SessionStoreService } from './session-store.service';\n\n@Injectable()\nexport class SessionMiddleware implements NestMiddleware {\n  constructor(private sessionStore: SessionStoreService) {}\n\n  async use(req: Request, res: Response, next: NextFunction) {\n    const sessionId = req.headers['x-session-id'] as string;\n\n    if (!sessionId) {\n      throw new UnauthorizedException('Session ID required');\n    }\n\n    const session = await this.sessionStore.getSession(sessionId);\n\n    if (!session) {\n      throw new UnauthorizedException('Invalid or expired session');\n    }\n\n    // Attach session to request\n    req['session'] = session;\n\n    next();\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"4-distributed-cache-with-rbac",children:"4. Distributed Cache with RBAC"}),"\n",(0,s.jsx)(n.p,{children:"Implement role-based access control for cached resources using Entra ID."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// rbac-cache.service.ts\nimport { Injectable, ForbiddenException, Logger } from '@nestjs/common';\nimport { RedisEntraIdService } from './redis-entraid.service';\n\nexport enum CachePermission {\n  READ = 'read',\n  WRITE = 'write',\n  DELETE = 'delete',\n  ADMIN = 'admin',\n}\n\nexport interface CacheEntry {\n  data: any;\n  permissions: {\n    [key: string]: CachePermission[]; // userId -> permissions\n  };\n  owner: string;\n  createdAt: number;\n  updatedAt: number;\n  metadata?: Record<string, any>;\n}\n\n@Injectable()\nexport class RBACCacheService {\n  private readonly logger = new Logger(RBACCacheService.name);\n  private readonly CACHE_PREFIX = 'rbac:cache:';\n\n  constructor(private redisEntraId: RedisEntraIdService) {}\n\n  private getCacheKey(key: string): string {\n    return `${this.CACHE_PREFIX}${key}`;\n  }\n\n  private hasPermission(entry: CacheEntry, userId: string, requiredPermission: CachePermission): boolean {\n    // Owner has all permissions\n    if (entry.owner === userId) {\n      return true;\n    }\n\n    // Check if user has the required permission\n    const userPermissions = entry.permissions[userId] || [];\n\n    // Admin permission grants all access\n    if (userPermissions.includes(CachePermission.ADMIN)) {\n      return true;\n    }\n\n    return userPermissions.includes(requiredPermission);\n  }\n\n  async set(key: string, data: any, userId: string, ttl?: number): Promise<void> {\n    const cacheKey = this.getCacheKey(key);\n    const now = Date.now();\n\n    const entry: CacheEntry = {\n      data,\n      permissions: {\n        [userId]: [CachePermission.ADMIN], // Owner gets admin permission\n      },\n      owner: userId,\n      createdAt: now,\n      updatedAt: now,\n    };\n\n    try {\n      const client = this.redisEntraId.getClient();\n      await client.set(cacheKey, JSON.stringify(entry));\n\n      if (ttl) {\n        await client.expire(cacheKey, ttl);\n      }\n\n      this.logger.log(`Cache entry created: ${key} by ${userId}`);\n    } catch (error) {\n      this.logger.error(`Failed to set cache: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async get(key: string, userId: string): Promise<any> {\n    const cacheKey = this.getCacheKey(key);\n\n    try {\n      const raw = await this.redisEntraId.get(cacheKey);\n\n      if (!raw) {\n        return null;\n      }\n\n      const entry: CacheEntry = JSON.parse(raw);\n\n      // Check read permission\n      if (!this.hasPermission(entry, userId, CachePermission.READ)) {\n        throw new ForbiddenException(`User ${userId} does not have read permission for ${key}`);\n      }\n\n      return entry.data;\n    } catch (error) {\n      if (error instanceof ForbiddenException) {\n        throw error;\n      }\n      this.logger.error(`Failed to get cache: ${error.message}`);\n      return null;\n    }\n  }\n\n  async update(key: string, data: any, userId: string): Promise<void> {\n    const cacheKey = this.getCacheKey(key);\n\n    try {\n      const raw = await this.redisEntraId.get(cacheKey);\n\n      if (!raw) {\n        throw new Error(`Cache entry not found: ${key}`);\n      }\n\n      const entry: CacheEntry = JSON.parse(raw);\n\n      // Check write permission\n      if (!this.hasPermission(entry, userId, CachePermission.WRITE)) {\n        throw new ForbiddenException(`User ${userId} does not have write permission for ${key}`);\n      }\n\n      // Update entry\n      entry.data = data;\n      entry.updatedAt = Date.now();\n\n      await this.redisEntraId.set(cacheKey, JSON.stringify(entry));\n\n      this.logger.log(`Cache entry updated: ${key} by ${userId}`);\n    } catch (error) {\n      this.logger.error(`Failed to update cache: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async delete(key: string, userId: string): Promise<void> {\n    const cacheKey = this.getCacheKey(key);\n\n    try {\n      const raw = await this.redisEntraId.get(cacheKey);\n\n      if (!raw) {\n        return; // Already deleted\n      }\n\n      const entry: CacheEntry = JSON.parse(raw);\n\n      // Check delete permission\n      if (!this.hasPermission(entry, userId, CachePermission.DELETE)) {\n        throw new ForbiddenException(`User ${userId} does not have delete permission for ${key}`);\n      }\n\n      await this.redisEntraId.del(cacheKey);\n\n      this.logger.log(`Cache entry deleted: ${key} by ${userId}`);\n    } catch (error) {\n      this.logger.error(`Failed to delete cache: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async grantPermission(\n    key: string,\n    ownerId: string,\n    targetUserId: string,\n    permissions: CachePermission[],\n  ): Promise<void> {\n    const cacheKey = this.getCacheKey(key);\n\n    try {\n      const raw = await this.redisEntraId.get(cacheKey);\n\n      if (!raw) {\n        throw new Error(`Cache entry not found: ${key}`);\n      }\n\n      const entry: CacheEntry = JSON.parse(raw);\n\n      // Only owner or admin can grant permissions\n      if (!this.hasPermission(entry, ownerId, CachePermission.ADMIN)) {\n        throw new ForbiddenException(`User ${ownerId} cannot grant permissions for ${key}`);\n      }\n\n      // Grant permissions\n      entry.permissions[targetUserId] = permissions;\n      entry.updatedAt = Date.now();\n\n      await this.redisEntraId.set(cacheKey, JSON.stringify(entry));\n\n      this.logger.log(`Permissions granted to ${targetUserId} for ${key}: ${permissions.join(', ')}`);\n    } catch (error) {\n      this.logger.error(`Failed to grant permissions: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async revokePermission(key: string, ownerId: string, targetUserId: string): Promise<void> {\n    const cacheKey = this.getCacheKey(key);\n\n    try {\n      const raw = await this.redisEntraId.get(cacheKey);\n\n      if (!raw) {\n        throw new Error(`Cache entry not found: ${key}`);\n      }\n\n      const entry: CacheEntry = JSON.parse(raw);\n\n      // Only owner or admin can revoke permissions\n      if (!this.hasPermission(entry, ownerId, CachePermission.ADMIN)) {\n        throw new ForbiddenException(`User ${ownerId} cannot revoke permissions for ${key}`);\n      }\n\n      // Cannot revoke owner's permissions\n      if (targetUserId === entry.owner) {\n        throw new ForbiddenException('Cannot revoke owner permissions');\n      }\n\n      // Revoke permissions\n      delete entry.permissions[targetUserId];\n      entry.updatedAt = Date.now();\n\n      await this.redisEntraId.set(cacheKey, JSON.stringify(entry));\n\n      this.logger.log(`Permissions revoked from ${targetUserId} for ${key}`);\n    } catch (error) {\n      this.logger.error(`Failed to revoke permissions: ${error.message}`);\n      throw error;\n    }\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"azure-setup-guide",children:"Azure Setup Guide"}),"\n",(0,s.jsx)(n.h3,{id:"1-enable-microsoft-entra-id-authentication-on-azure-cache-for-redis",children:"1. Enable Microsoft Entra ID Authentication on Azure Cache for Redis"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"# Using Azure CLI\n\n# Enable Entra ID authentication\naz redis update \\\n  --name your-cache-name \\\n  --resource-group your-resource-group \\\n  --enable-non-ssl-port false \\\n  --minimum-tls-version 1.2\n\n# Get cache details\naz redis show \\\n  --name your-cache-name \\\n  --resource-group your-resource-group\n"})}),"\n",(0,s.jsx)(n.h3,{id:"2-assign-redis-data-access-roles",children:"2. Assign Redis Data Access Roles"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'# Assign "Redis Cache Contributor" role to Managed Identity\naz role assignment create \\\n  --assignee <managed-identity-object-id> \\\n  --role "Redis Cache Contributor" \\\n  --scope /subscriptions/<subscription-id>/resourceGroups/<resource-group>/providers/Microsoft.Cache/redis/<cache-name>\n\n# Or use specific data plane roles:\n# - Redis Cache Contributor (full access)\n# - Redis Cache Reader (read-only)\n# - Redis Cache Owner (administrative access)\n'})}),"\n",(0,s.jsx)(n.h3,{id:"3-configure-managed-identity",children:"3. Configure Managed Identity"}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"For Azure App Service:"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"# Enable system-assigned managed identity\naz webapp identity assign \\\n  --name your-app-name \\\n  --resource-group your-resource-group\n\n# Get the principal ID\naz webapp identity show \\\n  --name your-app-name \\\n  --resource-group your-resource-group \\\n  --query principalId -o tsv\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"For Azure Kubernetes Service:"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-yaml",children:"# pod-identity.yaml\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: redis-app-sa\n  annotations:\n    azure.workload.identity/client-id: <managed-identity-client-id>\n---\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: redis-app\nspec:\n  template:\n    metadata:\n      labels:\n        azure.workload.identity/use: 'true'\n    spec:\n      serviceAccountName: redis-app-sa\n      containers:\n        - name: app\n          image: your-app-image\n          env:\n            - name: REDIS_ENTRA_ENABLED\n              value: 'true'\n            - name: REDIS_ENTRA_CREDENTIAL_TYPE\n              value: 'managed-identity'\n"})}),"\n",(0,s.jsx)(n.h2,{id:"best-practices",children:"Best Practices"}),"\n",(0,s.jsx)(n.h3,{id:"1-token-management",children:"1. Token Management"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// token-manager.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { TokenCredential, AccessToken } from '@azure/identity';\n\n@Injectable()\nexport class TokenManager {\n  private readonly logger = new Logger(TokenManager.name);\n  private tokenCache: Map<string, { token: string; expiresOn: number }> = new Map();\n\n  async getToken(credential: TokenCredential, scope: string, forceRefresh = false): Promise<string> {\n    const cacheKey = `${scope}`;\n\n    // Check cache\n    if (!forceRefresh && this.tokenCache.has(cacheKey)) {\n      const cached = this.tokenCache.get(cacheKey);\n      const now = Date.now();\n\n      // Return cached token if valid for at least 5 more minutes\n      if (cached.expiresOn - now > 5 * 60 * 1000) {\n        this.logger.debug('Using cached token');\n        return cached.token;\n      }\n    }\n\n    // Get new token\n    this.logger.debug('Fetching new token from Entra ID');\n    const tokenResponse: AccessToken = await credential.getToken(scope);\n\n    if (!tokenResponse?.token) {\n      throw new Error('Failed to obtain access token');\n    }\n\n    // Cache token\n    this.tokenCache.set(cacheKey, {\n      token: tokenResponse.token,\n      expiresOn: tokenResponse.expiresOnTimestamp,\n    });\n\n    return tokenResponse.token;\n  }\n\n  clearCache(): void {\n    this.tokenCache.clear();\n    this.logger.log('Token cache cleared');\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"2-health-checks",children:"2. Health Checks"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// redis-health.indicator.ts\nimport { Injectable } from '@nestjs/common';\nimport { HealthIndicator, HealthIndicatorResult, HealthCheckError } from '@nestjs/terminus';\nimport { RedisEntraIdService } from './redis-entraid.service';\n\n@Injectable()\nexport class RedisHealthIndicator extends HealthIndicator {\n  constructor(private redisEntraId: RedisEntraIdService) {\n    super();\n  }\n\n  async isHealthy(key: string): Promise<HealthIndicatorResult> {\n    try {\n      const client = this.redisEntraId.getClient();\n\n      // Test Redis connection with PING\n      const startTime = Date.now();\n      await client.ping();\n      const responseTime = Date.now() - startTime;\n\n      // Test authentication by setting a test key\n      const testKey = `health:check:${Date.now()}`;\n      await client.setEx(testKey, 10, 'ok');\n      await client.del(testKey);\n\n      return this.getStatus(key, true, {\n        responseTime: `${responseTime}ms`,\n        authentication: 'valid',\n      });\n    } catch (error) {\n      throw new HealthCheckError(\n        'Redis health check failed',\n        this.getStatus(key, false, {\n          error: error.message,\n        }),\n      );\n    }\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"3-monitoring-and-logging",children:"3. Monitoring and Logging"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// redis-metrics.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { RedisEntraIdService } from './redis-entraid.service';\n\nexport interface RedisMetrics {\n  totalOperations: number;\n  successfulOperations: number;\n  failedOperations: number;\n  averageLatency: number;\n  tokenRefreshCount: number;\n  lastTokenRefresh: number;\n}\n\n@Injectable()\nexport class RedisMetricsService {\n  private readonly logger = new Logger(RedisMetricsService.name);\n  private metrics: RedisMetrics = {\n    totalOperations: 0,\n    successfulOperations: 0,\n    failedOperations: 0,\n    averageLatency: 0,\n    tokenRefreshCount: 0,\n    lastTokenRefresh: 0,\n  };\n\n  constructor(private redisEntraId: RedisEntraIdService) {}\n\n  async executeWithMetrics<T>(operation: () => Promise<T>, operationName: string): Promise<T> {\n    const startTime = Date.now();\n    this.metrics.totalOperations++;\n\n    try {\n      const result = await operation();\n      const latency = Date.now() - startTime;\n\n      this.metrics.successfulOperations++;\n      this.updateAverageLatency(latency);\n\n      this.logger.debug(`${operationName} completed in ${latency}ms`);\n\n      return result;\n    } catch (error) {\n      this.metrics.failedOperations++;\n      this.logger.error(`${operationName} failed: ${error.message}`);\n      throw error;\n    }\n  }\n\n  private updateAverageLatency(latency: number): void {\n    const total = this.metrics.averageLatency * (this.metrics.successfulOperations - 1);\n    this.metrics.averageLatency = (total + latency) / this.metrics.successfulOperations;\n  }\n\n  recordTokenRefresh(): void {\n    this.metrics.tokenRefreshCount++;\n    this.metrics.lastTokenRefresh = Date.now();\n  }\n\n  getMetrics(): RedisMetrics {\n    return { ...this.metrics };\n  }\n\n  getSuccessRate(): number {\n    if (this.metrics.totalOperations === 0) {\n      return 100;\n    }\n    return (this.metrics.successfulOperations / this.metrics.totalOperations) * 100;\n  }\n\n  resetMetrics(): void {\n    this.metrics = {\n      totalOperations: 0,\n      successfulOperations: 0,\n      failedOperations: 0,\n      averageLatency: 0,\n      tokenRefreshCount: 0,\n      lastTokenRefresh: 0,\n    };\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"troubleshooting",children:"Troubleshooting"}),"\n",(0,s.jsx)(n.h3,{id:"common-issues",children:"Common Issues"}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"1. Authentication Failed"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// Add detailed error logging\ntry {\n  const token = await credential.getToken(scope);\n} catch (error) {\n  console.error('Authentication error:', {\n    message: error.message,\n    code: error.code,\n    stack: error.stack,\n  });\n\n  // Check if it's a credential-specific error\n  if (error.message.includes('ManagedIdentityCredential')) {\n    console.error('Managed Identity not available. Ensure it is enabled on your Azure resource.');\n  }\n}\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"2. Token Refresh Failures"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// Implement exponential backoff for token refresh\nprivate async refreshTokenWithRetry(maxRetries = 3): Promise<void> {\n  for (let attempt = 1; attempt <= maxRetries; attempt++) {\n    try {\n      await this.refreshToken();\n      return;\n    } catch (error) {\n      const delay = Math.pow(2, attempt) * 1000; // Exponential backoff\n      this.logger.warn(`Token refresh attempt ${attempt} failed. Retrying in ${delay}ms...`);\n\n      if (attempt === maxRetries) {\n        throw error;\n      }\n\n      await new Promise(resolve => setTimeout(resolve, delay));\n    }\n  }\n}\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"3. Connection Timeouts"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// Configure proper timeouts\nconst client = createClient({\n  socket: {\n    host,\n    port,\n    tls: true,\n    connectTimeout: 10000, // 10 seconds\n    commandTimeout: 5000, // 5 seconds\n    reconnectStrategy: (retries) => {\n      if (retries > 10) {\n        return new Error('Max reconnection attempts reached');\n      }\n      return Math.min(retries * 100, 3000);\n    },\n  },\n  username: 'default',\n  password: token,\n});\n"})}),"\n",(0,s.jsx)(n.h2,{id:"security-considerations",children:"Security Considerations"}),"\n",(0,s.jsxs)(n.ol,{children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Never log tokens"}),": Always redact tokens from logs"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Use TLS"}),": Always enable TLS for Redis connections in production"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Rotate managed identities"}),": Regularly review and rotate credentials"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Implement rate limiting"}),": Prevent token exhaustion attacks"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Monitor authentication failures"}),": Alert on suspicious patterns"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Use least privilege"}),": Grant minimum required permissions"]}),"\n"]}),"\n",(0,s.jsx)(n.h2,{id:"summary",children:"Summary"}),"\n",(0,s.jsx)(n.p,{children:"Microsoft Entra ID integration with Azure Cache for Redis provides:"}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Enterprise authentication"})," without managing passwords"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Centralized access control"})," through Azure RBAC"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Audit compliance"})," with comprehensive logging"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Seamless integration"})," with Azure managed identities"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Automatic token management"})," with refresh capabilities"]}),"\n"]}),"\n",(0,s.jsx)(n.p,{children:"This guide covered production-ready implementations including multi-environment configuration, session management, RBAC caching, health checks, and comprehensive monitoring."})]})}function h(e={}){const{wrapper:n}={...(0,i.R)(),...e.components};return n?(0,s.jsx)(n,{...e,children:(0,s.jsx)(l,{...e})}):l(e)}}}]);