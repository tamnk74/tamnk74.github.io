"use strict";(globalThis.webpackChunkdocs_site=globalThis.webpackChunkdocs_site||[]).push([[2543],{28453(e,n,s){s.d(n,{R:()=>a,x:()=>o});var t=s(96540);const i={},r=t.createContext(i);function a(e){const n=t.useContext(r);return t.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:a(e.components),t.createElement(r.Provider,{value:n},e.children)}},45795(e,n,s){s.r(n),s.d(n,{assets:()=>c,contentTitle:()=>o,default:()=>g,frontMatter:()=>a,metadata:()=>t,toc:()=>l});const t=JSON.parse('{"id":"backend/message-deduplication-ordering","title":"Message Deduplication & Ordering Guide","description":"Overview","source":"@site/docs/backend/message-deduplication-ordering.md","sourceDirName":"backend","slug":"/backend/message-deduplication-ordering","permalink":"/fullstack-dev/docs/backend/message-deduplication-ordering","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/message-deduplication-ordering.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Dead Letter Queues & Poison Message Handling Guide","permalink":"/fullstack-dev/docs/backend/dead-letter-queue-poison-message-handling"},"next":{"title":"Retry with Backoff & Idempotency Guide","permalink":"/fullstack-dev/docs/backend/retry-backoff-idempotency"}}');var i=s(74848),r=s(28453);const a={},o="Message Deduplication & Ordering Guide",c={},l=[{value:"Overview",id:"overview",level:2},{value:"Table of Contents",id:"table-of-contents",level:2},{value:"Message Deduplication Fundamentals",id:"message-deduplication-fundamentals",level:2},{value:"Why Message Deduplication?",id:"why-message-deduplication",level:3},{value:"Deduplication vs Idempotency",id:"deduplication-vs-idempotency",level:3},{value:"Idempotency Patterns",id:"idempotency-patterns",level:2},{value:"Idempotent Operations",id:"idempotent-operations",level:3},{value:"Idempotency Service",id:"idempotency-service",level:3},{value:"Idempotent Message Handler",id:"idempotent-message-handler",level:3},{value:"Deduplication Strategies",id:"deduplication-strategies",level:2},{value:"Message Deduplication Service",id:"message-deduplication-service",level:3},{value:"Message Consumer with Deduplication",id:"message-consumer-with-deduplication",level:3},{value:"Message Ordering Guarantees",id:"message-ordering-guarantees",level:2},{value:"Ordering Challenges",id:"ordering-challenges",level:3},{value:"Sequential Message Processor",id:"sequential-message-processor",level:3},{value:"Version-Based Ordering",id:"version-based-ordering",level:3},{value:"Exactly-Once Processing",id:"exactly-once-processing",level:2},{value:"Transactional Outbox Pattern",id:"transactional-outbox-pattern",level:3},{value:"Outbox Publisher",id:"outbox-publisher",level:3},{value:"Kafka Message Ordering",id:"kafka-message-ordering",level:2},{value:"Kafka Partition Key Strategy",id:"kafka-partition-key-strategy",level:3},{value:"Single Partition Consumer for Strict Ordering",id:"single-partition-consumer-for-strict-ordering",level:3},{value:"RabbitMQ Message Ordering",id:"rabbitmq-message-ordering",level:2},{value:"RabbitMQ with Single Consumer",id:"rabbitmq-with-single-consumer",level:3},{value:"Conflict Resolution",id:"conflict-resolution",level:2},{value:"Last-Write-Wins Strategy",id:"last-write-wins-strategy",level:3},{value:"Best Practices",id:"best-practices",level:2},{value:"1. Message Deduplication",id:"1-message-deduplication",level:3},{value:"2. Message Ordering",id:"2-message-ordering",level:3},{value:"3. Idempotency",id:"3-idempotency",level:3},{value:"4. Exactly-Once Processing",id:"4-exactly-once-processing",level:3},{value:"5. Conflict Resolution",id:"5-conflict-resolution",level:3},{value:"Summary",id:"summary",level:2}];function d(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",mermaid:"mermaid",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,r.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.header,{children:(0,i.jsx)(n.h1,{id:"message-deduplication--ordering-guide",children:"Message Deduplication & Ordering Guide"})}),"\n",(0,i.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,i.jsx)(n.p,{children:"Message deduplication and ordering are critical concerns in distributed message-driven systems. This guide covers idempotency patterns, deduplication strategies, message ordering guarantees, exactly-once processing, and conflict resolution for building reliable microservices with Kafka, RabbitMQ, and other message brokers."}),"\n",(0,i.jsx)(n.h2,{id:"table-of-contents",children:"Table of Contents"}),"\n",(0,i.jsxs)(n.ol,{children:["\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#message-deduplication-fundamentals",children:"Message Deduplication Fundamentals"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#idempotency-patterns",children:"Idempotency Patterns"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#deduplication-strategies",children:"Deduplication Strategies"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#message-ordering-guarantees",children:"Message Ordering Guarantees"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#exactly-once-processing",children:"Exactly-Once Processing"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#kafka-message-ordering",children:"Kafka Message Ordering"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#rabbitmq-message-ordering",children:"RabbitMQ Message Ordering"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#conflict-resolution",children:"Conflict Resolution"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"#best-practices",children:"Best Practices"})}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"message-deduplication-fundamentals",children:"Message Deduplication Fundamentals"}),"\n",(0,i.jsx)(n.h3,{id:"why-message-deduplication",children:"Why Message Deduplication?"}),"\n",(0,i.jsx)(n.mermaid,{value:"graph LR\n    A[Producer] --\x3e|1. Send Message| B[Broker]\n    B --\x3e|2. Ack Lost| A\n    A --\x3e|3. Retry Send| B\n    B --\x3e|4. Duplicate| C[Consumer]\n    C --\x3e|5. Process Twice| D[Database]\n\n    style B fill:#FF9800\n    style C fill:#f44336\n    style D fill:#f44336"}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Common Causes of Duplicates:"})}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Network failures and retries"}),"\n",(0,i.jsx)(n.li,{children:"Producer timeouts"}),"\n",(0,i.jsx)(n.li,{children:"Broker replication"}),"\n",(0,i.jsx)(n.li,{children:"Consumer rebalancing"}),"\n",(0,i.jsx)(n.li,{children:"At-least-once delivery semantics"}),"\n"]}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Impact of Duplicates:"})}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Incorrect data (double charges, inventory issues)"}),"\n",(0,i.jsx)(n.li,{children:"Violated business rules"}),"\n",(0,i.jsx)(n.li,{children:"Inconsistent state"}),"\n",(0,i.jsx)(n.li,{children:"Downstream cascade effects"}),"\n"]}),"\n",(0,i.jsx)(n.h3,{id:"deduplication-vs-idempotency",children:"Deduplication vs Idempotency"}),"\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.strong,{children:"Deduplication"}),": Preventing duplicate messages from being processed\n",(0,i.jsx)(n.strong,{children:"Idempotency"}),": Processing duplicates safely (same result)"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Deduplication - Detect and skip duplicates\nif (await this.isDuplicate(messageId)) {\n  return; // Skip processing\n}\nawait this.processMessage(message);\n\n// Idempotency - Process safely\nawait this.processIdempotently(message); // Same result if repeated\n"})}),"\n",(0,i.jsx)(n.h2,{id:"idempotency-patterns",children:"Idempotency Patterns"}),"\n",(0,i.jsx)(n.h3,{id:"idempotent-operations",children:"Idempotent Operations"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// entities/idempotent-operation.entity.ts\nimport { Entity, Column, PrimaryColumn, CreateDateColumn, Index } from 'typeorm';\n\n@Entity('idempotent_operations')\n@Index(['clientId', 'operationType', 'createdAt'])\nexport class IdempotentOperation {\n  @PrimaryColumn()\n  idempotencyKey: string; // Unique key for the operation\n\n  @Column()\n  clientId: string;\n\n  @Column()\n  operationType: string; // 'order.create', 'payment.process', etc.\n\n  @Column('jsonb')\n  requestPayload: any;\n\n  @Column('jsonb', { nullable: true })\n  responsePayload: any;\n\n  @Column({\n    type: 'enum',\n    enum: ['pending', 'processing', 'completed', 'failed'],\n    default: 'pending',\n  })\n  status: string;\n\n  @Column({ nullable: true })\n  errorMessage: string;\n\n  @CreateDateColumn()\n  createdAt: Date;\n\n  @Column({ type: 'timestamp', nullable: true })\n  completedAt: Date;\n\n  @Column({ default: 0 })\n  processingAttempts: number;\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"idempotency-service",children:"Idempotency Service"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/idempotency.service.ts\nimport { Injectable, Logger, ConflictException } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { IdempotentOperation } from '../entities/idempotent-operation.entity';\n\n@Injectable()\nexport class IdempotencyService {\n  private readonly logger = new Logger(IdempotencyService.name);\n\n  constructor(\n    @InjectRepository(IdempotentOperation)\n    private idempotentOpsRepository: Repository<IdempotentOperation>,\n  ) {}\n\n  /**\n   * Execute operation idempotently\n   */\n  async executeIdempotently<T>(\n    idempotencyKey: string,\n    clientId: string,\n    operationType: string,\n    requestPayload: any,\n    operation: () => Promise<T>,\n  ): Promise<T> {\n    // Check if operation already exists\n    const existing = await this.idempotentOpsRepository.findOne({\n      where: { idempotencyKey },\n    });\n\n    if (existing) {\n      return this.handleExistingOperation<T>(existing, requestPayload);\n    }\n\n    // Create new operation record\n    const idempotentOp = this.idempotentOpsRepository.create({\n      idempotencyKey,\n      clientId,\n      operationType,\n      requestPayload,\n      status: 'processing',\n      processingAttempts: 1,\n    });\n\n    await this.idempotentOpsRepository.save(idempotentOp);\n\n    try {\n      // Execute the operation\n      const result = await operation();\n\n      // Mark as completed\n      idempotentOp.status = 'completed';\n      idempotentOp.responsePayload = result;\n      idempotentOp.completedAt = new Date();\n      await this.idempotentOpsRepository.save(idempotentOp);\n\n      this.logger.log(`Idempotent operation ${idempotencyKey} completed successfully`);\n\n      return result;\n    } catch (error) {\n      // Mark as failed\n      idempotentOp.status = 'failed';\n      idempotentOp.errorMessage = error.message;\n      await this.idempotentOpsRepository.save(idempotentOp);\n\n      this.logger.error(`Idempotent operation ${idempotencyKey} failed: ${error.message}`);\n\n      throw error;\n    }\n  }\n\n  private async handleExistingOperation<T>(existing: IdempotentOperation, requestPayload: any): Promise<T> {\n    // Check if request payload matches (idempotency key collision detection)\n    if (JSON.stringify(existing.requestPayload) !== JSON.stringify(requestPayload)) {\n      throw new ConflictException(`Idempotency key collision: same key with different payload`);\n    }\n\n    // If completed, return cached result\n    if (existing.status === 'completed') {\n      this.logger.log(`Returning cached result for idempotency key ${existing.idempotencyKey}`);\n      return existing.responsePayload as T;\n    }\n\n    // If processing, wait or throw error\n    if (existing.status === 'processing') {\n      throw new ConflictException(`Operation ${existing.idempotencyKey} is currently being processed`);\n    }\n\n    // If failed, allow retry\n    if (existing.status === 'failed') {\n      existing.status = 'processing';\n      existing.processingAttempts++;\n      await this.idempotentOpsRepository.save(existing);\n\n      throw new Error(`Previous attempt failed. Retry attempt ${existing.processingAttempts}`);\n    }\n\n    throw new Error(`Unknown operation status: ${existing.status}`);\n  }\n\n  /**\n   * Clean up old idempotent operations\n   */\n  async cleanupOldOperations(daysToKeep: number = 30): Promise<number> {\n    const cutoffDate = new Date();\n    cutoffDate.setDate(cutoffDate.getDate() - daysToKeep);\n\n    const result = await this.idempotentOpsRepository\n      .createQueryBuilder()\n      .delete()\n      .where('completedAt < :cutoffDate', { cutoffDate })\n      .andWhere('status = :status', { status: 'completed' })\n      .execute();\n\n    this.logger.log(`Cleaned up ${result.affected} idempotent operations older than ${daysToKeep} days`);\n\n    return result.affected;\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"idempotent-message-handler",children:"Idempotent Message Handler"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// handlers/idempotent-message.handler.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { IdempotencyService } from '../services/idempotency.service';\n\n@Injectable()\nexport class IdempotentMessageHandler {\n  private readonly logger = new Logger(IdempotentMessageHandler.name);\n\n  constructor(private idempotencyService: IdempotencyService) {}\n\n  /**\n   * Handle message with idempotency guarantee\n   */\n  async handleMessage(message: any): Promise<void> {\n    const idempotencyKey = message.messageId || message.id;\n    const clientId = message.userId || message.clientId || 'system';\n    const operationType = message.type || 'unknown';\n\n    await this.idempotencyService.executeIdempotently(idempotencyKey, clientId, operationType, message, async () => {\n      // Actual message processing logic\n      return this.processMessage(message);\n    });\n  }\n\n  private async processMessage(message: any): Promise<any> {\n    this.logger.log(`Processing message: ${JSON.stringify(message)}`);\n\n    // Business logic here\n    switch (message.type) {\n      case 'order.created':\n        return this.handleOrderCreated(message);\n      case 'payment.processed':\n        return this.handlePaymentProcessed(message);\n      default:\n        throw new Error(`Unknown message type: ${message.type}`);\n    }\n  }\n\n  private async handleOrderCreated(message: any): Promise<any> {\n    // Process order creation idempotently\n    return { orderId: message.orderId, status: 'created' };\n  }\n\n  private async handlePaymentProcessed(message: any): Promise<any> {\n    // Process payment idempotently\n    return { paymentId: message.paymentId, status: 'processed' };\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"deduplication-strategies",children:"Deduplication Strategies"}),"\n",(0,i.jsx)(n.h3,{id:"message-deduplication-service",children:"Message Deduplication Service"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/message-deduplication.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { Redis } from 'ioredis';\nimport { InjectRedis } from '@nestjs-modules/ioredis';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\n\nexport interface DeduplicationConfig {\n  strategy: 'redis' | 'database' | 'bloom-filter';\n  ttlSeconds: number;\n  namespace: string;\n}\n\n@Entity('processed_messages')\n@Index(['messageId', 'processedAt'])\nexport class ProcessedMessage {\n  @PrimaryColumn()\n  messageId: string;\n\n  @Column()\n  messageType: string;\n\n  @Column('jsonb')\n  messagePayload: any;\n\n  @Column()\n  processorId: string;\n\n  @CreateDateColumn()\n  processedAt: Date;\n\n  @Column('timestamp')\n  expiresAt: Date;\n}\n\n@Injectable()\nexport class MessageDeduplicationService {\n  private readonly logger = new Logger(MessageDeduplicationService.name);\n  private readonly bloomFilter = new Map<string, Set<string>>(); // Simple in-memory bloom filter\n\n  constructor(\n    @InjectRedis() private readonly redis: Redis,\n    @InjectRepository(ProcessedMessage)\n    private processedMessagesRepository: Repository<ProcessedMessage>,\n  ) {}\n\n  /**\n   * Check if message has been processed (Redis strategy)\n   */\n  async isDuplicateRedis(messageId: string, namespace: string, ttlSeconds: number): Promise<boolean> {\n    const key = `dedup:${namespace}:${messageId}`;\n    const exists = await this.redis.exists(key);\n\n    if (exists) {\n      this.logger.warn(`Duplicate message detected: ${messageId}`);\n      return true;\n    }\n\n    // Mark as processed\n    await this.redis.setex(key, ttlSeconds, '1');\n    return false;\n  }\n\n  /**\n   * Check if message has been processed (Database strategy)\n   */\n  async isDuplicateDatabase(\n    messageId: string,\n    messageType: string,\n    processorId: string,\n    ttlSeconds: number,\n  ): Promise<boolean> {\n    const existing = await this.processedMessagesRepository.findOne({\n      where: { messageId },\n    });\n\n    if (existing) {\n      this.logger.warn(`Duplicate message detected: ${messageId}`);\n      return true;\n    }\n\n    // Mark as processed\n    const expiresAt = new Date();\n    expiresAt.setSeconds(expiresAt.getSeconds() + ttlSeconds);\n\n    await this.processedMessagesRepository.save({\n      messageId,\n      messageType,\n      messagePayload: {},\n      processorId,\n      expiresAt,\n    });\n\n    return false;\n  }\n\n  /**\n   * Check if message has been processed (Bloom Filter strategy)\n   */\n  isDuplicateBloomFilter(messageId: string, namespace: string): boolean {\n    if (!this.bloomFilter.has(namespace)) {\n      this.bloomFilter.set(namespace, new Set());\n    }\n\n    const filter = this.bloomFilter.get(namespace);\n\n    if (filter.has(messageId)) {\n      this.logger.warn(`Duplicate message detected (bloom filter): ${messageId}`);\n      return true;\n    }\n\n    filter.add(messageId);\n    return false;\n  }\n\n  /**\n   * Generic deduplication check\n   */\n  async isDuplicate(\n    messageId: string,\n    config: DeduplicationConfig,\n    messageType?: string,\n    processorId?: string,\n  ): Promise<boolean> {\n    switch (config.strategy) {\n      case 'redis':\n        return this.isDuplicateRedis(messageId, config.namespace, config.ttlSeconds);\n\n      case 'database':\n        return this.isDuplicateDatabase(\n          messageId,\n          messageType || 'unknown',\n          processorId || 'default',\n          config.ttlSeconds,\n        );\n\n      case 'bloom-filter':\n        return this.isDuplicateBloomFilter(messageId, config.namespace);\n\n      default:\n        throw new Error(`Unknown deduplication strategy: ${config.strategy}`);\n    }\n  }\n\n  /**\n   * Clean up expired processed messages\n   */\n  async cleanupExpiredMessages(): Promise<number> {\n    const result = await this.processedMessagesRepository\n      .createQueryBuilder()\n      .delete()\n      .where('expiresAt < :now', { now: new Date() })\n      .execute();\n\n    this.logger.log(`Cleaned up ${result.affected} expired processed messages`);\n    return result.affected;\n  }\n\n  /**\n   * Reset bloom filter (for testing or periodic cleanup)\n   */\n  resetBloomFilter(namespace?: string): void {\n    if (namespace) {\n      this.bloomFilter.delete(namespace);\n    } else {\n      this.bloomFilter.clear();\n    }\n    this.logger.log(`Bloom filter reset for namespace: ${namespace || 'all'}`);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"message-consumer-with-deduplication",children:"Message Consumer with Deduplication"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// consumers/deduplicated-message.consumer.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { Kafka, Consumer, EachMessagePayload } from 'kafkajs';\nimport { ConfigService } from '@nestjs/config';\nimport { MessageDeduplicationService } from '../services/message-deduplication.service';\nimport { IdempotentMessageHandler } from '../handlers/idempotent-message.handler';\n\n@Injectable()\nexport class DeduplicatedMessageConsumer implements OnModuleInit {\n  private readonly logger = new Logger(DeduplicatedMessageConsumer.name);\n  private consumer: Consumer;\n  private readonly kafka: Kafka;\n\n  constructor(\n    private configService: ConfigService,\n    private deduplicationService: MessageDeduplicationService,\n    private messageHandler: IdempotentMessageHandler,\n  ) {\n    this.kafka = new Kafka({\n      clientId: 'deduplicated-consumer',\n      brokers: this.configService.get('KAFKA_BROKERS').split(','),\n    });\n  }\n\n  async onModuleInit() {\n    this.consumer = this.kafka.consumer({\n      groupId: 'deduplicated-consumer-group',\n      sessionTimeout: 30000,\n      heartbeatInterval: 3000,\n    });\n\n    await this.consumer.connect();\n    await this.consumer.subscribe({\n      topics: ['orders', 'payments'],\n      fromBeginning: false,\n    });\n\n    await this.consumer.run({\n      eachMessage: async (payload) => this.handleMessage(payload),\n    });\n\n    this.logger.log('Deduplicated message consumer started');\n  }\n\n  private async handleMessage(payload: EachMessagePayload): Promise<void> {\n    const { topic, partition, message } = payload;\n    const messageId = message.key?.toString() || message.headers?.messageId?.toString();\n\n    if (!messageId) {\n      this.logger.error('Message without ID received - cannot deduplicate');\n      return;\n    }\n\n    try {\n      // Check for duplicates\n      const isDuplicate = await this.deduplicationService.isDuplicate(\n        messageId,\n        {\n          strategy: 'redis',\n          ttlSeconds: 3600, // 1 hour\n          namespace: topic,\n        },\n        topic,\n        'kafka-consumer',\n      );\n\n      if (isDuplicate) {\n        this.logger.warn(`Skipping duplicate message ${messageId} from topic ${topic}`);\n        return;\n      }\n\n      // Process message idempotently\n      const parsedMessage = JSON.parse(message.value.toString());\n      await this.messageHandler.handleMessage({\n        ...parsedMessage,\n        messageId,\n        topic,\n        partition,\n      });\n\n      this.logger.log(`Successfully processed message ${messageId}`);\n    } catch (error) {\n      this.logger.error(`Error processing message ${messageId}: ${error.message}`, error.stack);\n      throw error;\n    }\n  }\n\n  async onModuleDestroy() {\n    await this.consumer.disconnect();\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"message-ordering-guarantees",children:"Message Ordering Guarantees"}),"\n",(0,i.jsx)(n.h3,{id:"ordering-challenges",children:"Ordering Challenges"}),"\n",(0,i.jsx)(n.mermaid,{value:'graph TB\n    subgraph "Message Ordering Scenarios"\n        A[In-Order Producer]\n        B[Out-of-Order Network]\n        C[Reordered Broker]\n        D[Parallel Consumers]\n    end\n\n    A --\x3e|Message 1,2,3| B\n    B --\x3e|Message 3,1,2| C\n    C --\x3e|Message 1,2,3| D\n    D --\x3e|Process 1,3,2| E[Out of Order]\n\n    style E fill:#f44336'}),"\n",(0,i.jsx)(n.h3,{id:"sequential-message-processor",children:"Sequential Message Processor"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/sequential-message-processor.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\n\nexport interface SequentialMessage {\n  messageId: string;\n  sequenceNumber: number;\n  partitionKey: string;\n  payload: any;\n}\n\ninterface PartitionState {\n  lastProcessedSequence: number;\n  pendingMessages: Map<number, SequentialMessage>;\n}\n\n@Injectable()\nexport class SequentialMessageProcessorService {\n  private readonly logger = new Logger(SequentialMessageProcessorService.name);\n  private readonly partitionStates = new Map<string, PartitionState>();\n\n  /**\n   * Process message in sequence order\n   */\n  async processInSequence(\n    message: SequentialMessage,\n    processor: (msg: SequentialMessage) => Promise<void>,\n  ): Promise<void> {\n    const { partitionKey, sequenceNumber } = message;\n\n    // Initialize partition state if needed\n    if (!this.partitionStates.has(partitionKey)) {\n      this.partitionStates.set(partitionKey, {\n        lastProcessedSequence: 0,\n        pendingMessages: new Map(),\n      });\n    }\n\n    const state = this.partitionStates.get(partitionKey);\n\n    // Check if this is the next expected sequence\n    const expectedSequence = state.lastProcessedSequence + 1;\n\n    if (sequenceNumber === expectedSequence) {\n      // Process this message\n      await processor(message);\n      state.lastProcessedSequence = sequenceNumber;\n\n      this.logger.log(\n        `Processed message ${message.messageId} in sequence (${sequenceNumber}) for partition ${partitionKey}`,\n      );\n\n      // Process any pending messages that are now in sequence\n      await this.processPendingMessages(partitionKey, processor);\n    } else if (sequenceNumber > expectedSequence) {\n      // Future message - store for later\n      state.pendingMessages.set(sequenceNumber, message);\n\n      this.logger.warn(\n        `Message ${message.messageId} arrived out of order. Expected ${expectedSequence}, got ${sequenceNumber}. Storing for later.`,\n      );\n    } else {\n      // Old message - already processed or duplicate\n      this.logger.warn(\n        `Message ${message.messageId} with sequence ${sequenceNumber} is older than last processed ${state.lastProcessedSequence}. Skipping.`,\n      );\n    }\n  }\n\n  private async processPendingMessages(\n    partitionKey: string,\n    processor: (msg: SequentialMessage) => Promise<void>,\n  ): Promise<void> {\n    const state = this.partitionStates.get(partitionKey);\n\n    while (true) {\n      const nextSequence = state.lastProcessedSequence + 1;\n      const nextMessage = state.pendingMessages.get(nextSequence);\n\n      if (!nextMessage) {\n        break; // No more sequential messages\n      }\n\n      // Process the next message\n      await processor(nextMessage);\n      state.lastProcessedSequence = nextSequence;\n      state.pendingMessages.delete(nextSequence);\n\n      this.logger.log(`Processed pending message ${nextMessage.messageId} in sequence (${nextSequence})`);\n    }\n\n    // Check for gaps (missing messages)\n    if (state.pendingMessages.size > 0) {\n      const minPending = Math.min(...state.pendingMessages.keys());\n      const gap = minPending - state.lastProcessedSequence;\n\n      if (gap > 10) {\n        this.logger.error(\n          `Large gap detected in partition ${partitionKey}. Last processed: ${state.lastProcessedSequence}, next pending: ${minPending}. Gap: ${gap}`,\n        );\n      }\n    }\n  }\n\n  /**\n   * Get partition state for monitoring\n   */\n  getPartitionState(partitionKey: string): {\n    lastProcessed: number;\n    pendingCount: number;\n    pendingSequences: number[];\n  } {\n    const state = this.partitionStates.get(partitionKey);\n\n    if (!state) {\n      return {\n        lastProcessed: 0,\n        pendingCount: 0,\n        pendingSequences: [],\n      };\n    }\n\n    return {\n      lastProcessed: state.lastProcessedSequence,\n      pendingCount: state.pendingMessages.size,\n      pendingSequences: Array.from(state.pendingMessages.keys()).sort((a, b) => a - b),\n    };\n  }\n\n  /**\n   * Clean up partition state\n   */\n  cleanupPartition(partitionKey: string): void {\n    this.partitionStates.delete(partitionKey);\n    this.logger.log(`Cleaned up partition state for ${partitionKey}`);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"version-based-ordering",children:"Version-Based Ordering"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/version-based-ordering.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\n\nexport interface VersionedMessage {\n  entityId: string;\n  version: number;\n  timestamp: Date;\n  payload: any;\n}\n\ninterface EntityState {\n  currentVersion: number;\n  pendingVersions: Map<number, VersionedMessage>;\n  lastUpdated: Date;\n}\n\n@Injectable()\nexport class VersionBasedOrderingService {\n  private readonly logger = new Logger(VersionBasedOrderingService.name);\n  private readonly entityStates = new Map<string, EntityState>();\n\n  /**\n   * Process message based on version\n   */\n  async processWithVersionCheck(\n    message: VersionedMessage,\n    processor: (msg: VersionedMessage) => Promise<void>,\n  ): Promise<void> {\n    const { entityId, version } = message;\n\n    // Initialize entity state if needed\n    if (!this.entityStates.has(entityId)) {\n      this.entityStates.set(entityId, {\n        currentVersion: 0,\n        pendingVersions: new Map(),\n        lastUpdated: new Date(0),\n      });\n    }\n\n    const state = this.entityStates.get(entityId);\n\n    // Check if this is the next expected version\n    const expectedVersion = state.currentVersion + 1;\n\n    if (version === expectedVersion) {\n      // Process this version\n      await processor(message);\n      state.currentVersion = version;\n      state.lastUpdated = message.timestamp;\n\n      this.logger.log(`Processed message for entity ${entityId} at version ${version}`);\n\n      // Process any pending versions that are now ready\n      await this.processPendingVersions(entityId, processor);\n    } else if (version > expectedVersion) {\n      // Future version - store for later\n      state.pendingVersions.set(version, message);\n\n      this.logger.warn(\n        `Message for entity ${entityId} arrived with future version ${version}. Expected ${expectedVersion}. Storing for later.`,\n      );\n    } else {\n      // Old version - check timestamp for conflict resolution\n      if (message.timestamp > state.lastUpdated) {\n        this.logger.warn(\n          `Message for entity ${entityId} with old version ${version} but newer timestamp. Current version: ${state.currentVersion}`,\n        );\n        // Optionally handle conflict\n      } else {\n        this.logger.warn(`Ignoring old message for entity ${entityId}. Version ${version} < ${state.currentVersion}`);\n      }\n    }\n  }\n\n  private async processPendingVersions(\n    entityId: string,\n    processor: (msg: VersionedMessage) => Promise<void>,\n  ): Promise<void> {\n    const state = this.entityStates.get(entityId);\n\n    while (true) {\n      const nextVersion = state.currentVersion + 1;\n      const nextMessage = state.pendingVersions.get(nextVersion);\n\n      if (!nextMessage) {\n        break;\n      }\n\n      await processor(nextMessage);\n      state.currentVersion = nextVersion;\n      state.lastUpdated = nextMessage.timestamp;\n      state.pendingVersions.delete(nextVersion);\n\n      this.logger.log(`Processed pending message for entity ${entityId} at version ${nextVersion}`);\n    }\n  }\n\n  /**\n   * Get entity state\n   */\n  getEntityState(entityId: string): {\n    currentVersion: number;\n    pendingCount: number;\n    pendingVersions: number[];\n  } {\n    const state = this.entityStates.get(entityId);\n\n    if (!state) {\n      return {\n        currentVersion: 0,\n        pendingCount: 0,\n        pendingVersions: [],\n      };\n    }\n\n    return {\n      currentVersion: state.currentVersion,\n      pendingCount: state.pendingVersions.size,\n      pendingVersions: Array.from(state.pendingVersions.keys()).sort((a, b) => a - b),\n    };\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"exactly-once-processing",children:"Exactly-Once Processing"}),"\n",(0,i.jsx)(n.h3,{id:"transactional-outbox-pattern",children:"Transactional Outbox Pattern"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// entities/outbox.entity.ts\nimport { Entity, Column, PrimaryGeneratedColumn, CreateDateColumn, Index } from 'typeorm';\n\n@Entity('outbox')\n@Index(['status', 'createdAt'])\nexport class OutboxMessage {\n  @PrimaryGeneratedColumn('uuid')\n  id: string;\n\n  @Column()\n  aggregateId: string;\n\n  @Column()\n  aggregateType: string;\n\n  @Column()\n  eventType: string;\n\n  @Column('jsonb')\n  payload: any;\n\n  @Column({\n    type: 'enum',\n    enum: ['pending', 'sent', 'failed'],\n    default: 'pending',\n  })\n  status: string;\n\n  @Column({ default: 0 })\n  retryCount: number;\n\n  @CreateDateColumn()\n  createdAt: Date;\n\n  @Column({ type: 'timestamp', nullable: true })\n  sentAt: Date;\n}\n\n// services/transactional-outbox.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository, EntityManager } from 'typeorm';\nimport { OutboxMessage } from '../entities/outbox.entity';\n\n@Injectable()\nexport class TransactionalOutboxService {\n  private readonly logger = new Logger(TransactionalOutboxService.name);\n\n  constructor(\n    @InjectRepository(OutboxMessage)\n    private outboxRepository: Repository<OutboxMessage>,\n  ) {}\n\n  /**\n   * Save business entity and outbox message in single transaction\n   */\n  async saveWithOutbox<T>(\n    entityManager: EntityManager,\n    entity: T,\n    entityRepository: Repository<T>,\n    outboxData: {\n      aggregateId: string;\n      aggregateType: string;\n      eventType: string;\n      payload: any;\n    },\n  ): Promise<{ entity: T; outboxMessage: OutboxMessage }> {\n    // Save entity\n    const savedEntity = await entityManager.save(entityRepository.target, entity);\n\n    // Save outbox message in same transaction\n    const outboxMessage = entityManager.create(OutboxMessage, {\n      ...outboxData,\n      status: 'pending',\n    });\n\n    const savedOutboxMessage = await entityManager.save(OutboxMessage, outboxMessage);\n\n    this.logger.log(`Saved entity and outbox message in transaction for aggregate ${outboxData.aggregateId}`);\n\n    return {\n      entity: savedEntity,\n      outboxMessage: savedOutboxMessage,\n    };\n  }\n\n  /**\n   * Get pending outbox messages for publishing\n   */\n  async getPendingMessages(limit: number = 100): Promise<OutboxMessage[]> {\n    return this.outboxRepository.find({\n      where: { status: 'pending' },\n      order: { createdAt: 'ASC' },\n      take: limit,\n    });\n  }\n\n  /**\n   * Mark message as sent\n   */\n  async markAsSent(messageId: string): Promise<void> {\n    await this.outboxRepository.update(messageId, {\n      status: 'sent',\n      sentAt: new Date(),\n    });\n  }\n\n  /**\n   * Mark message as failed\n   */\n  async markAsFailed(messageId: string): Promise<void> {\n    await this.outboxRepository\n      .createQueryBuilder()\n      .update(OutboxMessage)\n      .set({\n        status: 'failed',\n        retryCount: () => 'retryCount + 1',\n      })\n      .where('id = :id', { id: messageId })\n      .execute();\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"outbox-publisher",children:"Outbox Publisher"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/outbox-publisher.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { Cron, CronExpression } from '@nestjs/schedule';\nimport { Kafka, Producer } from 'kafkajs';\nimport { ConfigService } from '@nestjs/config';\nimport { TransactionalOutboxService } from './transactional-outbox.service';\n\n@Injectable()\nexport class OutboxPublisherService implements OnModuleInit {\n  private readonly logger = new Logger(OutboxPublisherService.name);\n  private producer: Producer;\n  private readonly kafka: Kafka;\n\n  constructor(private configService: ConfigService, private outboxService: TransactionalOutboxService) {\n    this.kafka = new Kafka({\n      clientId: 'outbox-publisher',\n      brokers: this.configService.get('KAFKA_BROKERS').split(','),\n    });\n  }\n\n  async onModuleInit() {\n    this.producer = this.kafka.producer({\n      idempotent: true,\n      maxInFlightRequests: 5,\n      transactionalId: 'outbox-publisher-tx',\n    });\n\n    await this.producer.connect();\n    this.logger.log('Outbox publisher connected');\n  }\n\n  /**\n   * Publish pending outbox messages every 5 seconds\n   */\n  @Cron(CronExpression.EVERY_5_SECONDS)\n  async publishPendingMessages(): Promise<void> {\n    const pendingMessages = await this.outboxService.getPendingMessages(100);\n\n    if (pendingMessages.length === 0) {\n      return;\n    }\n\n    this.logger.log(`Publishing ${pendingMessages.length} pending outbox messages`);\n\n    for (const message of pendingMessages) {\n      try {\n        await this.producer.send({\n          topic: message.eventType,\n          messages: [\n            {\n              key: message.aggregateId,\n              value: JSON.stringify(message.payload),\n              headers: {\n                'message-id': message.id,\n                'aggregate-type': message.aggregateType,\n                'event-type': message.eventType,\n              },\n            },\n          ],\n        });\n\n        await this.outboxService.markAsSent(message.id);\n\n        this.logger.log(`Published outbox message ${message.id} to topic ${message.eventType}`);\n      } catch (error) {\n        this.logger.error(`Failed to publish outbox message ${message.id}: ${error.message}`);\n        await this.outboxService.markAsFailed(message.id);\n      }\n    }\n  }\n\n  async onModuleDestroy() {\n    await this.producer.disconnect();\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"kafka-message-ordering",children:"Kafka Message Ordering"}),"\n",(0,i.jsx)(n.h3,{id:"kafka-partition-key-strategy",children:"Kafka Partition Key Strategy"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/kafka-ordering.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { Kafka, Producer, CompressionTypes } from 'kafkajs';\nimport { ConfigService } from '@nestjs/config';\n\n@Injectable()\nexport class KafkaOrderingService {\n  private readonly logger = new Logger(KafkaOrderingService.name);\n  private producer: Producer;\n  private readonly kafka: Kafka;\n\n  constructor(private configService: ConfigService) {\n    this.kafka = new Kafka({\n      clientId: 'ordered-producer',\n      brokers: this.configService.get('KAFKA_BROKERS').split(','),\n    });\n\n    this.producer = this.kafka.producer({\n      // Idempotent producer ensures no duplicates\n      idempotent: true,\n      // Max in-flight requests = 1 ensures strict ordering per partition\n      maxInFlightRequests: 1,\n      // Enable transaction support for exactly-once\n      transactionalId: 'ordered-producer-tx',\n    });\n  }\n\n  async onModuleInit() {\n    await this.producer.connect();\n    this.logger.log('Kafka ordered producer connected');\n  }\n\n  /**\n   * Send message with partition key for ordering guarantee\n   */\n  async sendOrdered(\n    topic: string,\n    message: {\n      partitionKey: string; // Messages with same key go to same partition\n      data: any;\n      sequenceNumber?: number;\n    },\n  ): Promise<void> {\n    try {\n      const result = await this.producer.send({\n        topic,\n        compression: CompressionTypes.GZIP,\n        messages: [\n          {\n            // Partition key ensures ordering\n            key: message.partitionKey,\n            value: JSON.stringify({\n              ...message.data,\n              sequenceNumber: message.sequenceNumber,\n            }),\n            headers: {\n              'partition-key': message.partitionKey,\n              'sequence-number': message.sequenceNumber?.toString() || '0',\n              timestamp: Date.now().toString(),\n            },\n          },\n        ],\n      });\n\n      this.logger.log(\n        `Sent ordered message to ${topic}, partition ${result[0].partition}, offset ${result[0].baseOffset}`,\n      );\n    } catch (error) {\n      this.logger.error(`Failed to send ordered message: ${error.message}`);\n      throw error;\n    }\n  }\n\n  /**\n   * Send batch with transaction for exactly-once\n   */\n  async sendBatchTransactional(topic: string, messages: Array<{ partitionKey: string; data: any }>): Promise<void> {\n    const transaction = await this.producer.transaction();\n\n    try {\n      for (const message of messages) {\n        await transaction.send({\n          topic,\n          messages: [\n            {\n              key: message.partitionKey,\n              value: JSON.stringify(message.data),\n            },\n          ],\n        });\n      }\n\n      await transaction.commit();\n      this.logger.log(`Committed transaction with ${messages.length} messages`);\n    } catch (error) {\n      await transaction.abort();\n      this.logger.error(`Transaction aborted: ${error.message}`);\n      throw error;\n    }\n  }\n\n  async onModuleDestroy() {\n    await this.producer.disconnect();\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"single-partition-consumer-for-strict-ordering",children:"Single Partition Consumer for Strict Ordering"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// consumers/single-partition.consumer.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { Kafka, Consumer, EachMessagePayload } from 'kafkajs';\nimport { ConfigService } from '@nestjs/config';\nimport { SequentialMessageProcessorService, SequentialMessage } from '../services/sequential-message-processor.service';\n\n@Injectable()\nexport class SinglePartitionConsumer implements OnModuleInit {\n  private readonly logger = new Logger(SinglePartitionConsumer.name);\n  private consumer: Consumer;\n  private readonly kafka: Kafka;\n\n  constructor(private configService: ConfigService, private sequentialProcessor: SequentialMessageProcessorService) {\n    this.kafka = new Kafka({\n      clientId: 'single-partition-consumer',\n      brokers: this.configService.get('KAFKA_BROKERS').split(','),\n    });\n  }\n\n  async onModuleInit() {\n    this.consumer = this.kafka.consumer({\n      groupId: 'single-partition-consumer-group',\n      // Process messages one at a time per partition\n      maxInFlightRequests: 1,\n      sessionTimeout: 30000,\n    });\n\n    await this.consumer.connect();\n\n    // Subscribe to specific partition for strict ordering\n    await this.consumer.subscribe({\n      topic: 'orders',\n      fromBeginning: false,\n    });\n\n    await this.consumer.run({\n      // Process each partition sequentially\n      partitionsConsumedConcurrently: 1,\n      eachMessage: async (payload) => this.handleMessage(payload),\n    });\n\n    this.logger.log('Single partition consumer started');\n  }\n\n  private async handleMessage(payload: EachMessagePayload): Promise<void> {\n    const { topic, partition, message } = payload;\n    const partitionKey = message.key?.toString() || 'unknown';\n    const sequenceNumber = parseInt(message.headers?.['sequence-number']?.toString() || '0');\n\n    const sequentialMessage: SequentialMessage = {\n      messageId: `${topic}-${partition}-${message.offset}`,\n      sequenceNumber,\n      partitionKey,\n      payload: JSON.parse(message.value.toString()),\n    };\n\n    await this.sequentialProcessor.processInSequence(sequentialMessage, async (msg) => {\n      // Actual processing logic\n      this.logger.log(`Processing message in order: partition=${partition}, sequence=${msg.sequenceNumber}`);\n      // Business logic here\n    });\n  }\n\n  async onModuleDestroy() {\n    await this.consumer.disconnect();\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"rabbitmq-message-ordering",children:"RabbitMQ Message Ordering"}),"\n",(0,i.jsx)(n.h3,{id:"rabbitmq-with-single-consumer",children:"RabbitMQ with Single Consumer"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/rabbitmq-ordered.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport * as amqp from 'amqplib';\nimport { ConfigService } from '@nestjs/config';\n\n@Injectable()\nexport class RabbitMQOrderedService implements OnModuleInit {\n  private readonly logger = new Logger(RabbitMQOrderedService.name);\n  private connection: amqp.Connection;\n  private channel: amqp.Channel;\n\n  constructor(private configService: ConfigService) {}\n\n  async onModuleInit() {\n    await this.connect();\n    await this.setupOrderedQueue();\n  }\n\n  private async connect(): Promise<void> {\n    this.connection = await amqp.connect(this.configService.get('RABBITMQ_URL'));\n    this.channel = await this.connection.createChannel();\n\n    // Set prefetch to 1 for strict ordering\n    await this.channel.prefetch(1);\n\n    this.logger.log('RabbitMQ ordered service connected');\n  }\n\n  private async setupOrderedQueue(): Promise<void> {\n    // Create queue with single active consumer for ordering\n    await this.channel.assertQueue('ordered-queue', {\n      durable: true,\n      arguments: {\n        'x-single-active-consumer': true, // Only one consumer at a time\n      },\n    });\n  }\n\n  /**\n   * Publish ordered message\n   */\n  async publishOrdered(queue: string, message: any, sequenceNumber: number): Promise<void> {\n    const messageBuffer = Buffer.from(JSON.stringify(message));\n\n    this.channel.sendToQueue(queue, messageBuffer, {\n      persistent: true,\n      headers: {\n        'sequence-number': sequenceNumber,\n        timestamp: Date.now(),\n      },\n      messageId: `${queue}-${sequenceNumber}`,\n    });\n\n    this.logger.log(`Published ordered message to ${queue} with sequence ${sequenceNumber}`);\n  }\n\n  /**\n   * Consume with ordering guarantee\n   */\n  async consumeOrdered(\n    queue: string,\n    processor: (message: any, sequenceNumber: number) => Promise<void>,\n  ): Promise<void> {\n    await this.channel.consume(\n      queue,\n      async (msg) => {\n        if (!msg) return;\n\n        const sequenceNumber = parseInt(msg.properties.headers['sequence-number']?.toString() || '0');\n        const content = JSON.parse(msg.content.toString());\n\n        try {\n          await processor(content, sequenceNumber);\n          this.channel.ack(msg);\n\n          this.logger.log(`Processed ordered message from ${queue} with sequence ${sequenceNumber}`);\n        } catch (error) {\n          this.logger.error(`Failed to process message: ${error.message}`);\n          // Reject and requeue\n          this.channel.nack(msg, false, true);\n        }\n      },\n      {\n        noAck: false,\n        exclusive: true, // Exclusive consumer for ordering\n      },\n    );\n  }\n\n  async onModuleDestroy() {\n    await this.channel.close();\n    await this.connection.close();\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"conflict-resolution",children:"Conflict Resolution"}),"\n",(0,i.jsx)(n.h3,{id:"last-write-wins-strategy",children:"Last-Write-Wins Strategy"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// services/conflict-resolution.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\n\nexport interface ConflictingMessage {\n  entityId: string;\n  version: number;\n  timestamp: Date;\n  payload: any;\n}\n\n@Injectable()\nexport class ConflictResolutionService {\n  private readonly logger = new Logger(ConflictResolutionService.name);\n\n  /**\n   * Last-Write-Wins: Choose message with latest timestamp\n   */\n  lastWriteWins(current: ConflictingMessage, incoming: ConflictingMessage): ConflictingMessage {\n    if (incoming.timestamp > current.timestamp) {\n      this.logger.log(`LWW: Incoming message wins for entity ${incoming.entityId}`);\n      return incoming;\n    }\n\n    this.logger.log(`LWW: Current message wins for entity ${current.entityId}`);\n    return current;\n  }\n\n  /**\n   * Version-Based: Choose message with higher version\n   */\n  versionBased(current: ConflictingMessage, incoming: ConflictingMessage): ConflictingMessage {\n    if (incoming.version > current.version) {\n      this.logger.log(`Version-based: Incoming message (v${incoming.version}) wins over current (v${current.version})`);\n      return incoming;\n    }\n\n    this.logger.log(`Version-based: Current message (v${current.version}) wins over incoming (v${incoming.version})`);\n    return current;\n  }\n\n  /**\n   * Custom merge strategy\n   */\n  customMerge(\n    current: ConflictingMessage,\n    incoming: ConflictingMessage,\n    mergeStrategy: (current: any, incoming: any) => any,\n  ): ConflictingMessage {\n    const mergedPayload = mergeStrategy(current.payload, incoming.payload);\n\n    this.logger.log(`Custom merge applied for entity ${current.entityId}`);\n\n    return {\n      entityId: current.entityId,\n      version: Math.max(current.version, incoming.version) + 1,\n      timestamp: new Date(),\n      payload: mergedPayload,\n    };\n  }\n\n  /**\n   * Detect conflicts\n   */\n  hasConflict(current: ConflictingMessage, incoming: ConflictingMessage): boolean {\n    // Same version but different timestamps = conflict\n    if (current.version === incoming.version) {\n      const timeDiffMs = Math.abs(current.timestamp.getTime() - incoming.timestamp.getTime());\n\n      // If messages are within 100ms, consider it a conflict\n      if (timeDiffMs < 100) {\n        this.logger.warn(\n          `Conflict detected for entity ${current.entityId}: same version ${current.version}, timestamps differ by ${timeDiffMs}ms`,\n        );\n        return true;\n      }\n    }\n\n    return false;\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"best-practices",children:"Best Practices"}),"\n",(0,i.jsx)(n.h3,{id:"1-message-deduplication",children:"1. Message Deduplication"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Use unique message IDs"}),"\n",(0,i.jsx)(n.li,{children:"Implement idempotent operations"}),"\n",(0,i.jsx)(n.li,{children:"Store processed message IDs with TTL"}),"\n",(0,i.jsx)(n.li,{children:"Use Redis for high-performance deduplication"}),"\n",(0,i.jsx)(n.li,{children:"Use database for audit trail"}),"\n",(0,i.jsx)(n.li,{children:"Clean up old deduplication records"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["\u274c ",(0,i.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Rely on message broker deduplication alone"}),"\n",(0,i.jsx)(n.li,{children:"Skip message ID generation"}),"\n",(0,i.jsx)(n.li,{children:"Store deduplication records forever"}),"\n",(0,i.jsx)(n.li,{children:"Ignore duplicate detection failures"}),"\n",(0,i.jsx)(n.li,{children:"Use only in-memory deduplication (not persistent)"}),"\n",(0,i.jsx)(n.li,{children:"Deploy without testing duplicate scenarios"}),"\n"]}),"\n",(0,i.jsx)(n.h3,{id:"2-message-ordering",children:"2. Message Ordering"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Use partition keys consistently"}),"\n",(0,i.jsx)(n.li,{children:"Set maxInFlightRequests=1 for strict ordering"}),"\n",(0,i.jsx)(n.li,{children:"Implement sequence numbers"}),"\n",(0,i.jsx)(n.li,{children:"Buffer out-of-order messages"}),"\n",(0,i.jsx)(n.li,{children:"Monitor ordering violations"}),"\n",(0,i.jsx)(n.li,{children:"Document ordering guarantees"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["\u274c ",(0,i.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Assume messages arrive in order"}),"\n",(0,i.jsx)(n.li,{children:"Process messages in parallel per partition"}),"\n",(0,i.jsx)(n.li,{children:"Ignore sequence numbers"}),"\n",(0,i.jsx)(n.li,{children:"Skip gap detection"}),"\n",(0,i.jsx)(n.li,{children:"Forget to handle reordering"}),"\n",(0,i.jsx)(n.li,{children:"Deploy without ordering tests"}),"\n"]}),"\n",(0,i.jsx)(n.h3,{id:"3-idempotency",children:"3. Idempotency"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Design idempotent operations"}),"\n",(0,i.jsx)(n.li,{children:"Store operation results"}),"\n",(0,i.jsx)(n.li,{children:"Use idempotency keys"}),"\n",(0,i.jsx)(n.li,{children:"Handle concurrent requests"}),"\n",(0,i.jsx)(n.li,{children:"Implement optimistic locking"}),"\n",(0,i.jsx)(n.li,{children:"Test with duplicates"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["\u274c ",(0,i.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Assume exactly-once delivery"}),"\n",(0,i.jsx)(n.li,{children:"Skip idempotency checks"}),"\n",(0,i.jsx)(n.li,{children:"Perform side effects without guards"}),"\n",(0,i.jsx)(n.li,{children:"Ignore race conditions"}),"\n",(0,i.jsx)(n.li,{children:"Forget to handle failures"}),"\n",(0,i.jsx)(n.li,{children:"Deploy without duplicate testing"}),"\n"]}),"\n",(0,i.jsx)(n.h3,{id:"4-exactly-once-processing",children:"4. Exactly-Once Processing"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Use transactional outbox pattern"}),"\n",(0,i.jsx)(n.li,{children:"Implement idempotent consumers"}),"\n",(0,i.jsx)(n.li,{children:"Use database transactions"}),"\n",(0,i.jsx)(n.li,{children:"Enable Kafka transactions"}),"\n",(0,i.jsx)(n.li,{children:"Monitor processing guarantees"}),"\n",(0,i.jsx)(n.li,{children:"Test failure scenarios"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["\u274c ",(0,i.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Rely solely on broker guarantees"}),"\n",(0,i.jsx)(n.li,{children:"Skip transaction boundaries"}),"\n",(0,i.jsx)(n.li,{children:"Mix transactional and non-transactional operations"}),"\n",(0,i.jsx)(n.li,{children:"Ignore partial failures"}),"\n",(0,i.jsx)(n.li,{children:"Forget to commit offsets correctly"}),"\n",(0,i.jsx)(n.li,{children:"Deploy without end-to-end testing"}),"\n"]}),"\n",(0,i.jsx)(n.h3,{id:"5-conflict-resolution",children:"5. Conflict Resolution"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Choose appropriate strategy (LWW, version-based)"}),"\n",(0,i.jsx)(n.li,{children:"Use vector clocks for distributed systems"}),"\n",(0,i.jsx)(n.li,{children:"Implement custom merge logic"}),"\n",(0,i.jsx)(n.li,{children:"Log all conflicts"}),"\n",(0,i.jsx)(n.li,{children:"Monitor conflict rate"}),"\n",(0,i.jsx)(n.li,{children:"Provide conflict resolution UI"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["\u274c ",(0,i.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Ignore conflicts"}),"\n",(0,i.jsx)(n.li,{children:"Always choose latest timestamp"}),"\n",(0,i.jsx)(n.li,{children:"Lose data during resolution"}),"\n",(0,i.jsx)(n.li,{children:"Skip conflict detection"}),"\n",(0,i.jsx)(n.li,{children:"Forget to notify users"}),"\n",(0,i.jsx)(n.li,{children:"Deploy without conflict handling"}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"summary",children:"Summary"}),"\n",(0,i.jsx)(n.p,{children:"Effective message deduplication and ordering provides:"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Reliability"})," - No duplicate processing"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Consistency"})," - Correct order of operations"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Data Integrity"})," - No lost or corrupted data"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Idempotency"})," - Safe retry operations"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Exactly-Once"})," - Guaranteed processing semantics"]}),"\n"]}),"\n",(0,i.jsx)(n.p,{children:"Key implementation points:"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Idempotent operations with database tracking"}),"\n",(0,i.jsx)(n.li,{children:"Message deduplication (Redis, database, bloom filter)"}),"\n",(0,i.jsx)(n.li,{children:"Sequential message processing with buffering"}),"\n",(0,i.jsx)(n.li,{children:"Version-based ordering with conflict detection"}),"\n",(0,i.jsx)(n.li,{children:"Transactional outbox for exactly-once"}),"\n",(0,i.jsx)(n.li,{children:"Kafka partition keys for ordering"}),"\n",(0,i.jsx)(n.li,{children:"RabbitMQ single active consumer"}),"\n",(0,i.jsx)(n.li,{children:"Conflict resolution strategies (LWW, version-based, custom merge)"}),"\n",(0,i.jsx)(n.li,{children:"Comprehensive monitoring and gap detection"}),"\n",(0,i.jsx)(n.li,{children:"Proper cleanup of deduplication records"}),"\n"]}),"\n",(0,i.jsx)(n.p,{children:"Remember: Design for at-least-once delivery and implement idempotency - it's simpler than exactly-once!"})]})}function g(e={}){const{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);