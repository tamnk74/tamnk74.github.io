"use strict";(globalThis.webpackChunkdocs_site=globalThis.webpackChunkdocs_site||[]).push([[70253],{28453(e,n,t){t.d(n,{R:()=>s,x:()=>c});var i=t(96540);const r={},a=i.createContext(r);function s(e){const n=i.useContext(a);return i.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function c(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:s(e.components),i.createElement(a.Provider,{value:n},e.children)}},91325(e,n,t){t.r(n),t.d(n,{assets:()=>l,contentTitle:()=>c,default:()=>d,frontMatter:()=>s,metadata:()=>i,toc:()=>o});const i=JSON.parse('{"id":"backend/circuit-breakers-rate-limiting","title":"Circuit Breakers & Rate Limiting Guide","description":"Overview","source":"@site/docs/backend/circuit-breakers-rate-limiting.md","sourceDirName":"backend","slug":"/backend/circuit-breakers-rate-limiting","permalink":"/fullstack-dev/docs/backend/circuit-breakers-rate-limiting","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/circuit-breakers-rate-limiting.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"WebRTC Implementation Guide","permalink":"/fullstack-dev/docs/backend/webrtc-implementation"},"next":{"title":"Service Registry and Health Checks Implementation Guide","permalink":"/fullstack-dev/docs/backend/service-registry-health-checks"}}');var r=t(74848),a=t(28453);const s={},c="Circuit Breakers & Rate Limiting Guide",l={},o=[{value:"Overview",id:"overview",level:2},{value:"Table of Contents",id:"table-of-contents",level:2},{value:"Circuit Breaker Pattern",id:"circuit-breaker-pattern",level:2},{value:"What is a Circuit Breaker?",id:"what-is-a-circuit-breaker",level:3},{value:"Circuit Breaker States",id:"circuit-breaker-states",level:3},{value:"Why Use Circuit Breakers?",id:"why-use-circuit-breakers",level:3},{value:"Circuit Breaker Implementation",id:"circuit-breaker-implementation",level:2},{value:"Circuit Breaker Service (NestJS)",id:"circuit-breaker-service-nestjs",level:3},{value:"HTTP Client with Circuit Breaker",id:"http-client-with-circuit-breaker",level:3},{value:"Database Client with Circuit Breaker",id:"database-client-with-circuit-breaker",level:3},{value:"Rate Limiting Strategies",id:"rate-limiting-strategies",level:2},{value:"Rate Limiting Algorithms",id:"rate-limiting-algorithms",level:3},{value:"Token Bucket Rate Limiter",id:"token-bucket-rate-limiter",level:3},{value:"Sliding Window Rate Limiter",id:"sliding-window-rate-limiter",level:3},{value:"Rate Limiting Implementation",id:"rate-limiting-implementation",level:2},{value:"Rate Limiting Guard",id:"rate-limiting-guard",level:3},{value:"Rate Limit Decorator",id:"rate-limit-decorator",level:3},{value:"Controller with Rate Limiting",id:"controller-with-rate-limiting",level:3},{value:"Bulkhead Pattern",id:"bulkhead-pattern",level:2},{value:"Resource Isolation with Bulkheads",id:"resource-isolation-with-bulkheads",level:3},{value:"Service with Bulkhead Protection",id:"service-with-bulkhead-protection",level:3},{value:"Fallback Mechanisms",id:"fallback-mechanisms",level:2},{value:"Fallback Strategies",id:"fallback-strategies",level:3},{value:"Monitoring and Metrics",id:"monitoring-and-metrics",level:2},{value:"Circuit Breaker Metrics",id:"circuit-breaker-metrics",level:3},{value:"Rate Limiter Metrics",id:"rate-limiter-metrics",level:3},{value:"Bulkhead Metrics",id:"bulkhead-metrics",level:3},{value:"Advanced Patterns",id:"advanced-patterns",level:2},{value:"Adaptive Rate Limiting",id:"adaptive-rate-limiting",level:3},{value:"Retry with Exponential Backoff and Circuit Breaker",id:"retry-with-exponential-backoff-and-circuit-breaker",level:3},{value:"Best Practices",id:"best-practices",level:2},{value:"1. Circuit Breaker Design",id:"1-circuit-breaker-design",level:3},{value:"2. Rate Limiting Strategy",id:"2-rate-limiting-strategy",level:3},{value:"3. Bulkhead Implementation",id:"3-bulkhead-implementation",level:3},{value:"4. Fallback Mechanisms",id:"4-fallback-mechanisms",level:3},{value:"5. Monitoring",id:"5-monitoring",level:3},{value:"Summary",id:"summary",level:2}];function u(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",mermaid:"mermaid",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,a.R)(),...e.components};return(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(n.header,{children:(0,r.jsx)(n.h1,{id:"circuit-breakers--rate-limiting-guide",children:"Circuit Breakers & Rate Limiting Guide"})}),"\n",(0,r.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,r.jsx)(n.p,{children:"Circuit Breakers and Rate Limiting are essential resilience patterns for microservices architectures. This guide covers circuit breaker implementation, rate limiting strategies, fallback mechanisms, bulkhead patterns, and monitoring for building fault-tolerant distributed systems."}),"\n",(0,r.jsx)(n.h2,{id:"table-of-contents",children:"Table of Contents"}),"\n",(0,r.jsxs)(n.ol,{children:["\n",(0,r.jsx)(n.li,{children:(0,r.jsx)(n.a,{href:"#circuit-breaker-pattern",children:"Circuit Breaker Pattern"})}),"\n",(0,r.jsx)(n.li,{children:(0,r.jsx)(n.a,{href:"#circuit-breaker-implementation",children:"Circuit Breaker Implementation"})}),"\n",(0,r.jsx)(n.li,{children:(0,r.jsx)(n.a,{href:"#rate-limiting-strategies",children:"Rate Limiting Strategies"})}),"\n",(0,r.jsx)(n.li,{children:(0,r.jsx)(n.a,{href:"#rate-limiting-implementation",children:"Rate Limiting Implementation"})}),"\n",(0,r.jsx)(n.li,{children:(0,r.jsx)(n.a,{href:"#bulkhead-pattern",children:"Bulkhead Pattern"})}),"\n",(0,r.jsx)(n.li,{children:(0,r.jsx)(n.a,{href:"#fallback-mechanisms",children:"Fallback Mechanisms"})}),"\n",(0,r.jsx)(n.li,{children:(0,r.jsx)(n.a,{href:"#monitoring-and-metrics",children:"Monitoring and Metrics"})}),"\n",(0,r.jsx)(n.li,{children:(0,r.jsx)(n.a,{href:"#advanced-patterns",children:"Advanced Patterns"})}),"\n",(0,r.jsx)(n.li,{children:(0,r.jsx)(n.a,{href:"#best-practices",children:"Best Practices"})}),"\n"]}),"\n",(0,r.jsx)(n.h2,{id:"circuit-breaker-pattern",children:"Circuit Breaker Pattern"}),"\n",(0,r.jsx)(n.h3,{id:"what-is-a-circuit-breaker",children:"What is a Circuit Breaker?"}),"\n",(0,r.jsx)(n.mermaid,{value:"stateDiagram-v2\n    [*] --\x3e Closed\n    Closed --\x3e Open: Failure threshold exceeded\n    Open --\x3e HalfOpen: Timeout elapsed\n    HalfOpen --\x3e Closed: Success threshold met\n    HalfOpen --\x3e Open: Failure detected\n\n    Closed: CLOSED\\nRequests pass through\\nFailures counted\n    Open: OPEN\\nRequests fail fast\\nNo calls to service\n    HalfOpen: HALF-OPEN\\nLimited requests\\nTesting recovery"}),"\n",(0,r.jsx)(n.p,{children:"A Circuit Breaker prevents cascading failures by monitoring service calls and opening the circuit when failures exceed a threshold, allowing the system to fail fast and recover gracefully."}),"\n",(0,r.jsx)(n.h3,{id:"circuit-breaker-states",children:"Circuit Breaker States"}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.strong,{children:"CLOSED"})," (Normal Operation)"]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"All requests pass through to the service"}),"\n",(0,r.jsx)(n.li,{children:"Failures are counted"}),"\n",(0,r.jsx)(n.li,{children:"If failure rate exceeds threshold \u2192 OPEN"}),"\n"]}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.strong,{children:"OPEN"})," (Failing Fast)"]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Requests immediately fail without calling service"}),"\n",(0,r.jsx)(n.li,{children:"Prevents resource exhaustion"}),"\n",(0,r.jsx)(n.li,{children:"After timeout period \u2192 HALF-OPEN"}),"\n"]}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.strong,{children:"HALF-OPEN"})," (Testing Recovery)"]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Limited requests pass through"}),"\n",(0,r.jsx)(n.li,{children:"Tests if service has recovered"}),"\n",(0,r.jsx)(n.li,{children:"If successful \u2192 CLOSED"}),"\n",(0,r.jsx)(n.li,{children:"If failure \u2192 OPEN"}),"\n"]}),"\n",(0,r.jsx)(n.h3,{id:"why-use-circuit-breakers",children:"Why Use Circuit Breakers?"}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Benefits:"})}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Prevent Cascading Failures"})," - Stop failure propagation"]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Fast Failure"})," - Fail immediately instead of waiting for timeout"]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Resource Protection"})," - Prevent resource exhaustion"]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Automatic Recovery"})," - Self-healing when service recovers"]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Improved User Experience"})," - Return fallback responses quickly"]}),"\n"]}),"\n",(0,r.jsx)(n.h2,{id:"circuit-breaker-implementation",children:"Circuit Breaker Implementation"}),"\n",(0,r.jsx)(n.h3,{id:"circuit-breaker-service-nestjs",children:"Circuit Breaker Service (NestJS)"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/circuit-breaker.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\n\nexport enum CircuitState {\n  CLOSED = 'CLOSED',\n  OPEN = 'OPEN',\n  HALF_OPEN = 'HALF_OPEN',\n}\n\nexport interface CircuitBreakerConfig {\n  failureThreshold: number; // Number of failures to open circuit\n  successThreshold: number; // Number of successes to close circuit\n  timeout: number; // Milliseconds to wait before half-open\n  volumeThreshold: number; // Minimum requests before evaluating\n  errorThresholdPercentage: number; // Percentage of errors to trip circuit\n  rollingWindowMs: number; // Time window for error calculation\n}\n\ninterface CircuitStats {\n  failures: number;\n  successes: number;\n  totalRequests: number;\n  lastFailureTime?: Date;\n  lastSuccessTime?: Date;\n  consecutiveFailures: number;\n  consecutiveSuccesses: number;\n}\n\n@Injectable()\nexport class CircuitBreakerService {\n  private readonly logger = new Logger(CircuitBreakerService.name);\n  private readonly circuits = new Map<\n    string,\n    {\n      state: CircuitState;\n      stats: CircuitStats;\n      config: CircuitBreakerConfig;\n      nextAttempt?: Date;\n      requestTimestamps: number[];\n    }\n  >();\n\n  private readonly defaultConfig: CircuitBreakerConfig = {\n    failureThreshold: 5,\n    successThreshold: 2,\n    timeout: 60000, // 1 minute\n    volumeThreshold: 10,\n    errorThresholdPercentage: 50,\n    rollingWindowMs: 60000, // 1 minute\n  };\n\n  /**\n   * Execute operation with circuit breaker protection\n   */\n  async execute<T>(\n    circuitName: string,\n    operation: () => Promise<T>,\n    fallback?: () => Promise<T>,\n    config?: Partial<CircuitBreakerConfig>,\n  ): Promise<T> {\n    const circuitConfig = { ...this.defaultConfig, ...config };\n\n    if (!this.circuits.has(circuitName)) {\n      this.initializeCircuit(circuitName, circuitConfig);\n    }\n\n    const circuit = this.circuits.get(circuitName);\n\n    // Check if circuit is open\n    if (circuit.state === CircuitState.OPEN) {\n      if (this.shouldAttemptReset(circuit)) {\n        this.transitionToHalfOpen(circuitName);\n      } else {\n        this.logger.warn(`Circuit ${circuitName} is OPEN - failing fast`);\n\n        if (fallback) {\n          return fallback();\n        }\n\n        throw new Error(`Circuit breaker ${circuitName} is OPEN`);\n      }\n    }\n\n    // Execute operation\n    const startTime = Date.now();\n    try {\n      const result = await operation();\n      this.recordSuccess(circuitName);\n\n      this.logger.debug(`Circuit ${circuitName} - Request succeeded in ${Date.now() - startTime}ms`);\n\n      return result;\n    } catch (error) {\n      this.recordFailure(circuitName, error);\n\n      this.logger.error(`Circuit ${circuitName} - Request failed in ${Date.now() - startTime}ms: ${error.message}`);\n\n      if (fallback) {\n        this.logger.log(`Using fallback for circuit ${circuitName}`);\n        return fallback();\n      }\n\n      throw error;\n    }\n  }\n\n  private initializeCircuit(name: string, config: CircuitBreakerConfig): void {\n    this.circuits.set(name, {\n      state: CircuitState.CLOSED,\n      config,\n      stats: {\n        failures: 0,\n        successes: 0,\n        totalRequests: 0,\n        consecutiveFailures: 0,\n        consecutiveSuccesses: 0,\n      },\n      requestTimestamps: [],\n    });\n\n    this.logger.log(`Initialized circuit breaker: ${name}`);\n  }\n\n  private recordSuccess(circuitName: string): void {\n    const circuit = this.circuits.get(circuitName);\n\n    circuit.stats.successes++;\n    circuit.stats.totalRequests++;\n    circuit.stats.consecutiveSuccesses++;\n    circuit.stats.consecutiveFailures = 0;\n    circuit.stats.lastSuccessTime = new Date();\n    circuit.requestTimestamps.push(Date.now());\n\n    // Clean old timestamps\n    this.cleanOldTimestamps(circuit);\n\n    // Check if we should close the circuit\n    if (circuit.state === CircuitState.HALF_OPEN) {\n      if (circuit.stats.consecutiveSuccesses >= circuit.config.successThreshold) {\n        this.transitionToClosed(circuitName);\n      }\n    }\n  }\n\n  private recordFailure(circuitName: string, error: Error): void {\n    const circuit = this.circuits.get(circuitName);\n\n    circuit.stats.failures++;\n    circuit.stats.totalRequests++;\n    circuit.stats.consecutiveFailures++;\n    circuit.stats.consecutiveSuccesses = 0;\n    circuit.stats.lastFailureTime = new Date();\n    circuit.requestTimestamps.push(Date.now());\n\n    // Clean old timestamps\n    this.cleanOldTimestamps(circuit);\n\n    // Check if we should open the circuit\n    if (circuit.state === CircuitState.CLOSED || circuit.state === CircuitState.HALF_OPEN) {\n      if (this.shouldOpenCircuit(circuit)) {\n        this.transitionToOpen(circuitName);\n      }\n    }\n  }\n\n  private shouldOpenCircuit(circuit: any): boolean {\n    const { config, stats, requestTimestamps } = circuit;\n\n    // Need minimum volume\n    if (requestTimestamps.length < config.volumeThreshold) {\n      return false;\n    }\n\n    // Check consecutive failures\n    if (stats.consecutiveFailures >= config.failureThreshold) {\n      return true;\n    }\n\n    // Check error percentage in rolling window\n    const errorRate = (stats.failures / stats.totalRequests) * 100;\n    return errorRate >= config.errorThresholdPercentage;\n  }\n\n  private shouldAttemptReset(circuit: any): boolean {\n    if (!circuit.nextAttempt) {\n      return true;\n    }\n    return Date.now() >= circuit.nextAttempt.getTime();\n  }\n\n  private transitionToOpen(circuitName: string): void {\n    const circuit = this.circuits.get(circuitName);\n    circuit.state = CircuitState.OPEN;\n    circuit.nextAttempt = new Date(Date.now() + circuit.config.timeout);\n\n    this.logger.warn(\n      `Circuit ${circuitName} transitioned to OPEN. Next attempt at ${circuit.nextAttempt.toISOString()}`,\n    );\n  }\n\n  private transitionToHalfOpen(circuitName: string): void {\n    const circuit = this.circuits.get(circuitName);\n    circuit.state = CircuitState.HALF_OPEN;\n    circuit.stats.consecutiveSuccesses = 0;\n    circuit.stats.consecutiveFailures = 0;\n\n    this.logger.log(`Circuit ${circuitName} transitioned to HALF_OPEN`);\n  }\n\n  private transitionToClosed(circuitName: string): void {\n    const circuit = this.circuits.get(circuitName);\n    circuit.state = CircuitState.CLOSED;\n    circuit.stats.failures = 0;\n    circuit.stats.successes = 0;\n    circuit.stats.totalRequests = 0;\n    circuit.stats.consecutiveFailures = 0;\n    circuit.stats.consecutiveSuccesses = 0;\n    circuit.requestTimestamps = [];\n\n    this.logger.log(`Circuit ${circuitName} transitioned to CLOSED`);\n  }\n\n  private cleanOldTimestamps(circuit: any): void {\n    const cutoff = Date.now() - circuit.config.rollingWindowMs;\n    circuit.requestTimestamps = circuit.requestTimestamps.filter((ts) => ts > cutoff);\n  }\n\n  /**\n   * Get circuit status\n   */\n  getCircuitStatus(circuitName: string): any {\n    const circuit = this.circuits.get(circuitName);\n\n    if (!circuit) {\n      return null;\n    }\n\n    return {\n      name: circuitName,\n      state: circuit.state,\n      stats: circuit.stats,\n      nextAttempt: circuit.nextAttempt,\n      requestsInWindow: circuit.requestTimestamps.length,\n    };\n  }\n\n  /**\n   * Get all circuits status\n   */\n  getAllCircuitsStatus(): any[] {\n    return Array.from(this.circuits.entries()).map(([name, circuit]) => ({\n      name,\n      state: circuit.state,\n      stats: circuit.stats,\n      nextAttempt: circuit.nextAttempt,\n      requestsInWindow: circuit.requestTimestamps.length,\n    }));\n  }\n\n  /**\n   * Manually reset circuit\n   */\n  resetCircuit(circuitName: string): void {\n    if (this.circuits.has(circuitName)) {\n      this.transitionToClosed(circuitName);\n      this.logger.log(`Circuit ${circuitName} manually reset`);\n    }\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h3,{id:"http-client-with-circuit-breaker",children:"HTTP Client with Circuit Breaker"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/http-client-with-circuit-breaker.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { HttpService } from '@nestjs/axios';\nimport { CircuitBreakerService } from './circuit-breaker.service';\nimport { firstValueFrom } from 'rxjs';\nimport { AxiosRequestConfig, AxiosResponse } from 'axios';\n\n@Injectable()\nexport class HttpClientWithCircuitBreakerService {\n  private readonly logger = new Logger(HttpClientWithCircuitBreakerService.name);\n\n  constructor(private httpService: HttpService, private circuitBreaker: CircuitBreakerService) {}\n\n  /**\n   * GET request with circuit breaker\n   */\n  async get<T>(url: string, config?: AxiosRequestConfig, fallbackData?: T): Promise<T> {\n    const circuitName = this.getCircuitName('GET', url);\n\n    return this.circuitBreaker.execute<T>(\n      circuitName,\n      async () => {\n        const response = await firstValueFrom(this.httpService.get<T>(url, config));\n        return response.data;\n      },\n      fallbackData ? async () => fallbackData : undefined,\n      {\n        failureThreshold: 5,\n        timeout: 30000,\n        errorThresholdPercentage: 50,\n      },\n    );\n  }\n\n  /**\n   * POST request with circuit breaker\n   */\n  async post<T>(url: string, data: any, config?: AxiosRequestConfig, fallbackData?: T): Promise<T> {\n    const circuitName = this.getCircuitName('POST', url);\n\n    return this.circuitBreaker.execute<T>(\n      circuitName,\n      async () => {\n        const response = await firstValueFrom(this.httpService.post<T>(url, data, config));\n        return response.data;\n      },\n      fallbackData ? async () => fallbackData : undefined,\n    );\n  }\n\n  private getCircuitName(method: string, url: string): string {\n    // Extract service name from URL\n    const urlObj = new URL(url);\n    const serviceName = urlObj.hostname.split('.')[0];\n    return `${serviceName}-${method}`;\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h3,{id:"database-client-with-circuit-breaker",children:"Database Client with Circuit Breaker"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/database-with-circuit-breaker.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { CircuitBreakerService } from './circuit-breaker.service';\n\n@Injectable()\nexport class DatabaseWithCircuitBreakerService<T> {\n  private readonly logger = new Logger(DatabaseWithCircuitBreakerService.name);\n\n  constructor(\n    private circuitBreaker: CircuitBreakerService,\n    private repository: Repository<T>,\n    private entityName: string,\n  ) {}\n\n  /**\n   * Find with circuit breaker protection\n   */\n  async find(options?: any, fallbackData?: T[]): Promise<T[]> {\n    return this.circuitBreaker.execute(\n      `db-${this.entityName}-find`,\n      () => this.repository.find(options),\n      fallbackData ? async () => fallbackData : undefined,\n      {\n        failureThreshold: 3,\n        timeout: 20000,\n        errorThresholdPercentage: 60,\n      },\n    );\n  }\n\n  /**\n   * Find one with circuit breaker protection\n   */\n  async findOne(options: any, fallbackData?: T): Promise<T> {\n    return this.circuitBreaker.execute(\n      `db-${this.entityName}-findOne`,\n      () => this.repository.findOne(options),\n      fallbackData ? async () => fallbackData : undefined,\n    );\n  }\n\n  /**\n   * Save with circuit breaker protection\n   */\n  async save(entity: T): Promise<T> {\n    return this.circuitBreaker.execute(`db-${this.entityName}-save`, () => this.repository.save(entity));\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h2,{id:"rate-limiting-strategies",children:"Rate Limiting Strategies"}),"\n",(0,r.jsx)(n.h3,{id:"rate-limiting-algorithms",children:"Rate Limiting Algorithms"}),"\n",(0,r.jsx)(n.mermaid,{value:'graph TB\n    subgraph "Rate Limiting Algorithms"\n        A[Token Bucket]\n        B[Leaky Bucket]\n        C[Fixed Window]\n        D[Sliding Window]\n        E[Sliding Log]\n    end\n\n    A --\x3e A1[Tokens refill at fixed rate<br/>Allows bursts]\n    B --\x3e B1[Requests leak at fixed rate<br/>Smooths traffic]\n    C --\x3e C1[Count per time window<br/>Simple but spike at boundary]\n    D --\x3e D1[Rolling window<br/>More accurate]\n    E --\x3e E1[Log all requests<br/>Most accurate, expensive]\n\n    style A fill:#4CAF50\n    style B fill:#2196F3\n    style C fill:#FF9800\n    style D fill:#9C27B0\n    style E fill:#f44336'}),"\n",(0,r.jsx)(n.h3,{id:"token-bucket-rate-limiter",children:"Token Bucket Rate Limiter"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/rate-limiter/token-bucket.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { Redis } from 'ioredis';\nimport { InjectRedis } from '@nestjs-modules/ioredis';\n\nexport interface TokenBucketConfig {\n  capacity: number; // Maximum tokens in bucket\n  refillRate: number; // Tokens added per second\n  tokensPerRequest: number; // Tokens consumed per request\n}\n\n@Injectable()\nexport class TokenBucketRateLimiterService {\n  private readonly logger = new Logger(TokenBucketRateLimiterService.name);\n\n  constructor(@InjectRedis() private readonly redis: Redis) {}\n\n  /**\n   * Check if request is allowed (token bucket algorithm)\n   */\n  async isAllowed(\n    key: string,\n    config: TokenBucketConfig,\n  ): Promise<{ allowed: boolean; remainingTokens: number; retryAfter?: number }> {\n    const bucketKey = `rate_limit:token_bucket:${key}`;\n    const now = Date.now();\n\n    // Lua script for atomic token bucket check\n    const script = `\n      local bucketKey = KEYS[1]\n      local capacity = tonumber(ARGV[1])\n      local refillRate = tonumber(ARGV[2])\n      local tokensPerRequest = tonumber(ARGV[3])\n      local now = tonumber(ARGV[4])\n      \n      local bucket = redis.call('HMGET', bucketKey, 'tokens', 'lastRefill')\n      local tokens = tonumber(bucket[1]) or capacity\n      local lastRefill = tonumber(bucket[2]) or now\n      \n      -- Calculate token refill\n      local timePassed = (now - lastRefill) / 1000\n      local tokensToAdd = timePassed * refillRate\n      tokens = math.min(capacity, tokens + tokensToAdd)\n      \n      -- Check if enough tokens\n      if tokens >= tokensPerRequest then\n        tokens = tokens - tokensPerRequest\n        redis.call('HMSET', bucketKey, 'tokens', tokens, 'lastRefill', now)\n        redis.call('EXPIRE', bucketKey, 3600)\n        return {1, tokens, 0}\n      else\n        -- Calculate retry after\n        local tokensNeeded = tokensPerRequest - tokens\n        local retryAfter = math.ceil(tokensNeeded / refillRate)\n        return {0, tokens, retryAfter}\n      end\n    `;\n\n    const result = (await this.redis.eval(\n      script,\n      1,\n      bucketKey,\n      config.capacity,\n      config.refillRate,\n      config.tokensPerRequest,\n      now,\n    )) as [number, number, number];\n\n    const allowed = result[0] === 1;\n    const remainingTokens = result[1];\n    const retryAfter = result[2];\n\n    if (!allowed) {\n      this.logger.warn(`Rate limit exceeded for ${key}. Retry after ${retryAfter}s`);\n    }\n\n    return {\n      allowed,\n      remainingTokens,\n      retryAfter: retryAfter > 0 ? retryAfter : undefined,\n    };\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h3,{id:"sliding-window-rate-limiter",children:"Sliding Window Rate Limiter"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/rate-limiter/sliding-window.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { Redis } from 'ioredis';\nimport { InjectRedis } from '@nestjs-modules/ioredis';\n\nexport interface SlidingWindowConfig {\n  maxRequests: number; // Maximum requests in window\n  windowMs: number; // Time window in milliseconds\n}\n\n@Injectable()\nexport class SlidingWindowRateLimiterService {\n  private readonly logger = new Logger(SlidingWindowRateLimiterService.name);\n\n  constructor(@InjectRedis() private readonly redis: Redis) {}\n\n  /**\n   * Check if request is allowed (sliding window algorithm)\n   */\n  async isAllowed(\n    key: string,\n    config: SlidingWindowConfig,\n  ): Promise<{ allowed: boolean; remaining: number; resetAt: Date }> {\n    const windowKey = `rate_limit:sliding_window:${key}`;\n    const now = Date.now();\n    const windowStart = now - config.windowMs;\n\n    // Lua script for atomic sliding window check\n    const script = `\n      local windowKey = KEYS[1]\n      local now = tonumber(ARGV[1])\n      local windowStart = tonumber(ARGV[2])\n      local maxRequests = tonumber(ARGV[3])\n      local windowMs = tonumber(ARGV[4])\n      \n      -- Remove old entries\n      redis.call('ZREMRANGEBYSCORE', windowKey, '-inf', windowStart)\n      \n      -- Count requests in window\n      local count = redis.call('ZCARD', windowKey)\n      \n      if count < maxRequests then\n        -- Add current request\n        redis.call('ZADD', windowKey, now, now)\n        redis.call('PEXPIRE', windowKey, windowMs)\n        return {1, maxRequests - count - 1}\n      else\n        return {0, 0}\n      end\n    `;\n\n    const result = (await this.redis.eval(\n      script,\n      1,\n      windowKey,\n      now,\n      windowStart,\n      config.maxRequests,\n      config.windowMs,\n    )) as [number, number];\n\n    const allowed = result[0] === 1;\n    const remaining = result[1];\n    const resetAt = new Date(now + config.windowMs);\n\n    if (!allowed) {\n      this.logger.warn(`Rate limit exceeded for ${key}. Resets at ${resetAt.toISOString()}`);\n    }\n\n    return {\n      allowed,\n      remaining,\n      resetAt,\n    };\n  }\n\n  /**\n   * Get current usage\n   */\n  async getUsage(key: string, windowMs: number): Promise<{ count: number; oldestRequest: Date }> {\n    const windowKey = `rate_limit:sliding_window:${key}`;\n    const now = Date.now();\n    const windowStart = now - windowMs;\n\n    // Remove old entries\n    await this.redis.zremrangebyscore(windowKey, '-inf', windowStart);\n\n    // Get count and oldest request\n    const count = await this.redis.zcard(windowKey);\n    const oldest = await this.redis.zrange(windowKey, 0, 0, 'WITHSCORES');\n\n    return {\n      count,\n      oldestRequest: oldest.length > 0 ? new Date(parseInt(oldest[1])) : null,\n    };\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h2,{id:"rate-limiting-implementation",children:"Rate Limiting Implementation"}),"\n",(0,r.jsx)(n.h3,{id:"rate-limiting-guard",children:"Rate Limiting Guard"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// guards/rate-limit.guard.ts\nimport { Injectable, CanActivate, ExecutionContext, HttpException, HttpStatus, Logger } from '@nestjs/common';\nimport { Reflector } from '@nestjs/core';\nimport { SlidingWindowRateLimiterService } from '../services/rate-limiter/sliding-window.service';\nimport { TokenBucketRateLimiterService } from '../services/rate-limiter/token-bucket.service';\n\nexport interface RateLimitOptions {\n  type: 'sliding-window' | 'token-bucket';\n  // Sliding window options\n  maxRequests?: number;\n  windowMs?: number;\n  // Token bucket options\n  capacity?: number;\n  refillRate?: number;\n  tokensPerRequest?: number;\n  // Common options\n  keyGenerator?: (req: any) => string;\n  skipIf?: (req: any) => boolean;\n}\n\nexport const RATE_LIMIT_KEY = 'rate_limit';\n\n@Injectable()\nexport class RateLimitGuard implements CanActivate {\n  private readonly logger = new Logger(RateLimitGuard.name);\n\n  constructor(\n    private reflector: Reflector,\n    private slidingWindowLimiter: SlidingWindowRateLimiterService,\n    private tokenBucketLimiter: TokenBucketRateLimiterService,\n  ) {}\n\n  async canActivate(context: ExecutionContext): Promise<boolean> {\n    const options = this.reflector.get<RateLimitOptions>(RATE_LIMIT_KEY, context.getHandler());\n\n    if (!options) {\n      return true;\n    }\n\n    const request = context.switchToHttp().getRequest();\n\n    // Check skip condition\n    if (options.skipIf && options.skipIf(request)) {\n      return true;\n    }\n\n    // Generate key\n    const key = options.keyGenerator ? options.keyGenerator(request) : this.defaultKeyGenerator(request);\n\n    // Apply rate limiting based on type\n    if (options.type === 'sliding-window') {\n      const result = await this.slidingWindowLimiter.isAllowed(key, {\n        maxRequests: options.maxRequests || 100,\n        windowMs: options.windowMs || 60000,\n      });\n\n      if (!result.allowed) {\n        throw new HttpException(\n          {\n            statusCode: HttpStatus.TOO_MANY_REQUESTS,\n            message: 'Rate limit exceeded',\n            retryAfter: Math.ceil((result.resetAt.getTime() - Date.now()) / 1000),\n          },\n          HttpStatus.TOO_MANY_REQUESTS,\n        );\n      }\n\n      // Add rate limit headers\n      const response = context.switchToHttp().getResponse();\n      response.setHeader('X-RateLimit-Limit', options.maxRequests);\n      response.setHeader('X-RateLimit-Remaining', result.remaining);\n      response.setHeader('X-RateLimit-Reset', Math.ceil(result.resetAt.getTime() / 1000));\n    } else if (options.type === 'token-bucket') {\n      const result = await this.tokenBucketLimiter.isAllowed(key, {\n        capacity: options.capacity || 100,\n        refillRate: options.refillRate || 10,\n        tokensPerRequest: options.tokensPerRequest || 1,\n      });\n\n      if (!result.allowed) {\n        throw new HttpException(\n          {\n            statusCode: HttpStatus.TOO_MANY_REQUESTS,\n            message: 'Rate limit exceeded',\n            retryAfter: result.retryAfter,\n          },\n          HttpStatus.TOO_MANY_REQUESTS,\n        );\n      }\n\n      // Add rate limit headers\n      const response = context.switchToHttp().getResponse();\n      response.setHeader('X-RateLimit-Limit', options.capacity);\n      response.setHeader('X-RateLimit-Remaining', Math.floor(result.remainingTokens));\n    }\n\n    return true;\n  }\n\n  private defaultKeyGenerator(request: any): string {\n    // Use IP address as default key\n    const ip = request.ip || request.connection.remoteAddress;\n    const path = request.route?.path || request.url;\n    return `${ip}:${path}`;\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h3,{id:"rate-limit-decorator",children:"Rate Limit Decorator"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// decorators/rate-limit.decorator.ts\nimport { SetMetadata } from '@nestjs/common';\nimport { RateLimitOptions, RATE_LIMIT_KEY } from '../guards/rate-limit.guard';\n\nexport const RateLimit = (options: RateLimitOptions) => SetMetadata(RATE_LIMIT_KEY, options);\n"})}),"\n",(0,r.jsx)(n.h3,{id:"controller-with-rate-limiting",children:"Controller with Rate Limiting"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// controllers/api.controller.ts\nimport { Controller, Get, Post, Body, UseGuards, Req } from '@nestjs/common';\nimport { RateLimitGuard } from '../guards/rate-limit.guard';\nimport { RateLimit } from '../decorators/rate-limit.decorator';\n\n@Controller('api')\n@UseGuards(RateLimitGuard)\nexport class ApiController {\n  /**\n   * Public endpoint - 100 requests per minute per IP\n   */\n  @Get('public')\n  @RateLimit({\n    type: 'sliding-window',\n    maxRequests: 100,\n    windowMs: 60000, // 1 minute\n  })\n  getPublicData() {\n    return { message: 'Public data' };\n  }\n\n  /**\n   * Search endpoint - Token bucket (allows bursts)\n   */\n  @Get('search')\n  @RateLimit({\n    type: 'token-bucket',\n    capacity: 50,\n    refillRate: 10, // 10 tokens per second\n    tokensPerRequest: 1,\n  })\n  search() {\n    return { results: [] };\n  }\n\n  /**\n   * User-specific rate limiting\n   */\n  @Post('create')\n  @RateLimit({\n    type: 'sliding-window',\n    maxRequests: 10,\n    windowMs: 60000,\n    keyGenerator: (req) => `user:${req.user?.id}:create`,\n    skipIf: (req) => req.user?.role === 'admin', // Skip for admins\n  })\n  create(@Body() data: any) {\n    return { success: true };\n  }\n\n  /**\n   * Heavy operation - Lower limits\n   */\n  @Post('export')\n  @RateLimit({\n    type: 'token-bucket',\n    capacity: 5,\n    refillRate: 0.1, // 1 token per 10 seconds\n    tokensPerRequest: 1,\n  })\n  export() {\n    return { jobId: 'xxx' };\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h2,{id:"bulkhead-pattern",children:"Bulkhead Pattern"}),"\n",(0,r.jsx)(n.h3,{id:"resource-isolation-with-bulkheads",children:"Resource Isolation with Bulkheads"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/bulkhead.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\n\nexport interface BulkheadConfig {\n  maxConcurrent: number; // Maximum concurrent executions\n  maxQueueSize: number; // Maximum queue size\n  queueTimeoutMs: number; // Timeout for queued requests\n}\n\ninterface QueuedRequest<T> {\n  operation: () => Promise<T>;\n  resolve: (value: T) => void;\n  reject: (error: Error) => void;\n  enqueuedAt: Date;\n}\n\n@Injectable()\nexport class BulkheadService {\n  private readonly logger = new Logger(BulkheadService.name);\n  private readonly bulkheads = new Map<\n    string,\n    {\n      config: BulkheadConfig;\n      running: number;\n      queue: QueuedRequest<any>[];\n    }\n  >();\n\n  /**\n   * Execute operation with bulkhead protection\n   */\n  async execute<T>(bulkheadName: string, operation: () => Promise<T>, config: BulkheadConfig): Promise<T> {\n    if (!this.bulkheads.has(bulkheadName)) {\n      this.bulkheads.set(bulkheadName, {\n        config,\n        running: 0,\n        queue: [],\n      });\n    }\n\n    const bulkhead = this.bulkheads.get(bulkheadName);\n\n    // Check if we can execute immediately\n    if (bulkhead.running < config.maxConcurrent) {\n      return this.executeImmediately(bulkheadName, operation);\n    }\n\n    // Check queue capacity\n    if (bulkhead.queue.length >= config.maxQueueSize) {\n      throw new Error(`Bulkhead ${bulkheadName} queue full (${config.maxQueueSize})`);\n    }\n\n    // Queue the request\n    return this.enqueue<T>(bulkheadName, operation, config);\n  }\n\n  private async executeImmediately<T>(bulkheadName: string, operation: () => Promise<T>): Promise<T> {\n    const bulkhead = this.bulkheads.get(bulkheadName);\n    bulkhead.running++;\n\n    try {\n      const result = await operation();\n      return result;\n    } finally {\n      bulkhead.running--;\n      this.processQueue(bulkheadName);\n    }\n  }\n\n  private enqueue<T>(bulkheadName: string, operation: () => Promise<T>, config: BulkheadConfig): Promise<T> {\n    const bulkhead = this.bulkheads.get(bulkheadName);\n\n    return new Promise<T>((resolve, reject) => {\n      const queuedRequest: QueuedRequest<T> = {\n        operation,\n        resolve,\n        reject,\n        enqueuedAt: new Date(),\n      };\n\n      bulkhead.queue.push(queuedRequest);\n\n      // Set timeout\n      setTimeout(() => {\n        const index = bulkhead.queue.indexOf(queuedRequest);\n        if (index > -1) {\n          bulkhead.queue.splice(index, 1);\n          reject(new Error(`Bulkhead ${bulkheadName} queue timeout`));\n        }\n      }, config.queueTimeoutMs);\n\n      this.logger.debug(`Request queued in bulkhead ${bulkheadName}. Queue size: ${bulkhead.queue.length}`);\n    });\n  }\n\n  private async processQueue(bulkheadName: string): Promise<void> {\n    const bulkhead = this.bulkheads.get(bulkheadName);\n\n    while (bulkhead.running < bulkhead.config.maxConcurrent && bulkhead.queue.length > 0) {\n      const request = bulkhead.queue.shift();\n\n      if (!request) {\n        break;\n      }\n\n      // Check timeout\n      const queueTime = Date.now() - request.enqueuedAt.getTime();\n      if (queueTime > bulkhead.config.queueTimeoutMs) {\n        request.reject(new Error(`Bulkhead ${bulkheadName} queue timeout`));\n        continue;\n      }\n\n      // Execute\n      bulkhead.running++;\n\n      request\n        .operation()\n        .then(request.resolve)\n        .catch(request.reject)\n        .finally(() => {\n          bulkhead.running--;\n          this.processQueue(bulkheadName);\n        });\n    }\n  }\n\n  /**\n   * Get bulkhead status\n   */\n  getStatus(bulkheadName: string): any {\n    const bulkhead = this.bulkheads.get(bulkheadName);\n\n    if (!bulkhead) {\n      return null;\n    }\n\n    return {\n      name: bulkheadName,\n      running: bulkhead.running,\n      queued: bulkhead.queue.length,\n      maxConcurrent: bulkhead.config.maxConcurrent,\n      maxQueueSize: bulkhead.config.maxQueueSize,\n    };\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h3,{id:"service-with-bulkhead-protection",children:"Service with Bulkhead Protection"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/external-api.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { BulkheadService } from './bulkhead.service';\nimport { CircuitBreakerService } from './circuit-breaker.service';\n\n@Injectable()\nexport class ExternalApiService {\n  constructor(private bulkhead: BulkheadService, private circuitBreaker: CircuitBreakerService) {}\n\n  /**\n   * Call external API with bulkhead and circuit breaker\n   */\n  async callExternalApi(endpoint: string, data: any): Promise<any> {\n    // Bulkhead limits concurrent calls\n    return this.bulkhead.execute(\n      'external-api',\n      async () => {\n        // Circuit breaker prevents cascading failures\n        return this.circuitBreaker.execute(\n          'external-api',\n          async () => {\n            // Actual API call\n            const response = await fetch(endpoint, {\n              method: 'POST',\n              headers: { 'Content-Type': 'application/json' },\n              body: JSON.stringify(data),\n            });\n\n            return response.json();\n          },\n          async () => {\n            // Fallback\n            return { cached: true, data: [] };\n          },\n        );\n      },\n      {\n        maxConcurrent: 10, // Max 10 concurrent calls\n        maxQueueSize: 50, // Queue up to 50 requests\n        queueTimeoutMs: 5000, // 5 second queue timeout\n      },\n    );\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h2,{id:"fallback-mechanisms",children:"Fallback Mechanisms"}),"\n",(0,r.jsx)(n.h3,{id:"fallback-strategies",children:"Fallback Strategies"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/fallback-strategies.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { Redis } from 'ioredis';\nimport { InjectRedis } from '@nestjs-modules/ioredis';\n\n@Injectable()\nexport class FallbackStrategiesService {\n  private readonly logger = new Logger(FallbackStrategiesService.name);\n\n  constructor(@InjectRedis() private readonly redis: Redis) {}\n\n  /**\n   * Cached fallback - Return cached data\n   */\n  async cachedFallback<T>(cacheKey: string, operation: () => Promise<T>, ttlSeconds: number = 3600): Promise<T> {\n    try {\n      const result = await operation();\n\n      // Cache successful result\n      await this.redis.setex(`fallback:${cacheKey}`, ttlSeconds, JSON.stringify(result));\n\n      return result;\n    } catch (error) {\n      this.logger.warn(`Operation failed, using cached fallback for ${cacheKey}`);\n\n      // Return cached data\n      const cached = await this.redis.get(`fallback:${cacheKey}`);\n\n      if (cached) {\n        return JSON.parse(cached);\n      }\n\n      throw error;\n    }\n  }\n\n  /**\n   * Default value fallback\n   */\n  async defaultFallback<T>(operation: () => Promise<T>, defaultValue: T): Promise<T> {\n    try {\n      return await operation();\n    } catch (error) {\n      this.logger.warn(`Operation failed, using default fallback`);\n      return defaultValue;\n    }\n  }\n\n  /**\n   * Degraded service fallback - Return limited data\n   */\n  async degradedFallback<T>(\n    operation: () => Promise<T>,\n    degradedOperation: () => Promise<Partial<T>>,\n  ): Promise<T | Partial<T>> {\n    try {\n      return await operation();\n    } catch (error) {\n      this.logger.warn(`Operation failed, using degraded service`);\n      return degradedOperation();\n    }\n  }\n\n  /**\n   * Alternative service fallback\n   */\n  async alternativeFallback<T>(primaryOperation: () => Promise<T>, alternativeOperation: () => Promise<T>): Promise<T> {\n    try {\n      return await primaryOperation();\n    } catch (error) {\n      this.logger.warn(`Primary service failed, using alternative`);\n      return alternativeOperation();\n    }\n  }\n\n  /**\n   * Stale-while-revalidate fallback\n   */\n  async staleWhileRevalidate<T>(cacheKey: string, operation: () => Promise<T>, ttlSeconds: number = 300): Promise<T> {\n    const cached = await this.redis.get(`swr:${cacheKey}`);\n\n    if (cached) {\n      // Return stale data immediately\n      const staleData = JSON.parse(cached);\n\n      // Revalidate in background\n      this.revalidateInBackground(cacheKey, operation, ttlSeconds);\n\n      return staleData;\n    }\n\n    // No cache, fetch fresh data\n    const freshData = await operation();\n    await this.redis.setex(`swr:${cacheKey}`, ttlSeconds, JSON.stringify(freshData));\n\n    return freshData;\n  }\n\n  private async revalidateInBackground<T>(\n    cacheKey: string,\n    operation: () => Promise<T>,\n    ttlSeconds: number,\n  ): Promise<void> {\n    try {\n      const freshData = await operation();\n      await this.redis.setex(`swr:${cacheKey}`, ttlSeconds, JSON.stringify(freshData));\n    } catch (error) {\n      this.logger.error(`Background revalidation failed for ${cacheKey}`);\n    }\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h2,{id:"monitoring-and-metrics",children:"Monitoring and Metrics"}),"\n",(0,r.jsx)(n.h3,{id:"circuit-breaker-metrics",children:"Circuit Breaker Metrics"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/circuit-breaker-metrics.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { Counter, Gauge, Histogram } from 'prom-client';\n\n@Injectable()\nexport class CircuitBreakerMetricsService {\n  private readonly stateGauge = new Gauge({\n    name: 'circuit_breaker_state',\n    help: 'Circuit breaker state (0=closed, 1=open, 2=half-open)',\n    labelNames: ['circuit_name'],\n  });\n\n  private readonly requestCounter = new Counter({\n    name: 'circuit_breaker_requests_total',\n    help: 'Total requests through circuit breaker',\n    labelNames: ['circuit_name', 'result'],\n  });\n\n  private readonly fallbackCounter = new Counter({\n    name: 'circuit_breaker_fallback_total',\n    help: 'Total fallback executions',\n    labelNames: ['circuit_name'],\n  });\n\n  private readonly responseTimeHistogram = new Histogram({\n    name: 'circuit_breaker_response_time_seconds',\n    help: 'Response time through circuit breaker',\n    labelNames: ['circuit_name'],\n    buckets: [0.01, 0.05, 0.1, 0.5, 1, 2, 5],\n  });\n\n  recordState(circuitName: string, state: string): void {\n    const stateValue = state === 'CLOSED' ? 0 : state === 'OPEN' ? 1 : 2;\n    this.stateGauge.set({ circuit_name: circuitName }, stateValue);\n  }\n\n  recordRequest(circuitName: string, result: 'success' | 'failure'): void {\n    this.requestCounter.inc({ circuit_name: circuitName, result });\n  }\n\n  recordFallback(circuitName: string): void {\n    this.fallbackCounter.inc({ circuit_name: circuitName });\n  }\n\n  recordResponseTime(circuitName: string, durationSeconds: number): void {\n    this.responseTimeHistogram.observe({ circuit_name: circuitName }, durationSeconds);\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h3,{id:"rate-limiter-metrics",children:"Rate Limiter Metrics"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/rate-limiter-metrics.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { Counter, Gauge } from 'prom-client';\n\n@Injectable()\nexport class RateLimiterMetricsService {\n  private readonly limitedCounter = new Counter({\n    name: 'rate_limit_exceeded_total',\n    help: 'Total rate limit exceeded events',\n    labelNames: ['endpoint', 'key_type'],\n  });\n\n  private readonly allowedCounter = new Counter({\n    name: 'rate_limit_allowed_total',\n    help: 'Total rate limit allowed requests',\n    labelNames: ['endpoint', 'key_type'],\n  });\n\n  private readonly activeKeysGauge = new Gauge({\n    name: 'rate_limit_active_keys',\n    help: 'Number of active rate limit keys',\n    labelNames: ['endpoint'],\n  });\n\n  recordLimitExceeded(endpoint: string, keyType: string): void {\n    this.limitedCounter.inc({ endpoint, key_type: keyType });\n  }\n\n  recordAllowed(endpoint: string, keyType: string): void {\n    this.allowedCounter.inc({ endpoint, key_type: keyType });\n  }\n\n  updateActiveKeys(endpoint: string, count: number): void {\n    this.activeKeysGauge.set({ endpoint }, count);\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h3,{id:"bulkhead-metrics",children:"Bulkhead Metrics"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/bulkhead-metrics.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { Gauge, Histogram, Counter } from 'prom-client';\n\n@Injectable()\nexport class BulkheadMetricsService {\n  private readonly runningGauge = new Gauge({\n    name: 'bulkhead_running_requests',\n    help: 'Number of running requests in bulkhead',\n    labelNames: ['bulkhead_name'],\n  });\n\n  private readonly queuedGauge = new Gauge({\n    name: 'bulkhead_queued_requests',\n    help: 'Number of queued requests in bulkhead',\n    labelNames: ['bulkhead_name'],\n  });\n\n  private readonly queueTimeHistogram = new Histogram({\n    name: 'bulkhead_queue_time_seconds',\n    help: 'Time spent in bulkhead queue',\n    labelNames: ['bulkhead_name'],\n    buckets: [0.01, 0.05, 0.1, 0.5, 1, 2, 5],\n  });\n\n  private readonly rejectedCounter = new Counter({\n    name: 'bulkhead_rejected_total',\n    help: 'Total rejected requests (queue full or timeout)',\n    labelNames: ['bulkhead_name', 'reason'],\n  });\n\n  updateRunning(bulkheadName: string, count: number): void {\n    this.runningGauge.set({ bulkhead_name: bulkheadName }, count);\n  }\n\n  updateQueued(bulkheadName: string, count: number): void {\n    this.queuedGauge.set({ bulkhead_name: bulkheadName }, count);\n  }\n\n  recordQueueTime(bulkheadName: string, durationSeconds: number): void {\n    this.queueTimeHistogram.observe({ bulkhead_name: bulkheadName }, durationSeconds);\n  }\n\n  recordRejection(bulkheadName: string, reason: 'queue_full' | 'timeout'): void {\n    this.rejectedCounter.inc({ bulkhead_name: bulkheadName, reason });\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h2,{id:"advanced-patterns",children:"Advanced Patterns"}),"\n",(0,r.jsx)(n.h3,{id:"adaptive-rate-limiting",children:"Adaptive Rate Limiting"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/adaptive-rate-limiter.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { SlidingWindowRateLimiterService } from './rate-limiter/sliding-window.service';\n\n@Injectable()\nexport class AdaptiveRateLimiterService {\n  private readonly logger = new Logger(AdaptiveRateLimiterService.name);\n  private readonly baselineLatency = new Map<string, number>();\n  private readonly currentLimits = new Map<string, number>();\n\n  constructor(private slidingWindowLimiter: SlidingWindowRateLimiterService) {}\n\n  /**\n   * Adjust rate limit based on system performance\n   */\n  async adjustRateLimit(endpoint: string, currentLatency: number, baseLimit: number): Promise<number> {\n    const baseline = this.baselineLatency.get(endpoint) || currentLatency;\n    this.baselineLatency.set(endpoint, baseline);\n\n    let adjustedLimit = this.currentLimits.get(endpoint) || baseLimit;\n\n    // If latency is 2x baseline, reduce limit by 20%\n    if (currentLatency > baseline * 2) {\n      adjustedLimit = Math.floor(adjustedLimit * 0.8);\n      this.logger.warn(`Reducing rate limit for ${endpoint} to ${adjustedLimit} due to high latency`);\n    }\n    // If latency is normal and limit is below base, gradually increase\n    else if (currentLatency <= baseline * 1.2 && adjustedLimit < baseLimit) {\n      adjustedLimit = Math.min(baseLimit, Math.floor(adjustedLimit * 1.1));\n      this.logger.log(`Increasing rate limit for ${endpoint} to ${adjustedLimit}`);\n    }\n\n    this.currentLimits.set(endpoint, adjustedLimit);\n    return adjustedLimit;\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h3,{id:"retry-with-exponential-backoff-and-circuit-breaker",children:"Retry with Exponential Backoff and Circuit Breaker"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-typescript",children:"// services/resilient-retry.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { CircuitBreakerService } from './circuit-breaker.service';\n\nexport interface RetryConfig {\n  maxRetries: number;\n  initialDelayMs: number;\n  maxDelayMs: number;\n  backoffMultiplier: number;\n  retryableErrors: string[];\n}\n\n@Injectable()\nexport class ResilientRetryService {\n  private readonly logger = new Logger(ResilientRetryService.name);\n\n  constructor(private circuitBreaker: CircuitBreakerService) {}\n\n  /**\n   * Retry with exponential backoff and circuit breaker\n   */\n  async executeWithRetry<T>(\n    circuitName: string,\n    operation: () => Promise<T>,\n    config: RetryConfig,\n    fallback?: () => Promise<T>,\n  ): Promise<T> {\n    return this.circuitBreaker.execute(\n      circuitName,\n      async () => {\n        return this.retryWithBackoff(operation, config);\n      },\n      fallback,\n    );\n  }\n\n  private async retryWithBackoff<T>(operation: () => Promise<T>, config: RetryConfig): Promise<T> {\n    let lastError: Error;\n\n    for (let attempt = 0; attempt <= config.maxRetries; attempt++) {\n      try {\n        return await operation();\n      } catch (error) {\n        lastError = error;\n\n        // Check if error is retryable\n        const isRetryable = config.retryableErrors.some((pattern) =>\n          error.message.toLowerCase().includes(pattern.toLowerCase()),\n        );\n\n        if (!isRetryable || attempt === config.maxRetries) {\n          throw error;\n        }\n\n        // Calculate delay with exponential backoff\n        const delay = Math.min(config.initialDelayMs * Math.pow(config.backoffMultiplier, attempt), config.maxDelayMs);\n\n        this.logger.warn(`Retry ${attempt + 1}/${config.maxRetries} after ${delay}ms. Error: ${error.message}`);\n\n        await this.sleep(delay);\n      }\n    }\n\n    throw lastError;\n  }\n\n  private sleep(ms: number): Promise<void> {\n    return new Promise((resolve) => setTimeout(resolve, ms));\n  }\n}\n"})}),"\n",(0,r.jsx)(n.h2,{id:"best-practices",children:"Best Practices"}),"\n",(0,r.jsx)(n.h3,{id:"1-circuit-breaker-design",children:"1. Circuit Breaker Design"}),"\n",(0,r.jsxs)(n.p,{children:["\u2705 ",(0,r.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Set appropriate failure thresholds"}),"\n",(0,r.jsx)(n.li,{children:"Implement fallback strategies"}),"\n",(0,r.jsx)(n.li,{children:"Monitor circuit state changes"}),"\n",(0,r.jsx)(n.li,{children:"Use different circuits for different dependencies"}),"\n",(0,r.jsx)(n.li,{children:"Test circuit behavior under load"}),"\n",(0,r.jsx)(n.li,{children:"Log state transitions"}),"\n"]}),"\n",(0,r.jsxs)(n.p,{children:["\u274c ",(0,r.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Use same circuit for all services"}),"\n",(0,r.jsx)(n.li,{children:"Set thresholds too low (flapping)"}),"\n",(0,r.jsx)(n.li,{children:"Forget fallback mechanisms"}),"\n",(0,r.jsx)(n.li,{children:"Ignore half-open state"}),"\n",(0,r.jsx)(n.li,{children:"Deploy without testing"}),"\n",(0,r.jsx)(n.li,{children:"Share circuits across unrelated operations"}),"\n"]}),"\n",(0,r.jsx)(n.h3,{id:"2-rate-limiting-strategy",children:"2. Rate Limiting Strategy"}),"\n",(0,r.jsxs)(n.p,{children:["\u2705 ",(0,r.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Choose algorithm based on use case"}),"\n",(0,r.jsx)(n.li,{children:"Use sliding window for accuracy"}),"\n",(0,r.jsx)(n.li,{children:"Use token bucket for burst tolerance"}),"\n",(0,r.jsx)(n.li,{children:"Implement per-user and per-IP limits"}),"\n",(0,r.jsx)(n.li,{children:"Add rate limit headers to responses"}),"\n",(0,r.jsx)(n.li,{children:"Monitor rate limit metrics"}),"\n"]}),"\n",(0,r.jsxs)(n.p,{children:["\u274c ",(0,r.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Use fixed window (boundary issues)"}),"\n",(0,r.jsx)(n.li,{children:"Apply same limits to all endpoints"}),"\n",(0,r.jsx)(n.li,{children:"Forget to communicate limits to clients"}),"\n",(0,r.jsx)(n.li,{children:"Ignore authenticated vs anonymous users"}),"\n",(0,r.jsx)(n.li,{children:"Skip monitoring"}),"\n",(0,r.jsx)(n.li,{children:"Set limits too restrictive"}),"\n"]}),"\n",(0,r.jsx)(n.h3,{id:"3-bulkhead-implementation",children:"3. Bulkhead Implementation"}),"\n",(0,r.jsxs)(n.p,{children:["\u2705 ",(0,r.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Isolate critical resources"}),"\n",(0,r.jsx)(n.li,{children:"Set appropriate concurrency limits"}),"\n",(0,r.jsx)(n.li,{children:"Implement queue timeouts"}),"\n",(0,r.jsx)(n.li,{children:"Monitor queue sizes"}),"\n",(0,r.jsx)(n.li,{children:"Test under load"}),"\n",(0,r.jsx)(n.li,{children:"Document bulkhead boundaries"}),"\n"]}),"\n",(0,r.jsxs)(n.p,{children:["\u274c ",(0,r.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Share thread pools across services"}),"\n",(0,r.jsx)(n.li,{children:"Set limits too high (no protection)"}),"\n",(0,r.jsx)(n.li,{children:"Forget queue limits"}),"\n",(0,r.jsx)(n.li,{children:"Ignore queue timeouts"}),"\n",(0,r.jsx)(n.li,{children:"Skip monitoring"}),"\n",(0,r.jsx)(n.li,{children:"Deploy without load testing"}),"\n"]}),"\n",(0,r.jsx)(n.h3,{id:"4-fallback-mechanisms",children:"4. Fallback Mechanisms"}),"\n",(0,r.jsxs)(n.p,{children:["\u2705 ",(0,r.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Provide meaningful fallbacks"}),"\n",(0,r.jsx)(n.li,{children:"Cache successful responses"}),"\n",(0,r.jsx)(n.li,{children:"Use stale-while-revalidate"}),"\n",(0,r.jsx)(n.li,{children:"Return degraded service"}),"\n",(0,r.jsx)(n.li,{children:"Log fallback usage"}),"\n",(0,r.jsx)(n.li,{children:"Monitor fallback rate"}),"\n"]}),"\n",(0,r.jsxs)(n.p,{children:["\u274c ",(0,r.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Return empty responses"}),"\n",(0,r.jsx)(n.li,{children:"Use same fallback for all errors"}),"\n",(0,r.jsx)(n.li,{children:"Cache errors"}),"\n",(0,r.jsx)(n.li,{children:"Forget to invalidate cache"}),"\n",(0,r.jsx)(n.li,{children:"Hide fallback execution"}),"\n",(0,r.jsx)(n.li,{children:"Deploy without fallback testing"}),"\n"]}),"\n",(0,r.jsx)(n.h3,{id:"5-monitoring",children:"5. Monitoring"}),"\n",(0,r.jsxs)(n.p,{children:["\u2705 ",(0,r.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Track all resilience patterns"}),"\n",(0,r.jsx)(n.li,{children:"Alert on circuit opens"}),"\n",(0,r.jsx)(n.li,{children:"Monitor rate limit violations"}),"\n",(0,r.jsx)(n.li,{children:"Dashboard bulkhead usage"}),"\n",(0,r.jsx)(n.li,{children:"Track fallback execution rate"}),"\n",(0,r.jsx)(n.li,{children:"Set up automated alerts"}),"\n"]}),"\n",(0,r.jsxs)(n.p,{children:["\u274c ",(0,r.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Deploy without monitoring"}),"\n",(0,r.jsx)(n.li,{children:"Ignore circuit state changes"}),"\n",(0,r.jsx)(n.li,{children:"Skip rate limit metrics"}),"\n",(0,r.jsx)(n.li,{children:"Forget bulkhead metrics"}),"\n",(0,r.jsx)(n.li,{children:"Hide fallback usage"}),"\n",(0,r.jsx)(n.li,{children:"React only to outages"}),"\n"]}),"\n",(0,r.jsx)(n.h2,{id:"summary",children:"Summary"}),"\n",(0,r.jsx)(n.p,{children:"Effective Circuit Breakers and Rate Limiting provide:"}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Resilience"})," - Prevent cascading failures"]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Performance"})," - Fail fast instead of waiting"]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Stability"})," - Protect resources from overload"]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Recovery"})," - Automatic healing when services recover"]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"User Experience"})," - Quick responses with fallbacks"]}),"\n"]}),"\n",(0,r.jsx)(n.p,{children:"Key implementation points:"}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Circuit Breaker with three states (CLOSED, OPEN, HALF-OPEN)"}),"\n",(0,r.jsx)(n.li,{children:"Token Bucket for burst-tolerant rate limiting"}),"\n",(0,r.jsx)(n.li,{children:"Sliding Window for accurate rate limiting"}),"\n",(0,r.jsx)(n.li,{children:"Bulkhead Pattern for resource isolation"}),"\n",(0,r.jsx)(n.li,{children:"Multiple fallback strategies (cached, default, degraded, alternative)"}),"\n",(0,r.jsx)(n.li,{children:"Comprehensive Prometheus metrics"}),"\n",(0,r.jsx)(n.li,{children:"Adaptive rate limiting based on system performance"}),"\n",(0,r.jsx)(n.li,{children:"Combined retry with circuit breaker and exponential backoff"}),"\n",(0,r.jsx)(n.li,{children:"Rate limiting guards and decorators for NestJS"}),"\n",(0,r.jsx)(n.li,{children:"HTTP and database clients with circuit breaker protection"}),"\n"]}),"\n",(0,r.jsx)(n.p,{children:"Remember: Resilience patterns are about graceful degradation and maintaining system stability under failure!"})]})}function d(e={}){const{wrapper:n}={...(0,a.R)(),...e.components};return n?(0,r.jsx)(n,{...e,children:(0,r.jsx)(u,{...e})}):u(e)}}}]);