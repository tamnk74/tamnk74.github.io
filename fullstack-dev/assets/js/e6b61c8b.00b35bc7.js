"use strict";(self.webpackChunkdocs_site=self.webpackChunkdocs_site||[]).push([[21536],{5741:(e,n,t)=>{t.d(n,{R:()=>a,x:()=>c});var r=t(59729);const i={},s=r.createContext(i);function a(e){const n=r.useContext(s);return r.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function c(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:a(e.components),r.createElement(s.Provider,{value:n},e.children)}},68751:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>o,contentTitle:()=>c,default:()=>u,frontMatter:()=>a,metadata:()=>r,toc:()=>l});const r=JSON.parse('{"id":"backend/redis-search-guide","title":"Redis Search Guide - Full-Text Search & Aggregations","description":"\ud83d\udcd6 Overview","source":"@site/docs/backend/redis-search-guide.md","sourceDirName":"backend","slug":"/backend/redis-search-guide","permalink":"/fullstack-dev/docs/backend/redis-search-guide","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/redis-search-guide.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Redis Bloom Filters & Probabilistic Data Structures","permalink":"/fullstack-dev/docs/backend/redis-bloom-filters-guide"},"next":{"title":"Redis with Microsoft Entra ID Authentication","permalink":"/fullstack-dev/docs/backend/redis-entraid-guide"}}');var i=t(65813),s=t(5741);const a={},c="Redis Search Guide - Full-Text Search & Aggregations",o={},l=[{value:"\ud83d\udcd6 Overview",id:"-overview",level:2},{value:"\ud83c\udfaf Key Features",id:"-key-features",level:2},{value:"\ud83d\udcca RediSearch vs Traditional Search Engines",id:"-redisearch-vs-traditional-search-engines",level:2},{value:"\ud83d\ude80 Installation",id:"-installation",level:2},{value:"\ud83d\udca1 Real-World Use Cases",id:"-real-world-use-cases",level:2},{value:"Use Case 1: E-commerce Product Search",id:"use-case-1-e-commerce-product-search",level:3},{value:"Use Case 2: Blog Article Search with Vector Similarity",id:"use-case-2-blog-article-search-with-vector-similarity",level:3},{value:"Use Case 3: Geo-Location Search (Nearby Stores)",id:"use-case-3-geo-location-search-nearby-stores",level:3},{value:"Use Case 4: Analytics Aggregations",id:"use-case-4-analytics-aggregations",level:3},{value:"\ud83d\udcca Query Syntax Cheat Sheet",id:"-query-syntax-cheat-sheet",level:2},{value:"Full-Text Search",id:"full-text-search",level:3},{value:"Numeric Filters",id:"numeric-filters",level:3},{value:"Tag Filters",id:"tag-filters",level:3},{value:"Geo Filters",id:"geo-filters",level:3},{value:"\ud83c\udfaf Best Practices",id:"-best-practices",level:2},{value:"1. Index Design",id:"1-index-design",level:3},{value:"2. Query Optimization",id:"2-query-optimization",level:3},{value:"3. Pagination",id:"3-pagination",level:3},{value:"4. Vector Search Optimization",id:"4-vector-search-optimization",level:3},{value:"\ud83d\udcda Summary",id:"-summary",level:2}];function d(e){const n={br:"br",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",p:"p",pre:"pre",strong:"strong",table:"table",tbody:"tbody",td:"td",th:"th",thead:"thead",tr:"tr",ul:"ul",...(0,s.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.header,{children:(0,i.jsx)(n.h1,{id:"redis-search-guide---full-text-search--aggregations",children:"Redis Search Guide - Full-Text Search & Aggregations"})}),"\n",(0,i.jsx)(n.h2,{id:"-overview",children:"\ud83d\udcd6 Overview"}),"\n",(0,i.jsx)(n.p,{children:"RediSearch is a powerful full-text search and secondary indexing engine built on top of Redis. It provides advanced search capabilities including full-text search, aggregations, auto-completion, and geospatial queries - all with Redis-level performance."}),"\n",(0,i.jsx)(n.h2,{id:"-key-features",children:"\ud83c\udfaf Key Features"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Full-text search"})," with stemming, phonetic matching, and synonyms"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Vector similarity search"})," for AI/ML applications"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Aggregations"})," for analytics and reporting"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Auto-complete"})," with fuzzy matching"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Geospatial queries"})," with radius search"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Tag filtering"})," for faceted search"]}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.strong,{children:"Sorting and pagination"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.strong,{children:"Query highlighting"})}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"-redisearch-vs-traditional-search-engines",children:"\ud83d\udcca RediSearch vs Traditional Search Engines"}),"\n",(0,i.jsxs)(n.table,{children:[(0,i.jsx)(n.thead,{children:(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.th,{children:"Feature"}),(0,i.jsx)(n.th,{children:"RediSearch"}),(0,i.jsx)(n.th,{children:"Elasticsearch"}),(0,i.jsx)(n.th,{children:"PostgreSQL FTS"})]})}),(0,i.jsxs)(n.tbody,{children:[(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Setup"})}),(0,i.jsx)(n.td,{children:"Simple, bundled with Redis"}),(0,i.jsx)(n.td,{children:"Complex cluster"}),(0,i.jsx)(n.td,{children:"Built-in"})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Performance"})}),(0,i.jsx)(n.td,{children:"Microseconds"}),(0,i.jsx)(n.td,{children:"Milliseconds"}),(0,i.jsx)(n.td,{children:"Seconds"})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Memory"})}),(0,i.jsx)(n.td,{children:"In-memory"}),(0,i.jsx)(n.td,{children:"Disk + Cache"}),(0,i.jsx)(n.td,{children:"Disk"})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Scaling"})}),(0,i.jsx)(n.td,{children:"Redis Cluster"}),(0,i.jsx)(n.td,{children:"Sharding"}),(0,i.jsx)(n.td,{children:"Limited"})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Aggregations"})}),(0,i.jsx)(n.td,{children:"Yes"}),(0,i.jsx)(n.td,{children:"Yes (powerful)"}),(0,i.jsx)(n.td,{children:"Limited"})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"ML/Vector Search"})}),(0,i.jsx)(n.td,{children:"Yes"}),(0,i.jsx)(n.td,{children:"Yes"}),(0,i.jsx)(n.td,{children:"No"})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Best For"})}),(0,i.jsx)(n.td,{children:"Fast queries, caching"}),(0,i.jsx)(n.td,{children:"Complex analytics"}),(0,i.jsx)(n.td,{children:"Simple text search"})]})]})]}),"\n",(0,i.jsx)(n.h2,{id:"-installation",children:"\ud83d\ude80 Installation"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"npm install redis @redis/search\n"})}),"\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.strong,{children:"Note:"})," Requires Redis Stack or Redis with RediSearch module installed."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"# Docker - Redis Stack (includes RediSearch)\ndocker run -d -p 6379:6379 redis/redis-stack-server:latest\n"})}),"\n",(0,i.jsx)(n.h2,{id:"-real-world-use-cases",children:"\ud83d\udca1 Real-World Use Cases"}),"\n",(0,i.jsx)(n.h3,{id:"use-case-1-e-commerce-product-search",children:"Use Case 1: E-commerce Product Search"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// product-search.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { createClient, RedisClientType, SchemaFieldTypes, SearchOptions } from 'redis';\n\ninterface Product {\n  id: string;\n  name: string;\n  description: string;\n  category: string;\n  price: number;\n  brand: string;\n  tags: string[];\n  inStock: boolean;\n  rating: number;\n  reviewCount: number;\n  createdAt: number;\n}\n\ninterface SearchResult {\n  products: Product[];\n  total: number;\n  facets?: {\n    categories: Map<string, number>;\n    brands: Map<string, number>;\n    priceRanges: Map<string, number>;\n  };\n}\n\n@Injectable()\nexport class ProductSearchService implements OnModuleInit {\n  private readonly logger = new Logger('ProductSearchService');\n  private client: RedisClientType;\n  private readonly indexName = 'idx:products';\n\n  async onModuleInit() {\n    this.client = createClient({\n      url: process.env.REDIS_URL || 'redis://localhost:6379',\n    });\n\n    await this.client.connect();\n    await this.createIndex();\n  }\n\n  /**\n   * Create search index with field definitions\n   */\n  private async createIndex() {\n    try {\n      await this.client.ft.create(\n        this.indexName,\n        {\n          '$.name': {\n            type: SchemaFieldTypes.TEXT,\n            AS: 'name',\n            SORTABLE: true,\n          },\n          '$.description': {\n            type: SchemaFieldTypes.TEXT,\n            AS: 'description',\n          },\n          '$.category': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'category',\n            SEPARATOR: ',',\n          },\n          '$.brand': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'brand',\n          },\n          '$.price': {\n            type: SchemaFieldTypes.NUMERIC,\n            AS: 'price',\n            SORTABLE: true,\n          },\n          '$.tags': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'tags',\n            SEPARATOR: ',',\n          },\n          '$.inStock': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'inStock',\n          },\n          '$.rating': {\n            type: SchemaFieldTypes.NUMERIC,\n            AS: 'rating',\n            SORTABLE: true,\n          },\n          '$.reviewCount': {\n            type: SchemaFieldTypes.NUMERIC,\n            AS: 'reviewCount',\n            SORTABLE: true,\n          },\n          '$.createdAt': {\n            type: SchemaFieldTypes.NUMERIC,\n            AS: 'createdAt',\n            SORTABLE: true,\n          },\n        },\n        {\n          ON: 'JSON',\n          PREFIX: 'product:',\n        },\n      );\n\n      this.logger.log('Product search index created successfully');\n    } catch (error) {\n      if (error.message.includes('Index already exists')) {\n        this.logger.log('Product search index already exists');\n      } else {\n        throw error;\n      }\n    }\n  }\n\n  /**\n   * Index a product\n   */\n  async indexProduct(product: Product): Promise<void> {\n    const key = `product:${product.id}`;\n    await this.client.json.set(key, '$', product as any);\n    this.logger.log(`Product indexed: ${product.id}`);\n  }\n\n  /**\n   * Index multiple products in bulk\n   */\n  async indexProducts(products: Product[]): Promise<void> {\n    const pipeline = this.client.multi();\n\n    products.forEach((product) => {\n      pipeline.json.set(`product:${product.id}`, '$', product as any);\n    });\n\n    await pipeline.exec();\n    this.logger.log(`${products.length} products indexed`);\n  }\n\n  /**\n   * Full-text search with filters\n   */\n  async search(\n    query: string,\n    options: {\n      category?: string;\n      brands?: string[];\n      minPrice?: number;\n      maxPrice?: number;\n      inStock?: boolean;\n      minRating?: number;\n      tags?: string[];\n      sortBy?: 'price' | 'rating' | 'reviewCount' | 'createdAt';\n      sortOrder?: 'ASC' | 'DESC';\n      page?: number;\n      pageSize?: number;\n    } = {},\n  ): Promise<SearchResult> {\n    const {\n      category,\n      brands,\n      minPrice,\n      maxPrice,\n      inStock,\n      minRating,\n      tags,\n      sortBy = 'createdAt',\n      sortOrder = 'DESC',\n      page = 1,\n      pageSize = 20,\n    } = options;\n\n    // Build query\n    let searchQuery = query ? query : '*';\n\n    // Add filters\n    const filters: string[] = [];\n\n    if (category) {\n      filters.push(`@category:{${this.escapeTag(category)}}`);\n    }\n\n    if (brands && brands.length > 0) {\n      const brandFilter = brands.map((b) => this.escapeTag(b)).join('|');\n      filters.push(`@brand:{${brandFilter}}`);\n    }\n\n    if (minPrice !== undefined || maxPrice !== undefined) {\n      const min = minPrice ?? 0;\n      const max = maxPrice ?? '+inf';\n      filters.push(`@price:[${min} ${max}]`);\n    }\n\n    if (inStock !== undefined) {\n      filters.push(`@inStock:{${inStock}}`);\n    }\n\n    if (minRating !== undefined) {\n      filters.push(`@rating:[${minRating} +inf]`);\n    }\n\n    if (tags && tags.length > 0) {\n      const tagFilter = tags.map((t) => this.escapeTag(t)).join('|');\n      filters.push(`@tags:{${tagFilter}}`);\n    }\n\n    if (filters.length > 0) {\n      searchQuery = `${searchQuery} ${filters.join(' ')}`;\n    }\n\n    // Search options\n    const searchOptions: SearchOptions = {\n      LIMIT: {\n        from: (page - 1) * pageSize,\n        size: pageSize,\n      },\n      SORTBY: {\n        BY: sortBy,\n        DIRECTION: sortOrder,\n      },\n      RETURN: ['id', 'name', 'description', 'category', 'price', 'brand', 'tags', 'inStock', 'rating', 'reviewCount'],\n    };\n\n    const results = await this.client.ft.search(this.indexName, searchQuery, searchOptions);\n\n    const products = results.documents.map((doc) => ({\n      id: doc.value.id as string,\n      name: doc.value.name as string,\n      description: doc.value.description as string,\n      category: doc.value.category as string,\n      price: Number(doc.value.price),\n      brand: doc.value.brand as string,\n      tags: doc.value.tags ? (doc.value.tags as string).split(',') : [],\n      inStock: doc.value.inStock === 'true',\n      rating: Number(doc.value.rating),\n      reviewCount: Number(doc.value.reviewCount),\n      createdAt: Number(doc.value.createdAt || Date.now()),\n    }));\n\n    return {\n      products,\n      total: results.total,\n    };\n  }\n\n  /**\n   * Search with facets (for filters)\n   */\n  async searchWithFacets(query: string, options: any = {}): Promise<SearchResult> {\n    const results = await this.search(query, options);\n\n    // Get facets using aggregation\n    const facets = await this.getFacets(query, options);\n\n    return {\n      ...results,\n      facets,\n    };\n  }\n\n  /**\n   * Get facets for filters\n   */\n  private async getFacets(\n    query: string,\n    options: any = {},\n  ): Promise<{\n    categories: Map<string, number>;\n    brands: Map<string, number>;\n    priceRanges: Map<string, number>;\n  }> {\n    const searchQuery = query ? query : '*';\n\n    // Aggregate by category\n    const categoryAgg = await this.client.ft.aggregate(this.indexName, searchQuery, {\n      STEPS: [\n        {\n          type: 'GROUPBY',\n          properties: ['@category'],\n          REDUCE: [\n            {\n              type: 'COUNT',\n              AS: 'count',\n            },\n          ],\n        },\n        {\n          type: 'SORTBY',\n          BY: {\n            BY: '@count',\n            DIRECTION: 'DESC',\n          },\n        },\n      ],\n    });\n\n    // Aggregate by brand\n    const brandAgg = await this.client.ft.aggregate(this.indexName, searchQuery, {\n      STEPS: [\n        {\n          type: 'GROUPBY',\n          properties: ['@brand'],\n          REDUCE: [\n            {\n              type: 'COUNT',\n              AS: 'count',\n            },\n          ],\n        },\n        {\n          type: 'SORTBY',\n          BY: {\n            BY: '@count',\n            DIRECTION: 'DESC',\n          },\n        },\n      ],\n    });\n\n    // Aggregate by price ranges\n    const priceAgg = await this.client.ft.aggregate(this.indexName, searchQuery, {\n      STEPS: [\n        {\n          type: 'APPLY',\n          expression: 'floor(@price/50)*50',\n          AS: 'price_bucket',\n        },\n        {\n          type: 'GROUPBY',\n          properties: ['@price_bucket'],\n          REDUCE: [\n            {\n              type: 'COUNT',\n              AS: 'count',\n            },\n          ],\n        },\n        {\n          type: 'SORTBY',\n          BY: {\n            BY: '@price_bucket',\n            DIRECTION: 'ASC',\n          },\n        },\n      ],\n    });\n\n    return {\n      categories: new Map(categoryAgg.results.map((r) => [r.category as string, Number(r.count)])),\n      brands: new Map(brandAgg.results.map((r) => [r.brand as string, Number(r.count)])),\n      priceRanges: new Map(\n        priceAgg.results.map((r) => [`${r.price_bucket}-${Number(r.price_bucket) + 50}`, Number(r.count)]),\n      ),\n    };\n  }\n\n  /**\n   * Autocomplete suggestions\n   */\n  async autocomplete(prefix: string, limit = 10): Promise<string[]> {\n    const results = await this.client.ft.search(this.indexName, `${prefix}*`, {\n      LIMIT: { from: 0, size: limit },\n      RETURN: ['name'],\n    });\n\n    return results.documents.map((doc) => doc.value.name as string);\n  }\n\n  /**\n   * Get similar products (using tags)\n   */\n  async getSimilarProducts(productId: string, limit = 5): Promise<Product[]> {\n    // Get product\n    const product = await this.client.json.get(`product:${productId}`);\n    if (!product) return [];\n\n    const tags = (product as any).tags || [];\n    if (tags.length === 0) return [];\n\n    // Search for products with same tags\n    const tagFilter = tags.map((t: string) => this.escapeTag(t)).join('|');\n    const results = await this.search(`@tags:{${tagFilter}}`, {\n      pageSize: limit + 1,\n    });\n\n    // Filter out the current product\n    return results.products.filter((p) => p.id !== productId).slice(0, limit);\n  }\n\n  /**\n   * Get trending products (high rating and review count)\n   */\n  async getTrendingProducts(limit = 10): Promise<Product[]> {\n    const results = await this.client.ft.aggregate(this.indexName, '*', {\n      STEPS: [\n        {\n          type: 'APPLY',\n          expression: '@rating * log(@reviewCount + 1)',\n          AS: 'trending_score',\n        },\n        {\n          type: 'SORTBY',\n          BY: {\n            BY: '@trending_score',\n            DIRECTION: 'DESC',\n          },\n          MAX: limit,\n        },\n      ],\n      LOAD: ['id', 'name', 'price', 'rating', 'reviewCount'],\n    });\n\n    return results.results.map((r) => ({\n      id: r.id as string,\n      name: r.name as string,\n      price: Number(r.price),\n      rating: Number(r.rating),\n      reviewCount: Number(r.reviewCount),\n    })) as any;\n  }\n\n  /**\n   * Get products by price range with stats\n   */\n  async getProductsByPriceRange(\n    minPrice: number,\n    maxPrice: number,\n  ): Promise<{\n    products: Product[];\n    stats: {\n      count: number;\n      avgPrice: number;\n      minPrice: number;\n      maxPrice: number;\n    };\n  }> {\n    const results = await this.search('*', { minPrice, maxPrice });\n\n    const stats = await this.client.ft.aggregate(this.indexName, `@price:[${minPrice} ${maxPrice}]`, {\n      STEPS: [\n        {\n          type: 'GROUPBY',\n          properties: [],\n          REDUCE: [\n            { type: 'COUNT', AS: 'count' },\n            { type: 'AVG', property: '@price', AS: 'avg_price' },\n            { type: 'MIN', property: '@price', AS: 'min_price' },\n            { type: 'MAX', property: '@price', AS: 'max_price' },\n          ],\n        },\n      ],\n    });\n\n    const statsResult = stats.results[0] || {};\n\n    return {\n      products: results.products,\n      stats: {\n        count: Number(statsResult.count || 0),\n        avgPrice: Number(statsResult.avg_price || 0),\n        minPrice: Number(statsResult.min_price || 0),\n        maxPrice: Number(statsResult.max_price || 0),\n      },\n    };\n  }\n\n  /**\n   * Delete product from index\n   */\n  async deleteProduct(productId: string): Promise<void> {\n    await this.client.json.del(`product:${productId}`);\n    this.logger.log(`Product deleted from index: ${productId}`);\n  }\n\n  /**\n   * Get index statistics\n   */\n  async getIndexStats(): Promise<any> {\n    const info = await this.client.ft.info(this.indexName);\n    return {\n      numDocs: info.numDocs,\n      maxDocId: info.maxDocId,\n      numTerms: info.numTerms,\n      numRecords: info.numRecords,\n      indexing: info.indexing,\n      percentIndexed: info.percentIndexed,\n    };\n  }\n\n  /**\n   * Escape special characters in tags\n   */\n  private escapeTag(tag: string): string {\n    return tag.replace(/[,.<>{}[\\]\"':;!@#$%^&*()\\-+=~\\/ ]/g, '\\\\$&');\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"use-case-2-blog-article-search-with-vector-similarity",children:"Use Case 2: Blog Article Search with Vector Similarity"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// article-search.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { createClient, RedisClientType, SchemaFieldTypes } from 'redis';\nimport { OpenAI } from 'openai';\n\ninterface Article {\n  id: string;\n  title: string;\n  content: string;\n  author: string;\n  tags: string[];\n  publishedAt: number;\n  viewCount: number;\n  embedding?: number[];\n}\n\n@Injectable()\nexport class ArticleSearchService implements OnModuleInit {\n  private readonly logger = new Logger('ArticleSearchService');\n  private client: RedisClientType;\n  private openai: OpenAI;\n  private readonly indexName = 'idx:articles';\n\n  constructor() {\n    this.openai = new OpenAI({\n      apiKey: process.env.OPENAI_API_KEY,\n    });\n  }\n\n  async onModuleInit() {\n    this.client = createClient({\n      url: process.env.REDIS_URL || 'redis://localhost:6379',\n    });\n\n    await this.client.connect();\n    await this.createIndex();\n  }\n\n  /**\n   * Create index with vector field for semantic search\n   */\n  private async createIndex() {\n    try {\n      await this.client.ft.create(\n        this.indexName,\n        {\n          '$.title': {\n            type: SchemaFieldTypes.TEXT,\n            AS: 'title',\n            SORTABLE: true,\n          },\n          '$.content': {\n            type: SchemaFieldTypes.TEXT,\n            AS: 'content',\n          },\n          '$.author': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'author',\n          },\n          '$.tags': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'tags',\n            SEPARATOR: ',',\n          },\n          '$.publishedAt': {\n            type: SchemaFieldTypes.NUMERIC,\n            AS: 'publishedAt',\n            SORTABLE: true,\n          },\n          '$.viewCount': {\n            type: SchemaFieldTypes.NUMERIC,\n            AS: 'viewCount',\n            SORTABLE: true,\n          },\n          '$.embedding': {\n            type: SchemaFieldTypes.VECTOR,\n            AS: 'embedding',\n            ALGORITHM: 'HNSW',\n            TYPE: 'FLOAT32',\n            DIM: 1536, // OpenAI embedding dimension\n            DISTANCE_METRIC: 'COSINE',\n          },\n        },\n        {\n          ON: 'JSON',\n          PREFIX: 'article:',\n        },\n      );\n\n      this.logger.log('Article search index created with vector field');\n    } catch (error) {\n      if (error.message.includes('Index already exists')) {\n        this.logger.log('Article search index already exists');\n      } else {\n        throw error;\n      }\n    }\n  }\n\n  /**\n   * Generate embedding for text using OpenAI\n   */\n  private async generateEmbedding(text: string): Promise<number[]> {\n    const response = await this.openai.embeddings.create({\n      model: 'text-embedding-ada-002',\n      input: text,\n    });\n\n    return response.data[0].embedding;\n  }\n\n  /**\n   * Index article with embedding\n   */\n  async indexArticle(article: Omit<Article, 'embedding'>): Promise<void> {\n    // Generate embedding from title and content\n    const textForEmbedding = `${article.title}\\n\\n${article.content}`;\n    const embedding = await this.generateEmbedding(textForEmbedding);\n\n    const articleWithEmbedding: Article = {\n      ...article,\n      embedding,\n    };\n\n    await this.client.json.set(`article:${article.id}`, '$', articleWithEmbedding as any);\n\n    this.logger.log(`Article indexed with embedding: ${article.id}`);\n  }\n\n  /**\n   * Semantic search using vector similarity\n   */\n  async semanticSearch(query: string, limit = 10): Promise<Array<Article & { score: number }>> {\n    // Generate query embedding\n    const queryEmbedding = await this.generateEmbedding(query);\n\n    // Vector search\n    const results = await this.client.ft.search(this.indexName, `*=>[KNN ${limit} @embedding $query_vec AS score]`, {\n      PARAMS: {\n        query_vec: Buffer.from(new Float32Array(queryEmbedding).buffer),\n      },\n      SORTBY: {\n        BY: 'score',\n        DIRECTION: 'ASC', // Lower distance = more similar\n      },\n      RETURN: ['title', 'content', 'author', 'tags', 'publishedAt', 'score'],\n      DIALECT: 2,\n    });\n\n    return results.documents.map((doc) => ({\n      id: doc.id.replace('article:', ''),\n      title: doc.value.title as string,\n      content: doc.value.content as string,\n      author: doc.value.author as string,\n      tags: (doc.value.tags as string).split(','),\n      publishedAt: Number(doc.value.publishedAt),\n      viewCount: 0,\n      score: Number(doc.value.score),\n    }));\n  }\n\n  /**\n   * Hybrid search: combine full-text and semantic search\n   */\n  async hybridSearch(\n    query: string,\n    options: {\n      tags?: string[];\n      author?: string;\n      limit?: number;\n    } = {},\n  ): Promise<Array<Article & { score: number }>> {\n    const { tags, author, limit = 10 } = options;\n\n    // Build filters\n    const filters: string[] = [];\n    if (tags && tags.length > 0) {\n      const tagFilter = tags.map((t) => this.escapeTag(t)).join('|');\n      filters.push(`@tags:{${tagFilter}}`);\n    }\n    if (author) {\n      filters.push(`@author:{${this.escapeTag(author)}}`);\n    }\n\n    const filterQuery = filters.length > 0 ? `(${filters.join(' ')})` : '*';\n\n    // Generate query embedding\n    const queryEmbedding = await this.generateEmbedding(query);\n\n    // Hybrid query: filter + vector search\n    const results = await this.client.ft.search(\n      this.indexName,\n      `${filterQuery}=>[KNN ${limit} @embedding $query_vec AS vector_score]`,\n      {\n        PARAMS: {\n          query_vec: Buffer.from(new Float32Array(queryEmbedding).buffer),\n        },\n        SORTBY: {\n          BY: 'vector_score',\n          DIRECTION: 'ASC',\n        },\n        RETURN: ['title', 'content', 'author', 'tags', 'publishedAt', 'vector_score'],\n        DIALECT: 2,\n      },\n    );\n\n    return results.documents.map((doc) => ({\n      id: doc.id.replace('article:', ''),\n      title: doc.value.title as string,\n      content: doc.value.content as string,\n      author: doc.value.author as string,\n      tags: (doc.value.tags as string).split(','),\n      publishedAt: Number(doc.value.publishedAt),\n      viewCount: 0,\n      score: Number(doc.value.vector_score),\n    }));\n  }\n\n  /**\n   * Get related articles based on similarity\n   */\n  async getRelatedArticles(articleId: string, limit = 5): Promise<Array<Article & { score: number }>> {\n    // Get article embedding\n    const article = await this.client.json.get(`article:${articleId}`, {\n      path: '$.embedding',\n    });\n\n    if (!article || !(article as any)[0]) {\n      return [];\n    }\n\n    const embedding = (article as any)[0];\n\n    // Find similar articles\n    const results = await this.client.ft.search(\n      this.indexName,\n      `*=>[KNN ${limit + 1} @embedding $query_vec AS score]`,\n      {\n        PARAMS: {\n          query_vec: Buffer.from(new Float32Array(embedding).buffer),\n        },\n        SORTBY: {\n          BY: 'score',\n          DIRECTION: 'ASC',\n        },\n        RETURN: ['title', 'author', 'tags', 'publishedAt', 'score'],\n        DIALECT: 2,\n      },\n    );\n\n    // Filter out the source article\n    return results.documents\n      .filter((doc) => doc.id !== `article:${articleId}`)\n      .slice(0, limit)\n      .map((doc) => ({\n        id: doc.id.replace('article:', ''),\n        title: doc.value.title as string,\n        content: '',\n        author: doc.value.author as string,\n        tags: (doc.value.tags as string).split(','),\n        publishedAt: Number(doc.value.publishedAt),\n        viewCount: 0,\n        score: Number(doc.value.score),\n      }));\n  }\n\n  private escapeTag(tag: string): string {\n    return tag.replace(/[,.<>{}[\\]\"':;!@#$%^&*()\\-+=~\\/ ]/g, '\\\\$&');\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"use-case-3-geo-location-search-nearby-stores",children:"Use Case 3: Geo-Location Search (Nearby Stores)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// store-location.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { createClient, RedisClientType, SchemaFieldTypes } from 'redis';\n\ninterface Store {\n  id: string;\n  name: string;\n  address: string;\n  city: string;\n  state: string;\n  zipCode: string;\n  location: {\n    longitude: number;\n    latitude: number;\n  };\n  category: string;\n  rating: number;\n  isOpen: boolean;\n}\n\ninterface NearbyStore extends Store {\n  distance: number; // in kilometers\n}\n\n@Injectable()\nexport class StoreLocationService implements OnModuleInit {\n  private readonly logger = new Logger('StoreLocationService');\n  private client: RedisClientType;\n  private readonly indexName = 'idx:stores';\n\n  async onModuleInit() {\n    this.client = createClient({\n      url: process.env.REDIS_URL || 'redis://localhost:6379',\n    });\n\n    await this.client.connect();\n    await this.createIndex();\n  }\n\n  /**\n   * Create index with geo field\n   */\n  private async createIndex() {\n    try {\n      await this.client.ft.create(\n        this.indexName,\n        {\n          '$.name': {\n            type: SchemaFieldTypes.TEXT,\n            AS: 'name',\n          },\n          '$.address': {\n            type: SchemaFieldTypes.TEXT,\n            AS: 'address',\n          },\n          '$.city': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'city',\n          },\n          '$.state': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'state',\n          },\n          '$.zipCode': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'zipCode',\n          },\n          '$.location': {\n            type: SchemaFieldTypes.GEO,\n            AS: 'location',\n          },\n          '$.category': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'category',\n          },\n          '$.rating': {\n            type: SchemaFieldTypes.NUMERIC,\n            AS: 'rating',\n            SORTABLE: true,\n          },\n          '$.isOpen': {\n            type: SchemaFieldTypes.TAG,\n            AS: 'isOpen',\n          },\n        },\n        {\n          ON: 'JSON',\n          PREFIX: 'store:',\n        },\n      );\n\n      this.logger.log('Store location index created');\n    } catch (error) {\n      if (error.message.includes('Index already exists')) {\n        this.logger.log('Store location index already exists');\n      } else {\n        throw error;\n      }\n    }\n  }\n\n  /**\n   * Index a store\n   */\n  async indexStore(store: Store): Promise<void> {\n    // Convert location to GeoJSON format for Redis\n    const storeData = {\n      ...store,\n      location: `${store.location.longitude},${store.location.latitude}`,\n    };\n\n    await this.client.json.set(`store:${store.id}`, '$', storeData as any);\n    this.logger.log(`Store indexed: ${store.id}`);\n  }\n\n  /**\n   * Find nearby stores within radius\n   */\n  async findNearbyStores(\n    longitude: number,\n    latitude: number,\n    radiusKm: number,\n    options: {\n      category?: string;\n      minRating?: number;\n      openOnly?: boolean;\n      limit?: number;\n    } = {},\n  ): Promise<NearbyStore[]> {\n    const { category, minRating, openOnly, limit = 20 } = options;\n\n    // Build query\n    const filters: string[] = [`@location:[${longitude} ${latitude} ${radiusKm} km]`];\n\n    if (category) {\n      filters.push(`@category:{${this.escapeTag(category)}}`);\n    }\n\n    if (minRating !== undefined) {\n      filters.push(`@rating:[${minRating} +inf]`);\n    }\n\n    if (openOnly) {\n      filters.push('@isOpen:{true}');\n    }\n\n    const query = filters.join(' ');\n\n    const results = await this.client.ft.search(this.indexName, query, {\n      LIMIT: { from: 0, size: limit },\n      SORTBY: {\n        BY: 'location', // Sort by distance\n        DIRECTION: 'ASC',\n      },\n      RETURN: ['name', 'address', 'city', 'state', 'zipCode', 'location', 'category', 'rating', 'isOpen'],\n    });\n\n    return results.documents.map((doc) => {\n      const [lon, lat] = (doc.value.location as string).split(',');\n      const distance = this.calculateDistance(latitude, longitude, parseFloat(lat), parseFloat(lon));\n\n      return {\n        id: doc.id.replace('store:', ''),\n        name: doc.value.name as string,\n        address: doc.value.address as string,\n        city: doc.value.city as string,\n        state: doc.value.state as string,\n        zipCode: doc.value.zipCode as string,\n        location: {\n          longitude: parseFloat(lon),\n          latitude: parseFloat(lat),\n        },\n        category: doc.value.category as string,\n        rating: Number(doc.value.rating),\n        isOpen: doc.value.isOpen === 'true',\n        distance,\n      };\n    });\n  }\n\n  /**\n   * Get stores by city/state\n   */\n  async findStoresByLocation(\n    city?: string,\n    state?: string,\n    options: {\n      category?: string;\n      sortBy?: 'rating' | 'name';\n      limit?: number;\n    } = {},\n  ): Promise<Store[]> {\n    const { category, sortBy = 'rating', limit = 50 } = options;\n\n    const filters: string[] = [];\n\n    if (city) {\n      filters.push(`@city:{${this.escapeTag(city)}}`);\n    }\n\n    if (state) {\n      filters.push(`@state:{${this.escapeTag(state)}}`);\n    }\n\n    if (category) {\n      filters.push(`@category:{${this.escapeTag(category)}}`);\n    }\n\n    const query = filters.length > 0 ? filters.join(' ') : '*';\n\n    const results = await this.client.ft.search(this.indexName, query, {\n      LIMIT: { from: 0, size: limit },\n      SORTBY: {\n        BY: sortBy,\n        DIRECTION: 'DESC',\n      },\n    });\n\n    return results.documents.map((doc) => {\n      const [lon, lat] = (doc.value.location as string).split(',');\n      return {\n        id: doc.id.replace('store:', ''),\n        name: doc.value.name as string,\n        address: doc.value.address as string,\n        city: doc.value.city as string,\n        state: doc.value.state as string,\n        zipCode: doc.value.zipCode as string,\n        location: {\n          longitude: parseFloat(lon),\n          latitude: parseFloat(lat),\n        },\n        category: doc.value.category as string,\n        rating: Number(doc.value.rating),\n        isOpen: doc.value.isOpen === 'true',\n      };\n    });\n  }\n\n  /**\n   * Calculate distance between two coordinates (Haversine formula)\n   */\n  private calculateDistance(lat1: number, lon1: number, lat2: number, lon2: number): number {\n    const R = 6371; // Earth's radius in km\n    const dLat = this.deg2rad(lat2 - lat1);\n    const dLon = this.deg2rad(lon2 - lon1);\n    const a =\n      Math.sin(dLat / 2) * Math.sin(dLat / 2) +\n      Math.cos(this.deg2rad(lat1)) * Math.cos(this.deg2rad(lat2)) * Math.sin(dLon / 2) * Math.sin(dLon / 2);\n    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));\n    return R * c;\n  }\n\n  private deg2rad(deg: number): number {\n    return deg * (Math.PI / 180);\n  }\n\n  private escapeTag(tag: string): string {\n    return tag.replace(/[,.<>{}[\\]\"':;!@#$%^&*()\\-+=~\\/ ]/g, '\\\\$&');\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"use-case-4-analytics-aggregations",children:"Use Case 4: Analytics Aggregations"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// analytics-aggregation.service.ts\nimport { Injectable, Logger, OnModuleInit } from '@nestjs/common';\nimport { createClient, RedisClientType, SchemaFieldTypes } from 'redis';\n\ninterface OrderEvent {\n  id: string;\n  userId: string;\n  productId: string;\n  category: string;\n  amount: number;\n  quantity: number;\n  status: 'pending' | 'completed' | 'cancelled';\n  timestamp: number;\n  city: string;\n  country: string;\n}\n\ninterface SalesReport {\n  totalRevenue: number;\n  totalOrders: number;\n  avgOrderValue: number;\n  completedOrders: number;\n  cancelledOrders: number;\n}\n\n@Injectable()\nexport class AnalyticsAggregationService implements OnModuleInit {\n  private readonly logger = new Logger('AnalyticsAggregationService');\n  private client: RedisClientType;\n  private readonly indexName = 'idx:orders';\n\n  async onModuleInit() {\n    this.client = createClient({\n      url: process.env.REDIS_URL || 'redis://localhost:6379',\n    });\n\n    await this.client.connect();\n    await this.createIndex();\n  }\n\n  private async createIndex() {\n    try {\n      await this.client.ft.create(\n        this.indexName,\n        {\n          '$.userId': { type: SchemaFieldTypes.TAG, AS: 'userId' },\n          '$.productId': { type: SchemaFieldTypes.TAG, AS: 'productId' },\n          '$.category': { type: SchemaFieldTypes.TAG, AS: 'category' },\n          '$.amount': {\n            type: SchemaFieldTypes.NUMERIC,\n            AS: 'amount',\n            SORTABLE: true,\n          },\n          '$.quantity': { type: SchemaFieldTypes.NUMERIC, AS: 'quantity' },\n          '$.status': { type: SchemaFieldTypes.TAG, AS: 'status' },\n          '$.timestamp': {\n            type: SchemaFieldTypes.NUMERIC,\n            AS: 'timestamp',\n            SORTABLE: true,\n          },\n          '$.city': { type: SchemaFieldTypes.TAG, AS: 'city' },\n          '$.country': { type: SchemaFieldTypes.TAG, AS: 'country' },\n        },\n        { ON: 'JSON', PREFIX: 'order:' },\n      );\n      this.logger.log('Analytics index created');\n    } catch (error) {\n      if (!error.message.includes('Index already exists')) {\n        throw error;\n      }\n    }\n  }\n\n  /**\n   * Get sales report for date range\n   */\n  async getSalesReport(startTime: number, endTime: number): Promise<SalesReport> {\n    const result = await this.client.ft.aggregate(this.indexName, `@timestamp:[${startTime} ${endTime}]`, {\n      STEPS: [\n        {\n          type: 'GROUPBY',\n          properties: ['@status'],\n          REDUCE: [\n            { type: 'COUNT', AS: 'count' },\n            { type: 'SUM', property: '@amount', AS: 'total_amount' },\n            { type: 'AVG', property: '@amount', AS: 'avg_amount' },\n          ],\n        },\n      ],\n    });\n\n    let totalRevenue = 0;\n    let totalOrders = 0;\n    let completedOrders = 0;\n    let cancelledOrders = 0;\n\n    result.results.forEach((r) => {\n      const count = Number(r.count || 0);\n      const amount = Number(r.total_amount || 0);\n\n      totalOrders += count;\n      totalRevenue += amount;\n\n      if (r.status === 'completed') {\n        completedOrders = count;\n      } else if (r.status === 'cancelled') {\n        cancelledOrders = count;\n      }\n    });\n\n    return {\n      totalRevenue,\n      totalOrders,\n      avgOrderValue: totalOrders > 0 ? totalRevenue / totalOrders : 0,\n      completedOrders,\n      cancelledOrders,\n    };\n  }\n\n  /**\n   * Get sales by category\n   */\n  async getSalesByCategory(startTime: number, endTime: number) {\n    const result = await this.client.ft.aggregate(\n      this.indexName,\n      `@timestamp:[${startTime} ${endTime}] @status:{completed}`,\n      {\n        STEPS: [\n          {\n            type: 'GROUPBY',\n            properties: ['@category'],\n            REDUCE: [\n              { type: 'COUNT', AS: 'orders' },\n              { type: 'SUM', property: '@amount', AS: 'revenue' },\n              { type: 'AVG', property: '@amount', AS: 'avg_order' },\n            ],\n          },\n          {\n            type: 'SORTBY',\n            BY: { BY: '@revenue', DIRECTION: 'DESC' },\n          },\n        ],\n      },\n    );\n\n    return result.results.map((r) => ({\n      category: r.category as string,\n      orders: Number(r.orders),\n      revenue: Number(r.revenue),\n      avgOrder: Number(r.avg_order),\n    }));\n  }\n\n  /**\n   * Get top customers\n   */\n  async getTopCustomers(limit = 10) {\n    const result = await this.client.ft.aggregate(this.indexName, '@status:{completed}', {\n      STEPS: [\n        {\n          type: 'GROUPBY',\n          properties: ['@userId'],\n          REDUCE: [\n            { type: 'COUNT', AS: 'order_count' },\n            { type: 'SUM', property: '@amount', AS: 'total_spent' },\n            { type: 'AVG', property: '@amount', AS: 'avg_order' },\n          ],\n        },\n        {\n          type: 'SORTBY',\n          BY: { BY: '@total_spent', DIRECTION: 'DESC' },\n          MAX: limit,\n        },\n      ],\n    });\n\n    return result.results.map((r) => ({\n      userId: r.userId as string,\n      orderCount: Number(r.order_count),\n      totalSpent: Number(r.total_spent),\n      avgOrder: Number(r.avg_order),\n    }));\n  }\n\n  /**\n   * Get sales by location\n   */\n  async getSalesByLocation() {\n    const result = await this.client.ft.aggregate(this.indexName, '@status:{completed}', {\n      STEPS: [\n        {\n          type: 'GROUPBY',\n          properties: ['@country', '@city'],\n          REDUCE: [\n            { type: 'COUNT', AS: 'orders' },\n            { type: 'SUM', property: '@amount', AS: 'revenue' },\n          ],\n        },\n        {\n          type: 'SORTBY',\n          BY: { BY: '@revenue', DIRECTION: 'DESC' },\n        },\n      ],\n    });\n\n    return result.results.map((r) => ({\n      country: r.country as string,\n      city: r.city as string,\n      orders: Number(r.orders),\n      revenue: Number(r.revenue),\n    }));\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"-query-syntax-cheat-sheet",children:"\ud83d\udcca Query Syntax Cheat Sheet"}),"\n",(0,i.jsx)(n.h3,{id:"full-text-search",children:"Full-Text Search"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Exact phrase\nawait client.ft.search(index, '\"iPhone 15 Pro\"');\n\n// Multiple words (OR)\nawait client.ft.search(index, 'iPhone Samsung');\n\n// Multiple words (AND)\nawait client.ft.search(index, 'iPhone +wireless');\n\n// Negation\nawait client.ft.search(index, 'iPhone -refurbished');\n\n// Prefix matching\nawait client.ft.search(index, 'iph*');\n\n// Field-specific search\nawait client.ft.search(index, '@title:iPhone');\n"})}),"\n",(0,i.jsx)(n.h3,{id:"numeric-filters",children:"Numeric Filters"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Range\nawait client.ft.search(index, '@price:[100 500]');\n\n// Greater than\nawait client.ft.search(index, '@price:[(100 +inf]');\n\n// Less than\nawait client.ft.search(index, '@price:[-inf (500]');\n"})}),"\n",(0,i.jsx)(n.h3,{id:"tag-filters",children:"Tag Filters"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Single tag\nawait client.ft.search(index, '@category:{electronics}');\n\n// Multiple tags (OR)\nawait client.ft.search(index, '@category:{electronics|clothing}');\n\n// Multiple tags (AND)\nawait client.ft.search(index, '@category:{electronics} @brand:{apple}');\n"})}),"\n",(0,i.jsx)(n.h3,{id:"geo-filters",children:"Geo Filters"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Within radius\nawait client.ft.search(index, '@location:[-122.4194 37.7749 5 km]');\n"})}),"\n",(0,i.jsx)(n.h2,{id:"-best-practices",children:"\ud83c\udfaf Best Practices"}),"\n",(0,i.jsx)(n.h3,{id:"1-index-design",children:"1. Index Design"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// \u2705 Good: Selective indexing\nawait client.ft.create(index, {\n  '$.title': { type: SchemaFieldTypes.TEXT, AS: 'title' },\n  '$.price': { type: SchemaFieldTypes.NUMERIC, AS: 'price', SORTABLE: true },\n});\n\n// \u274c Bad: Indexing everything\nawait client.ft.create(index, {\n  '$.metadata': { type: SchemaFieldTypes.TEXT, AS: 'metadata' }, // Large field\n});\n"})}),"\n",(0,i.jsx)(n.h3,{id:"2-query-optimization",children:"2. Query Optimization"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// \u2705 Good: Specific filters first\nconst query = '@category:{electronics} @price:[100 500] iPhone';\n\n// \u274c Bad: Broad search first\nconst query = 'iPhone @category:{electronics} @price:[100 500]';\n"})}),"\n",(0,i.jsx)(n.h3,{id:"3-pagination",children:"3. Pagination"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// \u2705 Good: Use LIMIT\nconst results = await client.ft.search(index, query, {\n  LIMIT: { from: 0, size: 20 },\n});\n\n// \u274c Bad: Fetch all results\nconst results = await client.ft.search(index, query);\n"})}),"\n",(0,i.jsx)(n.h3,{id:"4-vector-search-optimization",children:"4. Vector Search Optimization"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// \u2705 Good: Use HNSW algorithm for large datasets\nawait client.ft.create(index, {\n  '$.embedding': {\n    type: SchemaFieldTypes.VECTOR,\n    ALGORITHM: 'HNSW', // Hierarchical Navigable Small World\n    TYPE: 'FLOAT32',\n    DIM: 1536,\n    DISTANCE_METRIC: 'COSINE',\n    M: 40, // Number of outgoing edges\n    EF_CONSTRUCTION: 200, // Construction time/quality trade-off\n  },\n});\n"})}),"\n",(0,i.jsx)(n.h2,{id:"-summary",children:"\ud83d\udcda Summary"}),"\n",(0,i.jsx)(n.p,{children:"RediSearch is perfect for:"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"E-commerce search"})," - Products, filters, facets",(0,i.jsx)(n.br,{}),"\n","\u2705 ",(0,i.jsx)(n.strong,{children:"Content search"})," - Articles, documents, blogs",(0,i.jsx)(n.br,{}),"\n","\u2705 ",(0,i.jsx)(n.strong,{children:"Location-based search"})," - Stores, restaurants, services",(0,i.jsx)(n.br,{}),"\n","\u2705 ",(0,i.jsx)(n.strong,{children:"Analytics"})," - Real-time aggregations and reports",(0,i.jsx)(n.br,{}),"\n","\u2705 ",(0,i.jsx)(n.strong,{children:"AI/ML applications"})," - Vector similarity search",(0,i.jsx)(n.br,{}),"\n","\u2705 ",(0,i.jsx)(n.strong,{children:"Autocomplete"})," - Fast suggestions"]}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Advantages:"})}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Sub-millisecond query performance"}),"\n",(0,i.jsx)(n.li,{children:"Real-time indexing"}),"\n",(0,i.jsx)(n.li,{children:"Powerful aggregations"}),"\n",(0,i.jsx)(n.li,{children:"Vector search support"}),"\n",(0,i.jsx)(n.li,{children:"Simple setup (Redis Stack)"}),"\n"]}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"When to use Elasticsearch instead:"})}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:"Complex aggregation pipelines"}),"\n",(0,i.jsx)(n.li,{children:"Large-scale log analysis"}),"\n",(0,i.jsx)(n.li,{children:"Advanced text analysis (NLP)"}),"\n",(0,i.jsx)(n.li,{children:"Distributed multi-node clusters"}),"\n",(0,i.jsx)(n.li,{children:"Heavy analytical workloads"}),"\n"]}),"\n",(0,i.jsxs)(n.p,{children:["Choose RediSearch for ",(0,i.jsx)(n.strong,{children:"speed-critical applications"})," where you need ",(0,i.jsx)(n.strong,{children:"real-time search"})," with ",(0,i.jsx)(n.strong,{children:"minimal latency"}),"!"]})]})}function u(e={}){const{wrapper:n}={...(0,s.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);