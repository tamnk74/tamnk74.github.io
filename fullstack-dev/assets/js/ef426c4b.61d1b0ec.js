"use strict";(globalThis.webpackChunkdocs_site=globalThis.webpackChunkdocs_site||[]).push([[11535],{28453(e,n,r){r.d(n,{R:()=>a,x:()=>o});var t=r(96540);const i={},s=t.createContext(i);function a(e){const n=t.useContext(s);return t.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:a(e.components),t.createElement(s.Provider,{value:n},e.children)}},47499(e,n,r){r.r(n),r.d(n,{assets:()=>c,contentTitle:()=>o,default:()=>d,frontMatter:()=>a,metadata:()=>t,toc:()=>l});const t=JSON.parse('{"id":"design-patterns/dependency-injection","title":"Dependency Injection","description":"Dependency Injection (DI) is a design pattern used to implement Inversion of Control (IoC), allowing the creation of dependent objects outside of a class and providing those objects to a class in different ways.","source":"@site/docs/design-patterns/dependency-injection.md","sourceDirName":"design-patterns","slug":"/design-patterns/dependency-injection","permalink":"/fullstack-dev/docs/design-patterns/dependency-injection","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/design-patterns/dependency-injection.md","tags":[],"version":"current","frontMatter":{}}');var i=r(74848),s=r(28453);const a={},o="Dependency Injection",c={},l=[{value:"Problem",id:"problem",level:2},{value:"Solution",id:"solution",level:2},{value:"Structure",id:"structure",level:2},{value:"How to Implement",id:"how-to-implement",level:2},{value:"1. Constructor Injection (Recommended)",id:"1-constructor-injection-recommended",level:3},{value:"2. Setter Injection",id:"2-setter-injection",level:3},{value:"3. Property Injection",id:"3-property-injection",level:3},{value:"4. Using DI Container",id:"4-using-di-container",level:3},{value:"TypeScript Example",id:"typescript-example",level:2},{value:"Complete Example with Multiple Patterns",id:"complete-example-with-multiple-patterns",level:3},{value:"Example with Decorators (TypeScript)",id:"example-with-decorators-typescript",level:3},{value:"Pros and Cons",id:"pros-and-cons",level:2},{value:"Real-World Use Cases",id:"real-world-use-cases",level:2},{value:"1. Web Application Services",id:"1-web-application-services",level:3},{value:"2. Testing with Mock Dependencies",id:"2-testing-with-mock-dependencies",level:3},{value:"3. Multi-Tenant Applications",id:"3-multi-tenant-applications",level:3},{value:"4. Plugin Architecture",id:"4-plugin-architecture",level:3},{value:"5. Environment-Specific Configuration",id:"5-environment-specific-configuration",level:3},{value:"6. Microservices Communication",id:"6-microservices-communication",level:3},{value:"7. Event-Driven Architecture",id:"7-event-driven-architecture",level:3},{value:"8. Caching Strategies",id:"8-caching-strategies",level:3},{value:"9. Feature Toggles",id:"9-feature-toggles",level:3},{value:"10. Monitoring and Observability",id:"10-monitoring-and-observability",level:3},{value:"Summary",id:"summary",level:2}];function g(e){const n={code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",hr:"hr",li:"li",ol:"ol",p:"p",pre:"pre",strong:"strong",table:"table",tbody:"tbody",td:"td",th:"th",thead:"thead",tr:"tr",ul:"ul",...(0,s.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.header,{children:(0,i.jsx)(n.h1,{id:"dependency-injection",children:"Dependency Injection"})}),"\n",(0,i.jsx)(n.p,{children:"Dependency Injection (DI) is a design pattern used to implement Inversion of Control (IoC), allowing the creation of dependent objects outside of a class and providing those objects to a class in different ways."}),"\n",(0,i.jsx)(n.h2,{id:"problem",children:"Problem"}),"\n",(0,i.jsx)(n.p,{children:"When classes create their own dependencies directly:"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Tight Coupling"}),": Classes are tightly coupled to specific implementations, making it difficult to change dependencies without modifying the class itself."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Testing Difficulties"}),": Hard to unit test classes because you cannot easily replace dependencies with mock objects or test doubles."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Code Reusability"}),": Classes become less reusable as they're bound to specific implementations of their dependencies."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Violation of SOLID Principles"}),": Particularly violates the Dependency Inversion Principle and Single Responsibility Principle."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Hidden Dependencies"}),": Dependencies are hidden inside the class, making it unclear what the class needs to function."]}),"\n"]}),"\n",(0,i.jsx)(n.p,{children:"Example of problematic code:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"class UserService {\n  private database: MySQLDatabase;\n  private logger: FileLogger;\n\n  constructor() {\n    this.database = new MySQLDatabase(); // Hard-coded dependency\n    this.logger = new FileLogger(); // Hard-coded dependency\n  }\n\n  getUser(id: string) {\n    this.logger.log('Fetching user');\n    return this.database.query(`SELECT * FROM users WHERE id = ${id}`);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"solution",children:"Solution"}),"\n",(0,i.jsx)(n.p,{children:"Dependency Injection solves these problems by:"}),"\n",(0,i.jsxs)(n.ol,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Externalizing Dependencies"}),": Dependencies are created outside the class and injected through constructor, setter methods, or interfaces."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Programming to Interfaces"}),": Classes depend on abstractions (interfaces) rather than concrete implementations."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Inversion of Control"}),": The control of creating and managing dependencies is inverted from the class to an external entity (IoC container or manual injection)."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Flexibility"}),": Easy to swap implementations without changing the class code."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Testability"}),": Dependencies can be easily mocked or stubbed for testing."]}),"\n"]}),"\n",(0,i.jsx)(n.p,{children:"There are three main types of Dependency Injection:"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Constructor Injection"}),": Dependencies are provided through the class constructor."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Setter Injection"}),": Dependencies are provided through setter methods."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Interface Injection"}),": The dependency provides an injector method that will inject the dependency into any client passed to it."]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"structure",children:"Structure"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-mermaid",children:"classDiagram\n    class Client {\n        -service: IService\n        +Client(service: IService)\n        +doSomething()\n    }\n    class IService {\n        <<interface>>\n        +execute()\n    }\n    class ConcreteServiceA {\n        +execute()\n    }\n    class ConcreteServiceB {\n        +execute()\n    }\n    class Injector {\n        +injectDependencies()\n    }\n\n    Client --\x3e IService\n    IService <|.. ConcreteServiceA\n    IService <|.. ConcreteServiceB\n    Injector ..> Client\n    Injector ..> ConcreteServiceA\n    Injector ..> ConcreteServiceB\n"})}),"\n",(0,i.jsx)(n.h2,{id:"how-to-implement",children:"How to Implement"}),"\n",(0,i.jsx)(n.h3,{id:"1-constructor-injection-recommended",children:"1. Constructor Injection (Recommended)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Step 1: Define interface\ninterface IDatabase {\n  query(sql: string): Promise<any>;\n}\n\n// Step 2: Create implementations\nclass MySQLDatabase implements IDatabase {\n  async query(sql: string): Promise<any> {\n    // MySQL specific implementation\n    return [];\n  }\n}\n\nclass PostgreSQLDatabase implements IDatabase {\n  async query(sql: string): Promise<any> {\n    // PostgreSQL specific implementation\n    return [];\n  }\n}\n\n// Step 3: Inject through constructor\nclass UserService {\n  constructor(private database: IDatabase) {}\n\n  async getUser(id: string) {\n    return this.database.query(`SELECT * FROM users WHERE id = ${id}`);\n  }\n}\n\n// Step 4: Use it\nconst mysqlDb = new MySQLDatabase();\nconst userService = new UserService(mysqlDb); // Inject dependency\n"})}),"\n",(0,i.jsx)(n.h3,{id:"2-setter-injection",children:"2. Setter Injection"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"class UserService {\n  private database?: IDatabase;\n\n  setDatabase(database: IDatabase) {\n    this.database = database;\n  }\n\n  async getUser(id: string) {\n    if (!this.database) {\n      throw new Error('Database not set');\n    }\n    return this.database.query(`SELECT * FROM users WHERE id = ${id}`);\n  }\n}\n\n// Usage\nconst userService = new UserService();\nuserService.setDatabase(new MySQLDatabase());\n"})}),"\n",(0,i.jsx)(n.h3,{id:"3-property-injection",children:"3. Property Injection"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"class UserService {\n  @Inject('IDatabase')\n  private database!: IDatabase;\n\n  async getUser(id: string) {\n    return this.database.query(`SELECT * FROM users WHERE id = ${id}`);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"4-using-di-container",children:"4. Using DI Container"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Simple DI Container\nclass Container {\n  private services: Map<string, any> = new Map();\n\n  register(name: string, service: any) {\n    this.services.set(name, service);\n  }\n\n  resolve<T>(name: string): T {\n    const service = this.services.get(name);\n    if (!service) {\n      throw new Error(`Service ${name} not found`);\n    }\n    return service;\n  }\n}\n\n// Setup\nconst container = new Container();\ncontainer.register('database', new MySQLDatabase());\ncontainer.register('userService', new UserService(container.resolve('database')));\n\n// Usage\nconst userService = container.resolve<UserService>('userService');\n"})}),"\n",(0,i.jsx)(n.h2,{id:"typescript-example",children:"TypeScript Example"}),"\n",(0,i.jsx)(n.h3,{id:"complete-example-with-multiple-patterns",children:"Complete Example with Multiple Patterns"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// ============= Interfaces =============\ninterface ILogger {\n  log(message: string): void;\n  error(message: string): void;\n}\n\ninterface IDatabase {\n  connect(): Promise<void>;\n  query<T>(sql: string, params?: any[]): Promise<T[]>;\n  disconnect(): Promise<void>;\n}\n\ninterface IEmailService {\n  sendEmail(to: string, subject: string, body: string): Promise<void>;\n}\n\ninterface IUserRepository {\n  findById(id: string): Promise<User | null>;\n  findByEmail(email: string): Promise<User | null>;\n  create(user: Omit<User, 'id'>): Promise<User>;\n  update(id: string, user: Partial<User>): Promise<User>;\n  delete(id: string): Promise<void>;\n}\n\n// ============= Models =============\ninterface User {\n  id: string;\n  email: string;\n  name: string;\n  createdAt: Date;\n}\n\n// ============= Implementations =============\nclass ConsoleLogger implements ILogger {\n  log(message: string): void {\n    console.log(`[LOG] ${new Date().toISOString()}: ${message}`);\n  }\n\n  error(message: string): void {\n    console.error(`[ERROR] ${new Date().toISOString()}: ${message}`);\n  }\n}\n\nclass FileLogger implements ILogger {\n  constructor(private filePath: string) {}\n\n  log(message: string): void {\n    // Write to file\n    console.log(`Writing to ${this.filePath}: ${message}`);\n  }\n\n  error(message: string): void {\n    // Write error to file\n    console.error(`Writing error to ${this.filePath}: ${message}`);\n  }\n}\n\nclass PostgreSQLDatabase implements IDatabase {\n  private connected: boolean = false;\n\n  constructor(private host: string, private port: number, private database: string, private logger: ILogger) {}\n\n  async connect(): Promise<void> {\n    this.logger.log(`Connecting to PostgreSQL at ${this.host}:${this.port}`);\n    this.connected = true;\n  }\n\n  async query<T>(sql: string, params?: any[]): Promise<T[]> {\n    if (!this.connected) {\n      throw new Error('Database not connected');\n    }\n    this.logger.log(`Executing query: ${sql}`);\n    // Simulate query execution\n    return [] as T[];\n  }\n\n  async disconnect(): Promise<void> {\n    this.logger.log('Disconnecting from database');\n    this.connected = false;\n  }\n}\n\nclass SMTPEmailService implements IEmailService {\n  constructor(private host: string, private port: number, private logger: ILogger) {}\n\n  async sendEmail(to: string, subject: string, body: string): Promise<void> {\n    this.logger.log(`Sending email to ${to}: ${subject}`);\n    // Simulate sending email via SMTP\n    console.log(`Email sent to ${to}`);\n  }\n}\n\nclass UserRepository implements IUserRepository {\n  constructor(private database: IDatabase, private logger: ILogger) {}\n\n  async findById(id: string): Promise<User | null> {\n    this.logger.log(`Finding user by ID: ${id}`);\n    const users = await this.database.query<User>('SELECT * FROM users WHERE id = $1', [id]);\n    return users[0] || null;\n  }\n\n  async findByEmail(email: string): Promise<User | null> {\n    this.logger.log(`Finding user by email: ${email}`);\n    const users = await this.database.query<User>('SELECT * FROM users WHERE email = $1', [email]);\n    return users[0] || null;\n  }\n\n  async create(user: Omit<User, 'id'>): Promise<User> {\n    this.logger.log(`Creating user: ${user.email}`);\n    const result = await this.database.query<User>(\n      'INSERT INTO users (email, name, created_at) VALUES ($1, $2, $3) RETURNING *',\n      [user.email, user.name, new Date()],\n    );\n    return result[0];\n  }\n\n  async update(id: string, user: Partial<User>): Promise<User> {\n    this.logger.log(`Updating user: ${id}`);\n    // Implementation details...\n    return {} as User;\n  }\n\n  async delete(id: string): Promise<void> {\n    this.logger.log(`Deleting user: ${id}`);\n    await this.database.query('DELETE FROM users WHERE id = $1', [id]);\n  }\n}\n\n// ============= Service Layer =============\nclass UserService {\n  constructor(private userRepository: IUserRepository, private emailService: IEmailService, private logger: ILogger) {}\n\n  async registerUser(email: string, name: string): Promise<User> {\n    this.logger.log(`Registering new user: ${email}`);\n\n    // Check if user exists\n    const existingUser = await this.userRepository.findByEmail(email);\n    if (existingUser) {\n      throw new Error('User already exists');\n    }\n\n    // Create user\n    const user = await this.userRepository.create({\n      email,\n      name,\n      createdAt: new Date(),\n    });\n\n    // Send welcome email\n    await this.emailService.sendEmail(email, 'Welcome!', `Hello ${name}, welcome to our platform!`);\n\n    this.logger.log(`User registered successfully: ${user.id}`);\n    return user;\n  }\n\n  async getUserById(id: string): Promise<User> {\n    const user = await this.userRepository.findById(id);\n    if (!user) {\n      throw new Error('User not found');\n    }\n    return user;\n  }\n\n  async deleteUser(id: string): Promise<void> {\n    const user = await this.userRepository.findById(id);\n    if (!user) {\n      throw new Error('User not found');\n    }\n\n    await this.userRepository.delete(id);\n\n    // Send goodbye email\n    await this.emailService.sendEmail(\n      user.email,\n      'Account Deleted',\n      `Goodbye ${user.name}, your account has been deleted.`,\n    );\n\n    this.logger.log(`User deleted: ${id}`);\n  }\n}\n\n// ============= DI Container =============\nclass DIContainer {\n  private services: Map<string, any> = new Map();\n  private singletons: Map<string, any> = new Map();\n\n  register(name: string, factory: (container: DIContainer) => any, singleton: boolean = false) {\n    this.services.set(name, { factory, singleton });\n  }\n\n  resolve<T>(name: string): T {\n    const service = this.services.get(name);\n    if (!service) {\n      throw new Error(`Service ${name} not registered`);\n    }\n\n    if (service.singleton) {\n      if (!this.singletons.has(name)) {\n        this.singletons.set(name, service.factory(this));\n      }\n      return this.singletons.get(name);\n    }\n\n    return service.factory(this);\n  }\n}\n\n// ============= Application Setup =============\nfunction setupContainer(): DIContainer {\n  const container = new DIContainer();\n\n  // Register logger (singleton)\n  container.register('logger', () => new ConsoleLogger(), true);\n\n  // Register database (singleton)\n  container.register(\n    'database',\n    (c) => new PostgreSQLDatabase('localhost', 5432, 'myapp', c.resolve<ILogger>('logger')),\n    true,\n  );\n\n  // Register email service (singleton)\n  container.register(\n    'emailService',\n    (c) => new SMTPEmailService('smtp.example.com', 587, c.resolve<ILogger>('logger')),\n    true,\n  );\n\n  // Register repository\n  container.register(\n    'userRepository',\n    (c) => new UserRepository(c.resolve<IDatabase>('database'), c.resolve<ILogger>('logger')),\n  );\n\n  // Register service\n  container.register(\n    'userService',\n    (c) =>\n      new UserService(\n        c.resolve<IUserRepository>('userRepository'),\n        c.resolve<IEmailService>('emailService'),\n        c.resolve<ILogger>('logger'),\n      ),\n  );\n\n  return container;\n}\n\n// ============= Usage =============\nasync function main() {\n  const container = setupContainer();\n\n  // Get database and connect\n  const database = container.resolve<IDatabase>('database');\n  await database.connect();\n\n  // Get user service\n  const userService = container.resolve<UserService>('userService');\n\n  // Register a new user\n  try {\n    const user = await userService.registerUser('john@example.com', 'John Doe');\n    console.log('User created:', user);\n\n    // Get user by ID\n    const fetchedUser = await userService.getUserById(user.id);\n    console.log('User fetched:', fetchedUser);\n\n    // Delete user\n    await userService.deleteUser(user.id);\n    console.log('User deleted');\n  } catch (error) {\n    console.error('Error:', error);\n  } finally {\n    await database.disconnect();\n  }\n}\n\n// Run the application\nmain().catch(console.error);\n"})}),"\n",(0,i.jsx)(n.h3,{id:"example-with-decorators-typescript",children:"Example with Decorators (TypeScript)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Decorator-based DI (similar to Angular/NestJS)\nfunction Injectable() {\n  return function (target: any) {\n    // Mark class as injectable\n    Reflect.defineMetadata('injectable', true, target);\n  };\n}\n\nfunction Inject(token: string) {\n  return function (target: any, propertyKey: string, parameterIndex: number) {\n    const existingInjections = Reflect.getMetadata('injections', target) || [];\n    existingInjections.push({ index: parameterIndex, token });\n    Reflect.defineMetadata('injections', existingInjections, target);\n  };\n}\n\n@Injectable()\nclass LoggerService {\n  log(message: string) {\n    console.log(message);\n  }\n}\n\n@Injectable()\nclass UserService {\n  constructor(@Inject('LoggerService') private logger: LoggerService) {}\n\n  getUser(id: string) {\n    this.logger.log(`Getting user ${id}`);\n    return { id, name: 'John' };\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"pros-and-cons",children:"Pros and Cons"}),"\n",(0,i.jsxs)(n.table,{children:[(0,i.jsx)(n.thead,{children:(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.th,{style:{textAlign:"left"},children:"Pros"}),(0,i.jsx)(n.th,{style:{textAlign:"left"},children:"Cons"})]})}),(0,i.jsxs)(n.tbody,{children:[(0,i.jsxs)(n.tr,{children:[(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2713 ",(0,i.jsx)(n.strong,{children:"Loose Coupling"}),": Classes are decoupled from their dependencies, making the system more flexible."]}),(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2717 ",(0,i.jsx)(n.strong,{children:"Complexity"}),": Adds additional complexity to the codebase, especially for small projects."]})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2713 ",(0,i.jsx)(n.strong,{children:"Testability"}),": Easy to unit test by injecting mock dependencies."]}),(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2717 ",(0,i.jsx)(n.strong,{children:"Learning Curve"}),": Developers need to understand DI concepts and frameworks."]})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2713 ",(0,i.jsx)(n.strong,{children:"Maintainability"}),": Changes to dependencies don't require changes to dependent classes."]}),(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2717 ",(0,i.jsx)(n.strong,{children:"Runtime Errors"}),": Some errors that could be caught at compile time may only appear at runtime."]})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2713 ",(0,i.jsx)(n.strong,{children:"Reusability"}),": Classes can be reused with different implementations of dependencies."]}),(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2717 ",(0,i.jsx)(n.strong,{children:"Configuration Overhead"}),": Requires configuration and setup of the DI container."]})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2713 ",(0,i.jsx)(n.strong,{children:"Single Responsibility"}),": Classes focus on their core responsibility, not on creating dependencies."]}),(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2717 ",(0,i.jsx)(n.strong,{children:"Debugging Difficulty"}),": Can be harder to trace the flow of dependencies in complex applications."]})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2713 ",(0,i.jsx)(n.strong,{children:"Open/Closed Principle"}),": Open for extension but closed for modification."]}),(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2717 ",(0,i.jsx)(n.strong,{children:"Performance Overhead"}),": DI containers may introduce slight performance overhead."]})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2713 ",(0,i.jsx)(n.strong,{children:"Dependency Inversion Principle"}),": High-level modules don't depend on low-level modules."]}),(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2717 ",(0,i.jsx)(n.strong,{children:"Over-engineering"}),": Can be overkill for simple applications with few dependencies."]})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2713 ",(0,i.jsx)(n.strong,{children:"Configuration Flexibility"}),": Easy to configure different implementations for different environments."]}),(0,i.jsxs)(n.td,{style:{textAlign:"left"},children:["\u2717 ",(0,i.jsx)(n.strong,{children:"Magic Behavior"}),': Framework-based DI can feel like "magic" and hide what\'s actually happening.']})]})]})]}),"\n",(0,i.jsx)(n.h2,{id:"real-world-use-cases",children:"Real-World Use Cases"}),"\n",(0,i.jsx)(n.h3,{id:"1-web-application-services",children:"1. Web Application Services"}),"\n",(0,i.jsx)(n.p,{children:"Injecting database connections, authentication services, and loggers into HTTP handlers."}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"JavaScript/TypeScript Implementation:"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Express.js with DI\nimport express from 'express';\n\ninterface IAuthService {\n  validateToken(token: string): Promise<boolean>;\n  getUserFromToken(token: string): Promise<User>;\n}\n\nclass JWTAuthService implements IAuthService {\n  constructor(private secret: string, private logger: ILogger) {}\n\n  async validateToken(token: string): Promise<boolean> {\n    this.logger.log('Validating JWT token');\n    // JWT validation logic\n    return true;\n  }\n\n  async getUserFromToken(token: string): Promise<User> {\n    // Extract user from token\n    return { id: '1', email: 'user@example.com', name: 'User' };\n  }\n}\n\nclass UserController {\n  constructor(private userService: UserService, private authService: IAuthService, private logger: ILogger) {}\n\n  async getUser(req: express.Request, res: express.Response) {\n    try {\n      const token = req.headers.authorization?.split(' ')[1];\n      if (!token || !(await this.authService.validateToken(token))) {\n        return res.status(401).json({ error: 'Unauthorized' });\n      }\n\n      const userId = req.params.id;\n      const user = await this.userService.getUserById(userId);\n      res.json(user);\n    } catch (error) {\n      this.logger.error(`Error fetching user: ${error}`);\n      res.status(500).json({ error: 'Internal server error' });\n    }\n  }\n\n  async createUser(req: express.Request, res: express.Response) {\n    try {\n      const { email, name } = req.body;\n      const user = await this.userService.registerUser(email, name);\n      res.status(201).json(user);\n    } catch (error) {\n      this.logger.error(`Error creating user: ${error}`);\n      res.status(500).json({ error: 'Internal server error' });\n    }\n  }\n}\n\n// Setup\nconst container = setupContainer();\nconst userController = new UserController(\n  container.resolve('userService'),\n  container.resolve('authService'),\n  container.resolve('logger'),\n);\n\nconst app = express();\napp.get('/users/:id', (req, res) => userController.getUser(req, res));\napp.post('/users', (req, res) => userController.createUser(req, res));\n"})}),"\n",(0,i.jsx)(n.h3,{id:"2-testing-with-mock-dependencies",children:"2. Testing with Mock Dependencies"}),"\n",(0,i.jsx)(n.p,{children:"Injecting mock dependencies for unit testing."}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"TypeScript Implementation:"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Mock implementations\nclass MockDatabase implements IDatabase {\n  private data: Map<string, any> = new Map();\n\n  async connect(): Promise<void> {\n    // No-op\n  }\n\n  async query<T>(sql: string, params?: any[]): Promise<T[]> {\n    // Return mock data\n    return Array.from(this.data.values()) as T[];\n  }\n\n  async disconnect(): Promise<void> {\n    // No-op\n  }\n\n  // Test helper\n  setMockData(key: string, value: any) {\n    this.data.set(key, value);\n  }\n}\n\nclass MockEmailService implements IEmailService {\n  public sentEmails: Array<{ to: string; subject: string; body: string }> = [];\n\n  async sendEmail(to: string, subject: string, body: string): Promise<void> {\n    this.sentEmails.push({ to, subject, body });\n  }\n}\n\nclass MockLogger implements ILogger {\n  public logs: string[] = [];\n  public errors: string[] = [];\n\n  log(message: string): void {\n    this.logs.push(message);\n  }\n\n  error(message: string): void {\n    this.errors.push(message);\n  }\n}\n\n// Test\ndescribe('UserService', () => {\n  let userService: UserService;\n  let mockDatabase: MockDatabase;\n  let mockEmailService: MockEmailService;\n  let mockLogger: MockLogger;\n\n  beforeEach(() => {\n    mockDatabase = new MockDatabase();\n    mockEmailService = new MockEmailService();\n    mockLogger = new MockLogger();\n\n    const mockRepository = new UserRepository(mockDatabase, mockLogger);\n    userService = new UserService(mockRepository, mockEmailService, mockLogger);\n  });\n\n  it('should register a new user', async () => {\n    const user = await userService.registerUser('test@example.com', 'Test User');\n\n    expect(user.email).toBe('test@example.com');\n    expect(mockEmailService.sentEmails).toHaveLength(1);\n    expect(mockEmailService.sentEmails[0].to).toBe('test@example.com');\n    expect(mockLogger.logs).toContain('Registering new user: test@example.com');\n  });\n\n  it('should throw error for duplicate email', async () => {\n    mockDatabase.setMockData('user1', {\n      id: '1',\n      email: 'test@example.com',\n      name: 'Existing User',\n    });\n\n    await expect(userService.registerUser('test@example.com', 'Test User')).rejects.toThrow('User already exists');\n  });\n});\n"})}),"\n",(0,i.jsx)(n.h3,{id:"3-multi-tenant-applications",children:"3. Multi-Tenant Applications"}),"\n",(0,i.jsx)(n.p,{children:"Injecting tenant-specific configurations and services."}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"TypeScript Implementation:"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"interface ITenantContext {\n  getTenantId(): string;\n  getDatabaseConnection(): IDatabase;\n  getConfiguration(): TenantConfig;\n}\n\ninterface TenantConfig {\n  features: string[];\n  limits: {\n    maxUsers: number;\n    maxStorage: number;\n  };\n  branding: {\n    logo: string;\n    primaryColor: string;\n  };\n}\n\nclass TenantContext implements ITenantContext {\n  constructor(private tenantId: string, private database: IDatabase, private config: TenantConfig) {}\n\n  getTenantId(): string {\n    return this.tenantId;\n  }\n\n  getDatabaseConnection(): IDatabase {\n    return this.database;\n  }\n\n  getConfiguration(): TenantConfig {\n    return this.config;\n  }\n}\n\nclass TenantAwareUserService {\n  constructor(private tenantContext: ITenantContext, private baseUserService: UserService, private logger: ILogger) {}\n\n  async registerUser(email: string, name: string): Promise<User> {\n    const config = this.tenantContext.getConfiguration();\n    const tenantId = this.tenantContext.getTenantId();\n\n    // Check tenant-specific limits\n    const existingUsers = await this.baseUserService.getUserCount();\n    if (existingUsers >= config.limits.maxUsers) {\n      throw new Error('Tenant user limit reached');\n    }\n\n    this.logger.log(`Registering user for tenant: ${tenantId}`);\n    return this.baseUserService.registerUser(email, name);\n  }\n}\n\n// Middleware to inject tenant context\nfunction tenantMiddleware(req: express.Request, res: express.Response, next: express.NextFunction) {\n  const tenantId = req.headers['x-tenant-id'] as string;\n\n  if (!tenantId) {\n    return res.status(400).json({ error: 'Tenant ID required' });\n  }\n\n  // Get tenant-specific database and config\n  const tenantDb = getTenantDatabase(tenantId);\n  const tenantConfig = getTenantConfig(tenantId);\n\n  // Create tenant context\n  req.tenantContext = new TenantContext(tenantId, tenantDb, tenantConfig);\n  next();\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"4-plugin-architecture",children:"4. Plugin Architecture"}),"\n",(0,i.jsx)(n.p,{children:"Injecting plugins dynamically based on configuration."}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"TypeScript Implementation:"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"interface IPlugin {\n  name: string;\n  initialize(): Promise<void>;\n  execute(context: any): Promise<any>;\n  shutdown(): Promise<void>;\n}\n\nclass AnalyticsPlugin implements IPlugin {\n  name = 'analytics';\n\n  constructor(private apiKey: string, private logger: ILogger) {}\n\n  async initialize(): Promise<void> {\n    this.logger.log('Analytics plugin initialized');\n  }\n\n  async execute(context: any): Promise<any> {\n    // Track event\n    this.logger.log(`Tracking event: ${context.event}`);\n  }\n\n  async shutdown(): Promise<void> {\n    this.logger.log('Analytics plugin shutdown');\n  }\n}\n\nclass CachePlugin implements IPlugin {\n  name = 'cache';\n  private cache: Map<string, any> = new Map();\n\n  constructor(private ttl: number, private logger: ILogger) {}\n\n  async initialize(): Promise<void> {\n    this.logger.log('Cache plugin initialized');\n  }\n\n  async execute(context: any): Promise<any> {\n    if (context.action === 'get') {\n      return this.cache.get(context.key);\n    } else if (context.action === 'set') {\n      this.cache.set(context.key, context.value);\n    }\n  }\n\n  async shutdown(): Promise<void> {\n    this.cache.clear();\n    this.logger.log('Cache plugin shutdown');\n  }\n}\n\nclass PluginManager {\n  private plugins: Map<string, IPlugin> = new Map();\n\n  constructor(private logger: ILogger) {}\n\n  async registerPlugin(plugin: IPlugin): Promise<void> {\n    await plugin.initialize();\n    this.plugins.set(plugin.name, plugin);\n    this.logger.log(`Plugin registered: ${plugin.name}`);\n  }\n\n  async executePlugin(name: string, context: any): Promise<any> {\n    const plugin = this.plugins.get(name);\n    if (!plugin) {\n      throw new Error(`Plugin not found: ${name}`);\n    }\n    return plugin.execute(context);\n  }\n\n  async shutdown(): Promise<void> {\n    for (const plugin of this.plugins.values()) {\n      await plugin.shutdown();\n    }\n    this.plugins.clear();\n  }\n}\n\n// Application with plugins\nclass Application {\n  constructor(private pluginManager: PluginManager, private logger: ILogger) {}\n\n  async start(): Promise<void> {\n    this.logger.log('Application starting...');\n\n    // Plugins are injected based on configuration\n    const config = loadConfiguration();\n\n    if (config.analytics.enabled) {\n      await this.pluginManager.registerPlugin(new AnalyticsPlugin(config.analytics.apiKey, this.logger));\n    }\n\n    if (config.cache.enabled) {\n      await this.pluginManager.registerPlugin(new CachePlugin(config.cache.ttl, this.logger));\n    }\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"5-environment-specific-configuration",children:"5. Environment-Specific Configuration"}),"\n",(0,i.jsx)(n.p,{children:"Injecting different services based on environment (development, staging, production)."}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"TypeScript Implementation:"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"type Environment = 'development' | 'staging' | 'production';\n\ninterface IConfigService {\n  get<T>(key: string): T;\n  getAll(): Record<string, any>;\n}\n\nclass DevelopmentConfigService implements IConfigService {\n  private config = {\n    apiUrl: 'http://localhost:3000',\n    databaseUrl: 'postgresql://localhost:5432/dev',\n    logLevel: 'debug',\n    enableDebugTools: true,\n  };\n\n  get<T>(key: string): T {\n    return this.config[key] as T;\n  }\n\n  getAll(): Record<string, any> {\n    return this.config;\n  }\n}\n\nclass ProductionConfigService implements IConfigService {\n  private config = {\n    apiUrl: 'https://api.production.com',\n    databaseUrl: process.env.DATABASE_URL,\n    logLevel: 'error',\n    enableDebugTools: false,\n  };\n\n  get<T>(key: string): T {\n    return this.config[key] as T;\n  }\n\n  getAll(): Record<string, any> {\n    return this.config;\n  }\n}\n\nfunction createConfigService(env: Environment): IConfigService {\n  switch (env) {\n    case 'development':\n      return new DevelopmentConfigService();\n    case 'production':\n      return new ProductionConfigService();\n    default:\n      return new DevelopmentConfigService();\n  }\n}\n\n// Container setup based on environment\nfunction setupEnvironmentContainer(env: Environment): DIContainer {\n  const container = new DIContainer();\n\n  // Environment-specific config\n  container.register('config', () => createConfigService(env), true);\n\n  // Environment-specific logger\n  if (env === 'development') {\n    container.register('logger', () => new ConsoleLogger(), true);\n  } else {\n    container.register(\n      'logger',\n      (c) => {\n        const config = c.resolve<IConfigService>('config');\n        return new FileLogger(config.get<string>('logFile'));\n      },\n      true,\n    );\n  }\n\n  // Database with environment config\n  container.register(\n    'database',\n    (c) => {\n      const config = c.resolve<IConfigService>('config');\n      return new PostgreSQLDatabase(config.get<string>('databaseUrl'), 5432, 'myapp', c.resolve<ILogger>('logger'));\n    },\n    true,\n  );\n\n  return container;\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"6-microservices-communication",children:"6. Microservices Communication"}),"\n",(0,i.jsx)(n.p,{children:"Injecting service clients for inter-service communication."}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"TypeScript Implementation:"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"interface IServiceClient {\n  call<T>(method: string, params: any): Promise<T>;\n}\n\nclass HTTPServiceClient implements IServiceClient {\n  constructor(private baseUrl: string, private logger: ILogger) {}\n\n  async call<T>(method: string, params: any): Promise<T> {\n    this.logger.log(`Calling ${method} on ${this.baseUrl}`);\n    // HTTP request implementation\n    const response = await fetch(`${this.baseUrl}/${method}`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify(params),\n    });\n    return response.json();\n  }\n}\n\nclass GRPCServiceClient implements IServiceClient {\n  constructor(private serviceUrl: string, private logger: ILogger) {}\n\n  async call<T>(method: string, params: any): Promise<T> {\n    this.logger.log(`gRPC call to ${method} on ${this.serviceUrl}`);\n    // gRPC implementation\n    return {} as T;\n  }\n}\n\ninterface IOrderService {\n  createOrder(order: any): Promise<any>;\n  getOrder(orderId: string): Promise<any>;\n}\n\ninterface IPaymentService {\n  processPayment(payment: any): Promise<any>;\n  refundPayment(paymentId: string): Promise<any>;\n}\n\nclass OrderServiceClient implements IOrderService {\n  constructor(private client: IServiceClient) {}\n\n  async createOrder(order: any): Promise<any> {\n    return this.client.call('createOrder', order);\n  }\n\n  async getOrder(orderId: string): Promise<any> {\n    return this.client.call('getOrder', { orderId });\n  }\n}\n\nclass PaymentServiceClient implements IPaymentService {\n  constructor(private client: IServiceClient) {}\n\n  async processPayment(payment: any): Promise<any> {\n    return this.client.call('processPayment', payment);\n  }\n\n  async refundPayment(paymentId: string): Promise<any> {\n    return this.client.call('refundPayment', { paymentId });\n  }\n}\n\n// Checkout service using injected service clients\nclass CheckoutService {\n  constructor(private orderService: IOrderService, private paymentService: IPaymentService, private logger: ILogger) {}\n\n  async checkout(cart: any, paymentDetails: any): Promise<any> {\n    this.logger.log('Starting checkout process');\n\n    try {\n      // Create order\n      const order = await this.orderService.createOrder(cart);\n      this.logger.log(`Order created: ${order.id}`);\n\n      // Process payment\n      const payment = await this.paymentService.processPayment({\n        orderId: order.id,\n        amount: order.total,\n        ...paymentDetails,\n      });\n      this.logger.log(`Payment processed: ${payment.id}`);\n\n      return { order, payment };\n    } catch (error) {\n      this.logger.error(`Checkout failed: ${error}`);\n      throw error;\n    }\n  }\n}\n\n// Setup microservices container\nfunction setupMicroservicesContainer(): DIContainer {\n  const container = new DIContainer();\n\n  container.register('logger', () => new ConsoleLogger(), true);\n\n  // Register service clients\n  container.register(\n    'orderServiceClient',\n    (c) => new HTTPServiceClient('http://order-service:3001', c.resolve<ILogger>('logger')),\n    true,\n  );\n\n  container.register(\n    'paymentServiceClient',\n    (c) => new GRPCServiceClient('payment-service:50051', c.resolve<ILogger>('logger')),\n    true,\n  );\n\n  // Register service interfaces\n  container.register('orderService', (c) => new OrderServiceClient(c.resolve('orderServiceClient')));\n\n  container.register('paymentService', (c) => new PaymentServiceClient(c.resolve('paymentServiceClient')));\n\n  // Register checkout service\n  container.register(\n    'checkoutService',\n    (c) =>\n      new CheckoutService(\n        c.resolve<IOrderService>('orderService'),\n        c.resolve<IPaymentService>('paymentService'),\n        c.resolve<ILogger>('logger'),\n      ),\n  );\n\n  return container;\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"7-event-driven-architecture",children:"7. Event-Driven Architecture"}),"\n",(0,i.jsx)(n.p,{children:"Injecting event emitters and handlers."}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"TypeScript Implementation:"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"interface IEventEmitter {\n  emit(event: string, data: any): void;\n  on(event: string, handler: (data: any) => void): void;\n  off(event: string, handler: (data: any) => void): void;\n}\n\nclass EventEmitter implements IEventEmitter {\n  private events: Map<string, Array<(data: any) => void>> = new Map();\n\n  emit(event: string, data: any): void {\n    const handlers = this.events.get(event) || [];\n    handlers.forEach((handler) => handler(data));\n  }\n\n  on(event: string, handler: (data: any) => void): void {\n    if (!this.events.has(event)) {\n      this.events.set(event, []);\n    }\n    this.events.get(event)!.push(handler);\n  }\n\n  off(event: string, handler: (data: any) => void): void {\n    const handlers = this.events.get(event) || [];\n    const index = handlers.indexOf(handler);\n    if (index > -1) {\n      handlers.splice(index, 1);\n    }\n  }\n}\n\nclass UserRegisteredHandler {\n  constructor(private emailService: IEmailService, private logger: ILogger) {}\n\n  async handle(data: any): Promise<void> {\n    this.logger.log(`Handling UserRegistered event for ${data.email}`);\n    await this.emailService.sendEmail(data.email, 'Welcome!', `Welcome ${data.name}!`);\n  }\n}\n\nclass UserActivityLogger {\n  constructor(private logger: ILogger) {}\n\n  handle(data: any): void {\n    this.logger.log(`User activity: ${data.action} by ${data.userId}`);\n  }\n}\n\nclass EventDrivenUserService {\n  constructor(private userRepository: IUserRepository, private eventEmitter: IEventEmitter, private logger: ILogger) {}\n\n  async registerUser(email: string, name: string): Promise<User> {\n    const user = await this.userRepository.create({ email, name, createdAt: new Date() });\n\n    // Emit event instead of directly sending email\n    this.eventEmitter.emit('user.registered', {\n      userId: user.id,\n      email: user.email,\n      name: user.name,\n    });\n\n    return user;\n  }\n}\n\n// Setup event handlers\nfunction setupEventDrivenContainer(): DIContainer {\n  const container = new DIContainer();\n\n  container.register('logger', () => new ConsoleLogger(), true);\n  container.register('eventEmitter', () => new EventEmitter(), true);\n\n  container.register(\n    'emailService',\n    (c) => new SMTPEmailService('smtp.example.com', 587, c.resolve<ILogger>('logger')),\n    true,\n  );\n\n  // Register event handlers\n  container.register(\n    'userRegisteredHandler',\n    (c) => new UserRegisteredHandler(c.resolve<IEmailService>('emailService'), c.resolve<ILogger>('logger')),\n  );\n\n  container.register('userActivityLogger', (c) => new UserActivityLogger(c.resolve<ILogger>('logger')));\n\n  // Wire up event handlers\n  const eventEmitter = container.resolve<IEventEmitter>('eventEmitter');\n  const userRegisteredHandler = container.resolve<UserRegisteredHandler>('userRegisteredHandler');\n  const userActivityLogger = container.resolve<UserActivityLogger>('userActivityLogger');\n\n  eventEmitter.on('user.registered', (data) => userRegisteredHandler.handle(data));\n  eventEmitter.on('user.registered', (data) => userActivityLogger.handle(data));\n\n  return container;\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"8-caching-strategies",children:"8. Caching Strategies"}),"\n",(0,i.jsx)(n.p,{children:"Injecting different caching implementations."}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"TypeScript Implementation:"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"interface ICache {\n  get<T>(key: string): Promise<T | null>;\n  set(key: string, value: any, ttl?: number): Promise<void>;\n  delete(key: string): Promise<void>;\n  clear(): Promise<void>;\n}\n\nclass MemoryCache implements ICache {\n  private cache: Map<string, { value: any; expiry: number }> = new Map();\n\n  async get<T>(key: string): Promise<T | null> {\n    const item = this.cache.get(key);\n    if (!item) return null;\n\n    if (Date.now() > item.expiry) {\n      this.cache.delete(key);\n      return null;\n    }\n\n    return item.value as T;\n  }\n\n  async set(key: string, value: any, ttl: number = 3600000): Promise<void> {\n    this.cache.set(key, {\n      value,\n      expiry: Date.now() + ttl,\n    });\n  }\n\n  async delete(key: string): Promise<void> {\n    this.cache.delete(key);\n  }\n\n  async clear(): Promise<void> {\n    this.cache.clear();\n  }\n}\n\nclass RedisCache implements ICache {\n  constructor(\n    private client: any, // Redis client\n    private logger: ILogger,\n  ) {}\n\n  async get<T>(key: string): Promise<T | null> {\n    this.logger.log(`Getting from Redis: ${key}`);\n    // const value = await this.client.get(key);\n    // return value ? JSON.parse(value) : null;\n    return null;\n  }\n\n  async set(key: string, value: any, ttl: number = 3600): Promise<void> {\n    this.logger.log(`Setting in Redis: ${key}`);\n    // await this.client.setex(key, ttl, JSON.stringify(value));\n  }\n\n  async delete(key: string): Promise<void> {\n    // await this.client.del(key);\n  }\n\n  async clear(): Promise<void> {\n    // await this.client.flushdb();\n  }\n}\n\nclass CachedUserService {\n  constructor(private userService: UserService, private cache: ICache, private logger: ILogger) {}\n\n  async getUserById(id: string): Promise<User> {\n    const cacheKey = `user:${id}`;\n\n    // Try cache first\n    const cached = await this.cache.get<User>(cacheKey);\n    if (cached) {\n      this.logger.log(`Cache hit for user ${id}`);\n      return cached;\n    }\n\n    // Cache miss - fetch from service\n    this.logger.log(`Cache miss for user ${id}`);\n    const user = await this.userService.getUserById(id);\n\n    // Store in cache\n    await this.cache.set(cacheKey, user, 300000); // 5 minutes\n\n    return user;\n  }\n}\n\n// Setup with different cache strategies\nfunction setupCacheContainer(cacheType: 'memory' | 'redis'): DIContainer {\n  const container = new DIContainer();\n\n  container.register('logger', () => new ConsoleLogger(), true);\n\n  // Choose cache implementation\n  if (cacheType === 'redis') {\n    container.register(\n      'cache',\n      (c) =>\n        new RedisCache(\n          {}, // Redis client\n          c.resolve<ILogger>('logger'),\n        ),\n      true,\n    );\n  } else {\n    container.register('cache', () => new MemoryCache(), true);\n  }\n\n  return container;\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"9-feature-toggles",children:"9. Feature Toggles"}),"\n",(0,i.jsx)(n.p,{children:"Injecting feature toggle service to enable/disable features."}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"TypeScript Implementation:"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"interface IFeatureToggle {\n  isEnabled(feature: string): boolean;\n  isEnabledForUser(feature: string, userId: string): boolean;\n}\n\nclass StaticFeatureToggle implements IFeatureToggle {\n  constructor(private features: Record<string, boolean>) {}\n\n  isEnabled(feature: string): boolean {\n    return this.features[feature] || false;\n  }\n\n  isEnabledForUser(feature: string, userId: string): boolean {\n    return this.isEnabled(feature);\n  }\n}\n\nclass DynamicFeatureToggle implements IFeatureToggle {\n  constructor(\n    private featureService: any, // External feature flag service\n    private logger: ILogger,\n  ) {}\n\n  isEnabled(feature: string): boolean {\n    // Fetch from remote service\n    this.logger.log(`Checking feature: ${feature}`);\n    return false;\n  }\n\n  isEnabledForUser(feature: string, userId: string): boolean {\n    // Check user-specific feature flags\n    this.logger.log(`Checking feature ${feature} for user ${userId}`);\n    return false;\n  }\n}\n\nclass FeatureAwareUserService {\n  constructor(private userService: UserService, private featureToggle: IFeatureToggle, private logger: ILogger) {}\n\n  async registerUser(email: string, name: string): Promise<User> {\n    // Check if new registration flow is enabled\n    if (this.featureToggle.isEnabled('enhanced-registration')) {\n      this.logger.log('Using enhanced registration flow');\n      // Enhanced registration logic\n    } else {\n      this.logger.log('Using standard registration flow');\n    }\n\n    const user = await this.userService.registerUser(email, name);\n\n    // Check if welcome email feature is enabled\n    if (this.featureToggle.isEnabled('welcome-email')) {\n      this.logger.log('Welcome email feature enabled');\n      // Send welcome email\n    }\n\n    return user;\n  }\n\n  async getUserDashboard(userId: string): Promise<any> {\n    const baseData = {\n      user: await this.userService.getUserById(userId),\n    };\n\n    // Conditionally add features based on toggles\n    if (this.featureToggle.isEnabledForUser('beta-dashboard', userId)) {\n      return {\n        ...baseData,\n        betaFeatures: ['analytics', 'recommendations'],\n      };\n    }\n\n    return baseData;\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"10-monitoring-and-observability",children:"10. Monitoring and Observability"}),"\n",(0,i.jsx)(n.p,{children:"Injecting telemetry, metrics, and tracing services."}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"TypeScript Implementation:"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"interface IMetrics {\n  increment(metric: string, tags?: Record<string, string>): void;\n  gauge(metric: string, value: number, tags?: Record<string, string>): void;\n  timing(metric: string, duration: number, tags?: Record<string, string>): void;\n}\n\ninterface ITracing {\n  startSpan(name: string): Span;\n}\n\ninterface Span {\n  setTag(key: string, value: any): void;\n  finish(): void;\n}\n\nclass DatadogMetrics implements IMetrics {\n  constructor(private apiKey: string) {}\n\n  increment(metric: string, tags?: Record<string, string>): void {\n    console.log(`[Datadog] Incrementing ${metric}`, tags);\n    // Send to Datadog\n  }\n\n  gauge(metric: string, value: number, tags?: Record<string, string>): void {\n    console.log(`[Datadog] Gauge ${metric}: ${value}`, tags);\n  }\n\n  timing(metric: string, duration: number, tags?: Record<string, string>): void {\n    console.log(`[Datadog] Timing ${metric}: ${duration}ms`, tags);\n  }\n}\n\nclass JaegerTracing implements ITracing {\n  startSpan(name: string): Span {\n    console.log(`[Jaeger] Starting span: ${name}`);\n    return {\n      setTag: (key, value) => console.log(`[Jaeger] Tag: ${key}=${value}`),\n      finish: () => console.log(`[Jaeger] Span finished`),\n    };\n  }\n}\n\nclass ObservableUserService {\n  constructor(\n    private userService: UserService,\n    private metrics: IMetrics,\n    private tracing: ITracing,\n    private logger: ILogger,\n  ) {}\n\n  async registerUser(email: string, name: string): Promise<User> {\n    const span = this.tracing.startSpan('registerUser');\n    span.setTag('email', email);\n\n    const startTime = Date.now();\n\n    try {\n      this.metrics.increment('user.registration.attempt');\n\n      const user = await this.userService.registerUser(email, name);\n\n      this.metrics.increment('user.registration.success');\n      this.metrics.timing('user.registration.duration', Date.now() - startTime);\n\n      span.setTag('userId', user.id);\n      span.finish();\n\n      return user;\n    } catch (error) {\n      this.metrics.increment('user.registration.failure', {\n        error: error.message,\n      });\n\n      span.setTag('error', true);\n      span.setTag('error.message', error.message);\n      span.finish();\n\n      throw error;\n    }\n  }\n\n  async getUserById(id: string): Promise<User> {\n    const span = this.tracing.startSpan('getUserById');\n    span.setTag('userId', id);\n\n    try {\n      this.metrics.increment('user.fetch.attempt');\n      const user = await this.userService.getUserById(id);\n      this.metrics.increment('user.fetch.success');\n      span.finish();\n      return user;\n    } catch (error) {\n      this.metrics.increment('user.fetch.failure');\n      span.setTag('error', true);\n      span.finish();\n      throw error;\n    }\n  }\n}\n\n// Setup observability\nfunction setupObservabilityContainer(): DIContainer {\n  const container = new DIContainer();\n\n  container.register('logger', () => new ConsoleLogger(), true);\n  container.register('metrics', () => new DatadogMetrics('api-key'), true);\n  container.register('tracing', () => new JaegerTracing(), true);\n\n  container.register(\n    'observableUserService',\n    (c) =>\n      new ObservableUserService(\n        c.resolve<UserService>('userService'),\n        c.resolve<IMetrics>('metrics'),\n        c.resolve<ITracing>('tracing'),\n        c.resolve<ILogger>('logger'),\n      ),\n  );\n\n  return container;\n}\n"})}),"\n",(0,i.jsx)(n.hr,{}),"\n",(0,i.jsx)(n.h2,{id:"summary",children:"Summary"}),"\n",(0,i.jsx)(n.p,{children:"Dependency Injection is a powerful design pattern that promotes loose coupling, testability, and maintainability in your applications. By injecting dependencies rather than hard-coding them, you create more flexible and modular code that's easier to test, maintain, and extend."}),"\n",(0,i.jsx)(n.p,{children:"Key takeaways:"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["Use ",(0,i.jsx)(n.strong,{children:"constructor injection"})," as the primary method for required dependencies"]}),"\n",(0,i.jsxs)(n.li,{children:["Depend on ",(0,i.jsx)(n.strong,{children:"interfaces/abstractions"})," rather than concrete implementations"]}),"\n",(0,i.jsxs)(n.li,{children:["Use ",(0,i.jsx)(n.strong,{children:"DI containers"})," for complex applications with many dependencies"]}),"\n",(0,i.jsx)(n.li,{children:"Apply DI consistently across your application architecture"}),"\n",(0,i.jsx)(n.li,{children:"Remember that DI adds complexity, so evaluate if it's appropriate for your project size"}),"\n"]})]})}function d(e={}){const{wrapper:n}={...(0,s.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(g,{...e})}):g(e)}}}]);