"use strict";(self.webpackChunkdocs_site=self.webpackChunkdocs_site||[]).push([[61984],{5741:(e,n,t)=>{t.d(n,{R:()=>r,x:()=>c});var a=t(59729);const s={},i=a.createContext(s);function r(e){const n=a.useContext(i);return a.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function c(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:r(e.components),a.createElement(i.Provider,{value:n},e.children)}},72045:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>o,contentTitle:()=>c,default:()=>d,frontMatter:()=>r,metadata:()=>a,toc:()=>m});const a=JSON.parse('{"id":"architecture-practices/architecture-execution/database-schema-version-control","title":"Database Schema Version Control & Conflict Detection","description":"This guide establishes database schema versioning practices for NestJS applications on GCP, ensuring database schemas are tracked as code, versioned alongside application code, and conflicts are detected during code merges.","source":"@site/docs/architecture-practices/architecture-execution/database-schema-version-control.md","sourceDirName":"architecture-practices/architecture-execution","slug":"/architecture-practices/architecture-execution/database-schema-version-control","permalink":"/fullstack-dev/docs/architecture-practices/architecture-execution/database-schema-version-control","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/architecture-practices/architecture-execution/database-schema-version-control.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Automated Database Migration Deployment","permalink":"/fullstack-dev/docs/architecture-practices/architecture-execution/automated-database-migrations"},"next":{"title":"Performance Testing Guide","permalink":"/fullstack-dev/docs/architecture-practices/architecture-execution/performance-testing"}}');var s=t(65813),i=t(5741);const r={},c="Database Schema Version Control & Conflict Detection",o={},m=[{value:"1. Overview",id:"1-overview",level:2},{value:"2. Schema as Code Principles",id:"2-schema-as-code-principles",level:2},{value:"2.1 Core Principles",id:"21-core-principles",level:3},{value:"2.2 Schema File Formats",id:"22-schema-file-formats",level:3},{value:"2.3 Recommended Repo Layout (Tracked Artifacts)",id:"23-recommended-repo-layout-tracked-artifacts",level:3},{value:"2.4 Exporting the Schema Snapshot (SQL/YAML)",id:"24-exporting-the-schema-snapshot-sqlyaml",level:2},{value:"2.5 GitHub Actions Gate: \u201cSnapshot Must Match\u201d",id:"25-github-actions-gate-snapshot-must-match",level:2},{value:"3. Prisma Schema Version Control",id:"3-prisma-schema-version-control",level:2},{value:"3.1 Prisma Schema File",id:"31-prisma-schema-file",level:3},{value:"3.2 Schema Metadata Tracking",id:"32-schema-metadata-tracking",level:3},{value:"4. TypeORM Schema Version Control",id:"4-typeorm-schema-version-control",level:2},{value:"4.1 Entity Definitions",id:"41-entity-definitions",level:3},{value:"4.2 Schema Export Service",id:"42-schema-export-service",level:3},{value:"5. GitHub Actions for Schema Validation",id:"5-github-actions-for-schema-validation",level:2},{value:"5.1 Schema Validation Workflow",id:"51-schema-validation-workflow",level:3},{value:"5.2 Schema Linting Script",id:"52-schema-linting-script",level:3},{value:"6. Conflict Detection &amp; Resolution",id:"6-conflict-detection--resolution",level:2},{value:"6.1 Git Merge Strategy",id:"61-git-merge-strategy",level:3},{value:"6.2 Custom Merge Script",id:"62-custom-merge-script",level:3},{value:"6.3 Conflict Detection Service",id:"63-conflict-detection-service",level:3},{value:"7. GCP Schema Repository",id:"7-gcp-schema-repository",level:2},{value:"7.1 Schema Backup to Cloud Storage",id:"71-schema-backup-to-cloud-storage",level:3},{value:"7.2 Terraform for Schema Bucket",id:"72-terraform-for-schema-bucket",level:3},{value:"8. Best Practices",id:"8-best-practices",level:2},{value:"8.1 Schema Change Workflow",id:"81-schema-change-workflow",level:3},{value:"8.2 Schema Conflict Resolution Guide",id:"82-schema-conflict-resolution-guide",level:3}];function l(e){const n={code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",hr:"hr",li:"li",ol:"ol",p:"p",pre:"pre",strong:"strong",table:"table",tbody:"tbody",td:"td",th:"th",thead:"thead",tr:"tr",ul:"ul",...(0,i.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(n.header,{children:(0,s.jsx)(n.h1,{id:"database-schema-version-control--conflict-detection",children:"Database Schema Version Control & Conflict Detection"})}),"\n",(0,s.jsx)(n.p,{children:"This guide establishes database schema versioning practices for NestJS applications on GCP, ensuring database schemas are tracked as code, versioned alongside application code, and conflicts are detected during code merges."}),"\n",(0,s.jsx)(n.h2,{id:"1-overview",children:"1. Overview"}),"\n",(0,s.jsx)(n.p,{children:"Database schema as code treats database definitions as first-class citizens in version control, enabling the same development practices (branching, merging, code review, CI/CD) used for application code. This approach prevents manual schema drift, enables conflict detection, and maintains database-application compatibility."}),"\n",(0,s.jsx)(n.h2,{id:"2-schema-as-code-principles",children:"2. Schema as Code Principles"}),"\n",(0,s.jsx)(n.h3,{id:"21-core-principles",children:"2.1 Core Principles"}),"\n",(0,s.jsxs)(n.table,{children:[(0,s.jsx)(n.thead,{children:(0,s.jsxs)(n.tr,{children:[(0,s.jsx)(n.th,{children:"Principle"}),(0,s.jsx)(n.th,{children:"Description"}),(0,s.jsx)(n.th,{children:"Benefit"})]})}),(0,s.jsxs)(n.tbody,{children:[(0,s.jsxs)(n.tr,{children:[(0,s.jsx)(n.td,{children:(0,s.jsx)(n.strong,{children:"Single Source of Truth"})}),(0,s.jsx)(n.td,{children:"Schema defined in version-controlled files"}),(0,s.jsx)(n.td,{children:"Eliminates drift between environments"})]}),(0,s.jsxs)(n.tr,{children:[(0,s.jsx)(n.td,{children:(0,s.jsx)(n.strong,{children:"Declarative Schema"})}),(0,s.jsx)(n.td,{children:"Define desired state, not migration steps"}),(0,s.jsx)(n.td,{children:"Easier to understand and review"})]}),(0,s.jsxs)(n.tr,{children:[(0,s.jsx)(n.td,{children:(0,s.jsx)(n.strong,{children:"Code Review for Schema"})}),(0,s.jsx)(n.td,{children:"Schema changes go through PR process"}),(0,s.jsx)(n.td,{children:"Catches issues before production"})]}),(0,s.jsxs)(n.tr,{children:[(0,s.jsx)(n.td,{children:(0,s.jsx)(n.strong,{children:"Automated Validation"})}),(0,s.jsx)(n.td,{children:"CI/CD validates schema consistency"}),(0,s.jsx)(n.td,{children:"Prevents invalid schema states"})]}),(0,s.jsxs)(n.tr,{children:[(0,s.jsx)(n.td,{children:(0,s.jsx)(n.strong,{children:"Conflict Detection"})}),(0,s.jsx)(n.td,{children:"Git detects schema merge conflicts"}),(0,s.jsx)(n.td,{children:"Prevents incompatible schema changes"})]}),(0,s.jsxs)(n.tr,{children:[(0,s.jsx)(n.td,{children:(0,s.jsx)(n.strong,{children:"Environment Parity"})}),(0,s.jsx)(n.td,{children:"Same schema source for all environments"}),(0,s.jsx)(n.td,{children:"Consistent behavior across stages"})]})]})]}),"\n",(0,s.jsx)(n.h3,{id:"22-schema-file-formats",children:"2.2 Schema File Formats"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-yaml",children:"# Comparison of schema formats\nformats:\n  prisma:\n    extension: .prisma\n    type: DSL (Domain Specific Language)\n    readability: Excellent\n    tooling: Native to Prisma\n    git_friendly: Yes\n    conflict_detection: Line-based\n\n  typeorm:\n    extension: .ts (Entity files)\n    type: TypeScript classes\n    readability: Good\n    tooling: TypeScript ecosystem\n    git_friendly: Yes\n    conflict_detection: Line-based\n\n  sql:\n    extension: .sql\n    type: SQL DDL statements\n    readability: Good for SQL users\n    tooling: Universal\n    git_friendly: Yes (with proper formatting)\n    conflict_detection: Line-based\n\n  yaml:\n    extension: .yml\n    type: YAML configuration\n    readability: Excellent\n    tooling: Liquibase, Flyway\n    git_friendly: Yes\n    conflict_detection: Line-based\n"})}),"\n",(0,s.jsx)(n.h3,{id:"23-recommended-repo-layout-tracked-artifacts",children:"2.3 Recommended Repo Layout (Tracked Artifacts)"}),"\n",(0,s.jsxs)(n.p,{children:["To satisfy \u201cdatabase objects follow the same policies as code\u201d and enable merge-time conflict detection, keep ",(0,s.jsx)(n.strong,{children:"both"}),":"]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Source-of-truth schema definition"})," (Prisma DSL / TypeORM entities / Liquibase YAML)"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Generated schema export snapshot(s)"})," (SQL/YAML) committed to the repo"]}),"\n"]}),"\n",(0,s.jsx)(n.p,{children:"Recommended layout:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"prisma/\n  schema.prisma                 # declarative schema (reviewed like code)\n  migrations/                   # ordered migrations (reviewed like code)\n\ndb/\n  schema.snapshot.sql           # exported schema snapshot (committed)\n  schema.snapshot.yml           # optional; if you use Liquibase-style exports\n  schema.manifest.json          # optional; hash + compatibility metadata\n\n.github/workflows/\n  schema-validation.yml         # CI gates: drift, snapshot, conflicts\n"})}),"\n",(0,s.jsxs)(n.p,{children:["Why commit ",(0,s.jsx)(n.code,{children:"db/schema.snapshot.sql"}),"?"]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:["Produces a ",(0,s.jsx)(n.strong,{children:"stable, reviewable diff"})," in PRs (especially for \u201cadd column / add index\u201d changes)."]}),"\n",(0,s.jsxs)(n.li,{children:["Lets Git surface ",(0,s.jsx)(n.strong,{children:"line-based merge conflicts"})," when two branches change the same DB object."]}),"\n",(0,s.jsx)(n.li,{children:"Enables a CI rule: \u201cif schema changed, snapshot must be updated\u201d, preventing silent drift."}),"\n"]}),"\n",(0,s.jsx)(n.h2,{id:"24-exporting-the-schema-snapshot-sqlyaml",children:"2.4 Exporting the Schema Snapshot (SQL/YAML)"}),"\n",(0,s.jsx)(n.p,{children:"There are two safe approaches:"}),"\n",(0,s.jsxs)(n.ol,{children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Export from a clean DB after applying migrations"})," (most accurate)"]}),"\n"]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:["CI spins up a temporary Postgres service, applies migrations, then exports ",(0,s.jsx)(n.code,{children:"--schema-only"}),"."]}),"\n"]}),"\n",(0,s.jsxs)(n.ol,{start:"2",children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Export from declarative schema definition"})," (fast, tool-dependent)"]}),"\n"]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:["Prisma: generate SQL via ",(0,s.jsx)(n.code,{children:"prisma migrate diff --from-empty ... --script"}),"."]}),"\n",(0,s.jsx)(n.li,{children:"Liquibase: generate changelog / snapshot from DB or reference."}),"\n"]}),"\n",(0,s.jsx)(n.p,{children:"Example (CI-friendly) snapshot generation for Postgres:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"# 1) Apply all migrations to a clean DB\nnpx prisma migrate deploy\n\n# 2) Export canonical schema snapshot\npg_dump --schema-only --no-owner --no-privileges \\\n  -h localhost -U test -d test_db \\\n  > db/schema.snapshot.sql\n\n# 3) Ensure stable formatting (optional but recommended)\n# e.g., run a SQL formatter here if your repo standardizes it\n"})}),"\n",(0,s.jsx)(n.h2,{id:"25-github-actions-gate-snapshot-must-match",children:"2.5 GitHub Actions Gate: \u201cSnapshot Must Match\u201d"}),"\n",(0,s.jsx)(n.p,{children:"Add a job that fails the PR if schema/migrations change but the committed snapshot wasn\u2019t updated."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-yaml",children:"# Add to .github/workflows/schema-validation.yml\njobs:\n  schema-snapshot-up-to-date:\n    name: Schema Snapshot Up-to-date\n    runs-on: ubuntu-latest\n    if: github.event_name == 'pull_request'\n\n    services:\n      postgres:\n        image: postgres:15\n        env:\n          POSTGRES_USER: test\n          POSTGRES_PASSWORD: test\n          POSTGRES_DB: test_db\n        options: >-\n          --health-cmd pg_isready\n          --health-interval 10s\n          --health-timeout 5s\n          --health-retries 5\n        ports:\n          - 5432:5432\n\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n          cache: 'pnpm'\n\n      - run: pnpm install\n\n      - name: Apply migrations\n        env:\n          DATABASE_URL: postgresql://test:test@localhost:5432/test_db\n        run: npx prisma migrate deploy\n\n      - name: Export schema snapshot\n        env:\n          PGPASSWORD: test\n        run: |\n          mkdir -p db\n          pg_dump --schema-only --no-owner --no-privileges \\\n            -h localhost -U test -d test_db \\\n            > db/schema.snapshot.sql\n\n      - name: Fail if snapshot not committed\n        run: |\n          if ! git diff --exit-code -- db/schema.snapshot.sql; then\n            echo \"\u274c db/schema.snapshot.sql is out of date. Regenerate and commit it.\"\n            echo \"Tip: run the same export locally after applying migrations.\"\n            exit 1\n          fi\n"})}),"\n",(0,s.jsx)(n.p,{children:"This is the key \u201cmerge conflict / conflict detection\u201d mechanism: it forces every DB change to produce a deterministic, reviewable diff that Git can merge (or flag as conflicting) using the same policies as code."}),"\n",(0,s.jsx)(n.h2,{id:"3-prisma-schema-version-control",children:"3. Prisma Schema Version Control"}),"\n",(0,s.jsx)(n.h3,{id:"31-prisma-schema-file",children:"3.1 Prisma Schema File"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-prisma",children:'// prisma/schema.prisma\n// Version-controlled database schema definition\n// Last updated: 2024-12-28\n// Compatible with app version: >= 2.5.0\n\ngenerator client {\n  provider = "prisma-client-js"\n  output   = "../node_modules/.prisma/client"\n}\n\ndatasource db {\n  provider = "postgresql"\n  url      = env("DATABASE_URL")\n}\n\n// ============================================\n// USER MANAGEMENT\n// ============================================\n\nmodel User {\n  id        Int      @id @default(autoincrement())\n  email     String   @unique @db.VarChar(255)\n  name      String?  @db.VarChar(255)\n  role      Role     @default(USER)\n  createdAt DateTime @default(now()) @map("created_at")\n  updatedAt DateTime @updatedAt @map("updated_at")\n\n  // Relations\n  orders    Order[]\n  addresses Address[]\n  reviews   Review[]\n\n  // Indexes\n  @@index([email])\n  @@index([createdAt])\n  @@map("users")\n}\n\nmodel Address {\n  id         Int     @id @default(autoincrement())\n  userId     Int     @map("user_id")\n  street     String  @db.VarChar(500)\n  city       String  @db.VarChar(100)\n  state      String  @db.VarChar(50)\n  postalCode String  @map("postal_code") @db.VarChar(20)\n  country    String  @db.VarChar(2) // ISO country code\n  isDefault  Boolean @default(false) @map("is_default")\n\n  user User @relation(fields: [userId], references: [id], onDelete: Cascade)\n\n  @@index([userId])\n  @@map("addresses")\n}\n\n// ============================================\n// PRODUCT CATALOG\n// ============================================\n\nmodel Product {\n  id          Int      @id @default(autoincrement())\n  sku         String   @unique @db.VarChar(50)\n  name        String   @db.VarChar(500)\n  description String?  @db.Text\n  price       Decimal  @db.Decimal(10, 2)\n  stock       Int      @default(0)\n  categoryId  Int      @map("category_id")\n  isActive    Boolean  @default(true) @map("is_active")\n  createdAt   DateTime @default(now()) @map("created_at")\n  updatedAt   DateTime @updatedAt @map("updated_at")\n\n  category   Category    @relation(fields: [categoryId], references: [id])\n  orderItems OrderItem[]\n  reviews    Review[]\n\n  @@index([sku])\n  @@index([categoryId])\n  @@index([isActive])\n  @@map("products")\n}\n\nmodel Category {\n  id          Int       @id @default(autoincrement())\n  name        String    @db.VarChar(100)\n  slug        String    @unique @db.VarChar(100)\n  description String?   @db.Text\n  parentId    Int?      @map("parent_id")\n\n  parent   Category?  @relation("CategoryHierarchy", fields: [parentId], references: [id])\n  children Category[] @relation("CategoryHierarchy")\n  products Product[]\n\n  @@index([slug])\n  @@index([parentId])\n  @@map("categories")\n}\n\n// ============================================\n// ORDER MANAGEMENT\n// ============================================\n\nmodel Order {\n  id           Int         @id @default(autoincrement())\n  orderNumber  String      @unique @map("order_number") @db.VarChar(50)\n  userId       Int         @map("user_id")\n  status       OrderStatus @default(PENDING)\n  totalAmount  Decimal     @map("total_amount") @db.Decimal(10, 2)\n  shippingFee  Decimal     @default(0) @map("shipping_fee") @db.Decimal(10, 2)\n  taxAmount    Decimal     @default(0) @map("tax_amount") @db.Decimal(10, 2)\n  paymentMethod String?    @map("payment_method") @db.VarChar(50)\n  createdAt    DateTime    @default(now()) @map("created_at")\n  updatedAt    DateTime    @updatedAt @map("updated_at")\n\n  user  User        @relation(fields: [userId], references: [id])\n  items OrderItem[]\n\n  @@index([userId])\n  @@index([orderNumber])\n  @@index([status])\n  @@index([createdAt])\n  @@map("orders")\n}\n\nmodel OrderItem {\n  id        Int      @id @default(autoincrement())\n  orderId   Int      @map("order_id")\n  productId Int      @map("product_id")\n  quantity  Int\n  unitPrice Decimal  @map("unit_price") @db.Decimal(10, 2)\n  subtotal  Decimal  @db.Decimal(10, 2)\n  createdAt DateTime @default(now()) @map("created_at")\n\n  order   Order   @relation(fields: [orderId], references: [id], onDelete: Cascade)\n  product Product @relation(fields: [productId], references: [id])\n\n  @@index([orderId])\n  @@index([productId])\n  @@map("order_items")\n}\n\n// ============================================\n// REVIEWS\n// ============================================\n\nmodel Review {\n  id        Int      @id @default(autoincrement())\n  productId Int      @map("product_id")\n  userId    Int      @map("user_id")\n  rating    Int      // 1-5\n  comment   String?  @db.Text\n  isVerified Boolean @default(false) @map("is_verified")\n  createdAt DateTime @default(now()) @map("created_at")\n  updatedAt DateTime @updatedAt @map("updated_at")\n\n  product Product @relation(fields: [productId], references: [id], onDelete: Cascade)\n  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)\n\n  @@unique([productId, userId]) // One review per user per product\n  @@index([productId])\n  @@index([userId])\n  @@map("reviews")\n}\n\n// ============================================\n// ENUMS\n// ============================================\n\nenum Role {\n  USER\n  ADMIN\n  MODERATOR\n}\n\nenum OrderStatus {\n  PENDING\n  CONFIRMED\n  PROCESSING\n  SHIPPED\n  DELIVERED\n  CANCELLED\n  REFUNDED\n}\n'})}),"\n",(0,s.jsx)(n.h3,{id:"32-schema-metadata-tracking",children:"3.2 Schema Metadata Tracking"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// src/database/schema-metadata.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { PrismaClient } from '@prisma/client';\nimport { execSync } from 'child_process';\nimport * as fs from 'fs';\nimport * as path from 'path';\nimport * as crypto from 'crypto';\n\nexport interface SchemaMetadata {\n  schemaVersion: string;\n  schemaHash: string;\n  lastModified: Date;\n  lastModifiedBy: string;\n  compatibleAppVersion: string;\n  databaseVersion: string;\n  conflicts: SchemaConflict[];\n}\n\nexport interface SchemaConflict {\n  type: 'table' | 'column' | 'index' | 'constraint';\n  object: string;\n  description: string;\n  severity: 'error' | 'warning';\n}\n\n@Injectable()\nexport class SchemaMetadataService {\n  private readonly logger = new Logger(SchemaMetadataService.name);\n  private prisma = new PrismaClient();\n\n  async getSchemaMetadata(): Promise<SchemaMetadata> {\n    const schemaPath = path.join(process.cwd(), 'prisma', 'schema.prisma');\n    const schemaContent = fs.readFileSync(schemaPath, 'utf8');\n    const schemaHash = this.calculateHash(schemaContent);\n\n    // Get Git information\n    const lastCommit = execSync('git log -1 --format=\"%H|%an|%ad\" -- prisma/schema.prisma', {\n      encoding: 'utf-8',\n    }).trim();\n\n    const [commitHash, author, date] = lastCommit.split('|');\n\n    // Get database version\n    const dbVersion = await this.getDatabaseVersion();\n\n    // Check for conflicts\n    const conflicts = await this.detectSchemaConflicts();\n\n    return {\n      schemaVersion: commitHash.substring(0, 8),\n      schemaHash,\n      lastModified: new Date(date),\n      lastModifiedBy: author,\n      compatibleAppVersion: this.extractAppVersion(schemaContent),\n      databaseVersion: dbVersion,\n      conflicts,\n    };\n  }\n\n  async validateSchemaSync(): Promise<boolean> {\n    try {\n      // Compare schema file with database state\n      const result = execSync('npx prisma migrate status', {\n        encoding: 'utf-8',\n      });\n\n      return !result.includes('following migrations have not yet been applied');\n    } catch (error) {\n      this.logger.error('Schema validation failed:', error.message);\n      return false;\n    }\n  }\n\n  async detectSchemaConflicts(): Promise<SchemaConflict[]> {\n    const conflicts: SchemaConflict[] = [];\n\n    try {\n      // Get current branch\n      const currentBranch = execSync('git branch --show-current', {\n        encoding: 'utf-8',\n      }).trim();\n\n      // Check for unmerged changes\n      const unmergedFiles = execSync('git diff --name-only --diff-filter=U', {\n        encoding: 'utf-8',\n      }).trim();\n\n      if (unmergedFiles.includes('prisma/schema.prisma')) {\n        conflicts.push({\n          type: 'table',\n          object: 'schema.prisma',\n          description: 'Schema file has merge conflicts',\n          severity: 'error',\n        });\n      }\n\n      // Check for pending migrations\n      const status = execSync('npx prisma migrate status', {\n        encoding: 'utf-8',\n      });\n\n      if (status.includes('drift detected')) {\n        conflicts.push({\n          type: 'table',\n          object: 'database',\n          description: 'Database schema drift detected',\n          severity: 'warning',\n        });\n      }\n    } catch (error) {\n      // Ignore git command errors in non-git environments\n    }\n\n    return conflicts;\n  }\n\n  async exportSchemaToSQL(): Promise<string> {\n    const outputPath = path.join(process.cwd(), 'exports', 'schema-export.sql');\n\n    // Create exports directory if it doesn't exist\n    fs.mkdirSync(path.dirname(outputPath), { recursive: true });\n\n    // Export current database schema\n    execSync(\n      `npx prisma db pull && npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script > ${outputPath}`,\n      {\n        encoding: 'utf-8',\n      },\n    );\n\n    return outputPath;\n  }\n\n  async compareSchemaVersions(baseRef: string, compareRef: string): Promise<string> {\n    try {\n      // Get schema at base ref\n      const baseSchem\u0430 = execSync(`git show ${baseRef}:prisma/schema.prisma`, {\n        encoding: 'utf-8',\n      });\n\n      // Get schema at compare ref\n      const compareSchema = execSync(`git show ${compareRef}:prisma/schema.prisma`, {\n        encoding: 'utf-8',\n      });\n\n      // Generate diff\n      const diffPath = path.join(process.cwd(), 'exports', 'schema-diff.txt');\n      fs.writeFileSync(diffPath, `=== Schema Comparison ===\\nBase: ${baseRef}\\nCompare: ${compareRef}\\n\\n`);\n\n      const diff = execSync(`git diff ${baseRef} ${compareRef} -- prisma/schema.prisma`, {\n        encoding: 'utf-8',\n      });\n\n      fs.appendFileSync(diffPath, diff);\n      return diffPath;\n    } catch (error) {\n      this.logger.error('Failed to compare schema versions:', error.message);\n      throw error;\n    }\n  }\n\n  private calculateHash(content: string): string {\n    return crypto.createHash('sha256').update(content).digest('hex').substring(0, 16);\n  }\n\n  private extractAppVersion(schemaContent: string): string {\n    const match = schemaContent.match(/Compatible with app version: >= ([\\d.]+)/);\n    return match ? match[1] : 'unknown';\n  }\n\n  private async getDatabaseVersion(): Promise<string> {\n    try {\n      const result = await this.prisma.$queryRaw<Array<{ version: string }>>`\n        SELECT version() as version\n      `;\n      return result[0]?.version || 'unknown';\n    } catch (error) {\n      return 'unknown';\n    }\n  }\n}\n\n// src/database/schema-metadata.controller.ts\nimport { Controller, Get, Query, UseGuards } from '@nestjs/common';\nimport { ApiTags, ApiOperation, ApiResponse } from '@nestjs/swagger';\nimport { SchemaMetadataService } from './schema-metadata.service';\n\n@ApiTags('Schema Management')\n@Controller('api/schema')\nexport class SchemaMetadataController {\n  constructor(private readonly schemaService: SchemaMetadataService) {}\n\n  @Get('metadata')\n  @ApiOperation({ summary: 'Get current schema metadata' })\n  async getMetadata() {\n    return this.schemaService.getSchemaMetadata();\n  }\n\n  @Get('validate')\n  @ApiOperation({ summary: 'Validate schema synchronization' })\n  async validateSync() {\n    const isValid = await this.schemaService.validateSchemaSync();\n    return { isValid, message: isValid ? 'Schema is in sync' : 'Schema drift detected' };\n  }\n\n  @Get('conflicts')\n  @ApiOperation({ summary: 'Detect schema conflicts' })\n  async detectConflicts() {\n    const conflicts = await this.schemaService.detectSchemaConflicts();\n    return { conflicts, hasConflicts: conflicts.length > 0 };\n  }\n\n  @Get('export')\n  @ApiOperation({ summary: 'Export current schema to SQL' })\n  async exportSchema() {\n    const filePath = await this.schemaService.exportSchemaToSQL();\n    return { filePath, message: 'Schema exported successfully' };\n  }\n\n  @Get('compare')\n  @ApiOperation({ summary: 'Compare schema between two Git refs' })\n  async compareVersions(@Query('base') baseRef: string = 'main', @Query('compare') compareRef: string = 'HEAD') {\n    const diffPath = await this.schemaService.compareSchemaVersions(baseRef, compareRef);\n    return { diffPath };\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"4-typeorm-schema-version-control",children:"4. TypeORM Schema Version Control"}),"\n",(0,s.jsx)(n.h3,{id:"41-entity-definitions",children:"4.1 Entity Definitions"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// src/entities/user.entity.ts\nimport { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn, UpdateDateColumn, OneToMany, Index } from 'typeorm';\nimport { Order } from './order.entity';\nimport { Address } from './address.entity';\n\n/**\n * User Entity\n * Schema Version: 2.5.0\n * Last Modified: 2024-12-28\n */\n@Entity('users')\n@Index(['email'])\n@Index(['createdAt'])\nexport class User {\n  @PrimaryGeneratedColumn()\n  id: number;\n\n  @Column({ type: 'varchar', length: 255, unique: true })\n  email: string;\n\n  @Column({ type: 'varchar', length: 255, nullable: true })\n  name: string;\n\n  @Column({ type: 'enum', enum: ['USER', 'ADMIN', 'MODERATOR'], default: 'USER' })\n  role: string;\n\n  @CreateDateColumn({ name: 'created_at' })\n  createdAt: Date;\n\n  @UpdateDateColumn({ name: 'updated_at' })\n  updatedAt: Date;\n\n  @OneToMany(() => Order, (order) => order.user)\n  orders: Order[];\n\n  @OneToMany(() => Address, (address) => address.user)\n  addresses: Address[];\n}\n\n// src/entities/product.entity.ts\nimport {\n  Entity,\n  PrimaryGeneratedColumn,\n  Column,\n  ManyToOne,\n  OneToMany,\n  CreateDateColumn,\n  UpdateDateColumn,\n  Index,\n  JoinColumn,\n} from 'typeorm';\nimport { Category } from './category.entity';\nimport { OrderItem } from './order-item.entity';\n\n/**\n * Product Entity\n * Schema Version: 2.5.0\n */\n@Entity('products')\n@Index(['sku'])\n@Index(['categoryId'])\n@Index(['isActive'])\nexport class Product {\n  @PrimaryGeneratedColumn()\n  id: number;\n\n  @Column({ type: 'varchar', length: 50, unique: true })\n  sku: string;\n\n  @Column({ type: 'varchar', length: 500 })\n  name: string;\n\n  @Column({ type: 'text', nullable: true })\n  description: string;\n\n  @Column({ type: 'decimal', precision: 10, scale: 2 })\n  price: number;\n\n  @Column({ type: 'int', default: 0 })\n  stock: number;\n\n  @Column({ name: 'category_id' })\n  categoryId: number;\n\n  @Column({ name: 'is_active', default: true })\n  isActive: boolean;\n\n  @CreateDateColumn({ name: 'created_at' })\n  createdAt: Date;\n\n  @UpdateDateColumn({ name: 'updated_at' })\n  updatedAt: Date;\n\n  @ManyToOne(() => Category, (category) => category.products)\n  @JoinColumn({ name: 'category_id' })\n  category: Category;\n\n  @OneToMany(() => OrderItem, (orderItem) => orderItem.product)\n  orderItems: OrderItem[];\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"42-schema-export-service",children:"4.2 Schema Export Service"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// src/database/typeorm-schema-export.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { DataSource } from 'typeorm';\nimport * as fs from 'fs';\nimport * as path from 'path';\n\n@Injectable()\nexport class TypeOrmSchemaExportService {\n  private readonly logger = new Logger(TypeOrmSchemaExportService.name);\n\n  constructor(private dataSource: DataSource) {}\n\n  async exportSchemaToSQL(): Promise<string> {\n    const entities = this.dataSource.entityMetadatas;\n    const outputPath = path.join(process.cwd(), 'exports', 'typeorm-schema.sql');\n\n    fs.mkdirSync(path.dirname(outputPath), { recursive: true });\n\n    let sql = '-- TypeORM Schema Export\\n';\n    sql += `-- Generated: ${new Date().toISOString()}\\n`;\n    sql += `-- Total Tables: ${entities.length}\\n\\n`;\n\n    // Generate CREATE TABLE statements\n    for (const entity of entities) {\n      sql += this.generateCreateTableSQL(entity);\n      sql += '\\n\\n';\n    }\n\n    fs.writeFileSync(outputPath, sql);\n    this.logger.log(`Schema exported to: ${outputPath}`);\n\n    return outputPath;\n  }\n\n  async exportSchemaToYAML(): Promise<string> {\n    const entities = this.dataSource.entityMetadatas;\n    const outputPath = path.join(process.cwd(), 'exports', 'typeorm-schema.yml');\n\n    const schema = {\n      version: '2.5.0',\n      generatedAt: new Date().toISOString(),\n      database: this.dataSource.options.type,\n      tables: entities.map((entity) => ({\n        name: entity.tableName,\n        columns: entity.columns.map((col) => ({\n          name: col.propertyName,\n          type: col.type,\n          nullable: col.isNullable,\n          unique: col.isUnique,\n          primary: col.isPrimary,\n          default: col.default,\n        })),\n        indexes: entity.indices.map((idx) => ({\n          name: idx.name,\n          columns: idx.columns.map((col) => col.propertyName),\n          unique: idx.isUnique,\n        })),\n        relations: entity.relations.map((rel) => ({\n          name: rel.propertyName,\n          type: rel.relationType,\n          target: rel.inverseEntityMetadata.tableName,\n        })),\n      })),\n    };\n\n    fs.writeFileSync(outputPath, JSON.stringify(schema, null, 2));\n    this.logger.log(`Schema exported to: ${outputPath}`);\n\n    return outputPath;\n  }\n\n  private generateCreateTableSQL(entity: any): string {\n    let sql = `CREATE TABLE ${entity.tableName} (\\n`;\n\n    // Add columns\n    const columns = entity.columns.map((col) => {\n      let colDef = `  ${col.databaseName} ${this.mapTypeToSQL(col.type)}`;\n      if (col.isPrimary) colDef += ' PRIMARY KEY';\n      if (!col.isNullable) colDef += ' NOT NULL';\n      if (col.isUnique) colDef += ' UNIQUE';\n      if (col.default !== undefined) colDef += ` DEFAULT ${col.default}`;\n      return colDef;\n    });\n\n    sql += columns.join(',\\n');\n    sql += '\\n);';\n\n    // Add indexes\n    entity.indices.forEach((idx) => {\n      if (!idx.isPrimary) {\n        const indexName = idx.name || `idx_${entity.tableName}_${idx.columns.map((c) => c.databaseName).join('_')}`;\n        sql += `\\nCREATE INDEX ${indexName} ON ${entity.tableName} (${idx.columns\n          .map((c) => c.databaseName)\n          .join(', ')});`;\n      }\n    });\n\n    return sql;\n  }\n\n  private mapTypeToSQL(type: any): string {\n    const typeMap: Record<string, string> = {\n      varchar: 'VARCHAR',\n      text: 'TEXT',\n      int: 'INTEGER',\n      decimal: 'DECIMAL',\n      boolean: 'BOOLEAN',\n      timestamp: 'TIMESTAMP',\n      enum: 'VARCHAR',\n    };\n\n    return typeMap[type] || 'TEXT';\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"5-github-actions-for-schema-validation",children:"5. GitHub Actions for Schema Validation"}),"\n",(0,s.jsx)(n.h3,{id:"51-schema-validation-workflow",children:"5.1 Schema Validation Workflow"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-yaml",children:'# .github/workflows/schema-validation.yml\nname: Database Schema Validation\n\non:\n  pull_request:\n    branches: [main, develop]\n    paths:\n      - \'prisma/**\'\n      - \'src/entities/**\'\n      - \'src/migrations/**\'\n  push:\n    branches: [main, develop]\n    paths:\n      - \'prisma/**\'\n      - \'src/entities/**\'\n\nenv:\n  NODE_VERSION: \'20\'\n\njobs:\n  detect-schema-changes:\n    name: Detect Schema Changes\n    runs-on: ubuntu-latest\n\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0 # Full history for comparison\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ env.NODE_VERSION }}\n          cache: \'pnpm\'\n\n      - name: Install dependencies\n        run: pnpm install\n\n      - name: Detect Prisma schema changes\n        id: schema-changes\n        run: |\n          # Get changed files\n          CHANGED_FILES=$(git diff --name-only ${{ github.event.pull_request.base.sha }} ${{ github.sha }})\n\n          echo "Changed files:"\n          echo "$CHANGED_FILES"\n\n          if echo "$CHANGED_FILES" | grep -q "prisma/schema.prisma"; then\n            echo "schema_changed=true" >> $GITHUB_OUTPUT\n            echo "\u26a0\ufe0f Schema file has been modified"\n          else\n            echo "schema_changed=false" >> $GITHUB_OUTPUT\n            echo "\u2705 No schema changes detected"\n          fi\n\n      - name: Generate schema diff\n        if: steps.schema-changes.outputs.schema_changed == \'true\'\n        run: |\n          # Compare schemas\n          git show ${{ github.event.pull_request.base.sha }}:prisma/schema.prisma > base-schema.prisma\n\n          echo "=== Schema Comparison ===" > schema-diff.txt\n          echo "Base: ${{ github.event.pull_request.base.sha }}" >> schema-diff.txt\n          echo "Current: ${{ github.sha }}" >> schema-diff.txt\n          echo "" >> schema-diff.txt\n\n          diff -u base-schema.prisma prisma/schema.prisma >> schema-diff.txt || true\n\n          cat schema-diff.txt\n\n      - name: Upload schema diff\n        if: steps.schema-changes.outputs.schema_changed == \'true\'\n        uses: actions/upload-artifact@v4\n        with:\n          name: schema-diff\n          path: schema-diff.txt\n\n  validate-schema-syntax:\n    name: Validate Schema Syntax\n    needs: detect-schema-changes\n    runs-on: ubuntu-latest\n\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ env.NODE_VERSION }}\n          cache: \'pnpm\'\n\n      - name: Install dependencies\n        run: pnpm install\n\n      - name: Validate Prisma schema\n        run: |\n          echo "\ud83d\udd0d Validating Prisma schema syntax..."\n          npx prisma validate\n          echo "\u2705 Schema syntax is valid"\n\n      - name: Check for schema conflicts\n        run: |\n          # Check if there are merge conflict markers\n          if grep -q "<<<<<<< HEAD" prisma/schema.prisma; then\n            echo "\u274c Merge conflict detected in schema file"\n            exit 1\n          fi\n          echo "\u2705 No merge conflicts in schema"\n\n      - name: Lint schema file\n        run: |\n          # Check for common issues\n          node scripts/lint-schema.js\n\n  check-migration-compatibility:\n    name: Check Migration Compatibility\n    needs: validate-schema-syntax\n    runs-on: ubuntu-latest\n\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ env.NODE_VERSION }}\n          cache: \'pnpm\'\n\n      - name: Install dependencies\n        run: pnpm install\n\n      - name: Check if migration is needed\n        id: check-migration\n        run: |\n          # Compare schema with last migration\n          npx prisma migrate diff \\\n            --from-schema-datamodel prisma/schema.prisma \\\n            --to-schema-datasource prisma/schema.prisma \\\n            --script > pending-changes.sql\n\n          if [ -s pending-changes.sql ]; then\n            echo "migration_needed=true" >> $GITHUB_OUTPUT\n            echo "\u26a0\ufe0f Schema changes require migration"\n            cat pending-changes.sql\n          else\n            echo "migration_needed=false" >> $GITHUB_OUTPUT\n            echo "\u2705 No migration required"\n          fi\n\n      - name: Verify migration exists\n        if: steps.check-migration.outputs.migration_needed == \'true\'\n        run: |\n          # Check if new migration was created\n          MIGRATIONS_COUNT=$(git diff --name-only ${{ github.event.pull_request.base.sha }} ${{ github.sha }} | grep "prisma/migrations/" | wc -l)\n\n          if [ "$MIGRATIONS_COUNT" -eq 0 ]; then\n            echo "\u274c Schema changed but no migration created"\n            echo "Run: npx prisma migrate dev --name <description>"\n            exit 1\n          fi\n\n          echo "\u2705 Migration files found: $MIGRATIONS_COUNT"\n\n  test-schema-on-fresh-db:\n    name: Test Schema on Fresh Database\n    needs: check-migration-compatibility\n    runs-on: ubuntu-latest\n\n    services:\n      postgres:\n        image: postgres:15\n        env:\n          POSTGRES_USER: test\n          POSTGRES_PASSWORD: test\n          POSTGRES_DB: test_db\n        options: >-\n          --health-cmd pg_isready\n          --health-interval 10s\n          --health-timeout 5s\n          --health-retries 5\n        ports:\n          - 5432:5432\n\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: actions/setup-node@v4\n        with:\n          node-version: ${{ env.NODE_VERSION }}\n          cache: \'pnpm\'\n\n      - name: Install dependencies\n        run: pnpm install\n\n      - name: Apply migrations to fresh database\n        env:\n          DATABASE_URL: postgresql://test:test@localhost:5432/test_db\n        run: |\n          echo "\ud83d\ude80 Applying all migrations to fresh database..."\n          npx prisma migrate deploy\n          echo "\u2705 All migrations applied successfully"\n\n      - name: Verify schema matches\n        env:\n          DATABASE_URL: postgresql://test:test@localhost:5432/test_db\n        run: |\n          echo "\ud83d\udd0d Verifying database schema matches Prisma schema..."\n          npx prisma db pull --force\n\n          # Check if pulled schema matches committed schema\n          if ! diff -q prisma/schema.prisma prisma/schema.prisma.bak 2>/dev/null; then\n            echo "\u26a0\ufe0f Warning: Generated schema differs from committed schema"\n          else\n            echo "\u2705 Schema verification passed"\n          fi\n\n      - name: Export final schema\n        env:\n          DATABASE_URL: postgresql://test:test@localhost:5432/test_db\n        run: |\n          # Export database schema for review\n          pg_dump -h localhost -U test -d test_db --schema-only > final-schema.sql\n          echo "Schema exported for review"\n\n      - name: Upload final schema\n        uses: actions/upload-artifact@v4\n        with:\n          name: final-schema-sql\n          path: final-schema.sql\n\n  comment-pr-with-changes:\n    name: Comment PR with Schema Changes\n    needs: [detect-schema-changes, test-schema-on-fresh-db]\n    if: github.event_name == \'pull_request\'\n    runs-on: ubuntu-latest\n    permissions:\n      pull-requests: write\n\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Download schema diff\n        uses: actions/download-artifact@v4\n        with:\n          name: schema-diff\n        continue-on-error: true\n\n      - name: Download final schema\n        uses: actions/download-artifact@v4\n        with:\n          name: final-schema-sql\n        continue-on-error: true\n\n      - name: Comment on PR\n        uses: actions/github-script@v7\n        with:\n          github-token: ${{ secrets.GITHUB_TOKEN }}\n          script: |\n            const fs = require(\'fs\');\n\n            let comment = \'## \ud83d\uddc3\ufe0f Database Schema Changes\\n\\n\';\n\n            // Read schema diff if exists\n            try {\n              const diff = fs.readFileSync(\'schema-diff.txt\', \'utf8\');\n              comment += \'### Schema Diff\\n```diff\\n\' + diff + \'\\n```\\n\\n\';\n            } catch (e) {\n              comment += \'\u2705 No schema changes detected\\n\\n\';\n            }\n\n            comment += \'### Validation Results\\n\';\n            comment += \'- \u2705 Schema syntax validated\\n\';\n            comment += \'- \u2705 No merge conflicts\\n\';\n            comment += \'- \u2705 Migrations compatible\\n\';\n            comment += \'- \u2705 Fresh database test passed\\n\\n\';\n\n            comment += \'### Review Checklist\\n\';\n            comment += \'- [ ] Schema changes reviewed\\n\';\n            comment += \'- [ ] Migration tested locally\\n\';\n            comment += \'- [ ] Backward compatibility considered\\n\';\n            comment += \'- [ ] Rollback plan documented\\n\';\n\n            github.rest.issues.createComment({\n              issue_number: context.issue.number,\n              owner: context.repo.owner,\n              repo: context.repo.repo,\n              body: comment\n            });\n'})}),"\n",(0,s.jsx)(n.h3,{id:"52-schema-linting-script",children:"5.2 Schema Linting Script"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"// scripts/lint-schema.js\nconst fs = require('fs');\nconst path = require('path');\n\nconst schemaPath = path.join(__dirname, '../prisma/schema.prisma');\nconst schema = fs.readFileSync(schemaPath, 'utf8');\n\nconsole.log('\ud83d\udd0d Linting Prisma schema...\\n');\n\nlet hasErrors = false;\nlet hasWarnings = false;\n\n// Check 1: Schema has version comment\nif (!schema.includes('Compatible with app version')) {\n  console.error('\u274c Error: Schema missing version compatibility comment');\n  hasErrors = true;\n} else {\n  console.log('\u2705 Version compatibility documented');\n}\n\n// Check 2: All tables have indexes\nconst models = schema.match(/model \\w+ {[\\s\\S]*?}/g) || [];\nmodels.forEach((model) => {\n  const modelName = model.match(/model (\\w+)/)[1];\n\n  if (!model.includes('@@index')) {\n    console.warn(`\u26a0\ufe0f  Warning: Model ${modelName} has no indexes`);\n    hasWarnings = true;\n  }\n\n  if (!model.includes('@@map')) {\n    console.warn(`\u26a0\ufe0f  Warning: Model ${modelName} missing @@ map to table name`);\n    hasWarnings = true;\n  }\n});\n\n// Check 3: All timestamp fields use snake_case\nconst timestampFields = ['createdAt', 'updatedAt'];\ntimestampFields.forEach((field) => {\n  const regex = new RegExp(`${field}\\\\s+DateTime.*(?!@map)`, 'g');\n  if (regex.test(schema)) {\n    console.warn(`\u26a0\ufe0f  Warning: ${field} field should use @map for snake_case column name`);\n    hasWarnings = true;\n  }\n});\n\n// Check 4: Foreign keys follow naming convention\nconst foreignKeys = schema.match(/\\w+Id\\s+Int/g) || [];\nforeignKeys.forEach((fk) => {\n  const fieldName = fk.match(/(\\w+)Id/)[1];\n  const mapPattern = new RegExp(`${fieldName}Id.*@map\\\\(\"${fieldName}_id\"\\\\)`);\n\n  if (!mapPattern.test(schema)) {\n    console.warn(`\u26a0\ufe0f  Warning: Foreign key ${fieldName}Id should map to ${fieldName}_id`);\n    hasWarnings = true;\n  }\n});\n\n// Check 5: No raw SQL in schema\nif (schema.includes('@@native') || schema.includes('sql`')) {\n  console.warn('\u26a0\ufe0f  Warning: Schema contains native SQL - may not be portable');\n  hasWarnings = true;\n}\n\n// Check 6: All enums are properly defined\nconst enumUsages = schema.match(/\\w+\\s+\\w+\\s+@default\\(\\w+\\)/g) || [];\nconst definedEnums = schema.match(/enum \\w+ {/g) || [];\n\nconsole.log(`\\n\ud83d\udcca Schema Statistics:`);\nconsole.log(`- Models: ${models.length}`);\nconsole.log(`- Enums: ${definedEnums.length}`);\nconsole.log(`- Foreign Keys: ${foreignKeys.length}`);\n\nif (hasErrors) {\n  console.error('\\n\u274c Schema linting failed with errors');\n  process.exit(1);\n}\n\nif (hasWarnings) {\n  console.warn('\\n\u26a0\ufe0f  Schema linting completed with warnings');\n  process.exit(0);\n}\n\nconsole.log('\\n\u2705 Schema linting passed');\n"})}),"\n",(0,s.jsx)(n.h2,{id:"6-conflict-detection--resolution",children:"6. Conflict Detection & Resolution"}),"\n",(0,s.jsx)(n.h3,{id:"61-git-merge-strategy",children:"6.1 Git Merge Strategy"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'# .gitattributes\n# Custom merge strategy for Prisma schema files\nprisma/schema.prisma merge=prisma-merge\n\n# Configure custom merge driver\n# Add to .git/config or run:\n# git config merge.prisma-merge.driver "node scripts/merge-schema.js %O %A %B %P"\n# git config merge.prisma-merge.name "Prisma schema merge driver"\n'})}),"\n",(0,s.jsx)(n.h3,{id:"62-custom-merge-script",children:"6.2 Custom Merge Script"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"// scripts/merge-schema.js\nconst fs = require('fs');\nconst path = require('path');\n\n/**\n * Custom merge driver for Prisma schema files\n * Arguments: %O (base) %A (current) %B (other) %P (path)\n */\nconst [base, current, other, schemaPath] = process.argv.slice(2);\n\nconsole.log('\ud83d\udd00 Merging Prisma schema files...');\n\nconst baseContent = fs.readFileSync(base, 'utf8');\nconst currentContent = fs.readFileSync(current, 'utf8');\nconst otherContent = fs.readFileSync(other, 'utf8');\n\n// Parse schema sections\nconst baseModels = extractModels(baseContent);\nconst currentModels = extractModels(currentContent);\nconst otherModels = extractModels(otherContent);\n\n// Detect conflicts\nconst conflicts = [];\n\n// Check for conflicting model changes\nObject.keys(currentModels).forEach((modelName) => {\n  if (otherModels[modelName]) {\n    // Both branches modified the same model\n    if (\n      currentModels[modelName] !== baseModels[modelName] &&\n      otherModels[modelName] !== baseModels[modelName] &&\n      currentModels[modelName] !== otherModels[modelName]\n    ) {\n      conflicts.push({\n        type: 'model',\n        name: modelName,\n        current: currentModels[modelName],\n        other: otherModels[modelName],\n      });\n    }\n  }\n});\n\nif (conflicts.length > 0) {\n  console.error('\u274c Merge conflicts detected in schema:');\n  conflicts.forEach((conflict) => {\n    console.error(`  - Model: ${conflict.name}`);\n  });\n\n  // Write conflict markers\n  let mergedContent = currentContent;\n  conflicts.forEach((conflict) => {\n    const conflictMarker = `\n<<<<<<< HEAD (Current)\n${conflict.current}\n=======\n${conflict.other}\n>>>>>>> MERGE_HEAD (Incoming)\n`;\n    mergedContent = mergedContent.replace(conflict.current, conflictMarker);\n  });\n\n  fs.writeFileSync(current, mergedContent);\n  process.exit(1); // Signal merge conflict\n}\n\n// Auto-merge: combine non-conflicting changes\nconst merged = mergeSchemas(baseContent, currentContent, otherContent);\nfs.writeFileSync(current, merged);\n\nconsole.log('\u2705 Schema merged successfully');\nprocess.exit(0);\n\nfunction extractModels(content) {\n  const models = {};\n  const modelRegex = /model\\s+(\\w+)\\s*{([\\s\\S]*?)}/g;\n\n  let match;\n  while ((match = modelRegex.exec(content)) !== null) {\n    models[match[1]] = match[0];\n  }\n\n  return models;\n}\n\nfunction mergeSchemas(base, current, other) {\n  // Simple merge: prefer current for conflicts\n  // In production, implement more sophisticated merge logic\n  return current;\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"63-conflict-detection-service",children:"6.3 Conflict Detection Service"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// src/database/schema-conflict-detector.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { execSync } from 'child_process';\nimport * as fs from 'fs';\n\nexport interface ConflictReport {\n  hasConflicts: boolean;\n  conflicts: SchemaConflict[];\n  recommendations: string[];\n}\n\nexport interface SchemaConflict {\n  location: string;\n  type: 'model' | 'field' | 'relation' | 'enum';\n  description: string;\n  currentVersion: string;\n  incomingVersion: string;\n  resolution?: string;\n}\n\n@Injectable()\nexport class SchemaConflictDetectorService {\n  private readonly logger = new Logger(SchemaConflictDetectorService.name);\n\n  detectConflicts(): ConflictReport {\n    const conflicts: SchemaConflict[] = [];\n    const recommendations: string[] = [];\n\n    try {\n      // Check for Git merge conflicts\n      const status = execSync('git status --porcelain', { encoding: 'utf-8' });\n\n      if (status.includes('UU prisma/schema.prisma')) {\n        this.logger.warn('Merge conflict detected in schema file');\n\n        const schemaContent = fs.readFileSync('prisma/schema.prisma', 'utf8');\n        const conflictSections = this.parseConflictMarkers(schemaContent);\n\n        conflictSections.forEach((section) => {\n          conflicts.push({\n            location: section.location,\n            type: section.type,\n            description: 'Conflicting schema definitions',\n            currentVersion: section.current,\n            incomingVersion: section.incoming,\n            resolution: this.suggestResolution(section),\n          });\n        });\n      }\n\n      // Check for schema drift\n      const driftCheck = execSync('npx prisma migrate status', {\n        encoding: 'utf-8',\n      });\n\n      if (driftCheck.includes('drift')) {\n        conflicts.push({\n          location: 'database',\n          type: 'model',\n          description: 'Database schema has drifted from Prisma schema',\n          currentVersion: 'database state',\n          incomingVersion: 'schema file',\n          resolution: 'Run: npx prisma migrate dev to sync',\n        });\n      }\n\n      // Generate recommendations\n      if (conflicts.length > 0) {\n        recommendations.push('Review each conflict carefully before resolving');\n        recommendations.push('Test schema changes on a separate branch');\n        recommendations.push('Coordinate with team members who made conflicting changes');\n        recommendations.push('Run full test suite after resolving conflicts');\n      }\n    } catch (error) {\n      this.logger.error('Error detecting conflicts:', error.message);\n    }\n\n    return {\n      hasConflicts: conflicts.length > 0,\n      conflicts,\n      recommendations,\n    };\n  }\n\n  private parseConflictMarkers(content: string): any[] {\n    const sections = [];\n    const conflictRegex = /<<<<<<< HEAD([\\s\\S]*?)=======([\\s\\S]*?)>>>>>>>/g;\n\n    let match;\n    while ((match = conflictRegex.exec(content)) !== null) {\n      sections.push({\n        location: this.identifyLocation(match[1]),\n        type: this.identifyType(match[1]),\n        current: match[1].trim(),\n        incoming: match[2].trim(),\n      });\n    }\n\n    return sections;\n  }\n\n  private identifyLocation(content: string): string {\n    const modelMatch = content.match(/model\\s+(\\w+)/);\n    if (modelMatch) return `Model: ${modelMatch[1]}`;\n\n    const enumMatch = content.match(/enum\\s+(\\w+)/);\n    if (enumMatch) return `Enum: ${enumMatch[1]}`;\n\n    return 'Unknown location';\n  }\n\n  private identifyType(content: string): 'model' | 'field' | 'relation' | 'enum' {\n    if (content.includes('model ')) return 'model';\n    if (content.includes('enum ')) return 'enum';\n    if (content.includes('@relation')) return 'relation';\n    return 'field';\n  }\n\n  private suggestResolution(section: any): string {\n    // Provide intelligent resolution suggestions\n    if (section.type === 'model') {\n      return 'Consider merging both changes or renaming to avoid conflict';\n    }\n    if (section.type === 'field') {\n      return 'Keep both fields if they serve different purposes, or merge into one';\n    }\n    return 'Manual review required';\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"7-gcp-schema-repository",children:"7. GCP Schema Repository"}),"\n",(0,s.jsx)(n.h3,{id:"71-schema-backup-to-cloud-storage",children:"7.1 Schema Backup to Cloud Storage"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// src/database/schema-backup.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { Storage } from '@google-cloud/storage';\nimport * as fs from 'fs';\nimport * as path from 'path';\n\n@Injectable()\nexport class SchemaBackupService {\n  private readonly logger = new Logger(SchemaBackupService.name);\n  private storage = new Storage();\n  private bucketName = process.env.GCS_SCHEMA_BUCKET || 'ecom-schema-versions';\n\n  async backupSchema(version: string): Promise<string> {\n    const schemaPath = path.join(process.cwd(), 'prisma', 'schema.prisma');\n    const schemaContent = fs.readFileSync(schemaPath, 'utf8');\n\n    const timestamp = new Date().toISOString().replace(/[:.]/g, '-');\n    const fileName = `schema-${version}-${timestamp}.prisma`;\n\n    const bucket = this.storage.bucket(this.bucketName);\n    const file = bucket.file(`schemas/${fileName}`);\n\n    await file.save(schemaContent, {\n      metadata: {\n        contentType: 'text/plain',\n        metadata: {\n          version,\n          timestamp: new Date().toISOString(),\n          appVersion: process.env.APP_VERSION || 'unknown',\n        },\n      },\n    });\n\n    this.logger.log(`Schema backed up to GCS: ${fileName}`);\n    return `gs://${this.bucketName}/schemas/${fileName}`;\n  }\n\n  async listSchemaVersions(): Promise<Array<{ name: string; created: Date; version: string }>> {\n    const bucket = this.storage.bucket(this.bucketName);\n    const [files] = await bucket.getFiles({ prefix: 'schemas/' });\n\n    return files.map((file) => ({\n      name: file.name,\n      created: new Date(file.metadata.timeCreated),\n      version: file.metadata.metadata?.version || 'unknown',\n    }));\n  }\n\n  async restoreSchema(fileName: string): Promise<void> {\n    const bucket = this.storage.bucket(this.bucketName);\n    const file = bucket.file(`schemas/${fileName}`);\n\n    const [content] = await file.download();\n    const schemaPath = path.join(process.cwd(), 'prisma', 'schema.prisma');\n\n    // Backup current schema before restore\n    await this.backupSchema('pre-restore');\n\n    fs.writeFileSync(schemaPath, content);\n    this.logger.log(`Schema restored from: ${fileName}`);\n  }\n\n  async compareSchemaVersions(version1: string, version2: string): Promise<string> {\n    const bucket = this.storage.bucket(this.bucketName);\n\n    const [content1] = await bucket.file(`schemas/${version1}`).download();\n    const [content2] = await bucket.file(`schemas/${version2}`).download();\n\n    // Generate diff\n    const diff = this.generateDiff(content1.toString(), content2.toString());\n    return diff;\n  }\n\n  private generateDiff(schema1: string, schema2: string): string {\n    // Simple line-by-line diff\n    const lines1 = schema1.split('\\n');\n    const lines2 = schema2.split('\\n');\n\n    let diff = '';\n    const maxLines = Math.max(lines1.length, lines2.length);\n\n    for (let i = 0; i < maxLines; i++) {\n      if (lines1[i] !== lines2[i]) {\n        diff += `Line ${i + 1}:\\n`;\n        diff += `- ${lines1[i] || '(removed)'}\\n`;\n        diff += `+ ${lines2[i] || '(added)'}\\n\\n`;\n      }\n    }\n\n    return diff;\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"72-terraform-for-schema-bucket",children:"7.2 Terraform for Schema Bucket"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-hcl",children:'# terraform/schema-storage.tf\nresource "google_storage_bucket" "schema_versions" {\n  name          = "${var.project_name}-schema-versions"\n  location      = var.region\n  force_destroy = false\n\n  versioning {\n    enabled = true\n  }\n\n  lifecycle_rule {\n    condition {\n      age = 90\n    }\n    action {\n      type = "Delete"\n    }\n  }\n\n  lifecycle_rule {\n    condition {\n      age = 30\n    }\n    action {\n      type          = "SetStorageClass"\n      storage_class = "NEARLINE"\n    }\n  }\n\n  uniform_bucket_level_access = true\n\n  labels = {\n    environment = var.environment\n    purpose     = "schema-versioning"\n  }\n}\n\nresource "google_storage_bucket_iam_member" "schema_writer" {\n  bucket = google_storage_bucket.schema_versions.name\n  role   = "roles/storage.objectCreator"\n  member = "serviceAccount:${google_service_account.app_sa.email}"\n}\n\nresource "google_storage_bucket_iam_member" "schema_reader" {\n  bucket = google_storage_bucket.schema_versions.name\n  role   = "roles/storage.objectViewer"\n  member = "serviceAccount:${google_service_account.app_sa.email}"\n}\n'})}),"\n",(0,s.jsx)(n.h2,{id:"8-best-practices",children:"8. Best Practices"}),"\n",(0,s.jsx)(n.h3,{id:"81-schema-change-workflow",children:"8.1 Schema Change Workflow"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-markdown",children:"# Database Schema Change Workflow\n\n## 1. Planning Phase\n\n- [ ] Document reason for schema change\n- [ ] Identify affected tables and relationships\n- [ ] Assess impact on application code\n- [ ] Plan migration strategy (if needed)\n- [ ] Consider backward compatibility\n\n## 2. Development Phase\n\n- [ ] Create feature branch: `git checkout -b feature/add-user-preferences`\n- [ ] Modify schema file (prisma/schema.prisma or entities)\n- [ ] Add version comment to schema\n- [ ] Generate migration: `npx prisma migrate dev --name add_user_preferences`\n- [ ] Update application code to use new schema\n- [ ] Write tests for schema changes\n\n## 3. Review Phase\n\n- [ ] Commit schema changes with descriptive message\n- [ ] Push branch and create pull request\n- [ ] CI runs schema validation\n- [ ] Review schema diff in PR\n- [ ] Team reviews changes\n- [ ] Address feedback\n\n## 4. Merge & Deploy\n\n- [ ] Merge to main branch after approval\n- [ ] CI automatically backs up current schema\n- [ ] CI runs migrations in staging\n- [ ] Verify staging deployment\n- [ ] Deploy to production with migration\n- [ ] Monitor for issues\n\n## 5. Post-Deployment\n\n- [ ] Verify schema applied correctly\n- [ ] Check application functionality\n- [ ] Monitor performance metrics\n- [ ] Document any issues\n- [ ] Update team on changes\n"})}),"\n",(0,s.jsx)(n.h3,{id:"82-schema-conflict-resolution-guide",children:"8.2 Schema Conflict Resolution Guide"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"/**\n * Common Schema Conflict Scenarios and Resolutions\n */\n\n// Scenario 1: Two developers add different columns to same table\n// Resolution: Accept both changes, merge columns\n\n// Before (base):\nmodel User {\n  id    Int    @id\n  email String\n}\n\n// Developer A adds:\nmodel User {\n  id    Int    @id\n  email String\n  name  String // Added by Dev A\n}\n\n// Developer B adds:\nmodel User {\n  id    Int    @id\n  email String\n  role  String // Added by Dev B\n}\n\n// Resolution (merge both):\nmodel User {\n  id    Int    @id\n  email String\n  name  String // From Dev A\n  role  String // From Dev B\n}\n\n// ============================================\n\n// Scenario 2: Same column added with different types\n// Resolution: Discuss with team, choose one type\n\n// Developer A adds:\nmodel Product {\n  price Decimal @db.Decimal(10, 2)\n}\n\n// Developer B adds:\nmodel Product {\n  price Int // Different type!\n}\n\n// Resolution: Use team standard (typically Decimal for currency)\nmodel Product {\n  price Decimal @db.Decimal(10, 2)\n}\n\n// ============================================\n\n// Scenario 3: Conflicting enum values\n// Resolution: Merge all unique enum values\n\n// Developer A adds:\nenum OrderStatus {\n  PENDING\n  SHIPPED\n  DELIVERED // Added by Dev A\n}\n\n// Developer B adds:\nenum OrderStatus {\n  PENDING\n  SHIPPED\n  CANCELLED // Added by Dev B\n}\n\n// Resolution (merge both):\nenum OrderStatus {\n  PENDING\n  SHIPPED\n  DELIVERED // From Dev A\n  CANCELLED // From Dev B\n}\n"})}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsx)(n.p,{children:"By implementing database schema version control with conflict detection, teams ensure database changes are tracked, reviewed, and merged safely, maintaining consistency between database schema and application code across all environments."})]})}function d(e={}){const{wrapper:n}={...(0,i.R)(),...e.components};return n?(0,s.jsx)(n,{...e,children:(0,s.jsx)(l,{...e})}):l(e)}}}]);