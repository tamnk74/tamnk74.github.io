"use strict";(globalThis.webpackChunkdocs_site=globalThis.webpackChunkdocs_site||[]).push([[20058],{28453(e,n,t){t.d(n,{R:()=>a,x:()=>o});var i=t(96540);const s={},r=i.createContext(s);function a(e){const n=i.useContext(r);return i.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:a(e.components),i.createElement(r.Provider,{value:n},e.children)}},42399(e,n,t){t.r(n),t.d(n,{assets:()=>d,contentTitle:()=>o,default:()=>m,frontMatter:()=>a,metadata:()=>i,toc:()=>l});const i=JSON.parse('{"id":"ai-machine-learning/ai-agent-huggingface-custom","title":"Building AI Document Wiki Agent with Custom Hugging Face Solution","description":"Build a fully customized AI documentation agent using Hugging Face models and transformers","source":"@site/docs/ai-machine-learning/ai-agent-huggingface-custom.md","sourceDirName":"ai-machine-learning","slug":"/ai-machine-learning/ai-agent-huggingface-custom","permalink":"/fullstack-dev/docs/ai-machine-learning/ai-agent-huggingface-custom","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/ai-machine-learning/ai-agent-huggingface-custom.md","tags":[{"inline":true,"label":"AI","permalink":"/fullstack-dev/docs/tags/ai"},{"inline":true,"label":"Hugging Face","permalink":"/fullstack-dev/docs/tags/hugging-face"},{"inline":true,"label":"Transformers","permalink":"/fullstack-dev/docs/tags/transformers"},{"inline":true,"label":"Custom Implementation","permalink":"/fullstack-dev/docs/tags/custom-implementation"}],"version":"current","frontMatter":{"id":"ai-agent-huggingface-custom","title":"Building AI Document Wiki Agent with Custom Hugging Face Solution","sidebar_label":"Option 3 - Custom HuggingFace","description":"Build a fully customized AI documentation agent using Hugging Face models and transformers","keywords":["AI agent","Hugging Face","transformers","custom solution","open source"],"tags":["AI","Hugging Face","Transformers","Custom Implementation"]},"sidebar":"tutorialSidebar","previous":{"title":"Option 2 - LlamaIndex + Local","permalink":"/fullstack-dev/docs/ai-machine-learning/ai-agent-llamaindex-local"},"next":{"title":"Option 4 - vLLM Production","permalink":"/fullstack-dev/docs/ai-machine-learning/ai-agent-vllm"}}');var s=t(74848),r=t(28453);const a={id:"ai-agent-huggingface-custom",title:"Building AI Document Wiki Agent with Custom Hugging Face Solution",sidebar_label:"Option 3 - Custom HuggingFace",description:"Build a fully customized AI documentation agent using Hugging Face models and transformers",keywords:["AI agent","Hugging Face","transformers","custom solution","open source"],tags:["AI","Hugging Face","Transformers","Custom Implementation"]},o="Building AI Document Wiki Agent with Custom Hugging Face Solution",d={},l=[{value:"Overview",id:"overview",level:2},{value:"Architecture Overview",id:"architecture-overview",level:2},{value:"System Architecture",id:"system-architecture",level:3},{value:"Query Flow Sequence",id:"query-flow-sequence",level:3},{value:"Component Diagram",id:"component-diagram",level:3},{value:"Data Flow - Custom Pipeline",id:"data-flow---custom-pipeline",level:3},{value:"Fine-tuning Pipeline",id:"fine-tuning-pipeline",level:3},{value:"Prerequisites",id:"prerequisites",level:2},{value:"Step 1: Project Setup",id:"step-1-project-setup",level:2},{value:"Step 2: Environment Configuration",id:"step-2-environment-configuration",level:2},{value:"Step 3: Custom Embedding Model",id:"step-3-custom-embedding-model",level:2},{value:"Step 4: Custom LLM Wrapper",id:"step-4-custom-llm-wrapper",level:2},{value:"Step 5: Document Loader",id:"step-5-document-loader",level:2},{value:"Step 6: FAISS Vector Store",id:"step-6-faiss-vector-store",level:2},{value:"Step 7: RAG Pipeline",id:"step-7-rag-pipeline",level:2},{value:"Step 8: Ingestion Script",id:"step-8-ingestion-script",level:2},{value:"Step 9: FastAPI Server",id:"step-9-fastapi-server",level:2},{value:"Usage",id:"usage",level:2},{value:"1. Install Dependencies",id:"1-install-dependencies",level:3},{value:"2. Ingest Documents",id:"2-ingest-documents",level:3},{value:"3. Run Server",id:"3-run-server",level:3},{value:"Advanced Customization",id:"advanced-customization",level:2},{value:"Fine-tune Embedding Model",id:"fine-tune-embedding-model",level:3},{value:"Use Different Vector Indices",id:"use-different-vector-indices",level:3},{value:"Next Steps",id:"next-steps",level:2},{value:"Resources",id:"resources",level:2}];function c(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",mermaid:"mermaid",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,r.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(n.header,{children:(0,s.jsx)(n.h1,{id:"building-ai-document-wiki-agent-with-custom-hugging-face-solution",children:"Building AI Document Wiki Agent with Custom Hugging Face Solution"})}),"\n",(0,s.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,s.jsxs)(n.p,{children:["This guide demonstrates building a ",(0,s.jsx)(n.strong,{children:"fully customized AI documentation agent"})," using ",(0,s.jsx)(n.strong,{children:"Hugging Face"})," models and the Transformers library. This approach gives you complete control over every aspect of the system."]}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Pros:"})}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"\u2705 Complete control and customization"}),"\n",(0,s.jsx)(n.li,{children:"\u2705 Can fine-tune models on your specific documentation"}),"\n",(0,s.jsx)(n.li,{children:"\u2705 Access to thousands of open-source models"}),"\n",(0,s.jsx)(n.li,{children:"\u2705 Can run completely locally or in cloud"}),"\n",(0,s.jsx)(n.li,{children:"\u2705 Highly optimizable for specific use cases"}),"\n",(0,s.jsx)(n.li,{children:"\u2705 No vendor lock-in"}),"\n"]}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Cons:"})}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"\u274c Requires more development and ML expertise"}),"\n",(0,s.jsx)(n.li,{children:"\u274c More complex setup and maintenance"}),"\n",(0,s.jsx)(n.li,{children:"\u274c Need to handle model hosting and scaling"}),"\n",(0,s.jsx)(n.li,{children:"\u274c Longer development time"}),"\n"]}),"\n",(0,s.jsx)(n.h2,{id:"architecture-overview",children:"Architecture Overview"}),"\n",(0,s.jsx)(n.h3,{id:"system-architecture",children:"System Architecture"}),"\n",(0,s.jsx)(n.mermaid,{value:'graph TB\n    subgraph "Client Layer"\n        CLI[CLI Interface]\n        API[FastAPI Server]\n        WEB[Web Interface]\n    end\n\n    subgraph "Custom Application Layer"\n        RAG[Custom RAG Pipeline]\n        QE[Query Handler]\n        CACHE[Query Cache]\n    end\n\n    subgraph "Model Layer"\n        EMBED[Custom Embedder<br/>SentenceTransformers]\n        LLM[Custom LLM<br/>Flan-T5/Mistral]\n        TOKENIZER[Tokenizer]\n    end\n\n    subgraph "Storage Layer"\n        FAISS[(FAISS Index<br/>Vector Store)]\n        META[Metadata Store<br/>JSON]\n        DOCS[\ud83d\udcc1 Documentation]\n    end\n\n    subgraph "Hugging Face Models"\n        HF_EMBED[sentence-transformers/<br/>all-MiniLM-L6-v2]\n        HF_LLM[google/flan-t5-xl<br/>or mistralai/Mistral-7B]\n    end\n\n    CLI --\x3e QE\n    API --\x3e QE\n    WEB --\x3e API\n\n    QE --\x3e RAG\n    QE --\x3e CACHE\n    RAG --\x3e EMBED\n    RAG --\x3e LLM\n    RAG --\x3e FAISS\n\n    EMBED -.->|loads from| HF_EMBED\n    LLM -.->|loads from| HF_LLM\n    LLM --\x3e TOKENIZER\n\n    DOCS --\x3e EMBED\n    EMBED --\x3e FAISS\n    FAISS --\x3e META\n\n    style HF_EMBED fill:#ff9d00\n    style HF_LLM fill:#ff9d00\n    style FAISS fill:#0066cc\n    style EMBED fill:#10b981\n    style LLM fill:#10b981'}),"\n",(0,s.jsx)(n.h3,{id:"query-flow-sequence",children:"Query Flow Sequence"}),"\n",(0,s.jsx)(n.mermaid,{value:"sequenceDiagram\n    participant User\n    participant API as FastAPI\n    participant RAG as RAG Pipeline\n    participant Cache as Query Cache\n    participant Embed as Custom Embedder\n    participant FAISS as FAISS Index\n    participant LLM as Custom LLM\n    participant GPU\n\n    User->>API: POST /api/query {question}\n    activate API\n\n    API->>Cache: check_cache(question)\n    activate Cache\n    Cache--\x3e>API: cache_miss\n    deactivate Cache\n\n    API->>RAG: query(question)\n    activate RAG\n\n    Note over RAG,Embed: 1. Embed Query\n    RAG->>Embed: embed_query(question)\n    activate Embed\n    Embed->>GPU: forward_pass\n    activate GPU\n    GPU--\x3e>Embed: embedding [384d]\n    deactivate GPU\n    Embed--\x3e>RAG: query_vector\n    deactivate Embed\n\n    Note over RAG,FAISS: 2. Vector Search\n    RAG->>FAISS: search(query_vector, k=4)\n    activate FAISS\n    FAISS->>FAISS: inner_product_search\n    FAISS--\x3e>RAG: top_k_documents + scores\n    deactivate FAISS\n\n    Note over RAG,LLM: 3. Generate Answer\n    RAG->>RAG: create_prompt(context, question)\n    RAG->>LLM: generate(prompt)\n    activate LLM\n    LLM->>GPU: forward_pass (with beam search)\n    activate GPU\n    GPU--\x3e>LLM: generated_tokens\n    deactivate GPU\n    LLM->>LLM: decode_tokens\n    LLM--\x3e>RAG: answer_text\n    deactivate LLM\n\n    RAG--\x3e>API: {answer, sources, metadata}\n    deactivate RAG\n\n    API->>Cache: add_to_cache(question, answer)\n    API--\x3e>User: {success, data}\n    deactivate API\n\n    Note over User,GPU: Fully customizable - can fine-tune all components"}),"\n",(0,s.jsx)(n.h3,{id:"component-diagram",children:"Component Diagram"}),"\n",(0,s.jsx)(n.mermaid,{value:'graph TB\n    subgraph "Custom Document Processing"\n        DL[Custom DocumentLoader]\n        DC[Custom DocumentChunker]\n        META_EXT[Metadata Extractor]\n    end\n\n    subgraph "Custom Embedding Layer"\n        EMB[SentenceTransformer]\n        BATCH[Batch Processor]\n        NORM[L2 Normalization]\n    end\n\n    subgraph "Custom Vector Store"\n        FAISS_IDX[FAISS IndexFlatIP]\n        FAISS_IVF[FAISS IndexIVFFlat<br/>Optional for large scale]\n        FAISS_HNSW[FAISS IndexHNSWFlat<br/>Optional for speed]\n    end\n\n    subgraph "Custom LLM Layer"\n        TOK[AutoTokenizer]\n        MODEL[AutoModelForSeq2SeqLM<br/>or AutoModelForCausalLM]\n        GEN[Generation Config]\n    end\n\n    subgraph "Custom RAG Pipeline"\n        PROMPT[Custom Prompt Builder]\n        RERANK[Optional Reranker]\n        SYNTH[Response Synthesizer]\n    end\n\n    DL --\x3e DC\n    DC --\x3e META_EXT\n    META_EXT --\x3e EMB\n\n    EMB --\x3e BATCH\n    BATCH --\x3e NORM\n    NORM --\x3e FAISS_IDX\n\n    FAISS_IDX -.->|can upgrade to| FAISS_IVF\n    FAISS_IDX -.->|can upgrade to| FAISS_HNSW\n\n    PROMPT --\x3e TOK\n    TOK --\x3e MODEL\n    MODEL --\x3e GEN\n\n    FAISS_IDX --\x3e PROMPT\n    RERANK -.->|optional| PROMPT\n    GEN --\x3e SYNTH\n\n    style EMB fill:#10b981\n    style MODEL fill:#10b981\n    style FAISS_IDX fill:#0066cc\n    style PROMPT fill:#f59e0b'}),"\n",(0,s.jsx)(n.h3,{id:"data-flow---custom-pipeline",children:"Data Flow - Custom Pipeline"}),"\n",(0,s.jsx)(n.mermaid,{value:'flowchart TD\n    START([Start Custom Agent]) --\x3e DOWNLOAD{Models<br/>Downloaded?}\n\n    DOWNLOAD --\x3e|No| HF_DOWNLOAD[Download from<br/>Hugging Face Hub]\n    DOWNLOAD --\x3e|Yes| CHECK_GPU\n    HF_DOWNLOAD --\x3e CHECK_GPU{GPU<br/>Available?}\n\n    CHECK_GPU --\x3e|Yes| LOAD_GPU[Load Models to GPU<br/>with FP16]\n    CHECK_GPU --\x3e|No| LOAD_CPU[Load Models to CPU<br/>with FP32]\n\n    LOAD_GPU --\x3e LOAD_DOCS\n    LOAD_CPU --\x3e LOAD_DOCS[Load Documents]\n\n    LOAD_DOCS --\x3e CUSTOM_CHUNK[Custom Chunking<br/>with overlap]\n    CUSTOM_CHUNK --\x3e BATCH_EMBED[Batch Embedding<br/>SentenceTransformers]\n    BATCH_EMBED --\x3e BUILD_INDEX{Choose Index<br/>Type}\n\n    BUILD_INDEX --\x3e|Small| FLAT[FAISS Flat<br/>Exact Search]\n    BUILD_INDEX --\x3e|Medium| IVF[FAISS IVF<br/>Fast Approximate]\n    BUILD_INDEX --\x3e|Large| HNSW[FAISS HNSW<br/>Fastest]\n\n    FLAT --\x3e SAVE\n    IVF --\x3e SAVE\n    HNSW --\x3e SAVE[Save Index + Metadata]\n\n    SAVE --\x3e READY[Agent Ready]\n    READY --\x3e QUERY_WAIT[Wait for Query]\n\n    QUERY_WAIT --\x3e USER_Q[User Query]\n    USER_Q --\x3e EMBED_Q[Embed Query]\n    EMBED_Q --\x3e SEARCH[Vector Search<br/>Inner Product]\n    SEARCH --\x3e RETRIEVE[Top-K Retrieval]\n    RETRIEVE --\x3e CUSTOM_PROMPT[Custom Prompt<br/>Engineering]\n    CUSTOM_PROMPT --\x3e GENERATE[Generate with<br/>Custom LLM]\n    GENERATE --\x3e POST_PROC[Post-processing]\n    POST_PROC --\x3e RESPONSE[Return Response]\n    RESPONSE --\x3e QUERY_WAIT\n\n    subgraph "Customizable Components"\n        CUSTOM_CHUNK\n        BATCH_EMBED\n        BUILD_INDEX\n        CUSTOM_PROMPT\n        GENERATE\n        POST_PROC\n    end\n\n    style START fill:#22c55e\n    style READY fill:#22c55e\n    style HF_DOWNLOAD fill:#ff9d00\n    style CUSTOM_CHUNK fill:#8b5cf6\n    style CUSTOM_PROMPT fill:#8b5cf6\n    style GENERATE fill:#10b981'}),"\n",(0,s.jsx)(n.h3,{id:"fine-tuning-pipeline",children:"Fine-tuning Pipeline"}),"\n",(0,s.jsx)(n.mermaid,{value:'flowchart LR\n    subgraph "Data Preparation"\n        RAW[Raw Documentation]\n        PAIRS[Generate Q&A Pairs]\n        SPLIT[Train/Val Split]\n    end\n\n    subgraph "Embedding Fine-tuning"\n        BASE_E[Base Embedding Model]\n        TRAIN_E[Training Loop]\n        EVAL_E[Evaluation]\n        FINE_E[Fine-tuned Embeddings]\n    end\n\n    subgraph "LLM Fine-tuning"\n        BASE_L[Base LLM]\n        LORA[LoRA Adapters]\n        TRAIN_L[Training Loop]\n        EVAL_L[Evaluation]\n        FINE_L[Fine-tuned LLM]\n    end\n\n    subgraph "Deployment"\n        QUANT[Quantization<br/>4-bit/8-bit]\n        OPTIMIZE[Optimization<br/>ONNX/TensorRT]\n        DEPLOY[Deploy Model]\n    end\n\n    RAW --\x3e PAIRS\n    PAIRS --\x3e SPLIT\n\n    SPLIT --\x3e BASE_E\n    BASE_E --\x3e TRAIN_E\n    TRAIN_E --\x3e EVAL_E\n    EVAL_E --\x3e FINE_E\n\n    SPLIT --\x3e BASE_L\n    BASE_L --\x3e LORA\n    LORA --\x3e TRAIN_L\n    TRAIN_L --\x3e EVAL_L\n    EVAL_L --\x3e FINE_L\n\n    FINE_E --\x3e QUANT\n    FINE_L --\x3e QUANT\n    QUANT --\x3e OPTIMIZE\n    OPTIMIZE --\x3e DEPLOY\n\n    style FINE_E fill:#22c55e\n    style FINE_L fill:#22c55e\n    style DEPLOY fill:#3b82f6'}),"\n",(0,s.jsx)(n.h2,{id:"prerequisites",children:"Prerequisites"}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Python 3.8+ with pip"}),"\n",(0,s.jsx)(n.li,{children:"CUDA-compatible GPU (recommended, 8GB+ VRAM)"}),"\n",(0,s.jsx)(n.li,{children:"32GB+ RAM recommended"}),"\n",(0,s.jsx)(n.li,{children:"Understanding of transformers and embeddings"}),"\n",(0,s.jsx)(n.li,{children:"Basic ML/AI knowledge"}),"\n"]}),"\n",(0,s.jsx)(n.h2,{id:"step-1-project-setup",children:"Step 1: Project Setup"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"# Create project\nmkdir project-doc-agent-custom\ncd project-doc-agent-custom\n\n# Create virtual environment\npython -m venv venv\nsource venv/bin/activate  # Windows: venv\\Scripts\\activate\n\n# Install dependencies\npip install transformers torch torchvision torchaudio\npip install sentence-transformers\npip install faiss-cpu  # or faiss-gpu for GPU support\npip install tiktoken\npip install fastapi uvicorn\npip install python-multipart\npip install pydantic\npip install python-dotenv\npip install numpy pandas\npip install pypdf python-docx markdown\n\n# Create structure\nmkdir -p src/{models,ingestion,retrieval,api,utils}\nmkdir -p data/{raw,processed,models}\nmkdir config\n"})}),"\n",(0,s.jsx)(n.h2,{id:"step-2-environment-configuration",children:"Step 2: Environment Configuration"}),"\n",(0,s.jsxs)(n.p,{children:["Create ",(0,s.jsx)(n.code,{children:".env"})," file:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"# Model Configuration\nEMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2\n# Alternatives:\n# - sentence-transformers/all-mpnet-base-v2 (better quality)\n# - BAAI/bge-small-en-v1.5 (good balance)\n\nLLM_MODEL=google/flan-t5-xl\n# Alternatives:\n# - google/flan-t5-large (smaller, faster)\n# - mistralai/Mistral-7B-Instruct-v0.2 (better quality, needs more resources)\n\n# Vector Store Configuration\nFAISS_INDEX_PATH=./data/faiss_index\nMETADATA_PATH=./data/metadata.json\n\n# Document Sources\nDOCS_PATH=./docs\nCODE_PATH=./src\nINCLUDE_CODE=false\n\n# Processing Configuration\nCHUNK_SIZE=512\nCHUNK_OVERLAP=50\nMAX_LENGTH=512\nBATCH_SIZE=8\n\n# API Configuration\nPORT=8000\nCORS_ORIGIN=*\n\n# Device Configuration\nDEVICE=cuda  # or 'cpu', 'mps' for Apple Silicon\nPRECISION=float16  # or 'float32' for CPU\n"})}),"\n",(0,s.jsx)(n.h2,{id:"step-3-custom-embedding-model",children:"Step 3: Custom Embedding Model"}),"\n",(0,s.jsxs)(n.p,{children:["Create ",(0,s.jsx)(n.code,{children:"src/models/embedder.py"}),":"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'import torch\nfrom sentence_transformers import SentenceTransformer\nfrom typing import List\nimport numpy as np\n\nclass DocumentEmbedder:\n    def __init__(self, model_name: str, device: str = "cuda"):\n        """Initialize embedding model"""\n        self.device = device if torch.cuda.is_available() else "cpu"\n        print(f"Loading embedding model: {model_name} on {self.device}")\n\n        self.model = SentenceTransformer(model_name, device=self.device)\n        self.embedding_dim = self.model.get_sentence_embedding_dimension()\n\n        print(f"Model loaded. Embedding dimension: {self.embedding_dim}")\n\n    def embed_documents(self, texts: List[str], batch_size: int = 32) -> np.ndarray:\n        """Embed a list of documents"""\n        print(f"Embedding {len(texts)} documents...")\n\n        embeddings = self.model.encode(\n            texts,\n            batch_size=batch_size,\n            show_progress_bar=True,\n            convert_to_numpy=True,\n            normalize_embeddings=True,\n        )\n\n        return embeddings\n\n    def embed_query(self, query: str) -> np.ndarray:\n        """Embed a single query"""\n        embedding = self.model.encode(\n            query,\n            convert_to_numpy=True,\n            normalize_embeddings=True,\n        )\n\n        return embedding\n\n    def get_embedding_dim(self) -> int:\n        """Get embedding dimension"""\n        return self.embedding_dim\n'})}),"\n",(0,s.jsx)(n.h2,{id:"step-4-custom-llm-wrapper",children:"Step 4: Custom LLM Wrapper"}),"\n",(0,s.jsxs)(n.p,{children:["Create ",(0,s.jsx)(n.code,{children:"src/models/llm.py"}),":"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'import torch\nfrom transformers import AutoTokenizer, AutoModelForSeq2SeqLM, AutoModelForCausalLM, pipeline\nfrom typing import List, Optional\n\nclass CustomLLM:\n    def __init__(\n        self,\n        model_name: str,\n        device: str = "cuda",\n        max_length: int = 512,\n        temperature: float = 0.7,\n    ):\n        """Initialize LLM"""\n        self.device = device if torch.cuda.is_available() else "cpu"\n        self.max_length = max_length\n        self.temperature = temperature\n\n        print(f"Loading LLM: {model_name} on {self.device}")\n\n        # Load tokenizer\n        self.tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n        # Determine model type and load accordingly\n        if "t5" in model_name.lower() or "flan" in model_name.lower():\n            self.model = AutoModelForSeq2SeqLM.from_pretrained(\n                model_name,\n                torch_dtype=torch.float16 if self.device == "cuda" else torch.float32,\n                low_cpu_mem_usage=True,\n            )\n            self.model_type = "seq2seq"\n        else:\n            self.model = AutoModelForCausalLM.from_pretrained(\n                model_name,\n                torch_dtype=torch.float16 if self.device == "cuda" else torch.float32,\n                low_cpu_mem_usage=True,\n            )\n            self.model_type = "causal"\n\n        self.model.to(self.device)\n        self.model.eval()\n\n        print(f"Model loaded. Type: {self.model_type}")\n\n    def generate(\n        self,\n        prompt: str,\n        max_new_tokens: int = 256,\n        temperature: Optional[float] = None,\n    ) -> str:\n        """Generate text from prompt"""\n        temp = temperature if temperature is not None else self.temperature\n\n        # Tokenize input\n        inputs = self.tokenizer(\n            prompt,\n            return_tensors="pt",\n            truncation=True,\n            max_length=self.max_length,\n        ).to(self.device)\n\n        # Generate\n        with torch.no_grad():\n            if self.model_type == "seq2seq":\n                outputs = self.model.generate(\n                    **inputs,\n                    max_new_tokens=max_new_tokens,\n                    temperature=temp,\n                    do_sample=temp > 0,\n                    top_p=0.9,\n                    num_beams=1,\n                )\n            else:\n                outputs = self.model.generate(\n                    **inputs,\n                    max_new_tokens=max_new_tokens,\n                    temperature=temp,\n                    do_sample=temp > 0,\n                    top_p=0.9,\n                    pad_token_id=self.tokenizer.eos_token_id,\n                )\n\n        # Decode\n        generated_text = self.tokenizer.decode(outputs[0], skip_special_tokens=True)\n\n        return generated_text\n\n    def __call__(self, prompt: str, **kwargs) -> str:\n        """Make the class callable"""\n        return self.generate(prompt, **kwargs)\n'})}),"\n",(0,s.jsx)(n.h2,{id:"step-5-document-loader",children:"Step 5: Document Loader"}),"\n",(0,s.jsxs)(n.p,{children:["Create ",(0,s.jsx)(n.code,{children:"src/ingestion/loader.py"}),":"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'import os\nfrom pathlib import Path\nfrom typing import List, Dict\nimport json\nimport pypdf\nimport markdown\nfrom docx import Document as DocxDocument\n\nclass Document:\n    def __init__(self, text: str, metadata: Dict):\n        self.text = text\n        self.metadata = metadata\n\nclass DocumentLoader:\n    def __init__(self, docs_path: str):\n        self.docs_path = Path(docs_path)\n\n    def load_text_file(self, file_path: Path) -> Document:\n        """Load a text file"""\n        with open(file_path, \'r\', encoding=\'utf-8\') as f:\n            text = f.read()\n\n        return Document(\n            text=text,\n            metadata={\n                "source": str(file_path),\n                "type": "text",\n                "extension": file_path.suffix,\n            }\n        )\n\n    def load_pdf_file(self, file_path: Path) -> Document:\n        """Load a PDF file"""\n        with open(file_path, \'rb\') as f:\n            pdf = pypdf.PdfReader(f)\n            text = ""\n            for page in pdf.pages:\n                text += page.extract_text()\n\n        return Document(\n            text=text,\n            metadata={\n                "source": str(file_path),\n                "type": "pdf",\n                "pages": len(pdf.pages),\n            }\n        )\n\n    def load_markdown_file(self, file_path: Path) -> Document:\n        """Load a markdown file"""\n        with open(file_path, \'r\', encoding=\'utf-8\') as f:\n            text = f.read()\n\n        # Convert markdown to plain text (optional)\n        # html = markdown.markdown(text)\n\n        return Document(\n            text=text,\n            metadata={\n                "source": str(file_path),\n                "type": "markdown",\n            }\n        )\n\n    def load_documents(self) -> List[Document]:\n        """Load all documents from directory"""\n        print(f"Loading documents from {self.docs_path}...")\n\n        documents = []\n\n        for file_path in self.docs_path.rglob("*"):\n            if not file_path.is_file():\n                continue\n\n            try:\n                if file_path.suffix in [".txt", ".text"]:\n                    doc = self.load_text_file(file_path)\n                    documents.append(doc)\n                elif file_path.suffix == ".pdf":\n                    doc = self.load_pdf_file(file_path)\n                    documents.append(doc)\n                elif file_path.suffix in [".md", ".markdown", ".mdx"]:\n                    doc = self.load_markdown_file(file_path)\n                    documents.append(doc)\n            except Exception as e:\n                print(f"Error loading {file_path}: {e}")\n\n        print(f"Loaded {len(documents)} documents")\n\n        return documents\n\nclass DocumentChunker:\n    def __init__(self, chunk_size: int = 512, chunk_overlap: int = 50):\n        self.chunk_size = chunk_size\n        self.chunk_overlap = chunk_overlap\n\n    def chunk_text(self, text: str) -> List[str]:\n        """Split text into overlapping chunks"""\n        words = text.split()\n        chunks = []\n\n        for i in range(0, len(words), self.chunk_size - self.chunk_overlap):\n            chunk = " ".join(words[i:i + self.chunk_size])\n            if chunk:\n                chunks.append(chunk)\n\n        return chunks\n\n    def chunk_documents(self, documents: List[Document]) -> List[Document]:\n        """Chunk all documents"""\n        print(f"Chunking {len(documents)} documents...")\n\n        chunked_docs = []\n\n        for doc in documents:\n            chunks = self.chunk_text(doc.text)\n\n            for i, chunk in enumerate(chunks):\n                chunked_doc = Document(\n                    text=chunk,\n                    metadata={\n                        **doc.metadata,\n                        "chunk_index": i,\n                        "total_chunks": len(chunks),\n                    }\n                )\n                chunked_docs.append(chunked_doc)\n\n        print(f"Created {len(chunked_docs)} chunks")\n\n        return chunked_docs\n'})}),"\n",(0,s.jsx)(n.h2,{id:"step-6-faiss-vector-store",children:"Step 6: FAISS Vector Store"}),"\n",(0,s.jsxs)(n.p,{children:["Create ",(0,s.jsx)(n.code,{children:"src/ingestion/vector_store.py"}),":"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'import faiss\nimport numpy as np\nimport json\nfrom pathlib import Path\nfrom typing import List, Tuple\nfrom ..models.embedder import DocumentEmbedder\n\nclass FAISSVectorStore:\n    def __init__(\n        self,\n        embedder: DocumentEmbedder,\n        index_path: str,\n        metadata_path: str,\n    ):\n        self.embedder = embedder\n        self.index_path = Path(index_path)\n        self.metadata_path = Path(metadata_path)\n        self.index = None\n        self.metadata = []\n        self.embedding_dim = embedder.get_embedding_dim()\n\n    def create_index(self, documents: List, batch_size: int = 32):\n        """Create FAISS index from documents"""\n        print("Creating FAISS index...")\n\n        # Extract texts\n        texts = [doc.text for doc in documents]\n\n        # Generate embeddings\n        embeddings = self.embedder.embed_documents(texts, batch_size=batch_size)\n\n        # Create FAISS index\n        self.index = faiss.IndexFlatIP(self.embedding_dim)  # Inner product (cosine sim)\n        self.index.add(embeddings.astype(\'float32\'))\n\n        # Store metadata\n        self.metadata = [doc.metadata for doc in documents]\n\n        # Save\n        self.save()\n\n        print(f"Index created with {self.index.ntotal} vectors")\n\n    def save(self):\n        """Save index and metadata to disk"""\n        self.index_path.parent.mkdir(parents=True, exist_ok=True)\n\n        # Save FAISS index\n        faiss.write_index(self.index, str(self.index_path))\n\n        # Save metadata\n        with open(self.metadata_path, \'w\') as f:\n            json.dump(self.metadata, f)\n\n        print(f"Index saved to {self.index_path}")\n\n    def load(self):\n        """Load index and metadata from disk"""\n        if not self.index_path.exists():\n            raise FileNotFoundError(f"Index not found at {self.index_path}")\n\n        # Load FAISS index\n        self.index = faiss.read_index(str(self.index_path))\n\n        # Load metadata\n        with open(self.metadata_path, \'r\') as f:\n            self.metadata = json.load(f)\n\n        print(f"Index loaded with {self.index.ntotal} vectors")\n\n    def search(self, query: str, k: int = 4) -> List[Tuple[str, Dict, float]]:\n        """Search for similar documents"""\n        # Embed query\n        query_embedding = self.embedder.embed_query(query)\n        query_embedding = query_embedding.reshape(1, -1).astype(\'float32\')\n\n        # Search\n        scores, indices = self.index.search(query_embedding, k)\n\n        # Prepare results\n        results = []\n        for score, idx in zip(scores[0], indices[0]):\n            if idx < len(self.metadata):\n                results.append((\n                    f"Document {idx}",  # You\'d load actual text here\n                    self.metadata[idx],\n                    float(score),\n                ))\n\n        return results\n'})}),"\n",(0,s.jsx)(n.h2,{id:"step-7-rag-pipeline",children:"Step 7: RAG Pipeline"}),"\n",(0,s.jsxs)(n.p,{children:["Create ",(0,s.jsx)(n.code,{children:"src/retrieval/rag_pipeline.py"}),":"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'from typing import List, Dict\nfrom ..models.llm import CustomLLM\nfrom ..ingestion.vector_store import FAISSVectorStore\n\nclass RAGPipeline:\n    def __init__(\n        self,\n        llm: CustomLLM,\n        vector_store: FAISSVectorStore,\n        k: int = 4,\n    ):\n        self.llm = llm\n        self.vector_store = vector_store\n        self.k = k\n\n    def create_prompt(self, question: str, context: List[str]) -> str:\n        """Create RAG prompt"""\n        context_str = "\\n\\n".join([\n            f"[Document {i+1}]\\n{text}"\n            for i, text in enumerate(context)\n        ])\n\n        prompt = f"""You are an AI assistant answering questions about a software project based on its documentation.\n\nContext:\n{context_str}\n\nQuestion: {question}\n\nInstructions:\n- Answer based only on the context provided\n- If you cannot find the answer in the context, say so clearly\n- Do not make up information\n- Be concise and accurate\n\nAnswer:"""\n\n        return prompt\n\n    def query(self, question: str) -> Dict:\n        """Query the RAG pipeline"""\n        print(f"\\nQuery: {question}")\n\n        # Retrieve relevant documents\n        results = self.vector_store.search(question, k=self.k)\n\n        # Extract context\n        context = [metadata.get("text", "") for _, metadata, _ in results]\n\n        # Create prompt\n        prompt = self.create_prompt(question, context)\n\n        # Generate answer\n        answer = self.llm.generate(prompt, max_new_tokens=256)\n\n        return {\n            "question": question,\n            "answer": answer,\n            "sources": [\n                {\n                    "metadata": metadata,\n                    "relevance": score,\n                }\n                for _, metadata, score in results\n            ],\n        }\n'})}),"\n",(0,s.jsx)(n.h2,{id:"step-8-ingestion-script",children:"Step 8: Ingestion Script"}),"\n",(0,s.jsxs)(n.p,{children:["Create ",(0,s.jsx)(n.code,{children:"src/ingest.py"}),":"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'import os\nfrom dotenv import load_dotenv\nfrom ingestion.loader import DocumentLoader, DocumentChunker, Document\nfrom ingestion.vector_store import FAISSVectorStore\nfrom models.embedder import DocumentEmbedder\n\nload_dotenv()\n\ndef main():\n    print("=== Starting Document Ingestion ===\\n")\n\n    # Configuration\n    docs_path = os.getenv("DOCS_PATH", "./docs")\n    embedding_model = os.getenv("EMBEDDING_MODEL", "sentence-transformers/all-MiniLM-L6-v2")\n    device = os.getenv("DEVICE", "cuda")\n    chunk_size = int(os.getenv("CHUNK_SIZE", "512"))\n    chunk_overlap = int(os.getenv("CHUNK_OVERLAP", "50"))\n\n    # Load documents\n    loader = DocumentLoader(docs_path)\n    documents = loader.load_documents()\n\n    # Chunk documents\n    chunker = DocumentChunker(chunk_size=chunk_size, chunk_overlap=chunk_overlap)\n    chunked_docs = chunker.chunk_documents(documents)\n\n    # Initialize embedder\n    embedder = DocumentEmbedder(embedding_model, device=device)\n\n    # Create vector store\n    vector_store = FAISSVectorStore(\n        embedder=embedder,\n        index_path=os.getenv("FAISS_INDEX_PATH", "./data/faiss_index"),\n        metadata_path=os.getenv("METADATA_PATH", "./data/metadata.json"),\n    )\n\n    vector_store.create_index(chunked_docs)\n\n    print("\\n=== Ingestion Complete ===")\n    print(f"\u2713 Processed {len(documents)} documents")\n    print(f"\u2713 Created {len(chunked_docs)} chunks")\n\nif __name__ == "__main__":\n    main()\n'})}),"\n",(0,s.jsx)(n.h2,{id:"step-9-fastapi-server",children:"Step 9: FastAPI Server"}),"\n",(0,s.jsxs)(n.p,{children:["Create ",(0,s.jsx)(n.code,{children:"src/api/server.py"}),":"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:'import os\nfrom dotenv import load_dotenv\nfrom fastapi import FastAPI, HTTPException\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom pydantic import BaseModel\nfrom typing import Optional\nimport uvicorn\n\nfrom models.embedder import DocumentEmbedder\nfrom models.llm import CustomLLM\nfrom ingestion.vector_store import FAISSVectorStore\nfrom retrieval.rag_pipeline import RAGPipeline\n\nload_dotenv()\n\napp = FastAPI(title="AI Document Wiki Agent (Custom)")\n\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=["*"],\n    allow_credentials=True,\n    allow_methods=["*"],\n    allow_headers=["*"],\n)\n\nclass QueryRequest(BaseModel):\n    question: str\n    sessionId: Optional[str] = None\n\n# Initialize components\nprint("Initializing AI Agent...")\n\nembedder = DocumentEmbedder(\n    model_name=os.getenv("EMBEDDING_MODEL"),\n    device=os.getenv("DEVICE", "cuda"),\n)\n\nllm = CustomLLM(\n    model_name=os.getenv("LLM_MODEL"),\n    device=os.getenv("DEVICE", "cuda"),\n    max_length=int(os.getenv("MAX_LENGTH", "512")),\n)\n\nvector_store = FAISSVectorStore(\n    embedder=embedder,\n    index_path=os.getenv("FAISS_INDEX_PATH"),\n    metadata_path=os.getenv("METADATA_PATH"),\n)\nvector_store.load()\n\nrag_pipeline = RAGPipeline(llm=llm, vector_store=vector_store, k=4)\n\nprint("\u2713 Agent ready!")\n\n@app.get("/health")\nasync def health():\n    return {"status": "ok"}\n\n@app.post("/api/query")\nasync def query(request: QueryRequest):\n    try:\n        result = rag_pipeline.query(request.question)\n        return {"success": True, "data": result}\n    except Exception as e:\n        raise HTTPException(status_code=500, detail=str(e))\n\nif __name__ == "__main__":\n    port = int(os.getenv("PORT", 8000))\n    uvicorn.run(app, host="0.0.0.0", port=port)\n'})}),"\n",(0,s.jsx)(n.h2,{id:"usage",children:"Usage"}),"\n",(0,s.jsx)(n.h3,{id:"1-install-dependencies",children:"1. Install Dependencies"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"pip install -r requirements.txt\n"})}),"\n",(0,s.jsx)(n.h3,{id:"2-ingest-documents",children:"2. Ingest Documents"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"python src/ingest.py\n"})}),"\n",(0,s.jsx)(n.h3,{id:"3-run-server",children:"3. Run Server"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"python src/api/server.py\n"})}),"\n",(0,s.jsx)(n.h2,{id:"advanced-customization",children:"Advanced Customization"}),"\n",(0,s.jsx)(n.h3,{id:"fine-tune-embedding-model",children:"Fine-tune Embedding Model"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:"from sentence_transformers import SentenceTransformer, InputExample, losses\nfrom torch.utils.data import DataLoader\n\n# Prepare training data\ntrain_examples = [\n    InputExample(texts=['query1', 'relevant doc1'], label=1.0),\n    InputExample(texts=['query2', 'irrelevant doc'], label=0.0),\n]\n\ntrain_dataloader = DataLoader(train_examples, shuffle=True, batch_size=16)\n\nmodel = SentenceTransformer('sentence-transformers/all-MiniLM-L6-v2')\ntrain_loss = losses.CosineSimilarityLoss(model)\n\nmodel.fit(\n    train_objectives=[(train_dataloader, train_loss)],\n    epochs=3,\n    warmup_steps=100,\n)\n\nmodel.save('./data/models/custom-embedder')\n"})}),"\n",(0,s.jsx)(n.h3,{id:"use-different-vector-indices",children:"Use Different Vector Indices"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",children:"# Use HNSW for faster search\nindex = faiss.IndexHNSWFlat(embedding_dim, 32)\nindex.hnsw.efConstruction = 40\n\n# Use IVF for large datasets\nquantizer = faiss.IndexFlatIP(embedding_dim)\nindex = faiss.IndexIVFFlat(quantizer, embedding_dim, 100)\n"})}),"\n",(0,s.jsx)(n.h2,{id:"next-steps",children:"Next Steps"}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"/fullstack-dev/docs/ai-machine-learning/ai-agent-document-wiki",children:"Compare All Options"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"/fullstack-dev/docs/ai-machine-learning/vllm-gcp-deployment",children:"Deployment Guide"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"/fullstack-dev/docs/ai-machine-learning/vertex-ai-gemini-guide",children:"Fine-tuning Guide"})}),"\n"]}),"\n",(0,s.jsx)(n.h2,{id:"resources",children:"Resources"}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"https://huggingface.co/docs/transformers/",children:"Hugging Face Transformers"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"https://www.sbert.net/",children:"Sentence Transformers"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"https://github.com/facebookresearch/faiss/wiki",children:"FAISS Documentation"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"https://huggingface.co/models",children:"Model Hub"})}),"\n"]})]})}function m(e={}){const{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,s.jsx)(n,{...e,children:(0,s.jsx)(c,{...e})}):c(e)}}}]);