"use strict";(globalThis.webpackChunkdocs_site=globalThis.webpackChunkdocs_site||[]).push([[10114],{12151(e,n,t){t.r(n),t.d(n,{assets:()=>c,contentTitle:()=>s,default:()=>m,frontMatter:()=>a,metadata:()=>r,toc:()=>d});const r=JSON.parse('{"id":"backend/webrtc-implementation","title":"WebRTC Implementation Guide","description":"Overview","source":"@site/docs/backend/webrtc-implementation.md","sourceDirName":"backend","slug":"/backend/webrtc-implementation","permalink":"/fullstack-dev/docs/backend/webrtc-implementation","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/webrtc-implementation.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"WebSocket for Real-Time Notifications","permalink":"/fullstack-dev/docs/backend/websocket-real-time-notifications"},"next":{"title":"Circuit Breakers & Rate Limiting Guide","permalink":"/fullstack-dev/docs/backend/circuit-breakers-rate-limiting"}}');var o=t(74848),i=t(28453);const a={},s="WebRTC Implementation Guide",c={},d=[{value:"Overview",id:"overview",level:2},{value:"Table of Contents",id:"table-of-contents",level:2},{value:"WebRTC Architecture",id:"webrtc-architecture",level:2},{value:"Core Components",id:"core-components",level:3},{value:"WebRTC Connection Flow",id:"webrtc-connection-flow",level:3},{value:"Signaling Server Implementation",id:"signaling-server-implementation",level:2},{value:"1. WebRTC Gateway Module",id:"1-webrtc-gateway-module",level:3},{value:"2. WebRTC Gateway Implementation",id:"2-webrtc-gateway-implementation",level:3},{value:"3. WebRTC Service",id:"3-webrtc-service",level:3},{value:"4. Room Service",id:"4-room-service",level:3},{value:"TURN/STUN Server Setup",id:"turnstun-server-setup",level:2},{value:"1. Coturn Configuration",id:"1-coturn-configuration",level:3},{value:"2. Kubernetes Deployment for Coturn",id:"2-kubernetes-deployment-for-coturn",level:3},{value:"3. ICE Server Configuration Service",id:"3-ice-server-configuration-service",level:3},{value:"Frontend Integration",id:"frontend-integration",level:2},{value:"1. React WebRTC Hook",id:"1-react-webrtc-hook",level:3},{value:"2. React Video Call Component",id:"2-react-video-call-component",level:3},{value:"Kubernetes Deployment",id:"kubernetes-deployment",level:2},{value:"1. WebRTC Service Deployment",id:"1-webrtc-service-deployment",level:3},{value:"Monitoring and Optimization",id:"monitoring-and-optimization",level:2},{value:"1. WebRTC Metrics Service",id:"1-webrtc-metrics-service",level:3},{value:"Best Practices",id:"best-practices",level:2},{value:"1. Connection Management",id:"1-connection-management",level:3},{value:"2. Media Quality",id:"2-media-quality",level:3},{value:"3. Security",id:"3-security",level:3},{value:"4. Scalability",id:"4-scalability",level:3},{value:"5. Performance Optimization",id:"5-performance-optimization",level:3}];function l(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",ol:"ol",p:"p",pre:"pre",ul:"ul",...(0,i.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(n.header,{children:(0,o.jsx)(n.h1,{id:"webrtc-implementation-guide",children:"WebRTC Implementation Guide"})}),"\n",(0,o.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,o.jsx)(n.p,{children:"WebRTC (Web Real-Time Communication) is an open-source project that enables real-time communication capabilities directly in web browsers and mobile applications. This guide covers implementing WebRTC for video calls, audio streaming, and data channels in a NestJS microservices architecture."}),"\n",(0,o.jsx)(n.h2,{id:"table-of-contents",children:"Table of Contents"}),"\n",(0,o.jsxs)(n.ol,{children:["\n",(0,o.jsx)(n.li,{children:(0,o.jsx)(n.a,{href:"#webrtc-architecture",children:"WebRTC Architecture"})}),"\n",(0,o.jsx)(n.li,{children:(0,o.jsx)(n.a,{href:"#signaling-server-implementation",children:"Signaling Server Implementation"})}),"\n",(0,o.jsx)(n.li,{children:(0,o.jsx)(n.a,{href:"#turnstun-server-setup",children:"TURN/STUN Server Setup"})}),"\n",(0,o.jsx)(n.li,{children:(0,o.jsx)(n.a,{href:"#frontend-integration",children:"Frontend Integration"})}),"\n",(0,o.jsx)(n.li,{children:(0,o.jsx)(n.a,{href:"#kubernetes-deployment",children:"Kubernetes Deployment"})}),"\n",(0,o.jsx)(n.li,{children:(0,o.jsx)(n.a,{href:"#monitoring-and-optimization",children:"Monitoring and Optimization"})}),"\n",(0,o.jsx)(n.li,{children:(0,o.jsx)(n.a,{href:"#best-practices",children:"Best Practices"})}),"\n"]}),"\n",(0,o.jsx)(n.h2,{id:"webrtc-architecture",children:"WebRTC Architecture"}),"\n",(0,o.jsx)(n.h3,{id:"core-components",children:"Core Components"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{children:"\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510                    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502   Client A      \u2502                    \u2502   Client B      \u2502\n\u2502   (Browser)     \u2502                    \u2502   (Browser)     \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518                    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n         \u2502                                      \u2502\n         \u2502  Signaling (WebSocket/Socket.io)    \u2502\n         \u2502                                      \u2502\n         \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n         \u2502              \u2502                       \u2502\n         \u2502      \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510             \u2502\n         \u2502      \u2502  Signaling     \u2502             \u2502\n         \u2502      \u2502  Server        \u2502             \u2502\n         \u2502      \u2502  (NestJS)      \u2502             \u2502\n         \u2502      \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518             \u2502\n         \u2502                                      \u2502\n         \u2502  STUN Request                        \u2502\n         \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n         \u2502              \u2502                       \u2502\n         \u2502      \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510             \u2502\n         \u2502      \u2502  STUN Server   \u2502             \u2502\n         \u2502      \u2502  (coturn)      \u2502             \u2502\n         \u2502      \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518             \u2502\n         \u2502                                      \u2502\n         \u2502  Direct P2P Connection (if possible) \u2502\n         \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n         \u2502      or via TURN Server              \u2502\n         \u2502                                      \u2502\n         \u2502      \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510             \u2502\n         \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2524  TURN Server   \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                \u2502  (coturn)      \u2502\n                \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n"})}),"\n",(0,o.jsx)(n.h3,{id:"webrtc-connection-flow",children:"WebRTC Connection Flow"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-mermaid",children:"sequenceDiagram\n    participant ClientA as Client A\n    participant Signal as Signaling Server\n    participant ClientB as Client B\n    participant STUN as STUN/TURN Server\n\n    ClientA->>Signal: Connect via WebSocket\n    ClientB->>Signal: Connect via WebSocket\n\n    ClientA->>ClientA: Create PeerConnection\n    ClientA->>STUN: Get ICE Candidates\n    STUN--\x3e>ClientA: Return ICE Candidates\n\n    ClientA->>ClientA: Create Offer (SDP)\n    ClientA->>Signal: Send Offer\n    Signal->>ClientB: Forward Offer\n\n    ClientB->>ClientB: Create PeerConnection\n    ClientB->>ClientB: Set Remote Description\n    ClientB->>STUN: Get ICE Candidates\n    STUN--\x3e>ClientB: Return ICE Candidates\n\n    ClientB->>ClientB: Create Answer (SDP)\n    ClientB->>Signal: Send Answer\n    Signal->>ClientA: Forward Answer\n\n    ClientA->>ClientA: Set Remote Description\n\n    ClientA->>ClientB: Exchange ICE Candidates\n    ClientB->>ClientA: Exchange ICE Candidates\n\n    ClientA->>ClientB: Establish P2P Connection\n    ClientB->>ClientA: Media Streaming Begins\n"})}),"\n",(0,o.jsx)(n.h2,{id:"signaling-server-implementation",children:"Signaling Server Implementation"}),"\n",(0,o.jsx)(n.h3,{id:"1-webrtc-gateway-module",children:"1. WebRTC Gateway Module"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"// services/webrtc-service/src/webrtc/webrtc-gateway.module.ts\nimport { Module } from '@nestjs/common';\nimport { WebRtcGateway } from './webrtc.gateway';\nimport { WebRtcService } from './webrtc.service';\nimport { RoomService } from './room.service';\nimport { RedisModule } from '@nestjs-modules/ioredis';\nimport { TypeOrmModule } from '@nestjs/typeorm';\nimport { Call } from './entities/call.entity';\nimport { Room } from './entities/room.entity';\nimport { Participant } from './entities/participant.entity';\n\n@Module({\n  imports: [\n    RedisModule.forRoot({\n      config: {\n        host: process.env.REDIS_HOST || 'localhost',\n        port: parseInt(process.env.REDIS_PORT || '6379'),\n      },\n    }),\n    TypeOrmModule.forFeature([Call, Room, Participant]),\n  ],\n  providers: [WebRtcGateway, WebRtcService, RoomService],\n  exports: [WebRtcService],\n})\nexport class WebRtcGatewayModule {}\n"})}),"\n",(0,o.jsx)(n.h3,{id:"2-webrtc-gateway-implementation",children:"2. WebRTC Gateway Implementation"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"// services/webrtc-service/src/webrtc/webrtc.gateway.ts\nimport {\n  WebSocketGateway,\n  WebSocketServer,\n  SubscribeMessage,\n  OnGatewayConnection,\n  OnGatewayDisconnect,\n  ConnectedSocket,\n  MessageBody,\n} from '@nestjs/websockets';\nimport { Server, Socket } from 'socket.io';\nimport { Logger, UseGuards } from '@nestjs/common';\nimport { WebRtcService } from './webrtc.service';\nimport { RoomService } from './room.service';\nimport { WsJwtGuard } from '../auth/ws-jwt.guard';\n\ninterface SignalData {\n  roomId: string;\n  userId: string;\n  signalData: any;\n  type: 'offer' | 'answer' | 'ice-candidate';\n}\n\n@WebSocketGateway({\n  cors: {\n    origin: process.env.CORS_ORIGIN || '*',\n    credentials: true,\n  },\n  namespace: '/webrtc',\n})\nexport class WebRtcGateway implements OnGatewayConnection, OnGatewayDisconnect {\n  @WebSocketServer()\n  server: Server;\n\n  private readonly logger = new Logger(WebRtcGateway.name);\n\n  constructor(private readonly webrtcService: WebRtcService, private readonly roomService: RoomService) {}\n\n  async handleConnection(client: Socket) {\n    try {\n      const userId = this.extractUserId(client);\n      this.logger.log(`Client connected: ${client.id}, User: ${userId}`);\n\n      // Store client connection\n      await this.webrtcService.addConnection(userId, client.id);\n    } catch (error) {\n      this.logger.error(`Connection error: ${error.message}`);\n      client.disconnect();\n    }\n  }\n\n  async handleDisconnect(client: Socket) {\n    try {\n      const userId = this.extractUserId(client);\n      this.logger.log(`Client disconnected: ${client.id}, User: ${userId}`);\n\n      // Clean up connection and notify room participants\n      const rooms = await this.webrtcService.getUserRooms(userId);\n\n      for (const room of rooms) {\n        await this.handleLeaveRoom(client, { roomId: room.id });\n      }\n\n      await this.webrtcService.removeConnection(userId, client.id);\n    } catch (error) {\n      this.logger.error(`Disconnect error: ${error.message}`);\n    }\n  }\n\n  @UseGuards(WsJwtGuard)\n  @SubscribeMessage('join-room')\n  async handleJoinRoom(\n    @ConnectedSocket() client: Socket,\n    @MessageBody() data: { roomId: string; userId: string; userName: string },\n  ) {\n    try {\n      const { roomId, userId, userName } = data;\n\n      // Join socket room\n      await client.join(roomId);\n\n      // Add participant to room\n      const room = await this.roomService.addParticipant(roomId, userId, userName);\n\n      // Get existing participants\n      const participants = await this.roomService.getParticipants(roomId);\n\n      // Notify existing participants about new user\n      client.to(roomId).emit('user-joined', {\n        userId,\n        userName,\n        participants: participants.filter((p) => p.userId !== userId),\n      });\n\n      // Send current participants to new user\n      client.emit('room-joined', {\n        roomId,\n        participants: participants.filter((p) => p.userId !== userId),\n      });\n\n      this.logger.log(`User ${userId} joined room ${roomId}`);\n\n      return { success: true, room };\n    } catch (error) {\n      this.logger.error(`Join room error: ${error.message}`);\n      return { success: false, error: error.message };\n    }\n  }\n\n  @UseGuards(WsJwtGuard)\n  @SubscribeMessage('leave-room')\n  async handleLeaveRoom(@ConnectedSocket() client: Socket, @MessageBody() data: { roomId: string }) {\n    try {\n      const userId = this.extractUserId(client);\n      const { roomId } = data;\n\n      // Leave socket room\n      await client.leave(roomId);\n\n      // Remove participant from room\n      await this.roomService.removeParticipant(roomId, userId);\n\n      // Notify other participants\n      client.to(roomId).emit('user-left', { userId });\n\n      this.logger.log(`User ${userId} left room ${roomId}`);\n\n      return { success: true };\n    } catch (error) {\n      this.logger.error(`Leave room error: ${error.message}`);\n      return { success: false, error: error.message };\n    }\n  }\n\n  @UseGuards(WsJwtGuard)\n  @SubscribeMessage('signal')\n  async handleSignal(@ConnectedSocket() client: Socket, @MessageBody() data: SignalData) {\n    try {\n      const { roomId, userId, signalData, type } = data;\n\n      this.logger.debug(`Signal received: ${type} from ${userId} in room ${roomId}`);\n\n      // Forward signal to specific user or broadcast to room\n      if (signalData.targetUserId) {\n        // Send to specific user\n        const targetSocketId = await this.webrtcService.getConnectionId(signalData.targetUserId);\n        if (targetSocketId) {\n          this.server.to(targetSocketId).emit('signal', {\n            type,\n            signalData,\n            fromUserId: userId,\n          });\n        }\n      } else {\n        // Broadcast to room (excluding sender)\n        client.to(roomId).emit('signal', {\n          type,\n          signalData,\n          fromUserId: userId,\n        });\n      }\n\n      return { success: true };\n    } catch (error) {\n      this.logger.error(`Signal error: ${error.message}`);\n      return { success: false, error: error.message };\n    }\n  }\n\n  @UseGuards(WsJwtGuard)\n  @SubscribeMessage('offer')\n  async handleOffer(\n    @ConnectedSocket() client: Socket,\n    @MessageBody() data: { roomId: string; targetUserId: string; offer: RTCSessionDescriptionInit },\n  ) {\n    try {\n      const userId = this.extractUserId(client);\n      const { roomId, targetUserId, offer } = data;\n\n      this.logger.debug(`Offer from ${userId} to ${targetUserId} in room ${roomId}`);\n\n      const targetSocketId = await this.webrtcService.getConnectionId(targetUserId);\n      if (targetSocketId) {\n        this.server.to(targetSocketId).emit('offer', {\n          fromUserId: userId,\n          offer,\n        });\n      }\n\n      return { success: true };\n    } catch (error) {\n      this.logger.error(`Offer error: ${error.message}`);\n      return { success: false, error: error.message };\n    }\n  }\n\n  @UseGuards(WsJwtGuard)\n  @SubscribeMessage('answer')\n  async handleAnswer(\n    @ConnectedSocket() client: Socket,\n    @MessageBody() data: { roomId: string; targetUserId: string; answer: RTCSessionDescriptionInit },\n  ) {\n    try {\n      const userId = this.extractUserId(client);\n      const { roomId, targetUserId, answer } = data;\n\n      this.logger.debug(`Answer from ${userId} to ${targetUserId} in room ${roomId}`);\n\n      const targetSocketId = await this.webrtcService.getConnectionId(targetUserId);\n      if (targetSocketId) {\n        this.server.to(targetSocketId).emit('answer', {\n          fromUserId: userId,\n          answer,\n        });\n      }\n\n      return { success: true };\n    } catch (error) {\n      this.logger.error(`Answer error: ${error.message}`);\n      return { success: false, error: error.message };\n    }\n  }\n\n  @UseGuards(WsJwtGuard)\n  @SubscribeMessage('ice-candidate')\n  async handleIceCandidate(\n    @ConnectedSocket() client: Socket,\n    @MessageBody() data: { roomId: string; targetUserId: string; candidate: RTCIceCandidateInit },\n  ) {\n    try {\n      const userId = this.extractUserId(client);\n      const { roomId, targetUserId, candidate } = data;\n\n      const targetSocketId = await this.webrtcService.getConnectionId(targetUserId);\n      if (targetSocketId) {\n        this.server.to(targetSocketId).emit('ice-candidate', {\n          fromUserId: userId,\n          candidate,\n        });\n      }\n\n      return { success: true };\n    } catch (error) {\n      this.logger.error(`ICE candidate error: ${error.message}`);\n      return { success: false, error: error.message };\n    }\n  }\n\n  @UseGuards(WsJwtGuard)\n  @SubscribeMessage('media-state')\n  async handleMediaState(\n    @ConnectedSocket() client: Socket,\n    @MessageBody() data: { roomId: string; audio: boolean; video: boolean },\n  ) {\n    try {\n      const userId = this.extractUserId(client);\n      const { roomId, audio, video } = data;\n\n      // Update participant media state\n      await this.roomService.updateParticipantMedia(roomId, userId, { audio, video });\n\n      // Notify other participants\n      client.to(roomId).emit('media-state-changed', {\n        userId,\n        audio,\n        video,\n      });\n\n      return { success: true };\n    } catch (error) {\n      this.logger.error(`Media state error: ${error.message}`);\n      return { success: false, error: error.message };\n    }\n  }\n\n  @UseGuards(WsJwtGuard)\n  @SubscribeMessage('screen-share')\n  async handleScreenShare(\n    @ConnectedSocket() client: Socket,\n    @MessageBody() data: { roomId: string; isSharing: boolean },\n  ) {\n    try {\n      const userId = this.extractUserId(client);\n      const { roomId, isSharing } = data;\n\n      // Notify other participants\n      client.to(roomId).emit('screen-share-changed', {\n        userId,\n        isSharing,\n      });\n\n      return { success: true };\n    } catch (error) {\n      this.logger.error(`Screen share error: ${error.message}`);\n      return { success: false, error: error.message };\n    }\n  }\n\n  private extractUserId(client: Socket): string {\n    // Extract user ID from JWT token or socket handshake\n    return client.handshake.auth?.userId || (client.handshake.query?.userId as string);\n  }\n}\n"})}),"\n",(0,o.jsx)(n.h3,{id:"3-webrtc-service",children:"3. WebRTC Service"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"// services/webrtc-service/src/webrtc/webrtc.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRedis } from '@nestjs-modules/ioredis';\nimport { Redis } from 'ioredis';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { Call } from './entities/call.entity';\n\n@Injectable()\nexport class WebRtcService {\n  private readonly logger = new Logger(WebRtcService.name);\n  private readonly CONNECTION_PREFIX = 'webrtc:connection:';\n  private readonly ROOM_PREFIX = 'webrtc:room:';\n\n  constructor(\n    @InjectRedis() private readonly redis: Redis,\n    @InjectRepository(Call)\n    private readonly callRepository: Repository<Call>,\n  ) {}\n\n  async addConnection(userId: string, socketId: string): Promise<void> {\n    const key = `${this.CONNECTION_PREFIX}${userId}`;\n    await this.redis.set(key, socketId, 'EX', 3600); // 1 hour expiry\n  }\n\n  async removeConnection(userId: string, socketId: string): Promise<void> {\n    const key = `${this.CONNECTION_PREFIX}${userId}`;\n    await this.redis.del(key);\n  }\n\n  async getConnectionId(userId: string): Promise<string | null> {\n    const key = `${this.CONNECTION_PREFIX}${userId}`;\n    return await this.redis.get(key);\n  }\n\n  async getUserRooms(userId: string): Promise<any[]> {\n    const pattern = `${this.ROOM_PREFIX}*`;\n    const keys = await this.redis.keys(pattern);\n    const rooms = [];\n\n    for (const key of keys) {\n      const members = await this.redis.smembers(key);\n      if (members.includes(userId)) {\n        const roomId = key.replace(this.ROOM_PREFIX, '');\n        rooms.push({ id: roomId, members });\n      }\n    }\n\n    return rooms;\n  }\n\n  async createCall(callData: {\n    roomId: string;\n    callerId: string;\n    calleeId: string;\n    type: 'audio' | 'video';\n  }): Promise<Call> {\n    const call = this.callRepository.create({\n      ...callData,\n      status: 'initiated',\n      startedAt: new Date(),\n    });\n\n    return await this.callRepository.save(call);\n  }\n\n  async updateCallStatus(\n    callId: string,\n    status: 'initiated' | 'ringing' | 'connected' | 'ended' | 'failed',\n  ): Promise<void> {\n    await this.callRepository.update(callId, { status });\n\n    if (status === 'ended') {\n      await this.callRepository.update(callId, { endedAt: new Date() });\n    }\n  }\n\n  async getCallHistory(userId: string, limit: number = 50): Promise<Call[]> {\n    return await this.callRepository.find({\n      where: [{ callerId: userId }, { calleeId: userId }],\n      order: { startedAt: 'DESC' },\n      take: limit,\n    });\n  }\n}\n"})}),"\n",(0,o.jsx)(n.h3,{id:"4-room-service",children:"4. Room Service"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"// services/webrtc-service/src/webrtc/room.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRedis } from '@nestjs-modules/ioredis';\nimport { Redis } from 'ioredis';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { Room } from './entities/room.entity';\nimport { Participant } from './entities/participant.entity';\n\n@Injectable()\nexport class RoomService {\n  private readonly logger = new Logger(RoomService.name);\n  private readonly ROOM_PREFIX = 'webrtc:room:';\n  private readonly ROOM_PARTICIPANTS_PREFIX = 'webrtc:room:participants:';\n\n  constructor(\n    @InjectRedis() private readonly redis: Redis,\n    @InjectRepository(Room)\n    private readonly roomRepository: Repository<Room>,\n    @InjectRepository(Participant)\n    private readonly participantRepository: Repository<Participant>,\n  ) {}\n\n  async createRoom(roomData: {\n    name: string;\n    createdBy: string;\n    maxParticipants?: number;\n    isPrivate?: boolean;\n  }): Promise<Room> {\n    const room = this.roomRepository.create({\n      ...roomData,\n      maxParticipants: roomData.maxParticipants || 10,\n      isPrivate: roomData.isPrivate || false,\n      isActive: true,\n    });\n\n    const savedRoom = await this.roomRepository.save(room);\n\n    // Store in Redis for quick access\n    await this.redis.set(\n      `${this.ROOM_PREFIX}${savedRoom.id}`,\n      JSON.stringify(savedRoom),\n      'EX',\n      86400, // 24 hours\n    );\n\n    return savedRoom;\n  }\n\n  async addParticipant(roomId: string, userId: string, userName: string): Promise<Room> {\n    const room = await this.roomRepository.findOne({ where: { id: roomId } });\n    if (!room) {\n      throw new Error('Room not found');\n    }\n\n    const participantCount = await this.getParticipantCount(roomId);\n    if (participantCount >= room.maxParticipants) {\n      throw new Error('Room is full');\n    }\n\n    // Add to Redis set\n    const participantKey = `${this.ROOM_PARTICIPANTS_PREFIX}${roomId}`;\n    const participantData = JSON.stringify({\n      userId,\n      userName,\n      joinedAt: new Date(),\n      audio: true,\n      video: true,\n    });\n\n    await this.redis.sadd(participantKey, participantData);\n    await this.redis.expire(participantKey, 86400);\n\n    // Save to database\n    const participant = this.participantRepository.create({\n      roomId,\n      userId,\n      userName,\n      joinedAt: new Date(),\n    });\n\n    await this.participantRepository.save(participant);\n\n    return room;\n  }\n\n  async removeParticipant(roomId: string, userId: string): Promise<void> {\n    const participantKey = `${this.ROOM_PARTICIPANTS_PREFIX}${roomId}`;\n    const participants = await this.redis.smembers(participantKey);\n\n    for (const participant of participants) {\n      const data = JSON.parse(participant);\n      if (data.userId === userId) {\n        await this.redis.srem(participantKey, participant);\n        break;\n      }\n    }\n\n    // Update database\n    await this.participantRepository.update({ roomId, userId, leftAt: null }, { leftAt: new Date() });\n\n    // Check if room is empty\n    const count = await this.getParticipantCount(roomId);\n    if (count === 0) {\n      await this.deactivateRoom(roomId);\n    }\n  }\n\n  async getParticipants(roomId: string): Promise<any[]> {\n    const participantKey = `${this.ROOM_PARTICIPANTS_PREFIX}${roomId}`;\n    const participants = await this.redis.smembers(participantKey);\n    return participants.map((p) => JSON.parse(p));\n  }\n\n  async getParticipantCount(roomId: string): Promise<number> {\n    const participantKey = `${this.ROOM_PARTICIPANTS_PREFIX}${roomId}`;\n    return await this.redis.scard(participantKey);\n  }\n\n  async updateParticipantMedia(\n    roomId: string,\n    userId: string,\n    media: { audio?: boolean; video?: boolean },\n  ): Promise<void> {\n    const participantKey = `${this.ROOM_PARTICIPANTS_PREFIX}${roomId}`;\n    const participants = await this.redis.smembers(participantKey);\n\n    for (const participant of participants) {\n      const data = JSON.parse(participant);\n      if (data.userId === userId) {\n        const updated = { ...data, ...media };\n        await this.redis.srem(participantKey, participant);\n        await this.redis.sadd(participantKey, JSON.stringify(updated));\n        break;\n      }\n    }\n  }\n\n  async deactivateRoom(roomId: string): Promise<void> {\n    await this.roomRepository.update(roomId, { isActive: false });\n    await this.redis.del(`${this.ROOM_PREFIX}${roomId}`);\n    await this.redis.del(`${this.ROOM_PARTICIPANTS_PREFIX}${roomId}`);\n  }\n\n  async getActiveRooms(): Promise<Room[]> {\n    return await this.roomRepository.find({\n      where: { isActive: true },\n      order: { createdAt: 'DESC' },\n    });\n  }\n}\n"})}),"\n",(0,o.jsx)(n.h2,{id:"turnstun-server-setup",children:"TURN/STUN Server Setup"}),"\n",(0,o.jsx)(n.h3,{id:"1-coturn-configuration",children:"1. Coturn Configuration"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-ini",children:"# /etc/turnserver.conf\nlistening-port=3478\ntls-listening-port=5349\n\n# External IP (replace with your server's public IP)\nexternal-ip=YOUR_PUBLIC_IP\n\n# Relay IP (usually same as external IP)\nrelay-ip=YOUR_PUBLIC_IP\n\n# Listening IPs\nlistening-ip=0.0.0.0\n\n# Realm\nrealm=yourwebrtcdomain.com\n\n# Server name\nserver-name=turnserver\n\n# Authentication\nlt-cred-mech\nuser=username:password\n\n# SSL certificates for TLS (optional but recommended)\ncert=/etc/coturn/certs/cert.pem\npkey=/etc/coturn/certs/key.pem\n\n# Logging\nverbose\nlog-file=/var/log/turnserver.log\n\n# Security\nno-cli\nno-loopback-peers\nno-multicast-peers\n\n# Performance\nmax-bps=500000\ntotal-quota=0\nstale-nonce\n\n# Fingerprint\nfingerprint\n\n# Ports range for relay\nmin-port=49152\nmax-port=65535\n"})}),"\n",(0,o.jsx)(n.h3,{id:"2-kubernetes-deployment-for-coturn",children:"2. Kubernetes Deployment for Coturn"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-yaml",children:"# k8s/webrtc/coturn-deployment.yaml\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: coturn-config\n  namespace: webrtc\ndata:\n  turnserver.conf: |\n    listening-port=3478\n    tls-listening-port=5349\n    external-ip=EXTERNAL_IP_PLACEHOLDER\n    relay-ip=EXTERNAL_IP_PLACEHOLDER\n    listening-ip=0.0.0.0\n    realm=yourwebrtcdomain.com\n    server-name=turnserver\n    lt-cred-mech\n    user=webrtc:secretpassword\n    verbose\n    log-file=/var/log/turnserver.log\n    no-cli\n    no-loopback-peers\n    no-multicast-peers\n    fingerprint\n    min-port=49152\n    max-port=65535\n---\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: coturn\n  namespace: webrtc\nspec:\n  replicas: 2\n  selector:\n    matchLabels:\n      app: coturn\n  template:\n    metadata:\n      labels:\n        app: coturn\n    spec:\n      containers:\n        - name: coturn\n          image: coturn/coturn:latest\n          ports:\n            - containerPort: 3478\n              protocol: UDP\n              name: turn-udp\n            - containerPort: 3478\n              protocol: TCP\n              name: turn-tcp\n            - containerPort: 5349\n              protocol: TCP\n              name: turn-tls\n            - containerPort: 49152\n              protocol: UDP\n              name: relay-start\n            - containerPort: 65535\n              protocol: UDP\n              name: relay-end\n          volumeMounts:\n            - name: coturn-config\n              mountPath: /etc/coturn\n              readOnly: true\n          resources:\n            requests:\n              memory: '256Mi'\n              cpu: '250m'\n            limits:\n              memory: '512Mi'\n              cpu: '500m'\n      volumes:\n        - name: coturn-config\n          configMap:\n            name: coturn-config\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: coturn-service\n  namespace: webrtc\nspec:\n  type: LoadBalancer\n  selector:\n    app: coturn\n  ports:\n    - name: turn-udp\n      port: 3478\n      targetPort: 3478\n      protocol: UDP\n    - name: turn-tcp\n      port: 3478\n      targetPort: 3478\n      protocol: TCP\n    - name: turn-tls\n      port: 5349\n      targetPort: 5349\n      protocol: TCP\n"})}),"\n",(0,o.jsx)(n.h3,{id:"3-ice-server-configuration-service",children:"3. ICE Server Configuration Service"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"// services/webrtc-service/src/webrtc/ice-server.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { ConfigService } from '@nestjs/config';\n\nexport interface IceServer {\n  urls: string | string[];\n  username?: string;\n  credential?: string;\n  credentialType?: 'password' | 'oauth';\n}\n\n@Injectable()\nexport class IceServerService {\n  constructor(private readonly configService: ConfigService) {}\n\n  getIceServers(): IceServer[] {\n    const stunServers = this.getStunServers();\n    const turnServers = this.getTurnServers();\n\n    return [...stunServers, ...turnServers];\n  }\n\n  private getStunServers(): IceServer[] {\n    return [\n      {\n        urls: 'stun:stun.l.google.com:19302',\n      },\n      {\n        urls: 'stun:stun1.l.google.com:19302',\n      },\n      {\n        urls: this.configService.get<string>('STUN_SERVER_URL') || 'stun:your-stun-server.com:3478',\n      },\n    ];\n  }\n\n  private getTurnServers(): IceServer[] {\n    const turnUrl = this.configService.get<string>('TURN_SERVER_URL');\n    const turnUsername = this.configService.get<string>('TURN_USERNAME');\n    const turnPassword = this.configService.get<string>('TURN_PASSWORD');\n\n    if (!turnUrl || !turnUsername || !turnPassword) {\n      return [];\n    }\n\n    return [\n      {\n        urls: [`turn:${turnUrl}:3478`, `turn:${turnUrl}:3478?transport=tcp`],\n        username: turnUsername,\n        credential: turnPassword,\n        credentialType: 'password',\n      },\n      {\n        urls: [`turns:${turnUrl}:5349`],\n        username: turnUsername,\n        credential: turnPassword,\n        credentialType: 'password',\n      },\n    ];\n  }\n\n  // Generate time-limited TURN credentials using HMAC\n  generateTurnCredentials(username: string, ttl: number = 86400): { username: string; credential: string } {\n    const timestamp = Math.floor(Date.now() / 1000) + ttl;\n    const tempUsername = `${timestamp}:${username}`;\n\n    const crypto = require('crypto');\n    const secret = this.configService.get<string>('TURN_SECRET');\n    const hmac = crypto.createHmac('sha1', secret);\n    hmac.update(tempUsername);\n    const credential = hmac.digest('base64');\n\n    return {\n      username: tempUsername,\n      credential,\n    };\n  }\n}\n"})}),"\n",(0,o.jsx)(n.h2,{id:"frontend-integration",children:"Frontend Integration"}),"\n",(0,o.jsx)(n.h3,{id:"1-react-webrtc-hook",children:"1. React WebRTC Hook"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"// frontend/src/hooks/useWebRTC.ts\nimport { useEffect, useRef, useState, useCallback } from 'react';\nimport { io, Socket } from 'socket.io-client';\n\nexport interface UseWebRTCOptions {\n  roomId: string;\n  userId: string;\n  userName: string;\n  token: string;\n  serverUrl: string;\n  iceServers?: RTCIceServer[];\n}\n\nexport interface Participant {\n  userId: string;\n  userName: string;\n  stream?: MediaStream;\n  audio: boolean;\n  video: boolean;\n}\n\nexport const useWebRTC = (options: UseWebRTCOptions) => {\n  const [socket, setSocket] = useState<Socket | null>(null);\n  const [localStream, setLocalStream] = useState<MediaStream | null>(null);\n  const [participants, setParticipants] = useState<Map<string, Participant>>(new Map());\n  const [isConnected, setIsConnected] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n\n  const peerConnections = useRef<Map<string, RTCPeerConnection>>(new Map());\n  const { roomId, userId, userName, token, serverUrl, iceServers } = options;\n\n  // Initialize socket connection\n  useEffect(() => {\n    const newSocket = io(`${serverUrl}/webrtc`, {\n      auth: { token, userId },\n      transports: ['websocket'],\n    });\n\n    newSocket.on('connect', () => {\n      console.log('Connected to signaling server');\n      setIsConnected(true);\n    });\n\n    newSocket.on('disconnect', () => {\n      console.log('Disconnected from signaling server');\n      setIsConnected(false);\n    });\n\n    newSocket.on('connect_error', (err) => {\n      console.error('Connection error:', err);\n      setError(err.message);\n    });\n\n    setSocket(newSocket);\n\n    return () => {\n      newSocket.close();\n    };\n  }, [serverUrl, token, userId]);\n\n  // Get local media stream\n  const getLocalStream = useCallback(async (audio: boolean = true, video: boolean = true) => {\n    try {\n      const stream = await navigator.mediaDevices.getUserMedia({\n        audio,\n        video: video ? { width: 1280, height: 720 } : false,\n      });\n      setLocalStream(stream);\n      return stream;\n    } catch (err) {\n      console.error('Error accessing media devices:', err);\n      setError('Failed to access camera/microphone');\n      throw err;\n    }\n  }, []);\n\n  // Create peer connection\n  const createPeerConnection = useCallback(\n    (targetUserId: string): RTCPeerConnection => {\n      const config: RTCConfiguration = {\n        iceServers: iceServers || [{ urls: 'stun:stun.l.google.com:19302' }],\n      };\n\n      const pc = new RTCPeerConnection(config);\n\n      // Add local stream tracks\n      if (localStream) {\n        localStream.getTracks().forEach((track) => {\n          pc.addTrack(track, localStream);\n        });\n      }\n\n      // Handle ICE candidates\n      pc.onicecandidate = (event) => {\n        if (event.candidate && socket) {\n          socket.emit('ice-candidate', {\n            roomId,\n            targetUserId,\n            candidate: event.candidate,\n          });\n        }\n      };\n\n      // Handle remote stream\n      pc.ontrack = (event) => {\n        console.log('Received remote track:', event.track.kind);\n        setParticipants((prev) => {\n          const updated = new Map(prev);\n          const participant = updated.get(targetUserId);\n          if (participant) {\n            participant.stream = event.streams[0];\n            updated.set(targetUserId, participant);\n          }\n          return updated;\n        });\n      };\n\n      // Handle connection state changes\n      pc.onconnectionstatechange = () => {\n        console.log(`Connection state with ${targetUserId}:`, pc.connectionState);\n        if (pc.connectionState === 'failed' || pc.connectionState === 'disconnected') {\n          // Handle reconnection logic here\n        }\n      };\n\n      peerConnections.current.set(targetUserId, pc);\n      return pc;\n    },\n    [localStream, socket, roomId, iceServers],\n  );\n\n  // Join room\n  const joinRoom = useCallback(async () => {\n    if (!socket || !localStream) {\n      console.error('Socket or local stream not ready');\n      return;\n    }\n\n    socket.emit('join-room', { roomId, userId, userName });\n\n    // Handle room joined\n    socket.on('room-joined', async ({ participants: existingParticipants }) => {\n      console.log('Room joined, existing participants:', existingParticipants);\n\n      // Create peer connections and send offers to existing participants\n      for (const participant of existingParticipants) {\n        const pc = createPeerConnection(participant.userId);\n        const offer = await pc.createOffer();\n        await pc.setLocalDescription(offer);\n\n        socket.emit('offer', {\n          roomId,\n          targetUserId: participant.userId,\n          offer,\n        });\n\n        setParticipants((prev) =>\n          new Map(prev).set(participant.userId, {\n            ...participant,\n            stream: undefined,\n          }),\n        );\n      }\n    });\n\n    // Handle new user joined\n    socket.on('user-joined', ({ userId: newUserId, userName: newUserName }) => {\n      console.log('New user joined:', newUserId);\n      setParticipants((prev) =>\n        new Map(prev).set(newUserId, {\n          userId: newUserId,\n          userName: newUserName,\n          audio: true,\n          video: true,\n        }),\n      );\n    });\n\n    // Handle offer\n    socket.on('offer', async ({ fromUserId, offer }) => {\n      console.log('Received offer from:', fromUserId);\n      const pc = createPeerConnection(fromUserId);\n      await pc.setRemoteDescription(new RTCSessionDescription(offer));\n      const answer = await pc.createAnswer();\n      await pc.setLocalDescription(answer);\n\n      socket.emit('answer', {\n        roomId,\n        targetUserId: fromUserId,\n        answer,\n      });\n    });\n\n    // Handle answer\n    socket.on('answer', async ({ fromUserId, answer }) => {\n      console.log('Received answer from:', fromUserId);\n      const pc = peerConnections.current.get(fromUserId);\n      if (pc) {\n        await pc.setRemoteDescription(new RTCSessionDescription(answer));\n      }\n    });\n\n    // Handle ICE candidate\n    socket.on('ice-candidate', async ({ fromUserId, candidate }) => {\n      const pc = peerConnections.current.get(fromUserId);\n      if (pc) {\n        await pc.addIceCandidate(new RTCIceCandidate(candidate));\n      }\n    });\n\n    // Handle user left\n    socket.on('user-left', ({ userId: leftUserId }) => {\n      console.log('User left:', leftUserId);\n      const pc = peerConnections.current.get(leftUserId);\n      if (pc) {\n        pc.close();\n        peerConnections.current.delete(leftUserId);\n      }\n      setParticipants((prev) => {\n        const updated = new Map(prev);\n        updated.delete(leftUserId);\n        return updated;\n      });\n    });\n\n    // Handle media state changes\n    socket.on('media-state-changed', ({ userId: changedUserId, audio, video }) => {\n      setParticipants((prev) => {\n        const updated = new Map(prev);\n        const participant = updated.get(changedUserId);\n        if (participant) {\n          participant.audio = audio;\n          participant.video = video;\n          updated.set(changedUserId, participant);\n        }\n        return updated;\n      });\n    });\n  }, [socket, localStream, roomId, userId, userName, createPeerConnection]);\n\n  // Leave room\n  const leaveRoom = useCallback(() => {\n    if (socket) {\n      socket.emit('leave-room', { roomId });\n    }\n\n    // Close all peer connections\n    peerConnections.current.forEach((pc) => pc.close());\n    peerConnections.current.clear();\n\n    // Stop local stream\n    if (localStream) {\n      localStream.getTracks().forEach((track) => track.stop());\n    }\n\n    setParticipants(new Map());\n  }, [socket, roomId, localStream]);\n\n  // Toggle audio\n  const toggleAudio = useCallback(() => {\n    if (localStream) {\n      const audioTrack = localStream.getAudioTracks()[0];\n      if (audioTrack) {\n        audioTrack.enabled = !audioTrack.enabled;\n        socket?.emit('media-state', {\n          roomId,\n          audio: audioTrack.enabled,\n          video: localStream.getVideoTracks()[0]?.enabled || false,\n        });\n      }\n    }\n  }, [localStream, socket, roomId]);\n\n  // Toggle video\n  const toggleVideo = useCallback(() => {\n    if (localStream) {\n      const videoTrack = localStream.getVideoTracks()[0];\n      if (videoTrack) {\n        videoTrack.enabled = !videoTrack.enabled;\n        socket?.emit('media-state', {\n          roomId,\n          audio: localStream.getAudioTracks()[0]?.enabled || false,\n          video: videoTrack.enabled,\n        });\n      }\n    }\n  }, [localStream, socket, roomId]);\n\n  // Screen sharing\n  const startScreenShare = useCallback(async () => {\n    try {\n      const screenStream = await navigator.mediaDevices.getDisplayMedia({\n        video: true,\n      });\n\n      // Replace video track in all peer connections\n      const screenTrack = screenStream.getVideoTracks()[0];\n      peerConnections.current.forEach((pc) => {\n        const sender = pc.getSenders().find((s) => s.track?.kind === 'video');\n        if (sender) {\n          sender.replaceTrack(screenTrack);\n        }\n      });\n\n      socket?.emit('screen-share', { roomId, isSharing: true });\n\n      // Handle screen share stopped\n      screenTrack.onended = () => {\n        stopScreenShare();\n      };\n\n      return screenStream;\n    } catch (err) {\n      console.error('Error starting screen share:', err);\n      setError('Failed to start screen sharing');\n      throw err;\n    }\n  }, [socket, roomId]);\n\n  const stopScreenShare = useCallback(() => {\n    if (localStream) {\n      const videoTrack = localStream.getVideoTracks()[0];\n      peerConnections.current.forEach((pc) => {\n        const sender = pc.getSenders().find((s) => s.track?.kind === 'video');\n        if (sender && videoTrack) {\n          sender.replaceTrack(videoTrack);\n        }\n      });\n    }\n\n    socket?.emit('screen-share', { roomId, isSharing: false });\n  }, [localStream, socket, roomId]);\n\n  return {\n    localStream,\n    participants,\n    isConnected,\n    error,\n    getLocalStream,\n    joinRoom,\n    leaveRoom,\n    toggleAudio,\n    toggleVideo,\n    startScreenShare,\n    stopScreenShare,\n  };\n};\n"})}),"\n",(0,o.jsx)(n.h3,{id:"2-react-video-call-component",children:"2. React Video Call Component"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"// frontend/src/components/VideoCall.tsx\nimport React, { useEffect, useRef, useState } from 'react';\nimport { useWebRTC } from '../hooks/useWebRTC';\n\ninterface VideoCallProps {\n  roomId: string;\n  userId: string;\n  userName: string;\n  token: string;\n  onLeave?: () => void;\n}\n\nexport const VideoCall: React.FC<VideoCallProps> = ({ roomId, userId, userName, token, onLeave }) => {\n  const {\n    localStream,\n    participants,\n    isConnected,\n    error,\n    getLocalStream,\n    joinRoom,\n    leaveRoom,\n    toggleAudio,\n    toggleVideo,\n    startScreenShare,\n    stopScreenShare,\n  } = useWebRTC({\n    roomId,\n    userId,\n    userName,\n    token,\n    serverUrl: process.env.REACT_APP_WEBRTC_SERVER_URL || 'http://localhost:3001',\n  });\n\n  const localVideoRef = useRef<HTMLVideoElement>(null);\n  const [isAudioEnabled, setIsAudioEnabled] = useState(true);\n  const [isVideoEnabled, setIsVideoEnabled] = useState(true);\n  const [isScreenSharing, setIsScreenSharing] = useState(false);\n\n  // Initialize and join room\n  useEffect(() => {\n    const init = async () => {\n      try {\n        await getLocalStream(true, true);\n      } catch (err) {\n        console.error('Failed to get local stream:', err);\n      }\n    };\n\n    init();\n\n    return () => {\n      leaveRoom();\n    };\n  }, []);\n\n  // Set local video stream\n  useEffect(() => {\n    if (localVideoRef.current && localStream) {\n      localVideoRef.current.srcObject = localStream;\n    }\n  }, [localStream]);\n\n  // Join room when local stream is ready\n  useEffect(() => {\n    if (localStream && isConnected) {\n      joinRoom();\n    }\n  }, [localStream, isConnected]);\n\n  const handleToggleAudio = () => {\n    toggleAudio();\n    setIsAudioEnabled((prev) => !prev);\n  };\n\n  const handleToggleVideo = () => {\n    toggleVideo();\n    setIsVideoEnabled((prev) => !prev);\n  };\n\n  const handleScreenShare = async () => {\n    if (isScreenSharing) {\n      stopScreenShare();\n      setIsScreenSharing(false);\n    } else {\n      try {\n        await startScreenShare();\n        setIsScreenSharing(true);\n      } catch (err) {\n        console.error('Screen share failed:', err);\n      }\n    }\n  };\n\n  const handleLeave = () => {\n    leaveRoom();\n    if (onLeave) {\n      onLeave();\n    }\n  };\n\n  if (error) {\n    return (\n      <div className=\"flex items-center justify-center h-screen bg-gray-900 text-white\">\n        <div className=\"text-center\">\n          <h2 className=\"text-2xl font-bold mb-4\">Error</h2>\n          <p>{error}</p>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"flex flex-col h-screen bg-gray-900\">\n      {/* Header */}\n      <div className=\"flex items-center justify-between p-4 bg-gray-800\">\n        <h1 className=\"text-white text-xl font-bold\">Room: {roomId}</h1>\n        <div className=\"flex space-x-2\">\n          <span className={`px-3 py-1 rounded ${isConnected ? 'bg-green-500' : 'bg-red-500'} text-white`}>\n            {isConnected ? 'Connected' : 'Disconnected'}\n          </span>\n        </div>\n      </div>\n\n      {/* Video Grid */}\n      <div className=\"flex-1 grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 p-4 overflow-auto\">\n        {/* Local Video */}\n        <div className=\"relative bg-gray-800 rounded-lg overflow-hidden\">\n          <video ref={localVideoRef} autoPlay playsInline muted className=\"w-full h-full object-cover\" />\n          <div className=\"absolute bottom-2 left-2 bg-black bg-opacity-50 text-white px-2 py-1 rounded\">\n            {userName} (You)\n          </div>\n          {!isVideoEnabled && (\n            <div className=\"absolute inset-0 flex items-center justify-center bg-gray-700\">\n              <div className=\"text-white text-4xl\">\ud83d\udcf7</div>\n            </div>\n          )}\n        </div>\n\n        {/* Remote Videos */}\n        {Array.from(participants.values()).map((participant) => (\n          <ParticipantVideo key={participant.userId} participant={participant} />\n        ))}\n      </div>\n\n      {/* Controls */}\n      <div className=\"flex items-center justify-center space-x-4 p-6 bg-gray-800\">\n        <button\n          onClick={handleToggleAudio}\n          className={`p-4 rounded-full ${\n            isAudioEnabled ? 'bg-gray-600' : 'bg-red-600'\n          } text-white hover:opacity-80 transition`}\n          title={isAudioEnabled ? 'Mute' : 'Unmute'}\n        >\n          {isAudioEnabled ? '\ud83c\udfa4' : '\ud83d\udd07'}\n        </button>\n\n        <button\n          onClick={handleToggleVideo}\n          className={`p-4 rounded-full ${\n            isVideoEnabled ? 'bg-gray-600' : 'bg-red-600'\n          } text-white hover:opacity-80 transition`}\n          title={isVideoEnabled ? 'Stop Video' : 'Start Video'}\n        >\n          {isVideoEnabled ? '\ud83d\udcf9' : '\ud83d\udcf7'}\n        </button>\n\n        <button\n          onClick={handleScreenShare}\n          className={`p-4 rounded-full ${\n            isScreenSharing ? 'bg-blue-600' : 'bg-gray-600'\n          } text-white hover:opacity-80 transition`}\n          title={isScreenSharing ? 'Stop Sharing' : 'Share Screen'}\n        >\n          \ud83d\udda5\ufe0f\n        </button>\n\n        <button\n          onClick={handleLeave}\n          className=\"px-6 py-4 rounded-full bg-red-600 text-white hover:bg-red-700 transition font-bold\"\n        >\n          Leave Call\n        </button>\n      </div>\n    </div>\n  );\n};\n\n// Participant Video Component\ninterface ParticipantVideoProps {\n  participant: {\n    userId: string;\n    userName: string;\n    stream?: MediaStream;\n    audio: boolean;\n    video: boolean;\n  };\n}\n\nconst ParticipantVideo: React.FC<ParticipantVideoProps> = ({ participant }) => {\n  const videoRef = useRef<HTMLVideoElement>(null);\n\n  useEffect(() => {\n    if (videoRef.current && participant.stream) {\n      videoRef.current.srcObject = participant.stream;\n    }\n  }, [participant.stream]);\n\n  return (\n    <div className=\"relative bg-gray-800 rounded-lg overflow-hidden\">\n      <video ref={videoRef} autoPlay playsInline className=\"w-full h-full object-cover\" />\n      <div className=\"absolute bottom-2 left-2 bg-black bg-opacity-50 text-white px-2 py-1 rounded flex items-center space-x-2\">\n        <span>{participant.userName}</span>\n        {!participant.audio && <span className=\"text-red-500\">\ud83d\udd07</span>}\n      </div>\n      {!participant.video && (\n        <div className=\"absolute inset-0 flex items-center justify-center bg-gray-700\">\n          <div className=\"text-white text-4xl\">\ud83d\udc64</div>\n        </div>\n      )}\n    </div>\n  );\n};\n"})}),"\n",(0,o.jsx)(n.h2,{id:"kubernetes-deployment",children:"Kubernetes Deployment"}),"\n",(0,o.jsx)(n.h3,{id:"1-webrtc-service-deployment",children:"1. WebRTC Service Deployment"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-yaml",children:"# k8s/webrtc/webrtc-service-deployment.yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: webrtc-service\n  namespace: webrtc\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: webrtc-service\n  template:\n    metadata:\n      labels:\n        app: webrtc-service\n    spec:\n      containers:\n        - name: webrtc-service\n          image: gcr.io/YOUR_PROJECT/webrtc-service:latest\n          env:\n            - name: NODE_ENV\n              value: 'production'\n            - name: REDIS_HOST\n              value: 'redis-service.webrtc.svc.cluster.local'\n            - name: REDIS_PORT\n              value: '6379'\n            - name: DATABASE_URL\n              valueFrom:\n                secretKeyRef:\n                  name: webrtc-secrets\n                  key: database-url\n            - name: JWT_SECRET\n              valueFrom:\n                secretKeyRef:\n                  name: webrtc-secrets\n                  key: jwt-secret\n            - name: TURN_SERVER_URL\n              value: 'coturn-service.webrtc.svc.cluster.local'\n            - name: TURN_USERNAME\n              valueFrom:\n                secretKeyRef:\n                  name: webrtc-secrets\n                  key: turn-username\n            - name: TURN_PASSWORD\n              valueFrom:\n                secretKeyRef:\n                  name: webrtc-secrets\n                  key: turn-password\n            - name: CORS_ORIGIN\n              value: 'https://your-frontend-domain.com'\n          ports:\n            - containerPort: 3000\n              name: http\n            - containerPort: 3001\n              name: websocket\n          resources:\n            requests:\n              memory: '512Mi'\n              cpu: '250m'\n            limits:\n              memory: '1Gi'\n              cpu: '500m'\n          livenessProbe:\n            httpGet:\n              path: /health\n              port: 3000\n            initialDelaySeconds: 30\n            periodSeconds: 10\n          readinessProbe:\n            httpGet:\n              path: /health\n              port: 3000\n            initialDelaySeconds: 10\n            periodSeconds: 5\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: webrtc-service\n  namespace: webrtc\nspec:\n  type: LoadBalancer\n  selector:\n    app: webrtc-service\n  ports:\n    - name: http\n      port: 80\n      targetPort: 3000\n    - name: websocket\n      port: 3001\n      targetPort: 3001\n  sessionAffinity: ClientIP\n  sessionAffinityConfig:\n    clientIP:\n      timeoutSeconds: 3600\n"})}),"\n",(0,o.jsx)(n.h2,{id:"monitoring-and-optimization",children:"Monitoring and Optimization"}),"\n",(0,o.jsx)(n.h3,{id:"1-webrtc-metrics-service",children:"1. WebRTC Metrics Service"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-typescript",children:"// services/webrtc-service/src/monitoring/webrtc-metrics.service.ts\nimport { Injectable } from '@nestjs/common';\nimport { Counter, Histogram, Gauge, register } from 'prom-client';\n\n@Injectable()\nexport class WebRtcMetricsService {\n  private readonly activeConnections = new Gauge({\n    name: 'webrtc_active_connections',\n    help: 'Number of active WebRTC connections',\n    labelNames: ['room'],\n  });\n\n  private readonly callsTotal = new Counter({\n    name: 'webrtc_calls_total',\n    help: 'Total number of WebRTC calls',\n    labelNames: ['type', 'status'],\n  });\n\n  private readonly callDuration = new Histogram({\n    name: 'webrtc_call_duration_seconds',\n    help: 'Duration of WebRTC calls in seconds',\n    labelNames: ['room'],\n    buckets: [10, 30, 60, 300, 600, 1800, 3600],\n  });\n\n  private readonly signalLatency = new Histogram({\n    name: 'webrtc_signal_latency_seconds',\n    help: 'Latency of signaling messages',\n    labelNames: ['type'],\n    buckets: [0.001, 0.005, 0.01, 0.05, 0.1, 0.5, 1],\n  });\n\n  recordActiveConnection(roomId: string, count: number): void {\n    this.activeConnections.set({ room: roomId }, count);\n  }\n\n  recordCall(type: 'audio' | 'video', status: 'initiated' | 'connected' | 'ended' | 'failed'): void {\n    this.callsTotal.inc({ type, status });\n  }\n\n  recordCallDuration(roomId: string, duration: number): void {\n    this.callDuration.observe({ room: roomId }, duration);\n  }\n\n  recordSignalLatency(type: string, latency: number): void {\n    this.signalLatency.observe({ type }, latency);\n  }\n\n  getMetrics(): string {\n    return register.metrics();\n  }\n}\n"})}),"\n",(0,o.jsx)(n.h2,{id:"best-practices",children:"Best Practices"}),"\n",(0,o.jsx)(n.h3,{id:"1-connection-management",children:"1. Connection Management"}),"\n",(0,o.jsxs)(n.ul,{children:["\n",(0,o.jsx)(n.li,{children:"Implement connection health checks"}),"\n",(0,o.jsx)(n.li,{children:"Handle reconnection logic for unstable networks"}),"\n",(0,o.jsx)(n.li,{children:"Use session affinity for load balancers"}),"\n",(0,o.jsx)(n.li,{children:"Implement proper cleanup on disconnect"}),"\n"]}),"\n",(0,o.jsx)(n.h3,{id:"2-media-quality",children:"2. Media Quality"}),"\n",(0,o.jsxs)(n.ul,{children:["\n",(0,o.jsx)(n.li,{children:"Implement adaptive bitrate based on network conditions"}),"\n",(0,o.jsx)(n.li,{children:"Use VP9 or H.264 codecs for better compression"}),"\n",(0,o.jsx)(n.li,{children:"Implement simulcast for multi-party calls"}),"\n",(0,o.jsx)(n.li,{children:"Monitor packet loss and adjust quality"}),"\n"]}),"\n",(0,o.jsx)(n.h3,{id:"3-security",children:"3. Security"}),"\n",(0,o.jsxs)(n.ul,{children:["\n",(0,o.jsx)(n.li,{children:"Always use HTTPS for signaling"}),"\n",(0,o.jsx)(n.li,{children:"Implement proper authentication and authorization"}),"\n",(0,o.jsx)(n.li,{children:"Use DTLS-SRTP for media encryption"}),"\n",(0,o.jsx)(n.li,{children:"Validate all signaling messages"}),"\n",(0,o.jsx)(n.li,{children:"Implement rate limiting"}),"\n"]}),"\n",(0,o.jsx)(n.h3,{id:"4-scalability",children:"4. Scalability"}),"\n",(0,o.jsxs)(n.ul,{children:["\n",(0,o.jsx)(n.li,{children:"Use Redis for session state management"}),"\n",(0,o.jsx)(n.li,{children:"Implement horizontal scaling for signaling servers"}),"\n",(0,o.jsx)(n.li,{children:"Use multiple TURN servers for load distribution"}),"\n",(0,o.jsx)(n.li,{children:"Consider using SFU for large conferences"}),"\n"]}),"\n",(0,o.jsx)(n.h3,{id:"5-performance-optimization",children:"5. Performance Optimization"}),"\n",(0,o.jsxs)(n.ul,{children:["\n",(0,o.jsx)(n.li,{children:"Enable gzip compression for signaling"}),"\n",(0,o.jsx)(n.li,{children:"Use WebSocket binary frames when possible"}),"\n",(0,o.jsx)(n.li,{children:"Implement connection pooling"}),"\n",(0,o.jsx)(n.li,{children:"Monitor and optimize bandwidth usage"}),"\n",(0,o.jsx)(n.li,{children:"Use CDN for STUN/TURN server distribution"}),"\n"]}),"\n",(0,o.jsx)(n.p,{children:"This comprehensive guide covers the essential aspects of implementing WebRTC in a production environment. Adjust the configurations and code samples based on your specific requirements and infrastructure."})]})}function m(e={}){const{wrapper:n}={...(0,i.R)(),...e.components};return n?(0,o.jsx)(n,{...e,children:(0,o.jsx)(l,{...e})}):l(e)}},28453(e,n,t){t.d(n,{R:()=>a,x:()=>s});var r=t(96540);const o={},i=r.createContext(o);function a(e){const n=r.useContext(i);return r.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function s(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:a(e.components),r.createElement(i.Provider,{value:n},e.children)}}}]);