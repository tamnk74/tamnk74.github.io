"use strict";(self.webpackChunkdocs_site=self.webpackChunkdocs_site||[]).push([[40591],{5741:(e,n,t)=>{t.d(n,{R:()=>s,x:()=>o});var r=t(59729);const a={},i=r.createContext(a);function s(e){const n=r.useContext(i);return r.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(a):e.components||a:s(e.components),r.createElement(i.Provider,{value:n},e.children)}},66555:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>d,contentTitle:()=>o,default:()=>p,frontMatter:()=>s,metadata:()=>r,toc:()=>c});const r=JSON.parse('{"id":"backend/data-consistency-saga-pattern","title":"Data Consistency and Saga Pattern Guide","description":"Overview","source":"@site/docs/backend/data-consistency-saga-pattern.md","sourceDirName":"backend","slug":"/backend/data-consistency-saga-pattern","permalink":"/fullstack-dev/docs/backend/data-consistency-saga-pattern","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/data-consistency-saga-pattern.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Distributed Tracing and Context Propagation Guide","permalink":"/fullstack-dev/docs/backend/distributed-tracing-context-propagation"},"next":{"title":"Event Sourcing and Outbox Pattern Guide","permalink":"/fullstack-dev/docs/backend/event-sourcing-outbox-pattern"}}');var a=t(65813),i=t(5741);const s={},o="Data Consistency and Saga Pattern Guide",d={},c=[{value:"Overview",id:"overview",level:2},{value:"Table of Contents",id:"table-of-contents",level:2},{value:"Data Consistency Challenges",id:"data-consistency-challenges",level:2},{value:"The Distributed Transaction Problem",id:"the-distributed-transaction-problem",level:3},{value:"CAP Theorem Trade-offs",id:"cap-theorem-trade-offs",level:3},{value:"Saga Pattern Fundamentals",id:"saga-pattern-fundamentals",level:2},{value:"What is a Saga?",id:"what-is-a-saga",level:3},{value:"Saga Types",id:"saga-types",level:3},{value:"Order Saga Example",id:"order-saga-example",level:3},{value:"Choreography-Based Saga",id:"choreography-based-saga",level:2},{value:"Architecture",id:"architecture",level:3},{value:"Implementation",id:"implementation",level:3},{value:"Orchestration-Based Saga",id:"orchestration-based-saga",level:2},{value:"Architecture with Temporal",id:"architecture-with-temporal",level:3},{value:"Temporal Workflow Implementation",id:"temporal-workflow-implementation",level:3},{value:"Compensation and Rollback",id:"compensation-and-rollback",level:2},{value:"Compensation Patterns",id:"compensation-patterns",level:3},{value:"Saga with Compensation Manager",id:"saga-with-compensation-manager",level:3},{value:"Event Sourcing Integration",id:"event-sourcing-integration",level:2},{value:"Event Store Implementation",id:"event-store-implementation",level:3},{value:"Order Aggregate with Event Sourcing",id:"order-aggregate-with-event-sourcing",level:3},{value:"Monitoring and Debugging",id:"monitoring-and-debugging",level:2},{value:"Saga State Tracking",id:"saga-state-tracking",level:3},{value:"Saga Dashboard Metrics",id:"saga-dashboard-metrics",level:3},{value:"Best Practices",id:"best-practices",level:2},{value:"1. Saga Design Principles",id:"1-saga-design-principles",level:3},{value:"2. Choosing Between Choreography and Orchestration",id:"2-choosing-between-choreography-and-orchestration",level:3},{value:"3. Error Handling",id:"3-error-handling",level:3},{value:"4. Data Consistency",id:"4-data-consistency",level:3},{value:"5. Testing Strategies",id:"5-testing-strategies",level:3},{value:"6. Monitoring and Observability",id:"6-monitoring-and-observability",level:3},{value:"Summary",id:"summary",level:2}];function l(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",mermaid:"mermaid",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,i.R)(),...e.components};return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(n.header,{children:(0,a.jsx)(n.h1,{id:"data-consistency-and-saga-pattern-guide",children:"Data Consistency and Saga Pattern Guide"})}),"\n",(0,a.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,a.jsx)(n.p,{children:"Maintaining data consistency across distributed microservices is one of the most challenging aspects of microservices architecture. This guide covers distributed transaction management using the Saga pattern, eventual consistency strategies, and implementing reliable data workflows across service boundaries."}),"\n",(0,a.jsx)(n.h2,{id:"table-of-contents",children:"Table of Contents"}),"\n",(0,a.jsxs)(n.ol,{children:["\n",(0,a.jsx)(n.li,{children:(0,a.jsx)(n.a,{href:"#data-consistency-challenges",children:"Data Consistency Challenges"})}),"\n",(0,a.jsx)(n.li,{children:(0,a.jsx)(n.a,{href:"#saga-pattern-fundamentals",children:"Saga Pattern Fundamentals"})}),"\n",(0,a.jsx)(n.li,{children:(0,a.jsx)(n.a,{href:"#choreography-based-saga",children:"Choreography-Based Saga"})}),"\n",(0,a.jsx)(n.li,{children:(0,a.jsx)(n.a,{href:"#orchestration-based-saga",children:"Orchestration-Based Saga"})}),"\n",(0,a.jsx)(n.li,{children:(0,a.jsx)(n.a,{href:"#compensation-and-rollback",children:"Compensation and Rollback"})}),"\n",(0,a.jsx)(n.li,{children:(0,a.jsx)(n.a,{href:"#event-sourcing-integration",children:"Event Sourcing Integration"})}),"\n",(0,a.jsx)(n.li,{children:(0,a.jsx)(n.a,{href:"#monitoring-and-debugging",children:"Monitoring and Debugging"})}),"\n",(0,a.jsx)(n.li,{children:(0,a.jsx)(n.a,{href:"#best-practices",children:"Best Practices"})}),"\n"]}),"\n",(0,a.jsx)(n.h2,{id:"data-consistency-challenges",children:"Data Consistency Challenges"}),"\n",(0,a.jsx)(n.h3,{id:"the-distributed-transaction-problem",children:"The Distributed Transaction Problem"}),"\n",(0,a.jsx)(n.mermaid,{value:'graph TB\n    subgraph "Monolithic Application"\n        A[ACID Transaction]\n        B[Database]\n        A --\x3e B\n    end\n\n    subgraph "Microservices Architecture"\n        C[Order Service]\n        D[Payment Service]\n        E[Inventory Service]\n        F[Shipping Service]\n\n        G[(Order DB)]\n        H[(Payment DB)]\n        I[(Inventory DB)]\n        J[(Shipping DB)]\n\n        C --\x3e G\n        D --\x3e H\n        E --\x3e I\n        F --\x3e J\n    end\n\n    style A fill:#4CAF50\n    style B fill:#4CAF50\n    style C fill:#FF9800\n    style D fill:#FF9800\n    style E fill:#FF9800\n    style F fill:#FF9800'}),"\n",(0,a.jsx)(n.h3,{id:"cap-theorem-trade-offs",children:"CAP Theorem Trade-offs"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"/**\n * CAP Theorem in Microservices:\n * - Consistency: All nodes see the same data at the same time\n * - Availability: Every request receives a response\n * - Partition Tolerance: System continues despite network partitions\n *\n * In distributed systems, you can only guarantee 2 out of 3\n */\n\n// E-commerce Example: Order Processing\ninterface OrderProcessing {\n  // CP (Consistency + Partition Tolerance)\n  // Choose when: Financial transactions, inventory updates\n  // Trade-off: May sacrifice availability during network issues\n  strictConsistency: {\n    approach: 'Two-Phase Commit (2PC)';\n    impact: 'Blocking operations, lower availability';\n  };\n\n  // AP (Availability + Partition Tolerance)\n  // Choose when: Social features, analytics, recommendations\n  // Trade-off: Eventual consistency, temporary inconsistencies\n  eventualConsistency: {\n    approach: 'Saga Pattern, Event Sourcing';\n    impact: 'Higher availability, complex error handling';\n  };\n}\n"})}),"\n",(0,a.jsx)(n.h2,{id:"saga-pattern-fundamentals",children:"Saga Pattern Fundamentals"}),"\n",(0,a.jsx)(n.h3,{id:"what-is-a-saga",children:"What is a Saga?"}),"\n",(0,a.jsx)(n.p,{children:"A Saga is a sequence of local transactions where each transaction updates data within a single service. If a step fails, the saga executes compensating transactions to undo the changes made by preceding steps."}),"\n",(0,a.jsx)(n.h3,{id:"saga-types",children:"Saga Types"}),"\n",(0,a.jsx)(n.mermaid,{value:"graph LR\n    A[Saga Pattern] --\x3e B[Choreography]\n    A --\x3e C[Orchestration]\n\n    B --\x3e D[Event-Driven<br/>Decentralized]\n    C --\x3e E[Central Coordinator<br/>Workflow Engine]\n\n    style A fill:#2196F3\n    style B fill:#4CAF50\n    style C fill:#FF9800"}),"\n",(0,a.jsx)(n.h3,{id:"order-saga-example",children:"Order Saga Example"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// Order Saga Flow\nconst orderSagaFlow = {\n  steps: [\n    '1. Create Order (Order Service)',\n    '2. Reserve Inventory (Inventory Service)',\n    '3. Process Payment (Payment Service)',\n    '4. Create Shipment (Shipping Service)',\n    '5. Update Order Status (Order Service)',\n  ],\n  compensations: [\n    '5. Cancel Order (Order Service)',\n    '4. Cancel Shipment (Shipping Service)',\n    '3. Refund Payment (Payment Service)',\n    '2. Release Inventory (Inventory Service)',\n    '1. Delete Order (Order Service)',\n  ],\n};\n"})}),"\n",(0,a.jsx)(n.h2,{id:"choreography-based-saga",children:"Choreography-Based Saga"}),"\n",(0,a.jsx)(n.h3,{id:"architecture",children:"Architecture"}),"\n",(0,a.jsx)(n.mermaid,{value:"sequenceDiagram\n    participant Client\n    participant OrderService\n    participant EventBus\n    participant InventoryService\n    participant PaymentService\n    participant ShippingService\n\n    Client->>OrderService: Create Order\n    OrderService->>EventBus: OrderCreated Event\n\n    EventBus->>InventoryService: OrderCreated\n    InventoryService->>InventoryService: Reserve Items\n    InventoryService->>EventBus: InventoryReserved Event\n\n    EventBus->>PaymentService: InventoryReserved\n    PaymentService->>PaymentService: Process Payment\n    PaymentService->>EventBus: PaymentProcessed Event\n\n    EventBus->>ShippingService: PaymentProcessed\n    ShippingService->>ShippingService: Create Shipment\n    ShippingService->>EventBus: ShipmentCreated Event\n\n    EventBus->>OrderService: ShipmentCreated\n    OrderService->>Client: Order Confirmed"}),"\n",(0,a.jsx)(n.h3,{id:"implementation",children:"Implementation"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// shared/events/src/order-events.ts\nexport enum OrderEventType {\n  ORDER_CREATED = 'order.created',\n  INVENTORY_RESERVED = 'inventory.reserved',\n  INVENTORY_RESERVATION_FAILED = 'inventory.reservation.failed',\n  PAYMENT_PROCESSED = 'payment.processed',\n  PAYMENT_FAILED = 'payment.failed',\n  SHIPMENT_CREATED = 'shipment.created',\n  SHIPMENT_FAILED = 'shipment.failed',\n  ORDER_COMPLETED = 'order.completed',\n  ORDER_CANCELLED = 'order.cancelled',\n}\n\nexport interface OrderCreatedEvent {\n  orderId: string;\n  userId: string;\n  items: Array<{\n    productId: string;\n    quantity: number;\n    price: number;\n  }>;\n  totalAmount: number;\n  timestamp: Date;\n}\n\nexport interface InventoryReservedEvent {\n  orderId: string;\n  reservationId: string;\n  items: Array<{\n    productId: string;\n    quantity: number;\n  }>;\n  timestamp: Date;\n}\n\nexport interface PaymentProcessedEvent {\n  orderId: string;\n  paymentId: string;\n  amount: number;\n  timestamp: Date;\n}\n"})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/sagas/order-saga.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { OnEvent } from '@nestjs/event-emitter';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { KafkaService } from '@ecommerce/kafka';\nimport { Order, OrderStatus } from '../entities/order.entity';\nimport {\n  OrderEventType,\n  OrderCreatedEvent,\n  InventoryReservedEvent,\n  PaymentProcessedEvent,\n  ShipmentCreatedEvent,\n} from '@ecommerce/events';\n\n@Injectable()\nexport class OrderSagaService {\n  private readonly logger = new Logger(OrderSagaService.name);\n\n  constructor(\n    @InjectRepository(Order)\n    private orderRepository: Repository<Order>,\n    private kafkaService: KafkaService,\n  ) {}\n\n  // Step 1: Handle new order creation\n  async createOrder(createOrderDto: CreateOrderDto): Promise<Order> {\n    this.logger.log(`Creating order for user ${createOrderDto.userId}`);\n\n    // Create order in pending state\n    const order = await this.orderRepository.save({\n      userId: createOrderDto.userId,\n      items: createOrderDto.items,\n      totalAmount: createOrderDto.totalAmount,\n      status: OrderStatus.PENDING,\n      sagaState: 'ORDER_CREATED',\n    });\n\n    // Publish OrderCreated event to start the saga\n    const event: OrderCreatedEvent = {\n      orderId: order.id,\n      userId: order.userId,\n      items: order.items,\n      totalAmount: order.totalAmount,\n      timestamp: new Date(),\n    };\n\n    await this.kafkaService.publish(OrderEventType.ORDER_CREATED, event);\n\n    this.logger.log(`Order ${order.id} created, saga initiated`);\n    return order;\n  }\n\n  // Step 2: Handle inventory reservation success\n  @OnEvent(OrderEventType.INVENTORY_RESERVED)\n  async handleInventoryReserved(event: InventoryReservedEvent): Promise<void> {\n    this.logger.log(`Inventory reserved for order ${event.orderId}`);\n\n    await this.orderRepository.update(event.orderId, {\n      sagaState: 'INVENTORY_RESERVED',\n      reservationId: event.reservationId,\n    });\n\n    // Saga continues automatically when Payment Service receives the event\n  }\n\n  // Step 3: Handle inventory reservation failure (compensation)\n  @OnEvent(OrderEventType.INVENTORY_RESERVATION_FAILED)\n  async handleInventoryReservationFailed(event: any): Promise<void> {\n    this.logger.error(`Inventory reservation failed for order ${event.orderId}`);\n\n    await this.orderRepository.update(event.orderId, {\n      status: OrderStatus.CANCELLED,\n      sagaState: 'FAILED_INVENTORY',\n      failureReason: event.reason,\n    });\n\n    // Publish cancellation event\n    await this.kafkaService.publish(OrderEventType.ORDER_CANCELLED, {\n      orderId: event.orderId,\n      reason: 'Insufficient inventory',\n      timestamp: new Date(),\n    });\n  }\n\n  // Step 4: Handle payment success\n  @OnEvent(OrderEventType.PAYMENT_PROCESSED)\n  async handlePaymentProcessed(event: PaymentProcessedEvent): Promise<void> {\n    this.logger.log(`Payment processed for order ${event.orderId}`);\n\n    await this.orderRepository.update(event.orderId, {\n      sagaState: 'PAYMENT_PROCESSED',\n      paymentId: event.paymentId,\n    });\n  }\n\n  // Step 5: Handle payment failure (compensation)\n  @OnEvent(OrderEventType.PAYMENT_FAILED)\n  async handlePaymentFailed(event: any): Promise<void> {\n    this.logger.error(`Payment failed for order ${event.orderId}`);\n\n    const order = await this.orderRepository.findOne({\n      where: { id: event.orderId },\n    });\n\n    // Trigger compensation: Release inventory\n    if (order.reservationId) {\n      await this.kafkaService.publish('inventory.release', {\n        reservationId: order.reservationId,\n        orderId: event.orderId,\n        timestamp: new Date(),\n      });\n    }\n\n    await this.orderRepository.update(event.orderId, {\n      status: OrderStatus.CANCELLED,\n      sagaState: 'FAILED_PAYMENT',\n      failureReason: event.reason,\n    });\n\n    await this.kafkaService.publish(OrderEventType.ORDER_CANCELLED, {\n      orderId: event.orderId,\n      reason: 'Payment failed',\n      timestamp: new Date(),\n    });\n  }\n\n  // Step 6: Handle shipment creation success\n  @OnEvent(OrderEventType.SHIPMENT_CREATED)\n  async handleShipmentCreated(event: ShipmentCreatedEvent): Promise<void> {\n    this.logger.log(`Shipment created for order ${event.orderId}`);\n\n    await this.orderRepository.update(event.orderId, {\n      status: OrderStatus.CONFIRMED,\n      sagaState: 'COMPLETED',\n      shipmentId: event.shipmentId,\n    });\n\n    // Saga completed successfully\n    await this.kafkaService.publish(OrderEventType.ORDER_COMPLETED, {\n      orderId: event.orderId,\n      timestamp: new Date(),\n    });\n\n    this.logger.log(`Order ${event.orderId} saga completed successfully`);\n  }\n\n  // Step 7: Handle shipment failure (compensation)\n  @OnEvent(OrderEventType.SHIPMENT_FAILED)\n  async handleShipmentFailed(event: any): Promise<void> {\n    this.logger.error(`Shipment failed for order ${event.orderId}`);\n\n    const order = await this.orderRepository.findOne({\n      where: { id: event.orderId },\n    });\n\n    // Trigger compensations in reverse order\n    // 1. Refund payment\n    if (order.paymentId) {\n      await this.kafkaService.publish('payment.refund', {\n        paymentId: order.paymentId,\n        orderId: event.orderId,\n        amount: order.totalAmount,\n        timestamp: new Date(),\n      });\n    }\n\n    // 2. Release inventory\n    if (order.reservationId) {\n      await this.kafkaService.publish('inventory.release', {\n        reservationId: order.reservationId,\n        orderId: event.orderId,\n        timestamp: new Date(),\n      });\n    }\n\n    await this.orderRepository.update(event.orderId, {\n      status: OrderStatus.CANCELLED,\n      sagaState: 'FAILED_SHIPMENT',\n      failureReason: event.reason,\n    });\n\n    await this.kafkaService.publish(OrderEventType.ORDER_CANCELLED, {\n      orderId: event.orderId,\n      reason: 'Shipment creation failed',\n      timestamp: new Date(),\n    });\n  }\n}\n"})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// services/inventory-service/src/sagas/inventory-saga.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { OnEvent } from '@nestjs/event-emitter';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { KafkaService } from '@ecommerce/kafka';\nimport { InventoryReservation } from '../entities/inventory-reservation.entity';\nimport { Product } from '../entities/product.entity';\nimport { OrderCreatedEvent, OrderEventType } from '@ecommerce/events';\n\n@Injectable()\nexport class InventorySagaService {\n  private readonly logger = new Logger(InventorySagaService.name);\n\n  constructor(\n    @InjectRepository(InventoryReservation)\n    private reservationRepository: Repository<InventoryReservation>,\n    @InjectRepository(Product)\n    private productRepository: Repository<Product>,\n    private kafkaService: KafkaService,\n  ) {}\n\n  @OnEvent(OrderEventType.ORDER_CREATED)\n  async handleOrderCreated(event: OrderCreatedEvent): Promise<void> {\n    this.logger.log(`Handling order created: ${event.orderId}`);\n\n    try {\n      // Check inventory availability\n      for (const item of event.items) {\n        const product = await this.productRepository.findOne({\n          where: { id: item.productId },\n        });\n\n        if (!product || product.availableQuantity < item.quantity) {\n          throw new Error(`Insufficient inventory for product ${item.productId}`);\n        }\n      }\n\n      // Reserve inventory\n      const reservation = await this.reservationRepository.save({\n        orderId: event.orderId,\n        items: event.items,\n        status: 'RESERVED',\n        expiresAt: new Date(Date.now() + 15 * 60 * 1000), // 15 minutes\n      });\n\n      // Update product quantities\n      for (const item of event.items) {\n        await this.productRepository.decrement({ id: item.productId }, 'availableQuantity', item.quantity);\n      }\n\n      // Publish success event\n      await this.kafkaService.publish(OrderEventType.INVENTORY_RESERVED, {\n        orderId: event.orderId,\n        reservationId: reservation.id,\n        items: event.items,\n        timestamp: new Date(),\n      });\n\n      this.logger.log(`Inventory reserved for order ${event.orderId}`);\n    } catch (error) {\n      this.logger.error(`Failed to reserve inventory: ${error.message}`);\n\n      // Publish failure event\n      await this.kafkaService.publish(OrderEventType.INVENTORY_RESERVATION_FAILED, {\n        orderId: event.orderId,\n        reason: error.message,\n        timestamp: new Date(),\n      });\n    }\n  }\n\n  // Compensation: Release inventory\n  @OnEvent('inventory.release')\n  async handleInventoryRelease(event: any): Promise<void> {\n    this.logger.log(`Releasing inventory for order ${event.orderId}`);\n\n    const reservation = await this.reservationRepository.findOne({\n      where: { id: event.reservationId },\n    });\n\n    if (reservation && reservation.status === 'RESERVED') {\n      // Restore product quantities\n      for (const item of reservation.items) {\n        await this.productRepository.increment({ id: item.productId }, 'availableQuantity', item.quantity);\n      }\n\n      await this.reservationRepository.update(reservation.id, {\n        status: 'RELEASED',\n      });\n\n      this.logger.log(`Inventory released for order ${event.orderId}`);\n    }\n  }\n}\n"})}),"\n",(0,a.jsx)(n.h2,{id:"orchestration-based-saga",children:"Orchestration-Based Saga"}),"\n",(0,a.jsx)(n.h3,{id:"architecture-with-temporal",children:"Architecture with Temporal"}),"\n",(0,a.jsx)(n.mermaid,{value:"graph TB\n    A[Client] --\x3e B[Order Service]\n    B --\x3e C[Saga Orchestrator<br/>Temporal Workflow]\n\n    C --\x3e D[Inventory Service]\n    C --\x3e E[Payment Service]\n    C --\x3e F[Shipping Service]\n\n    C --\x3e G[Temporal Server]\n    G --\x3e H[(Workflow State)]\n\n    style C fill:#2196F3\n    style G fill:#4CAF50"}),"\n",(0,a.jsx)(n.h3,{id:"temporal-workflow-implementation",children:"Temporal Workflow Implementation"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/workflows/order-saga.workflow.ts\nimport { proxyActivities, sleep } from '@temporalio/workflow';\nimport type * as activities from '../activities';\n\nconst { createOrder, reserveInventory, processPayment, createShipment, releaseInventory, refundPayment, cancelOrder } =\n  proxyActivities<typeof activities>({\n    startToCloseTimeout: '5 minutes',\n    retry: {\n      initialInterval: '1s',\n      backoffCoefficient: 2,\n      maximumAttempts: 3,\n    },\n  });\n\nexport interface OrderSagaInput {\n  userId: string;\n  items: Array<{\n    productId: string;\n    quantity: number;\n    price: number;\n  }>;\n  totalAmount: number;\n  paymentMethod: string;\n}\n\nexport interface OrderSagaResult {\n  orderId: string;\n  status: 'completed' | 'failed';\n  failureReason?: string;\n}\n\nexport async function orderSagaWorkflow(input: OrderSagaInput): Promise<OrderSagaResult> {\n  let orderId: string;\n  let reservationId: string;\n  let paymentId: string;\n\n  try {\n    // Step 1: Create Order\n    const orderResult = await createOrder({\n      userId: input.userId,\n      items: input.items,\n      totalAmount: input.totalAmount,\n    });\n    orderId = orderResult.orderId;\n\n    // Step 2: Reserve Inventory\n    const inventoryResult = await reserveInventory({\n      orderId,\n      items: input.items,\n    });\n    reservationId = inventoryResult.reservationId;\n\n    // Step 3: Process Payment\n    const paymentResult = await processPayment({\n      orderId,\n      userId: input.userId,\n      amount: input.totalAmount,\n      paymentMethod: input.paymentMethod,\n    });\n    paymentId = paymentResult.paymentId;\n\n    // Step 4: Create Shipment\n    await createShipment({\n      orderId,\n      userId: input.userId,\n      items: input.items,\n    });\n\n    return {\n      orderId,\n      status: 'completed',\n    };\n  } catch (error) {\n    // Compensation logic (executed in reverse order)\n    console.error('Saga failed, executing compensations:', error);\n\n    // Compensate Shipment (if it was created)\n    // Shipping service handles this internally\n\n    // Compensate Payment\n    if (paymentId) {\n      try {\n        await refundPayment({ paymentId, orderId });\n      } catch (refundError) {\n        console.error('Refund failed:', refundError);\n        // Log for manual intervention\n      }\n    }\n\n    // Compensate Inventory\n    if (reservationId) {\n      try {\n        await releaseInventory({ reservationId, orderId });\n      } catch (releaseError) {\n        console.error('Inventory release failed:', releaseError);\n      }\n    }\n\n    // Compensate Order\n    if (orderId) {\n      try {\n        await cancelOrder({ orderId, reason: error.message });\n      } catch (cancelError) {\n        console.error('Order cancellation failed:', cancelError);\n      }\n    }\n\n    return {\n      orderId,\n      status: 'failed',\n      failureReason: error.message,\n    };\n  }\n}\n"})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/activities/order.activities.ts\nimport { Injectable } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { HttpService } from '@nestjs/axios';\nimport { Order, OrderStatus } from '../entities/order.entity';\n\n@Injectable()\nexport class OrderActivities {\n  constructor(\n    @InjectRepository(Order)\n    private orderRepository: Repository<Order>,\n    private httpService: HttpService,\n  ) {}\n\n  async createOrder(input: { userId: string; items: any[]; totalAmount: number }): Promise<{ orderId: string }> {\n    const order = await this.orderRepository.save({\n      userId: input.userId,\n      items: input.items,\n      totalAmount: input.totalAmount,\n      status: OrderStatus.PENDING,\n    });\n\n    return { orderId: order.id };\n  }\n\n  async reserveInventory(input: { orderId: string; items: any[] }): Promise<{ reservationId: string }> {\n    const response = await this.httpService\n      .post('http://inventory-service/reservations', {\n        orderId: input.orderId,\n        items: input.items,\n      })\n      .toPromise();\n\n    if (response.status !== 200) {\n      throw new Error('Failed to reserve inventory');\n    }\n\n    return { reservationId: response.data.reservationId };\n  }\n\n  async processPayment(input: {\n    orderId: string;\n    userId: string;\n    amount: number;\n    paymentMethod: string;\n  }): Promise<{ paymentId: string }> {\n    const response = await this.httpService\n      .post('http://payment-service/payments', {\n        orderId: input.orderId,\n        userId: input.userId,\n        amount: input.amount,\n        paymentMethod: input.paymentMethod,\n      })\n      .toPromise();\n\n    if (response.status !== 200) {\n      throw new Error('Payment processing failed');\n    }\n\n    return { paymentId: response.data.paymentId };\n  }\n\n  async createShipment(input: { orderId: string; userId: string; items: any[] }): Promise<{ shipmentId: string }> {\n    const response = await this.httpService\n      .post('http://shipping-service/shipments', {\n        orderId: input.orderId,\n        userId: input.userId,\n        items: input.items,\n      })\n      .toPromise();\n\n    if (response.status !== 200) {\n      throw new Error('Shipment creation failed');\n    }\n\n    return { shipmentId: response.data.shipmentId };\n  }\n\n  // Compensation Activities\n\n  async releaseInventory(input: { reservationId: string; orderId: string }): Promise<void> {\n    await this.httpService.delete(`http://inventory-service/reservations/${input.reservationId}`).toPromise();\n  }\n\n  async refundPayment(input: { paymentId: string; orderId: string }): Promise<void> {\n    await this.httpService\n      .post(`http://payment-service/payments/${input.paymentId}/refund`, {\n        orderId: input.orderId,\n      })\n      .toPromise();\n  }\n\n  async cancelOrder(input: { orderId: string; reason: string }): Promise<void> {\n    await this.orderRepository.update(input.orderId, {\n      status: OrderStatus.CANCELLED,\n      failureReason: input.reason,\n    });\n  }\n}\n"})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/temporal/temporal.service.ts\nimport { Injectable, OnModuleInit, Logger } from '@nestjs/common';\nimport { Connection, Client } from '@temporalio/client';\nimport { Worker } from '@temporalio/worker';\nimport { OrderActivities } from '../activities/order.activities';\nimport { orderSagaWorkflow } from '../workflows/order-saga.workflow';\n\n@Injectable()\nexport class TemporalService implements OnModuleInit {\n  private readonly logger = new Logger(TemporalService.name);\n  private client: Client;\n  private worker: Worker;\n\n  async onModuleInit() {\n    await this.initializeClient();\n    await this.initializeWorker();\n  }\n\n  private async initializeClient() {\n    const connection = await Connection.connect({\n      address: process.env.TEMPORAL_ADDRESS || 'localhost:7233',\n    });\n\n    this.client = new Client({\n      connection,\n      namespace: 'ecommerce',\n    });\n\n    this.logger.log('Temporal client initialized');\n  }\n\n  private async initializeWorker() {\n    this.worker = await Worker.create({\n      connection: await Connection.connect({\n        address: process.env.TEMPORAL_ADDRESS || 'localhost:7233',\n      }),\n      namespace: 'ecommerce',\n      taskQueue: 'order-saga',\n      workflowsPath: require.resolve('../workflows'),\n      activities: new OrderActivities(),\n      // Inject dependencies\n    });\n\n    await this.worker.run();\n    this.logger.log('Temporal worker started');\n  }\n\n  async startOrderSaga(input: any): Promise<string> {\n    const handle = await this.client.workflow.start(orderSagaWorkflow, {\n      taskQueue: 'order-saga',\n      workflowId: `order-saga-${Date.now()}`,\n      args: [input],\n    });\n\n    this.logger.log(`Started order saga workflow: ${handle.workflowId}`);\n    return handle.workflowId;\n  }\n\n  async getWorkflowResult(workflowId: string): Promise<any> {\n    const handle = this.client.workflow.getHandle(workflowId);\n    return await handle.result();\n  }\n}\n"})}),"\n",(0,a.jsx)(n.h2,{id:"compensation-and-rollback",children:"Compensation and Rollback"}),"\n",(0,a.jsx)(n.h3,{id:"compensation-patterns",children:"Compensation Patterns"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// shared/saga/src/compensation-manager.ts\nimport { Injectable, Logger } from '@nestjs/common';\n\nexport interface CompensationAction {\n  actionName: string;\n  execute: () => Promise<void>;\n  retries?: number;\n  timeout?: number;\n}\n\n@Injectable()\nexport class CompensationManager {\n  private readonly logger = new Logger(CompensationManager.name);\n  private compensationStack: CompensationAction[] = [];\n\n  // Register a compensation action\n  registerCompensation(action: CompensationAction): void {\n    this.compensationStack.push(action);\n    this.logger.debug(`Registered compensation: ${action.actionName}`);\n  }\n\n  // Execute all compensations in reverse order (LIFO)\n  async executeCompensations(): Promise<void> {\n    this.logger.log(`Executing ${this.compensationStack.length} compensations`);\n\n    const errors: Array<{ action: string; error: Error }> = [];\n\n    // Execute in reverse order\n    while (this.compensationStack.length > 0) {\n      const action = this.compensationStack.pop();\n\n      try {\n        this.logger.log(`Executing compensation: ${action.actionName}`);\n        await this.executeWithRetry(action);\n        this.logger.log(`Compensation completed: ${action.actionName}`);\n      } catch (error) {\n        this.logger.error(`Compensation failed: ${action.actionName}`, error.stack);\n        errors.push({ action: action.actionName, error });\n      }\n    }\n\n    if (errors.length > 0) {\n      this.logger.error('Some compensations failed:', errors);\n      // Alert operations team for manual intervention\n      throw new Error(`Compensations failed: ${errors.map((e) => e.action).join(', ')}`);\n    }\n  }\n\n  private async executeWithRetry(action: CompensationAction): Promise<void> {\n    const maxRetries = action.retries || 3;\n    let lastError: Error;\n\n    for (let attempt = 1; attempt <= maxRetries; attempt++) {\n      try {\n        const timeoutPromise = new Promise((_, reject) =>\n          setTimeout(() => reject(new Error('Compensation timeout')), action.timeout || 30000),\n        );\n\n        await Promise.race([action.execute(), timeoutPromise]);\n        return; // Success\n      } catch (error) {\n        lastError = error;\n        this.logger.warn(`Compensation attempt ${attempt}/${maxRetries} failed: ${action.actionName}`);\n\n        if (attempt < maxRetries) {\n          await this.delay(Math.pow(2, attempt) * 1000); // Exponential backoff\n        }\n      }\n    }\n\n    throw lastError;\n  }\n\n  private delay(ms: number): Promise<void> {\n    return new Promise((resolve) => setTimeout(resolve, ms));\n  }\n\n  clear(): void {\n    this.compensationStack = [];\n  }\n}\n"})}),"\n",(0,a.jsx)(n.h3,{id:"saga-with-compensation-manager",children:"Saga with Compensation Manager"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/sagas/order-saga-with-compensation.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { CompensationManager } from '@ecommerce/saga';\n\n@Injectable()\nexport class OrderSagaWithCompensation {\n  private readonly logger = new Logger(OrderSagaWithCompensation.name);\n\n  constructor(\n    private compensationManager: CompensationManager,\n    private inventoryService: InventoryService,\n    private paymentService: PaymentService,\n    private shippingService: ShippingService,\n  ) {}\n\n  async executeOrderSaga(orderData: CreateOrderDto): Promise<Order> {\n    this.compensationManager.clear();\n\n    try {\n      // Step 1: Create Order\n      const order = await this.createOrder(orderData);\n      this.compensationManager.registerCompensation({\n        actionName: 'Cancel Order',\n        execute: async () => {\n          await this.cancelOrder(order.id);\n        },\n      });\n\n      // Step 2: Reserve Inventory\n      const reservation = await this.inventoryService.reserve(order.items);\n      this.compensationManager.registerCompensation({\n        actionName: 'Release Inventory',\n        execute: async () => {\n          await this.inventoryService.release(reservation.id);\n        },\n      });\n\n      // Step 3: Process Payment\n      const payment = await this.paymentService.process({\n        orderId: order.id,\n        amount: order.totalAmount,\n      });\n      this.compensationManager.registerCompensation({\n        actionName: 'Refund Payment',\n        execute: async () => {\n          await this.paymentService.refund(payment.id);\n        },\n      });\n\n      // Step 4: Create Shipment\n      const shipment = await this.shippingService.create({\n        orderId: order.id,\n        items: order.items,\n      });\n\n      // All steps succeeded\n      return order;\n    } catch (error) {\n      this.logger.error('Saga failed, executing compensations', error.stack);\n\n      // Execute compensations in reverse order\n      await this.compensationManager.executeCompensations();\n\n      throw error;\n    }\n  }\n\n  private async createOrder(orderData: CreateOrderDto): Promise<Order> {\n    // Implementation\n    return {} as Order;\n  }\n\n  private async cancelOrder(orderId: string): Promise<void> {\n    // Implementation\n  }\n}\n"})}),"\n",(0,a.jsx)(n.h2,{id:"event-sourcing-integration",children:"Event Sourcing Integration"}),"\n",(0,a.jsx)(n.h3,{id:"event-store-implementation",children:"Event Store Implementation"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// shared/event-sourcing/src/event-store.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { EventStoreEvent } from './entities/event-store-event.entity';\n\nexport interface DomainEvent {\n  aggregateId: string;\n  aggregateType: string;\n  eventType: string;\n  eventData: any;\n  metadata?: any;\n}\n\n@Injectable()\nexport class EventStoreService {\n  private readonly logger = new Logger(EventStoreService.name);\n\n  constructor(\n    @InjectRepository(EventStoreEvent)\n    private eventRepository: Repository<EventStoreEvent>,\n  ) {}\n\n  async append(event: DomainEvent): Promise<void> {\n    const storedEvent = await this.eventRepository.save({\n      aggregateId: event.aggregateId,\n      aggregateType: event.aggregateType,\n      eventType: event.eventType,\n      eventData: event.eventData,\n      metadata: event.metadata,\n      version: await this.getNextVersion(event.aggregateId),\n      timestamp: new Date(),\n    });\n\n    this.logger.debug(`Event stored: ${event.eventType} for ${event.aggregateType}:${event.aggregateId}`);\n  }\n\n  async getEvents(aggregateId: string): Promise<EventStoreEvent[]> {\n    return this.eventRepository.find({\n      where: { aggregateId },\n      order: { version: 'ASC' },\n    });\n  }\n\n  async getEventsSince(aggregateId: string, version: number): Promise<EventStoreEvent[]> {\n    return this.eventRepository.find({\n      where: { aggregateId },\n      order: { version: 'ASC' },\n    });\n  }\n\n  private async getNextVersion(aggregateId: string): Promise<number> {\n    const lastEvent = await this.eventRepository.findOne({\n      where: { aggregateId },\n      order: { version: 'DESC' },\n    });\n\n    return lastEvent ? lastEvent.version + 1 : 1;\n  }\n}\n"})}),"\n",(0,a.jsx)(n.h3,{id:"order-aggregate-with-event-sourcing",children:"Order Aggregate with Event Sourcing"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/aggregates/order.aggregate.ts\nimport { AggregateRoot } from '@nestjs/cqrs';\n\nexport class OrderCreatedEvent {\n  constructor(\n    public readonly orderId: string,\n    public readonly userId: string,\n    public readonly items: any[],\n    public readonly totalAmount: number,\n  ) {}\n}\n\nexport class InventoryReservedEvent {\n  constructor(public readonly orderId: string, public readonly reservationId: string) {}\n}\n\nexport class PaymentProcessedEvent {\n  constructor(public readonly orderId: string, public readonly paymentId: string) {}\n}\n\nexport class OrderCompletedEvent {\n  constructor(public readonly orderId: string) {}\n}\n\nexport class OrderCancelledEvent {\n  constructor(public readonly orderId: string, public readonly reason: string) {}\n}\n\nexport class OrderAggregate extends AggregateRoot {\n  private id: string;\n  private userId: string;\n  private items: any[];\n  private totalAmount: number;\n  private status: string;\n  private reservationId?: string;\n  private paymentId?: string;\n\n  constructor(id?: string) {\n    super();\n    this.id = id;\n  }\n\n  // Command: Create Order\n  createOrder(userId: string, items: any[], totalAmount: number): void {\n    this.apply(new OrderCreatedEvent(this.id, userId, items, totalAmount));\n  }\n\n  // Command: Reserve Inventory\n  reserveInventory(reservationId: string): void {\n    this.apply(new InventoryReservedEvent(this.id, reservationId));\n  }\n\n  // Command: Process Payment\n  processPayment(paymentId: string): void {\n    this.apply(new PaymentProcessedEvent(this.id, paymentId));\n  }\n\n  // Command: Complete Order\n  completeOrder(): void {\n    if (this.status === 'PAYMENT_PROCESSED') {\n      this.apply(new OrderCompletedEvent(this.id));\n    }\n  }\n\n  // Command: Cancel Order\n  cancelOrder(reason: string): void {\n    this.apply(new OrderCancelledEvent(this.id, reason));\n  }\n\n  // Event Handlers (State Mutations)\n\n  onOrderCreatedEvent(event: OrderCreatedEvent): void {\n    this.id = event.orderId;\n    this.userId = event.userId;\n    this.items = event.items;\n    this.totalAmount = event.totalAmount;\n    this.status = 'CREATED';\n  }\n\n  onInventoryReservedEvent(event: InventoryReservedEvent): void {\n    this.reservationId = event.reservationId;\n    this.status = 'INVENTORY_RESERVED';\n  }\n\n  onPaymentProcessedEvent(event: PaymentProcessedEvent): void {\n    this.paymentId = event.paymentId;\n    this.status = 'PAYMENT_PROCESSED';\n  }\n\n  onOrderCompletedEvent(event: OrderCompletedEvent): void {\n    this.status = 'COMPLETED';\n  }\n\n  onOrderCancelledEvent(event: OrderCancelledEvent): void {\n    this.status = 'CANCELLED';\n  }\n\n  // Getters\n  getId(): string {\n    return this.id;\n  }\n\n  getStatus(): string {\n    return this.status;\n  }\n}\n"})}),"\n",(0,a.jsx)(n.h2,{id:"monitoring-and-debugging",children:"Monitoring and Debugging"}),"\n",(0,a.jsx)(n.h3,{id:"saga-state-tracking",children:"Saga State Tracking"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// shared/saga/src/saga-state-tracker.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { SagaExecution } from './entities/saga-execution.entity';\n\nexport enum SagaStatus {\n  STARTED = 'STARTED',\n  IN_PROGRESS = 'IN_PROGRESS',\n  COMPLETED = 'COMPLETED',\n  COMPENSATING = 'COMPENSATING',\n  COMPENSATION_COMPLETED = 'COMPENSATION_COMPLETED',\n  FAILED = 'FAILED',\n}\n\n@Injectable()\nexport class SagaStateTracker {\n  private readonly logger = new Logger(SagaStateTracker.name);\n\n  constructor(\n    @InjectRepository(SagaExecution)\n    private sagaRepository: Repository<SagaExecution>,\n  ) {}\n\n  async startSaga(sagaId: string, sagaType: string, input: any): Promise<void> {\n    await this.sagaRepository.save({\n      sagaId,\n      sagaType,\n      status: SagaStatus.STARTED,\n      input,\n      steps: [],\n      startedAt: new Date(),\n    });\n\n    this.logger.log(`Saga started: ${sagaType}:${sagaId}`);\n  }\n\n  async recordStep(sagaId: string, stepName: string, status: 'success' | 'failed', data?: any): Promise<void> {\n    const saga = await this.sagaRepository.findOne({ where: { sagaId } });\n\n    if (saga) {\n      saga.steps.push({\n        name: stepName,\n        status,\n        data,\n        timestamp: new Date(),\n      });\n\n      saga.currentStep = stepName;\n      saga.status = status === 'failed' ? SagaStatus.COMPENSATING : SagaStatus.IN_PROGRESS;\n\n      await this.sagaRepository.save(saga);\n\n      this.logger.debug(`Saga ${sagaId} step recorded: ${stepName} - ${status}`);\n    }\n  }\n\n  async completeSaga(sagaId: string, result: any): Promise<void> {\n    await this.sagaRepository.update(sagaId, {\n      status: SagaStatus.COMPLETED,\n      result,\n      completedAt: new Date(),\n    });\n\n    this.logger.log(`Saga completed: ${sagaId}`);\n  }\n\n  async failSaga(sagaId: string, error: string): Promise<void> {\n    await this.sagaRepository.update(sagaId, {\n      status: SagaStatus.FAILED,\n      error,\n      completedAt: new Date(),\n    });\n\n    this.logger.error(`Saga failed: ${sagaId} - ${error}`);\n  }\n\n  async getSagaState(sagaId: string): Promise<SagaExecution> {\n    return this.sagaRepository.findOne({ where: { sagaId } });\n  }\n\n  async getStuckSagas(timeoutMinutes: number = 30): Promise<SagaExecution[]> {\n    const cutoffTime = new Date(Date.now() - timeoutMinutes * 60 * 1000);\n\n    return this.sagaRepository\n      .createQueryBuilder('saga')\n      .where('saga.status IN (:...statuses)', {\n        statuses: [SagaStatus.STARTED, SagaStatus.IN_PROGRESS],\n      })\n      .andWhere('saga.startedAt < :cutoffTime', { cutoffTime })\n      .getMany();\n  }\n}\n"})}),"\n",(0,a.jsx)(n.h3,{id:"saga-dashboard-metrics",children:"Saga Dashboard Metrics"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/monitoring/saga-metrics.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { Counter, Histogram, Gauge } from 'prom-client';\n\n@Injectable()\nexport class SagaMetricsService {\n  private readonly logger = new Logger(SagaMetricsService.name);\n\n  private sagaStartedCounter = new Counter({\n    name: 'saga_started_total',\n    help: 'Total number of sagas started',\n    labelNames: ['saga_type'],\n  });\n\n  private sagaCompletedCounter = new Counter({\n    name: 'saga_completed_total',\n    help: 'Total number of sagas completed',\n    labelNames: ['saga_type', 'status'],\n  });\n\n  private sagaDurationHistogram = new Histogram({\n    name: 'saga_duration_seconds',\n    help: 'Saga execution duration in seconds',\n    labelNames: ['saga_type', 'status'],\n    buckets: [1, 5, 10, 30, 60, 120, 300],\n  });\n\n  private sagaCompensationsCounter = new Counter({\n    name: 'saga_compensations_total',\n    help: 'Total number of saga compensations executed',\n    labelNames: ['saga_type', 'step'],\n  });\n\n  private activeSagasGauge = new Gauge({\n    name: 'saga_active_count',\n    help: 'Number of currently active sagas',\n    labelNames: ['saga_type'],\n  });\n\n  recordSagaStarted(sagaType: string): void {\n    this.sagaStartedCounter.inc({ saga_type: sagaType });\n    this.activeSagasGauge.inc({ saga_type: sagaType });\n  }\n\n  recordSagaCompleted(sagaType: string, status: 'success' | 'failed', durationMs: number): void {\n    this.sagaCompletedCounter.inc({ saga_type: sagaType, status });\n    this.sagaDurationHistogram.observe({ saga_type: sagaType, status }, durationMs / 1000);\n    this.activeSagasGauge.dec({ saga_type: sagaType });\n  }\n\n  recordCompensation(sagaType: string, step: string): void {\n    this.sagaCompensationsCounter.inc({ saga_type: sagaType, step });\n  }\n}\n"})}),"\n",(0,a.jsx)(n.h2,{id:"best-practices",children:"Best Practices"}),"\n",(0,a.jsx)(n.h3,{id:"1-saga-design-principles",children:"1. Saga Design Principles"}),"\n",(0,a.jsxs)(n.p,{children:["\u2705 ",(0,a.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"Keep sagas simple and focused on a single business process"}),"\n",(0,a.jsx)(n.li,{children:"Design compensations for every forward transaction"}),"\n",(0,a.jsx)(n.li,{children:"Use idempotent operations (can be safely retried)"}),"\n",(0,a.jsx)(n.li,{children:"Track saga state for observability"}),"\n",(0,a.jsx)(n.li,{children:"Set timeouts for saga steps"}),"\n"]}),"\n",(0,a.jsxs)(n.p,{children:["\u274c ",(0,a.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"Create deeply nested or overly complex sagas"}),"\n",(0,a.jsx)(n.li,{children:"Assume all operations will succeed"}),"\n",(0,a.jsx)(n.li,{children:"Ignore partial failure scenarios"}),"\n",(0,a.jsx)(n.li,{children:"Skip compensation logic"}),"\n",(0,a.jsx)(n.li,{children:"Let sagas run indefinitely"}),"\n"]}),"\n",(0,a.jsx)(n.h3,{id:"2-choosing-between-choreography-and-orchestration",children:"2. Choosing Between Choreography and Orchestration"}),"\n",(0,a.jsx)(n.p,{children:(0,a.jsx)(n.strong,{children:"Use Choreography When:"})}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"Services are loosely coupled"}),"\n",(0,a.jsx)(n.li,{children:"Saga has few steps (2-4)"}),"\n",(0,a.jsx)(n.li,{children:"Team prefers event-driven architecture"}),"\n",(0,a.jsx)(n.li,{children:"Services can autonomously make decisions"}),"\n"]}),"\n",(0,a.jsx)(n.p,{children:(0,a.jsx)(n.strong,{children:"Use Orchestration When:"})}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"Complex business logic with many steps (5+)"}),"\n",(0,a.jsx)(n.li,{children:"Need centralized visibility and control"}),"\n",(0,a.jsx)(n.li,{children:"Require strict ordering of operations"}),"\n",(0,a.jsx)(n.li,{children:"Need to modify saga flow frequently"}),"\n"]}),"\n",(0,a.jsx)(n.h3,{id:"3-error-handling",children:"3. Error Handling"}),"\n",(0,a.jsxs)(n.p,{children:["\u2705 ",(0,a.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"Implement retry logic with exponential backoff"}),"\n",(0,a.jsx)(n.li,{children:"Use circuit breakers for external service calls"}),"\n",(0,a.jsx)(n.li,{children:"Log all saga events and state transitions"}),"\n",(0,a.jsx)(n.li,{children:"Alert on stuck or failed sagas"}),"\n",(0,a.jsx)(n.li,{children:"Plan for manual intervention"}),"\n"]}),"\n",(0,a.jsxs)(n.p,{children:["\u274c ",(0,a.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"Retry indefinitely without backoff"}),"\n",(0,a.jsx)(n.li,{children:"Ignore compensation failures"}),"\n",(0,a.jsx)(n.li,{children:"Leave sagas in limbo state"}),"\n",(0,a.jsx)(n.li,{children:"Skip error logging"}),"\n",(0,a.jsx)(n.li,{children:"Assume compensations always succeed"}),"\n"]}),"\n",(0,a.jsx)(n.h3,{id:"4-data-consistency",children:"4. Data Consistency"}),"\n",(0,a.jsxs)(n.p,{children:["\u2705 ",(0,a.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"Accept eventual consistency"}),"\n",(0,a.jsx)(n.li,{children:"Use versioning for concurrent updates"}),"\n",(0,a.jsx)(n.li,{children:"Implement optimistic locking"}),"\n",(0,a.jsx)(n.li,{children:"Validate state before compensation"}),"\n",(0,a.jsx)(n.li,{children:"Use correlation IDs across services"}),"\n"]}),"\n",(0,a.jsxs)(n.p,{children:["\u274c ",(0,a.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"Expect immediate consistency"}),"\n",(0,a.jsx)(n.li,{children:"Allow lost updates"}),"\n",(0,a.jsx)(n.li,{children:"Skip conflict detection"}),"\n",(0,a.jsx)(n.li,{children:"Assume strong consistency"}),"\n",(0,a.jsx)(n.li,{children:"Create tight coupling between services"}),"\n"]}),"\n",(0,a.jsx)(n.h3,{id:"5-testing-strategies",children:"5. Testing Strategies"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-typescript",children:"// Example: Testing Saga with Compensations\ndescribe('OrderSaga', () => {\n  it('should complete successfully when all steps succeed', async () => {\n    const saga = new OrderSaga(/* dependencies */);\n    const result = await saga.execute(orderData);\n\n    expect(result.status).toBe('completed');\n    expect(mockInventoryService.reserve).toHaveBeenCalled();\n    expect(mockPaymentService.process).toHaveBeenCalled();\n  });\n\n  it('should compensate when payment fails', async () => {\n    mockPaymentService.process.mockRejectedValue(new Error('Payment failed'));\n\n    const saga = new OrderSaga(/* dependencies */);\n    await expect(saga.execute(orderData)).rejects.toThrow();\n\n    // Verify compensations were called\n    expect(mockInventoryService.release).toHaveBeenCalled();\n    expect(mockOrderService.cancel).toHaveBeenCalled();\n  });\n\n  it('should handle compensation failures gracefully', async () => {\n    mockPaymentService.process.mockRejectedValue(new Error('Payment failed'));\n    mockInventoryService.release.mockRejectedValue(new Error('Release failed'));\n\n    const saga = new OrderSaga(/* dependencies */);\n    await expect(saga.execute(orderData)).rejects.toThrow();\n\n    // Verify error was logged for manual intervention\n    expect(mockLogger.error).toHaveBeenCalledWith(expect.stringContaining('Compensation failed'));\n  });\n});\n"})}),"\n",(0,a.jsx)(n.h3,{id:"6-monitoring-and-observability",children:"6. Monitoring and Observability"}),"\n",(0,a.jsxs)(n.p,{children:["\u2705 ",(0,a.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"Track saga execution time"}),"\n",(0,a.jsx)(n.li,{children:"Monitor compensation rates"}),"\n",(0,a.jsx)(n.li,{children:"Alert on stuck sagas"}),"\n",(0,a.jsx)(n.li,{children:"Visualize saga flows"}),"\n",(0,a.jsx)(n.li,{children:"Log correlation IDs"}),"\n"]}),"\n",(0,a.jsxs)(n.p,{children:["\u274c ",(0,a.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"Run sagas without monitoring"}),"\n",(0,a.jsx)(n.li,{children:"Ignore high compensation rates"}),"\n",(0,a.jsx)(n.li,{children:"Skip timeout configuration"}),"\n",(0,a.jsx)(n.li,{children:"Lose track of saga state"}),"\n",(0,a.jsx)(n.li,{children:"Forget distributed tracing"}),"\n"]}),"\n",(0,a.jsx)(n.h2,{id:"summary",children:"Summary"}),"\n",(0,a.jsx)(n.p,{children:"Key takeaways for implementing sagas:"}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Saga Pattern"})," - Sequence of local transactions with compensations"]}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Two Approaches"})," - Choreography (event-driven) vs Orchestration (workflow)"]}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Eventual Consistency"})," - Accept that data will be consistent eventually"]}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Compensation Logic"})," - Every step needs a rollback mechanism"]}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Idempotency"})," - Operations must be safely retryable"]}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Monitoring"})," - Track saga state, duration, and failures"]}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Testing"})," - Test both happy path and compensation scenarios"]}),"\n"]}),"\n",(0,a.jsx)(n.p,{children:"Remember: Sagas trade immediate consistency for availability and partition tolerance. Design compensations carefully and monitor saga health!"})]})}function p(e={}){const{wrapper:n}={...(0,i.R)(),...e.components};return n?(0,a.jsx)(n,{...e,children:(0,a.jsx)(l,{...e})}):l(e)}}}]);