"use strict";(globalThis.webpackChunkdocs_site=globalThis.webpackChunkdocs_site||[]).push([[36656],{28453(e,n,t){t.d(n,{R:()=>o,x:()=>i});var r=t(96540);const s={},a=r.createContext(s);function o(e){const n=r.useContext(a);return r.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function i(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:o(e.components),r.createElement(a.Provider,{value:n},e.children)}},83745(e,n,t){t.r(n),t.d(n,{assets:()=>d,contentTitle:()=>i,default:()=>v,frontMatter:()=>o,metadata:()=>r,toc:()=>l});const r=JSON.parse('{"id":"backend/event-sourcing-outbox-pattern","title":"Event Sourcing and Outbox Pattern Guide","description":"Overview","source":"@site/docs/backend/event-sourcing-outbox-pattern.md","sourceDirName":"backend","slug":"/backend/event-sourcing-outbox-pattern","permalink":"/fullstack-dev/docs/backend/event-sourcing-outbox-pattern","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/event-sourcing-outbox-pattern.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Retry with Backoff & Idempotency Guide","permalink":"/fullstack-dev/docs/backend/retry-backoff-idempotency"},"next":{"title":"Server-Sent Events for Real-Time Notifications","permalink":"/fullstack-dev/docs/backend/server-sent-events-notifications"}}');var s=t(74848),a=t(28453);const o={},i="Event Sourcing and Outbox Pattern Guide",d={},l=[{value:"Overview",id:"overview",level:2},{value:"Table of Contents",id:"table-of-contents",level:2},{value:"Event Sourcing Fundamentals",id:"event-sourcing-fundamentals",level:2},{value:"What is Event Sourcing?",id:"what-is-event-sourcing",level:3},{value:"Benefits and Drawbacks",id:"benefits-and-drawbacks",level:3},{value:"Event Store vs Traditional Database",id:"event-store-vs-traditional-database",level:3},{value:"Outbox Pattern",id:"outbox-pattern",level:2},{value:"The Dual Write Problem",id:"the-dual-write-problem",level:3},{value:"Outbox Pattern Architecture",id:"outbox-pattern-architecture",level:3},{value:"Outbox Table Schema",id:"outbox-table-schema",level:3},{value:"Event Store Implementation",id:"event-store-implementation",level:2},{value:"Event Store Entity",id:"event-store-entity",level:3},{value:"Event Store Service",id:"event-store-service",level:3},{value:"Aggregate Design",id:"aggregate-design",level:2},{value:"Base Aggregate Root",id:"base-aggregate-root",level:3},{value:"Order Aggregate Example",id:"order-aggregate-example",level:3},{value:"Event Publishing",id:"event-publishing",level:2},{value:"Outbox Processor",id:"outbox-processor",level:3},{value:"Repository with Event Publishing",id:"repository-with-event-publishing",level:3},{value:"Event Replay and Projections",id:"event-replay-and-projections",level:2},{value:"Projection Service",id:"projection-service",level:3},{value:"Snapshots",id:"snapshots",level:2},{value:"Snapshot Entity",id:"snapshot-entity",level:3},{value:"Snapshot Service",id:"snapshot-service",level:3},{value:"CQRS Integration",id:"cqrs-integration",level:2},{value:"Command Handler",id:"command-handler",level:3},{value:"Query Handler",id:"query-handler",level:3},{value:"Best Practices",id:"best-practices",level:2},{value:"1. Event Design",id:"1-event-design",level:3},{value:"2. Aggregate Design",id:"2-aggregate-design",level:3},{value:"3. Event Store",id:"3-event-store",level:3},{value:"4. Outbox Pattern",id:"4-outbox-pattern",level:3},{value:"5. Projections",id:"5-projections",level:3},{value:"Summary",id:"summary",level:2}];function c(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",li:"li",mermaid:"mermaid",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,a.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(n.header,{children:(0,s.jsx)(n.h1,{id:"event-sourcing-and-outbox-pattern-guide",children:"Event Sourcing and Outbox Pattern Guide"})}),"\n",(0,s.jsx)(n.h2,{id:"overview",children:"Overview"}),"\n",(0,s.jsx)(n.p,{children:"Event Sourcing and the Outbox pattern are crucial architectural patterns for building reliable, event-driven microservices. Event Sourcing stores state as a sequence of events, while the Outbox pattern ensures reliable event publishing without dual-write problems. This guide covers implementation strategies, best practices, and production patterns."}),"\n",(0,s.jsx)(n.h2,{id:"table-of-contents",children:"Table of Contents"}),"\n",(0,s.jsxs)(n.ol,{children:["\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"#event-sourcing-fundamentals",children:"Event Sourcing Fundamentals"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"#outbox-pattern",children:"Outbox Pattern"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"#event-store-implementation",children:"Event Store Implementation"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"#aggregate-design",children:"Aggregate Design"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"#event-publishing",children:"Event Publishing"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"#event-replay-and-projections",children:"Event Replay and Projections"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"#snapshots",children:"Snapshots"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"#cqrs-integration",children:"CQRS Integration"})}),"\n",(0,s.jsx)(n.li,{children:(0,s.jsx)(n.a,{href:"#best-practices",children:"Best Practices"})}),"\n"]}),"\n",(0,s.jsx)(n.h2,{id:"event-sourcing-fundamentals",children:"Event Sourcing Fundamentals"}),"\n",(0,s.jsx)(n.h3,{id:"what-is-event-sourcing",children:"What is Event Sourcing?"}),"\n",(0,s.jsx)(n.mermaid,{value:'graph LR\n    subgraph "Traditional CRUD"\n        A[Current State] --\x3e B[(Database)]\n        C[Update] --\x3e A\n    end\n\n    subgraph "Event Sourcing"\n        D[Event 1: Created] --\x3e E[(Event Store)]\n        F[Event 2: Updated] --\x3e E\n        G[Event 3: Approved] --\x3e E\n        E --\x3e H[Replay Events]\n        H --\x3e I[Current State]\n    end\n\n    style E fill:#4CAF50\n    style B fill:#FF9800'}),"\n",(0,s.jsx)(n.h3,{id:"benefits-and-drawbacks",children:"Benefits and Drawbacks"}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Benefits:"})}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Complete audit trail of all changes"}),"\n",(0,s.jsx)(n.li,{children:"Time travel - reconstruct state at any point"}),"\n",(0,s.jsx)(n.li,{children:"Event replay for debugging"}),"\n",(0,s.jsx)(n.li,{children:"Natural fit for event-driven architectures"}),"\n",(0,s.jsx)(n.li,{children:"No data loss - all events preserved"}),"\n"]}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Drawbacks:"})}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Complexity in querying current state"}),"\n",(0,s.jsx)(n.li,{children:"Storage growth over time"}),"\n",(0,s.jsx)(n.li,{children:"Learning curve for developers"}),"\n",(0,s.jsx)(n.li,{children:"Eventual consistency"}),"\n",(0,s.jsx)(n.li,{children:"Schema evolution challenges"}),"\n"]}),"\n",(0,s.jsx)(n.h3,{id:"event-store-vs-traditional-database",children:"Event Store vs Traditional Database"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// Traditional CRUD approach\ninterface Order {\n  id: string;\n  status: 'pending' | 'confirmed' | 'shipped' | 'delivered';\n  items: OrderItem[];\n  totalAmount: number;\n  createdAt: Date;\n  updatedAt: Date;\n}\n\n// Update operation (loses history)\nawait orderRepository.update(orderId, { status: 'shipped' });\n\n// Event Sourcing approach\ninterface OrderEvent {\n  eventId: string;\n  aggregateId: string;\n  eventType: string;\n  eventData: any;\n  metadata: {\n    userId: string;\n    timestamp: Date;\n    version: number;\n  };\n}\n\n// Events preserve complete history\nconst events = [\n  { eventType: 'OrderCreated', eventData: { items, totalAmount } },\n  { eventType: 'PaymentReceived', eventData: { paymentId } },\n  { eventType: 'OrderShipped', eventData: { trackingNumber } },\n  { eventType: 'OrderDelivered', eventData: { deliveredAt } },\n];\n"})}),"\n",(0,s.jsx)(n.h2,{id:"outbox-pattern",children:"Outbox Pattern"}),"\n",(0,s.jsx)(n.h3,{id:"the-dual-write-problem",children:"The Dual Write Problem"}),"\n",(0,s.jsx)(n.mermaid,{value:"sequenceDiagram\n    participant App as Application\n    participant DB as Database\n    participant Queue as Message Queue\n\n    Note over App,Queue: \u274c Dual Write Problem\n    App->>DB: Save Order\n    DB--\x3e>App: Success\n    App->>Queue: Publish Event\n    Queue--\x3e>App: Failure! \ud83d\udca5\n    Note over App,Queue: Order saved but event not published\n\n    Note over App,Queue: \u2705 Outbox Pattern Solution\n    App->>DB: Save Order + Outbox Event (Single Transaction)\n    DB--\x3e>App: Success\n\n    App->>DB: Poll Outbox\n    DB--\x3e>App: Pending Events\n    App->>Queue: Publish Event\n    Queue--\x3e>App: Success\n    App->>DB: Mark Event as Published"}),"\n",(0,s.jsx)(n.h3,{id:"outbox-pattern-architecture",children:"Outbox Pattern Architecture"}),"\n",(0,s.jsx)(n.mermaid,{value:"graph TB\n    A[Business Logic] --\x3e B[Database Transaction]\n    B --\x3e C[Save Entity]\n    B --\x3e D[Save Outbox Event]\n\n    E[Outbox Processor] --\x3e F[Poll Outbox Table]\n    F --\x3e G[Publish to Message Queue]\n    G --\x3e H[Mark as Published]\n\n    C --\x3e I[(Main Tables)]\n    D --\x3e J[(Outbox Table)]\n    F --\x3e J\n    H --\x3e J\n\n    G --\x3e K[Kafka/RabbitMQ]\n\n    style B fill:#4CAF50\n    style E fill:#2196F3"}),"\n",(0,s.jsx)(n.h3,{id:"outbox-table-schema",children:"Outbox Table Schema"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// shared/outbox/src/entities/outbox-event.entity.ts\nimport { Entity, Column, PrimaryGeneratedColumn, Index, CreateDateColumn } from 'typeorm';\n\nexport enum OutboxEventStatus {\n  PENDING = 'PENDING',\n  PUBLISHED = 'PUBLISHED',\n  FAILED = 'FAILED',\n}\n\n@Entity('outbox_events')\n@Index(['status', 'createdAt'])\n@Index(['aggregateId'])\nexport class OutboxEvent {\n  @PrimaryGeneratedColumn('uuid')\n  id: string;\n\n  @Column()\n  @Index()\n  aggregateId: string;\n\n  @Column()\n  aggregateType: string;\n\n  @Column()\n  eventType: string;\n\n  @Column('jsonb')\n  eventData: any;\n\n  @Column('jsonb', { nullable: true })\n  metadata: {\n    userId?: string;\n    traceId?: string;\n    correlationId?: string;\n    causationId?: string;\n  };\n\n  @Column({\n    type: 'enum',\n    enum: OutboxEventStatus,\n    default: OutboxEventStatus.PENDING,\n  })\n  @Index()\n  status: OutboxEventStatus;\n\n  @CreateDateColumn()\n  createdAt: Date;\n\n  @Column({ nullable: true })\n  publishedAt: Date;\n\n  @Column({ nullable: true })\n  failedAt: Date;\n\n  @Column({ nullable: true })\n  errorMessage: string;\n\n  @Column({ default: 0 })\n  retryCount: number;\n\n  @Column({ default: 3 })\n  maxRetries: number;\n\n  @Column()\n  version: number;\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"event-store-implementation",children:"Event Store Implementation"}),"\n",(0,s.jsx)(n.h3,{id:"event-store-entity",children:"Event Store Entity"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// shared/event-sourcing/src/entities/event-store.entity.ts\nimport { Entity, Column, PrimaryGeneratedColumn, Index, CreateDateColumn } from 'typeorm';\n\n@Entity('event_store')\n@Index(['aggregateId', 'version'], { unique: true })\n@Index(['aggregateType', 'createdAt'])\nexport class EventStoreEvent {\n  @PrimaryGeneratedColumn('uuid')\n  id: string;\n\n  @Column()\n  @Index()\n  aggregateId: string;\n\n  @Column()\n  @Index()\n  aggregateType: string;\n\n  @Column()\n  eventType: string;\n\n  @Column('jsonb')\n  eventData: any;\n\n  @Column('jsonb', { nullable: true })\n  metadata: {\n    userId?: string;\n    traceId?: string;\n    correlationId?: string;\n    causationId?: string;\n    ipAddress?: string;\n    userAgent?: string;\n  };\n\n  @Column()\n  version: number;\n\n  @CreateDateColumn()\n  @Index()\n  createdAt: Date;\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"event-store-service",children:"Event Store Service"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// shared/event-sourcing/src/event-store.service.ts\nimport { Injectable, Logger, ConflictException } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository, DataSource } from 'typeorm';\nimport { EventStoreEvent } from './entities/event-store.entity';\nimport { OutboxEvent, OutboxEventStatus } from '@ecommerce/outbox';\n\nexport interface DomainEvent {\n  aggregateId: string;\n  aggregateType: string;\n  eventType: string;\n  eventData: any;\n  metadata?: any;\n}\n\n@Injectable()\nexport class EventStoreService {\n  private readonly logger = new Logger(EventStoreService.name);\n\n  constructor(\n    @InjectRepository(EventStoreEvent)\n    private eventStoreRepository: Repository<EventStoreEvent>,\n    @InjectRepository(OutboxEvent)\n    private outboxRepository: Repository<OutboxEvent>,\n    private dataSource: DataSource,\n  ) {}\n\n  /**\n   * Append event to event store and outbox in a single transaction\n   * This ensures atomicity - both or neither are saved\n   */\n  async appendEvent(event: DomainEvent): Promise<EventStoreEvent> {\n    return this.dataSource.transaction(async (manager) => {\n      // Get current version for this aggregate\n      const currentVersion = await this.getCurrentVersion(event.aggregateId, manager);\n      const newVersion = currentVersion + 1;\n\n      // Save to event store\n      const eventStoreEvent = manager.create(EventStoreEvent, {\n        aggregateId: event.aggregateId,\n        aggregateType: event.aggregateType,\n        eventType: event.eventType,\n        eventData: event.eventData,\n        metadata: event.metadata,\n        version: newVersion,\n      });\n\n      let savedEvent: EventStoreEvent;\n      try {\n        savedEvent = await manager.save(eventStoreEvent);\n      } catch (error) {\n        if (error.code === '23505') {\n          // Unique constraint violation\n          throw new ConflictException(`Version ${newVersion} already exists for aggregate ${event.aggregateId}`);\n        }\n        throw error;\n      }\n\n      // Save to outbox for reliable publishing\n      const outboxEvent = manager.create(OutboxEvent, {\n        aggregateId: event.aggregateId,\n        aggregateType: event.aggregateType,\n        eventType: event.eventType,\n        eventData: event.eventData,\n        metadata: event.metadata,\n        status: OutboxEventStatus.PENDING,\n        version: newVersion,\n      });\n\n      await manager.save(outboxEvent);\n\n      this.logger.log(\n        `Event stored: ${event.eventType} for ${event.aggregateType}:${event.aggregateId} (v${newVersion})`,\n      );\n\n      return savedEvent;\n    });\n  }\n\n  /**\n   * Append multiple events in a single transaction\n   */\n  async appendEvents(events: DomainEvent[]): Promise<EventStoreEvent[]> {\n    if (events.length === 0) return [];\n\n    // Verify all events are for the same aggregate\n    const aggregateId = events[0].aggregateId;\n    if (!events.every((e) => e.aggregateId === aggregateId)) {\n      throw new Error('All events must belong to the same aggregate');\n    }\n\n    return this.dataSource.transaction(async (manager) => {\n      const currentVersion = await this.getCurrentVersion(aggregateId, manager);\n      const savedEvents: EventStoreEvent[] = [];\n\n      for (let i = 0; i < events.length; i++) {\n        const event = events[i];\n        const newVersion = currentVersion + i + 1;\n\n        // Save to event store\n        const eventStoreEvent = manager.create(EventStoreEvent, {\n          aggregateId: event.aggregateId,\n          aggregateType: event.aggregateType,\n          eventType: event.eventType,\n          eventData: event.eventData,\n          metadata: event.metadata,\n          version: newVersion,\n        });\n\n        const savedEvent = await manager.save(eventStoreEvent);\n        savedEvents.push(savedEvent);\n\n        // Save to outbox\n        const outboxEvent = manager.create(OutboxEvent, {\n          aggregateId: event.aggregateId,\n          aggregateType: event.aggregateType,\n          eventType: event.eventType,\n          eventData: event.eventData,\n          metadata: event.metadata,\n          status: OutboxEventStatus.PENDING,\n          version: newVersion,\n        });\n\n        await manager.save(outboxEvent);\n      }\n\n      this.logger.log(`${events.length} events stored for aggregate ${aggregateId}`);\n\n      return savedEvents;\n    });\n  }\n\n  /**\n   * Get all events for an aggregate\n   */\n  async getEvents(aggregateId: string): Promise<EventStoreEvent[]> {\n    return this.eventStoreRepository.find({\n      where: { aggregateId },\n      order: { version: 'ASC' },\n    });\n  }\n\n  /**\n   * Get events since a specific version\n   */\n  async getEventsSince(aggregateId: string, version: number): Promise<EventStoreEvent[]> {\n    return this.eventStoreRepository\n      .createQueryBuilder('event')\n      .where('event.aggregateId = :aggregateId', { aggregateId })\n      .andWhere('event.version > :version', { version })\n      .orderBy('event.version', 'ASC')\n      .getMany();\n  }\n\n  /**\n   * Get events by type for analytics/reporting\n   */\n  async getEventsByType(eventType: string, startDate?: Date, endDate?: Date): Promise<EventStoreEvent[]> {\n    const query = this.eventStoreRepository\n      .createQueryBuilder('event')\n      .where('event.eventType = :eventType', { eventType });\n\n    if (startDate) {\n      query.andWhere('event.createdAt >= :startDate', { startDate });\n    }\n\n    if (endDate) {\n      query.andWhere('event.createdAt <= :endDate', { endDate });\n    }\n\n    return query.orderBy('event.createdAt', 'DESC').getMany();\n  }\n\n  /**\n   * Stream events (for large datasets)\n   */\n  async *streamEvents(aggregateType: string, batchSize: number = 100): AsyncGenerator<EventStoreEvent[]> {\n    let lastId = '';\n\n    while (true) {\n      const query = this.eventStoreRepository\n        .createQueryBuilder('event')\n        .where('event.aggregateType = :aggregateType', { aggregateType })\n        .orderBy('event.id', 'ASC')\n        .take(batchSize);\n\n      if (lastId) {\n        query.andWhere('event.id > :lastId', { lastId });\n      }\n\n      const events = await query.getMany();\n\n      if (events.length === 0) {\n        break;\n      }\n\n      yield events;\n\n      lastId = events[events.length - 1].id;\n\n      if (events.length < batchSize) {\n        break;\n      }\n    }\n  }\n\n  private async getCurrentVersion(aggregateId: string, manager?: any): Promise<number> {\n    const repo = manager ? manager.getRepository(EventStoreEvent) : this.eventStoreRepository;\n\n    const lastEvent = await repo\n      .createQueryBuilder('event')\n      .where('event.aggregateId = :aggregateId', { aggregateId })\n      .orderBy('event.version', 'DESC')\n      .getOne();\n\n    return lastEvent ? lastEvent.version : 0;\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"aggregate-design",children:"Aggregate Design"}),"\n",(0,s.jsx)(n.h3,{id:"base-aggregate-root",children:"Base Aggregate Root"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// shared/event-sourcing/src/aggregate-root.ts\nimport { AggregateRoot as NestAggregateRoot } from '@nestjs/cqrs';\n\nexport abstract class AggregateRoot extends NestAggregateRoot {\n  private version: number = 0;\n  private uncommittedEvents: any[] = [];\n\n  protected apply(event: any): void {\n    this.applyEvent(event);\n    this.uncommittedEvents.push(event);\n  }\n\n  protected abstract applyEvent(event: any): void;\n\n  loadFromHistory(events: any[]): void {\n    events.forEach((event) => {\n      this.applyEvent(event);\n      this.version++;\n    });\n  }\n\n  getUncommittedEvents(): any[] {\n    return this.uncommittedEvents;\n  }\n\n  markEventsAsCommitted(): void {\n    this.uncommittedEvents = [];\n  }\n\n  getVersion(): number {\n    return this.version;\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"order-aggregate-example",children:"Order Aggregate Example"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/aggregates/order.aggregate.ts\nimport { AggregateRoot } from '@ecommerce/event-sourcing';\n\n// Domain Events\nexport class OrderCreatedEvent {\n  constructor(\n    public readonly orderId: string,\n    public readonly userId: string,\n    public readonly items: OrderItem[],\n    public readonly totalAmount: number,\n  ) {}\n}\n\nexport class OrderItemAddedEvent {\n  constructor(public readonly orderId: string, public readonly item: OrderItem) {}\n}\n\nexport class OrderItemRemovedEvent {\n  constructor(public readonly orderId: string, public readonly itemId: string) {}\n}\n\nexport class PaymentReceivedEvent {\n  constructor(public readonly orderId: string, public readonly paymentId: string, public readonly amount: number) {}\n}\n\nexport class OrderShippedEvent {\n  constructor(public readonly orderId: string, public readonly trackingNumber: string) {}\n}\n\nexport class OrderCancelledEvent {\n  constructor(public readonly orderId: string, public readonly reason: string) {}\n}\n\n// Aggregate\nexport class OrderAggregate extends AggregateRoot {\n  private id: string;\n  private userId: string;\n  private items: Map<string, OrderItem> = new Map();\n  private totalAmount: number = 0;\n  private status: OrderStatus = OrderStatus.DRAFT;\n  private paymentId?: string;\n  private trackingNumber?: string;\n\n  constructor(id: string) {\n    super();\n    this.id = id;\n  }\n\n  // Commands\n\n  static create(orderId: string, userId: string, items: OrderItem[]): OrderAggregate {\n    const order = new OrderAggregate(orderId);\n    const totalAmount = items.reduce((sum, item) => sum + item.price * item.quantity, 0);\n\n    order.apply(new OrderCreatedEvent(orderId, userId, items, totalAmount));\n    return order;\n  }\n\n  addItem(item: OrderItem): void {\n    if (this.status !== OrderStatus.DRAFT) {\n      throw new Error('Cannot add items to a non-draft order');\n    }\n\n    this.apply(new OrderItemAddedEvent(this.id, item));\n  }\n\n  removeItem(itemId: string): void {\n    if (this.status !== OrderStatus.DRAFT) {\n      throw new Error('Cannot remove items from a non-draft order');\n    }\n\n    if (!this.items.has(itemId)) {\n      throw new Error('Item not found in order');\n    }\n\n    this.apply(new OrderItemRemovedEvent(this.id, itemId));\n  }\n\n  receivePayment(paymentId: string, amount: number): void {\n    if (this.status !== OrderStatus.PENDING_PAYMENT) {\n      throw new Error('Order is not awaiting payment');\n    }\n\n    if (amount < this.totalAmount) {\n      throw new Error('Payment amount is insufficient');\n    }\n\n    this.apply(new PaymentReceivedEvent(this.id, paymentId, amount));\n  }\n\n  ship(trackingNumber: string): void {\n    if (this.status !== OrderStatus.PAID) {\n      throw new Error('Order must be paid before shipping');\n    }\n\n    this.apply(new OrderShippedEvent(this.id, trackingNumber));\n  }\n\n  cancel(reason: string): void {\n    if (this.status === OrderStatus.SHIPPED || this.status === OrderStatus.DELIVERED) {\n      throw new Error('Cannot cancel a shipped or delivered order');\n    }\n\n    this.apply(new OrderCancelledEvent(this.id, reason));\n  }\n\n  // Event Handlers (State Mutations)\n\n  protected applyEvent(event: any): void {\n    if (event instanceof OrderCreatedEvent) {\n      this.onOrderCreated(event);\n    } else if (event instanceof OrderItemAddedEvent) {\n      this.onOrderItemAdded(event);\n    } else if (event instanceof OrderItemRemovedEvent) {\n      this.onOrderItemRemoved(event);\n    } else if (event instanceof PaymentReceivedEvent) {\n      this.onPaymentReceived(event);\n    } else if (event instanceof OrderShippedEvent) {\n      this.onOrderShipped(event);\n    } else if (event instanceof OrderCancelledEvent) {\n      this.onOrderCancelled(event);\n    }\n  }\n\n  private onOrderCreated(event: OrderCreatedEvent): void {\n    this.id = event.orderId;\n    this.userId = event.userId;\n    event.items.forEach((item) => this.items.set(item.id, item));\n    this.totalAmount = event.totalAmount;\n    this.status = OrderStatus.DRAFT;\n  }\n\n  private onOrderItemAdded(event: OrderItemAddedEvent): void {\n    this.items.set(event.item.id, event.item);\n    this.totalAmount += event.item.price * event.item.quantity;\n  }\n\n  private onOrderItemRemoved(event: OrderItemRemovedEvent): void {\n    const item = this.items.get(event.itemId);\n    if (item) {\n      this.totalAmount -= item.price * item.quantity;\n      this.items.delete(event.itemId);\n    }\n  }\n\n  private onPaymentReceived(event: PaymentReceivedEvent): void {\n    this.paymentId = event.paymentId;\n    this.status = OrderStatus.PAID;\n  }\n\n  private onOrderShipped(event: OrderShippedEvent): void {\n    this.trackingNumber = event.trackingNumber;\n    this.status = OrderStatus.SHIPPED;\n  }\n\n  private onOrderCancelled(event: OrderCancelledEvent): void {\n    this.status = OrderStatus.CANCELLED;\n  }\n\n  // Getters\n\n  getId(): string {\n    return this.id;\n  }\n\n  getStatus(): OrderStatus {\n    return this.status;\n  }\n\n  getTotalAmount(): number {\n    return this.totalAmount;\n  }\n\n  getItems(): OrderItem[] {\n    return Array.from(this.items.values());\n  }\n}\n\nenum OrderStatus {\n  DRAFT = 'DRAFT',\n  PENDING_PAYMENT = 'PENDING_PAYMENT',\n  PAID = 'PAID',\n  SHIPPED = 'SHIPPED',\n  DELIVERED = 'DELIVERED',\n  CANCELLED = 'CANCELLED',\n}\n\ninterface OrderItem {\n  id: string;\n  productId: string;\n  name: string;\n  price: number;\n  quantity: number;\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"event-publishing",children:"Event Publishing"}),"\n",(0,s.jsx)(n.h3,{id:"outbox-processor",children:"Outbox Processor"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// shared/outbox/src/outbox-processor.service.ts\nimport { Injectable, Logger, OnModuleInit, OnModuleDestroy } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository, LessThan } from 'typeorm';\nimport { Cron, CronExpression } from '@nestjs/schedule';\nimport { OutboxEvent, OutboxEventStatus } from './entities/outbox-event.entity';\nimport { KafkaService } from '@ecommerce/kafka';\n\n@Injectable()\nexport class OutboxProcessorService implements OnModuleInit, OnModuleDestroy {\n  private readonly logger = new Logger(OutboxProcessorService.name);\n  private isProcessing = false;\n  private processingInterval: NodeJS.Timer;\n\n  constructor(\n    @InjectRepository(OutboxEvent)\n    private outboxRepository: Repository<OutboxEvent>,\n    private kafkaService: KafkaService,\n  ) {}\n\n  async onModuleInit() {\n    // Start immediate processing\n    this.processOutbox();\n\n    // Set up periodic processing\n    this.processingInterval = setInterval(() => {\n      if (!this.isProcessing) {\n        this.processOutbox();\n      }\n    }, 5000); // Process every 5 seconds\n  }\n\n  onModuleDestroy() {\n    if (this.processingInterval) {\n      clearInterval(this.processingInterval);\n    }\n  }\n\n  /**\n   * Process pending outbox events\n   */\n  @Cron(CronExpression.EVERY_10_SECONDS)\n  async processOutbox(): Promise<void> {\n    if (this.isProcessing) {\n      return;\n    }\n\n    this.isProcessing = true;\n\n    try {\n      const batchSize = 100;\n      const events = await this.outboxRepository.find({\n        where: { status: OutboxEventStatus.PENDING },\n        order: { createdAt: 'ASC' },\n        take: batchSize,\n      });\n\n      if (events.length === 0) {\n        return;\n      }\n\n      this.logger.debug(`Processing ${events.length} outbox events`);\n\n      for (const event of events) {\n        await this.processEvent(event);\n      }\n\n      // Retry failed events\n      await this.retryFailedEvents();\n    } catch (error) {\n      this.logger.error('Error processing outbox', error.stack);\n    } finally {\n      this.isProcessing = false;\n    }\n  }\n\n  private async processEvent(event: OutboxEvent): Promise<void> {\n    try {\n      // Publish to Kafka\n      await this.kafkaService.publish(event.eventType, {\n        aggregateId: event.aggregateId,\n        aggregateType: event.aggregateType,\n        eventType: event.eventType,\n        eventData: event.eventData,\n        metadata: {\n          ...event.metadata,\n          version: event.version,\n          publishedAt: new Date(),\n        },\n      });\n\n      // Mark as published\n      await this.outboxRepository.update(event.id, {\n        status: OutboxEventStatus.PUBLISHED,\n        publishedAt: new Date(),\n      });\n\n      this.logger.debug(`Published event: ${event.eventType} (${event.id})`);\n    } catch (error) {\n      this.logger.error(`Failed to publish event ${event.id}: ${error.message}`, error.stack);\n\n      // Update retry count\n      const retryCount = event.retryCount + 1;\n      const status = retryCount >= event.maxRetries ? OutboxEventStatus.FAILED : OutboxEventStatus.PENDING;\n\n      await this.outboxRepository.update(event.id, {\n        retryCount,\n        status,\n        failedAt: status === OutboxEventStatus.FAILED ? new Date() : undefined,\n        errorMessage: error.message,\n      });\n    }\n  }\n\n  private async retryFailedEvents(): Promise<void> {\n    // Retry events that failed less than max retries\n    const retryableEvents = await this.outboxRepository.find({\n      where: {\n        status: OutboxEventStatus.PENDING,\n        retryCount: LessThan(3),\n      },\n      order: { createdAt: 'ASC' },\n      take: 10,\n    });\n\n    for (const event of retryableEvents) {\n      await this.processEvent(event);\n    }\n  }\n\n  /**\n   * Clean up old published events (optional)\n   */\n  @Cron(CronExpression.EVERY_DAY_AT_MIDNIGHT)\n  async cleanupOldEvents(): Promise<void> {\n    const retentionDays = 30;\n    const cutoffDate = new Date();\n    cutoffDate.setDate(cutoffDate.getDate() - retentionDays);\n\n    const result = await this.outboxRepository\n      .createQueryBuilder()\n      .delete()\n      .where('status = :status', { status: OutboxEventStatus.PUBLISHED })\n      .andWhere('publishedAt < :cutoffDate', { cutoffDate })\n      .execute();\n\n    this.logger.log(`Cleaned up ${result.affected} old outbox events`);\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"repository-with-event-publishing",children:"Repository with Event Publishing"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/repositories/order.repository.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { EventStoreService, DomainEvent } from '@ecommerce/event-sourcing';\nimport { OrderAggregate } from '../aggregates/order.aggregate';\n\n@Injectable()\nexport class OrderRepository {\n  private readonly logger = new Logger(OrderRepository.name);\n\n  constructor(private eventStore: EventStoreService) {}\n\n  /**\n   * Load aggregate from event store\n   */\n  async load(orderId: string): Promise<OrderAggregate> {\n    const events = await this.eventStore.getEvents(orderId);\n\n    if (events.length === 0) {\n      throw new Error(`Order ${orderId} not found`);\n    }\n\n    const order = new OrderAggregate(orderId);\n    order.loadFromHistory(events.map((e) => this.toDomainEvent(e)));\n\n    return order;\n  }\n\n  /**\n   * Save aggregate (append new events)\n   */\n  async save(order: OrderAggregate): Promise<void> {\n    const uncommittedEvents = order.getUncommittedEvents();\n\n    if (uncommittedEvents.length === 0) {\n      return;\n    }\n\n    const domainEvents: DomainEvent[] = uncommittedEvents.map((event) => ({\n      aggregateId: order.getId(),\n      aggregateType: 'Order',\n      eventType: event.constructor.name,\n      eventData: this.serializeEvent(event),\n      metadata: {\n        // Add correlation metadata\n        traceId: '...', // From correlation service\n        userId: '...',\n      },\n    }));\n\n    // Save events (both event store and outbox in transaction)\n    await this.eventStore.appendEvents(domainEvents);\n\n    // Mark events as committed\n    order.markEventsAsCommitted();\n\n    this.logger.log(`Saved ${uncommittedEvents.length} events for order ${order.getId()}`);\n  }\n\n  private toDomainEvent(eventStoreEvent: any): any {\n    // Reconstruct domain event from stored event\n    const EventClass = this.getEventClass(eventStoreEvent.eventType);\n    return Object.assign(new EventClass(), eventStoreEvent.eventData);\n  }\n\n  private serializeEvent(event: any): any {\n    // Convert domain event to plain object for storage\n    const { constructor, ...data } = event;\n    return data;\n  }\n\n  private getEventClass(eventType: string): any {\n    // Map event type name to class\n    const eventClasses = {\n      OrderCreatedEvent: require('../aggregates/order.aggregate').OrderCreatedEvent,\n      OrderItemAddedEvent: require('../aggregates/order.aggregate').OrderItemAddedEvent,\n      // ... other events\n    };\n\n    return eventClasses[eventType];\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"event-replay-and-projections",children:"Event Replay and Projections"}),"\n",(0,s.jsx)(n.h3,{id:"projection-service",children:"Projection Service"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/projections/order-projection.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { OnEvent } from '@nestjs/event-emitter';\nimport { OrderReadModel } from './entities/order-read-model.entity';\n\n@Injectable()\nexport class OrderProjectionService {\n  private readonly logger = new Logger(OrderProjectionService.name);\n\n  constructor(\n    @InjectRepository(OrderReadModel)\n    private orderReadModelRepository: Repository<OrderReadModel>,\n  ) {}\n\n  @OnEvent('OrderCreatedEvent')\n  async handleOrderCreated(event: any): Promise<void> {\n    await this.orderReadModelRepository.save({\n      id: event.orderId,\n      userId: event.userId,\n      items: event.items,\n      totalAmount: event.totalAmount,\n      status: 'DRAFT',\n      createdAt: new Date(),\n      updatedAt: new Date(),\n    });\n\n    this.logger.debug(`Projected OrderCreatedEvent: ${event.orderId}`);\n  }\n\n  @OnEvent('PaymentReceivedEvent')\n  async handlePaymentReceived(event: any): Promise<void> {\n    await this.orderReadModelRepository.update(event.orderId, {\n      paymentId: event.paymentId,\n      status: 'PAID',\n      updatedAt: new Date(),\n    });\n\n    this.logger.debug(`Projected PaymentReceivedEvent: ${event.orderId}`);\n  }\n\n  @OnEvent('OrderShippedEvent')\n  async handleOrderShipped(event: any): Promise<void> {\n    await this.orderReadModelRepository.update(event.orderId, {\n      trackingNumber: event.trackingNumber,\n      status: 'SHIPPED',\n      shippedAt: new Date(),\n      updatedAt: new Date(),\n    });\n\n    this.logger.debug(`Projected OrderShippedEvent: ${event.orderId}`);\n  }\n\n  @OnEvent('OrderCancelledEvent')\n  async handleOrderCancelled(event: any): Promise<void> {\n    await this.orderReadModelRepository.update(event.orderId, {\n      status: 'CANCELLED',\n      cancelledAt: new Date(),\n      cancellationReason: event.reason,\n      updatedAt: new Date(),\n    });\n\n    this.logger.debug(`Projected OrderCancelledEvent: ${event.orderId}`);\n  }\n\n  /**\n   * Rebuild projection from event store\n   */\n  async rebuildProjection(orderId: string): Promise<void> {\n    this.logger.log(`Rebuilding projection for order ${orderId}`);\n\n    // Delete existing read model\n    await this.orderReadModelRepository.delete(orderId);\n\n    // Replay events\n    const aggregate = await this.orderRepository.load(orderId);\n\n    // Project current state\n    await this.orderReadModelRepository.save({\n      id: aggregate.getId(),\n      status: aggregate.getStatus(),\n      totalAmount: aggregate.getTotalAmount(),\n      items: aggregate.getItems(),\n      // ... other fields\n    });\n\n    this.logger.log(`Rebuilt projection for order ${orderId}`);\n  }\n\n  /**\n   * Rebuild all projections\n   */\n  async rebuildAllProjections(): Promise<void> {\n    this.logger.log('Rebuilding all order projections');\n\n    // Clear all read models\n    await this.orderReadModelRepository.clear();\n\n    // Stream all events and replay\n    for await (const eventBatch of this.eventStore.streamEvents('Order')) {\n      for (const event of eventBatch) {\n        // Process each event\n        const eventHandler = this[`handle${event.eventType}`];\n        if (eventHandler) {\n          await eventHandler.call(this, event.eventData);\n        }\n      }\n    }\n\n    this.logger.log('Finished rebuilding all order projections');\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"snapshots",children:"Snapshots"}),"\n",(0,s.jsx)(n.h3,{id:"snapshot-entity",children:"Snapshot Entity"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// shared/event-sourcing/src/entities/snapshot.entity.ts\nimport { Entity, Column, PrimaryColumn, CreateDateColumn, Index } from 'typeorm';\n\n@Entity('snapshots')\n@Index(['aggregateType', 'createdAt'])\nexport class Snapshot {\n  @PrimaryColumn()\n  aggregateId: string;\n\n  @Column()\n  aggregateType: string;\n\n  @Column('jsonb')\n  state: any;\n\n  @Column()\n  version: number;\n\n  @CreateDateColumn()\n  createdAt: Date;\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"snapshot-service",children:"Snapshot Service"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// shared/event-sourcing/src/snapshot.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { Snapshot } from './entities/snapshot.entity';\nimport { EventStoreService } from './event-store.service';\n\n@Injectable()\nexport class SnapshotService {\n  private readonly logger = new Logger(SnapshotService.name);\n  private readonly snapshotInterval = 10; // Take snapshot every 10 events\n\n  constructor(\n    @InjectRepository(Snapshot)\n    private snapshotRepository: Repository<Snapshot>,\n    private eventStore: EventStoreService,\n  ) {}\n\n  async saveSnapshot(aggregateId: string, aggregateType: string, state: any, version: number): Promise<void> {\n    await this.snapshotRepository.save({\n      aggregateId,\n      aggregateType,\n      state,\n      version,\n    });\n\n    this.logger.debug(`Snapshot saved for ${aggregateType}:${aggregateId} at version ${version}`);\n  }\n\n  async getSnapshot(aggregateId: string): Promise<Snapshot | null> {\n    return this.snapshotRepository.findOne({\n      where: { aggregateId },\n      order: { version: 'DESC' },\n    });\n  }\n\n  async shouldTakeSnapshot(version: number): boolean {\n    return version % this.snapshotInterval === 0;\n  }\n\n  /**\n   * Load aggregate with snapshot optimization\n   */\n  async loadAggregateWithSnapshot<T>(\n    aggregateId: string,\n    aggregateFactory: (state?: any) => T,\n    applyEvents: (aggregate: T, events: any[]) => void,\n  ): Promise<T> {\n    // Try to load from snapshot\n    const snapshot = await this.getSnapshot(aggregateId);\n\n    let aggregate: T;\n    let startVersion = 0;\n\n    if (snapshot) {\n      // Restore from snapshot\n      aggregate = aggregateFactory(snapshot.state);\n      startVersion = snapshot.version;\n      this.logger.debug(`Loaded snapshot at version ${startVersion} for ${aggregateId}`);\n    } else {\n      // No snapshot, create new\n      aggregate = aggregateFactory();\n    }\n\n    // Load events since snapshot\n    const events = await this.eventStore.getEventsSince(aggregateId, startVersion);\n\n    if (events.length > 0) {\n      applyEvents(aggregate, events);\n      this.logger.debug(`Applied ${events.length} events since snapshot for ${aggregateId}`);\n    }\n\n    return aggregate;\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"cqrs-integration",children:"CQRS Integration"}),"\n",(0,s.jsx)(n.h3,{id:"command-handler",children:"Command Handler"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/commands/create-order.handler.ts\nimport { CommandHandler, ICommandHandler, EventPublisher } from '@nestjs/cqrs';\nimport { OrderRepository } from '../repositories/order.repository';\nimport { OrderAggregate } from '../aggregates/order.aggregate';\n\nexport class CreateOrderCommand {\n  constructor(public readonly orderId: string, public readonly userId: string, public readonly items: OrderItem[]) {}\n}\n\n@CommandHandler(CreateOrderCommand)\nexport class CreateOrderHandler implements ICommandHandler<CreateOrderCommand> {\n  constructor(private orderRepository: OrderRepository, private publisher: EventPublisher) {}\n\n  async execute(command: CreateOrderCommand): Promise<void> {\n    // Create aggregate\n    const order = OrderAggregate.create(command.orderId, command.userId, command.items);\n\n    // Merge with event publisher for auto-publishing\n    const orderWithPublisher = this.publisher.mergeObjectContext(order);\n\n    // Save (appends events to event store and outbox)\n    await this.orderRepository.save(order);\n\n    // Commit (publishes events to event bus)\n    orderWithPublisher.commit();\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h3,{id:"query-handler",children:"Query Handler"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-typescript",children:"// services/order-service/src/queries/get-order.handler.ts\nimport { IQueryHandler, QueryHandler } from '@nestjs/cqrs';\nimport { InjectRepository } from '@nestjs/typeorm';\nimport { Repository } from 'typeorm';\nimport { OrderReadModel } from '../projections/entities/order-read-model.entity';\n\nexport class GetOrderQuery {\n  constructor(public readonly orderId: string) {}\n}\n\n@QueryHandler(GetOrderQuery)\nexport class GetOrderHandler implements IQueryHandler<GetOrderQuery> {\n  constructor(\n    @InjectRepository(OrderReadModel)\n    private orderReadModelRepository: Repository<OrderReadModel>,\n  ) {}\n\n  async execute(query: GetOrderQuery): Promise<OrderReadModel> {\n    return this.orderReadModelRepository.findOneOrFail({\n      where: { id: query.orderId },\n    });\n  }\n}\n"})}),"\n",(0,s.jsx)(n.h2,{id:"best-practices",children:"Best Practices"}),"\n",(0,s.jsx)(n.h3,{id:"1-event-design",children:"1. Event Design"}),"\n",(0,s.jsxs)(n.p,{children:["\u2705 ",(0,s.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Name events in past tense (OrderCreated, not CreateOrder)"}),"\n",(0,s.jsx)(n.li,{children:"Make events immutable"}),"\n",(0,s.jsx)(n.li,{children:"Include all necessary data in events"}),"\n",(0,s.jsx)(n.li,{children:"Use semantic versioning for event schemas"}),"\n",(0,s.jsx)(n.li,{children:"Keep events small and focused"}),"\n"]}),"\n",(0,s.jsxs)(n.p,{children:["\u274c ",(0,s.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Change published event structures"}),"\n",(0,s.jsx)(n.li,{children:"Include sensitive data without encryption"}),"\n",(0,s.jsx)(n.li,{children:"Create overly generic events"}),"\n",(0,s.jsx)(n.li,{children:"Forget correlation metadata"}),"\n",(0,s.jsx)(n.li,{children:"Mix commands and events"}),"\n"]}),"\n",(0,s.jsx)(n.h3,{id:"2-aggregate-design",children:"2. Aggregate Design"}),"\n",(0,s.jsxs)(n.p,{children:["\u2705 ",(0,s.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Keep aggregates focused and small"}),"\n",(0,s.jsx)(n.li,{children:"Enforce business rules in aggregates"}),"\n",(0,s.jsx)(n.li,{children:"Use value objects for complex data"}),"\n",(0,s.jsx)(n.li,{children:"Make state changes only through events"}),"\n",(0,s.jsx)(n.li,{children:"Implement proper validation"}),"\n"]}),"\n",(0,s.jsxs)(n.p,{children:["\u274c ",(0,s.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Create god aggregates"}),"\n",(0,s.jsx)(n.li,{children:"Access other aggregates directly"}),"\n",(0,s.jsx)(n.li,{children:"Perform I/O in aggregates"}),"\n",(0,s.jsx)(n.li,{children:"Skip event versioning"}),"\n",(0,s.jsx)(n.li,{children:"Expose mutable state"}),"\n"]}),"\n",(0,s.jsx)(n.h3,{id:"3-event-store",children:"3. Event Store"}),"\n",(0,s.jsxs)(n.p,{children:["\u2705 ",(0,s.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Use optimistic locking (version numbers)"}),"\n",(0,s.jsx)(n.li,{children:"Index by aggregate ID"}),"\n",(0,s.jsx)(n.li,{children:"Partition by aggregate type"}),"\n",(0,s.jsx)(n.li,{children:"Implement snapshots for long-lived aggregates"}),"\n",(0,s.jsx)(n.li,{children:"Monitor event store performance"}),"\n"]}),"\n",(0,s.jsxs)(n.p,{children:["\u274c ",(0,s.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Delete events"}),"\n",(0,s.jsx)(n.li,{children:"Modify published events"}),"\n",(0,s.jsx)(n.li,{children:"Skip version checks"}),"\n",(0,s.jsx)(n.li,{children:"Ignore concurrency conflicts"}),"\n",(0,s.jsx)(n.li,{children:"Forget about storage growth"}),"\n"]}),"\n",(0,s.jsx)(n.h3,{id:"4-outbox-pattern",children:"4. Outbox Pattern"}),"\n",(0,s.jsxs)(n.p,{children:["\u2705 ",(0,s.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Process outbox in background"}),"\n",(0,s.jsx)(n.li,{children:"Implement retry logic"}),"\n",(0,s.jsx)(n.li,{children:"Monitor outbox lag"}),"\n",(0,s.jsx)(n.li,{children:"Clean up old published events"}),"\n",(0,s.jsx)(n.li,{children:"Use database transactions"}),"\n"]}),"\n",(0,s.jsxs)(n.p,{children:["\u274c ",(0,s.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Publish events directly from business logic"}),"\n",(0,s.jsx)(n.li,{children:"Skip error handling"}),"\n",(0,s.jsx)(n.li,{children:"Let outbox table grow indefinitely"}),"\n",(0,s.jsx)(n.li,{children:"Forget idempotency"}),"\n",(0,s.jsx)(n.li,{children:"Ignore failed events"}),"\n"]}),"\n",(0,s.jsx)(n.h3,{id:"5-projections",children:"5. Projections"}),"\n",(0,s.jsxs)(n.p,{children:["\u2705 ",(0,s.jsx)(n.strong,{children:"Do:"})]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Build projections asynchronously"}),"\n",(0,s.jsx)(n.li,{children:"Support projection rebuilds"}),"\n",(0,s.jsx)(n.li,{children:"Version projection schemas"}),"\n",(0,s.jsx)(n.li,{children:"Use separate databases for reads"}),"\n",(0,s.jsx)(n.li,{children:"Monitor projection lag"}),"\n"]}),"\n",(0,s.jsxs)(n.p,{children:["\u274c ",(0,s.jsx)(n.strong,{children:"Don't:"})]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Query event store directly for reads"}),"\n",(0,s.jsx)(n.li,{children:"Couple projections to event store schema"}),"\n",(0,s.jsx)(n.li,{children:"Skip error handling"}),"\n",(0,s.jsx)(n.li,{children:"Forget about eventual consistency"}),"\n",(0,s.jsx)(n.li,{children:"Build real-time projections without caching"}),"\n"]}),"\n",(0,s.jsx)(n.h2,{id:"summary",children:"Summary"}),"\n",(0,s.jsx)(n.p,{children:"Event Sourcing and Outbox pattern provide:"}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Complete Audit Trail"})," - Every state change is recorded"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Reliable Event Publishing"})," - No dual-write problems"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Temporal Queries"})," - Reconstruct state at any point"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Event Replay"})," - Rebuild projections from events"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Scalability"})," - Separate read and write models"]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"Debugging"})," - Full history for troubleshooting"]}),"\n"]}),"\n",(0,s.jsx)(n.p,{children:"Key implementation points:"}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"Store all state changes as immutable events"}),"\n",(0,s.jsx)(n.li,{children:"Use outbox pattern for reliable publishing"}),"\n",(0,s.jsx)(n.li,{children:"Implement CQRS for read/write separation"}),"\n",(0,s.jsx)(n.li,{children:"Use snapshots for performance optimization"}),"\n",(0,s.jsx)(n.li,{children:"Monitor outbox processing lag"}),"\n",(0,s.jsx)(n.li,{children:"Support projection rebuilds"}),"\n"]}),"\n",(0,s.jsx)(n.p,{children:"Remember: Event Sourcing adds complexity but provides powerful capabilities for audit, compliance, and debugging!"})]})}function v(e={}){const{wrapper:n}={...(0,a.R)(),...e.components};return n?(0,s.jsx)(n,{...e,children:(0,s.jsx)(c,{...e})}):c(e)}}}]);