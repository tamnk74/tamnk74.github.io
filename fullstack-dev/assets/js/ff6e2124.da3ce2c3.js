"use strict";(self.webpackChunkdocs_site=self.webpackChunkdocs_site||[]).push([[44069],{5741:(e,n,s)=>{s.d(n,{R:()=>a,x:()=>c});var t=s(59729);const i={},r=t.createContext(i);function a(e){const n=t.useContext(r);return t.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function c(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:a(e.components),t.createElement(r.Provider,{value:n},e.children)}},88855:(e,n,s)=>{s.r(n),s.d(n,{assets:()=>o,contentTitle:()=>c,default:()=>h,frontMatter:()=>a,metadata:()=>t,toc:()=>l});const t=JSON.parse('{"id":"backend/redis-pubsub-scaling-patterns","title":"Redis Pub/Sub Scaling Patterns","description":"\ud83d\udcd6 Overview","source":"@site/docs/backend/redis-pubsub-scaling-patterns.md","sourceDirName":"backend","slug":"/backend/redis-pubsub-scaling-patterns","permalink":"/fullstack-dev/docs/backend/redis-pubsub-scaling-patterns","draft":false,"unlisted":false,"editUrl":"https://github.com/tamnk74/fullstack-dev/tree/main/docs-site/docs/backend/redis-pubsub-scaling-patterns.md","tags":[],"version":"current","frontMatter":{},"sidebar":"tutorialSidebar","previous":{"title":"Kafka Implementation Guide for E-commerce Microservices","permalink":"/fullstack-dev/docs/backend/kafka-implementation"},"next":{"title":"Redis Bloom Filters & Probabilistic Data Structures","permalink":"/fullstack-dev/docs/backend/redis-bloom-filters-guide"}}');var i=s(65813),r=s(5741);const a={},c="Redis Pub/Sub Scaling Patterns",o={},l=[{value:"\ud83d\udcd6 Overview",id:"-overview",level:2},{value:"\ud83c\udfaf When to Use Redis Pub/Sub",id:"-when-to-use-redis-pubsub",level:2},{value:"\u2705 Best Use Cases",id:"-best-use-cases",level:3},{value:"\u274c When NOT to Use",id:"-when-not-to-use",level:3},{value:"\ud83c\udfd7\ufe0f Architecture Patterns",id:"\ufe0f-architecture-patterns",level:2},{value:"Pattern 1: Multi-Instance WebSocket Broadcasting",id:"pattern-1-multi-instance-websocket-broadcasting",level:3},{value:"\ud83d\ude80 Implementation Examples",id:"-implementation-examples",level:2},{value:"Example 1: Socket.IO with Redis Adapter (Production-Ready)",id:"example-1-socketio-with-redis-adapter-production-ready",level:3},{value:"Backend Implementation",id:"backend-implementation",level:4},{value:"Example 2: Native Redis Pub/Sub for Cache Invalidation",id:"example-2-native-redis-pubsub-for-cache-invalidation",level:3},{value:"Example 3: Distributed Cache Service with Pub/Sub",id:"example-3-distributed-cache-service-with-pubsub",level:3},{value:"Example 4: Real-Time Chat with Pub/Sub",id:"example-4-real-time-chat-with-pubsub",level:3},{value:"Example 5: Real-Time Analytics Dashboard",id:"example-5-real-time-analytics-dashboard",level:3},{value:"Example 6: Product Inventory Updates",id:"example-6-product-inventory-updates",level:3},{value:"\ud83d\udcca Performance Optimization",id:"-performance-optimization",level:2},{value:"1. Message Batching",id:"1-message-batching",level:3},{value:"2. Message Compression",id:"2-message-compression",level:3},{value:"3. Pattern-Based Subscriptions",id:"3-pattern-based-subscriptions",level:3},{value:"\ud83d\udd0d Monitoring &amp; Debugging",id:"-monitoring--debugging",level:2},{value:"Pub/Sub Metrics",id:"pubsub-metrics",level:3},{value:"\ud83d\udea8 Error Handling &amp; Resilience",id:"-error-handling--resilience",level:2},{value:"Retry Logic",id:"retry-logic",level:3},{value:"\ud83d\udcda Best Practices",id:"-best-practices",level:2},{value:"1. Channel Naming Convention",id:"1-channel-naming-convention",level:3},{value:"2. Message Schema Validation",id:"2-message-schema-validation",level:3},{value:"3. Graceful Shutdown",id:"3-graceful-shutdown",level:3},{value:"\ud83c\udfaf Summary",id:"-summary",level:2}];function d(e){const n={br:"br",code:"code",h1:"h1",h2:"h2",h3:"h3",h4:"h4",header:"header",li:"li",mermaid:"mermaid",p:"p",pre:"pre",strong:"strong",table:"table",tbody:"tbody",td:"td",th:"th",thead:"thead",tr:"tr",ul:"ul",...(0,r.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.header,{children:(0,i.jsx)(n.h1,{id:"redis-pubsub-scaling-patterns",children:"Redis Pub/Sub Scaling Patterns"})}),"\n",(0,i.jsx)(n.h2,{id:"-overview",children:"\ud83d\udcd6 Overview"}),"\n",(0,i.jsx)(n.p,{children:"Redis Pub/Sub is a messaging pattern that enables publishers to send messages to channels without knowledge of subscribers, and subscribers to receive messages from channels without knowledge of publishers. This decoupling makes it perfect for building scalable, distributed real-time systems."}),"\n",(0,i.jsx)(n.h2,{id:"-when-to-use-redis-pubsub",children:"\ud83c\udfaf When to Use Redis Pub/Sub"}),"\n",(0,i.jsx)(n.h3,{id:"-best-use-cases",children:"\u2705 Best Use Cases"}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Multi-instance WebSocket broadcasting"})," (Socket.IO, native WebSocket)"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Real-time notifications"})," across multiple app servers"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Live updates"})," (dashboards, analytics)"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Cache invalidation"})," in distributed systems"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Event broadcasting"})," in microservices"]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Chat applications"})," with multiple servers"]}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.strong,{children:"Live activity feeds"})}),"\n",(0,i.jsx)(n.li,{children:(0,i.jsx)(n.strong,{children:"Real-time collaboration tools"})}),"\n"]}),"\n",(0,i.jsx)(n.h3,{id:"-when-not-to-use",children:"\u274c When NOT to Use"}),"\n",(0,i.jsxs)(n.table,{children:[(0,i.jsx)(n.thead,{children:(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.th,{children:"Scenario"}),(0,i.jsx)(n.th,{children:"Better Alternative"})]})}),(0,i.jsxs)(n.tbody,{children:[(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Message persistence required"})}),(0,i.jsx)(n.td,{children:"Kafka, RabbitMQ, AWS SQS"})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Guaranteed delivery needed"})}),(0,i.jsx)(n.td,{children:"Message queues with acknowledgments"})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Complex routing logic"})}),(0,i.jsx)(n.td,{children:"RabbitMQ with exchanges"})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Message ordering critical"})}),(0,i.jsx)(n.td,{children:"Kafka with partitions"})]}),(0,i.jsxs)(n.tr,{children:[(0,i.jsx)(n.td,{children:(0,i.jsx)(n.strong,{children:"Large message payloads"})}),(0,i.jsx)(n.td,{children:"S3 + event notification"})]})]})]}),"\n",(0,i.jsx)(n.h2,{id:"\ufe0f-architecture-patterns",children:"\ud83c\udfd7\ufe0f Architecture Patterns"}),"\n",(0,i.jsx)(n.h3,{id:"pattern-1-multi-instance-websocket-broadcasting",children:"Pattern 1: Multi-Instance WebSocket Broadcasting"}),"\n",(0,i.jsx)(n.mermaid,{value:'graph TB\n    subgraph "Clients"\n        Client1[Client 1]\n        Client2[Client 2]\n        Client3[Client 3]\n        Client4[Client 4]\n    end\n\n    subgraph "Load Balancer"\n        LB[Nginx/ALB<br/>Sticky Sessions]\n    end\n\n    subgraph "WebSocket Servers"\n        WS1[WebSocket Server 1<br/>Port 3001]\n        WS2[WebSocket Server 2<br/>Port 3002]\n        WS3[WebSocket Server 3<br/>Port 3003]\n    end\n\n    subgraph "Redis Pub/Sub"\n        RedisPubSub[(Redis<br/>Pub/Sub Channels)]\n    end\n\n    subgraph "Backend Services"\n        OrderService[Order Service]\n        NotificationService[Notification Service]\n        ChatService[Chat Service]\n    end\n\n    Client1 --\x3e LB\n    Client2 --\x3e LB\n    Client3 --\x3e LB\n    Client4 --\x3e LB\n\n    LB --\x3e WS1\n    LB --\x3e WS2\n    LB --\x3e WS3\n\n    WS1 <--\x3e|Subscribe/Publish| RedisPubSub\n    WS2 <--\x3e|Subscribe/Publish| RedisPubSub\n    WS3 <--\x3e|Subscribe/Publish| RedisPubSub\n\n    OrderService --\x3e|Publish Event| RedisPubSub\n    NotificationService --\x3e|Publish Event| RedisPubSub\n    ChatService --\x3e|Publish Event| RedisPubSub\n\n    classDef client fill:#61dafb\n    classDef lb fill:#ff9800\n    classDef ws fill:#4caf50\n    classDef redis fill:#dc382d\n    classDef service fill:#9c27b0\n\n    class Client1,Client2,Client3,Client4 client\n    class LB lb\n    class WS1,WS2,WS3 ws\n    class RedisPubSub redis\n    class OrderService,NotificationService,ChatService service'}),"\n",(0,i.jsx)(n.h2,{id:"-implementation-examples",children:"\ud83d\ude80 Implementation Examples"}),"\n",(0,i.jsx)(n.h3,{id:"example-1-socketio-with-redis-adapter-production-ready",children:"Example 1: Socket.IO with Redis Adapter (Production-Ready)"}),"\n",(0,i.jsx)(n.h4,{id:"backend-implementation",children:"Backend Implementation"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// redis-adapter.config.ts\nimport { createAdapter } from '@socket.io/redis-adapter';\nimport { createClient } from 'redis';\nimport { Logger } from '@nestjs/common';\n\nconst logger = new Logger('RedisAdapter');\n\nexport async function setupRedisAdapter(io: any) {\n  const pubClient = createClient({\n    url: process.env.REDIS_URL || 'redis://localhost:6379',\n    password: process.env.REDIS_PASSWORD,\n    socket: {\n      reconnectStrategy: (retries) => {\n        if (retries > 20) {\n          logger.error('Redis connection failed after 20 retries');\n          return new Error('Redis connection failed');\n        }\n        return Math.min(retries * 50, 2000);\n      },\n    },\n  });\n\n  const subClient = pubClient.duplicate();\n\n  pubClient.on('error', (err) => logger.error('Redis Pub Client Error:', err));\n  subClient.on('error', (err) => logger.error('Redis Sub Client Error:', err));\n\n  pubClient.on('connect', () => logger.log('Redis Pub Client Connected'));\n  subClient.on('connect', () => logger.log('Redis Sub Client Connected'));\n\n  await Promise.all([pubClient.connect(), subClient.connect()]);\n\n  io.adapter(createAdapter(pubClient, subClient));\n\n  logger.log('Redis adapter configured successfully');\n\n  return { pubClient, subClient };\n}\n"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// websocket.gateway.ts\nimport {\n  WebSocketGateway,\n  WebSocketServer,\n  OnGatewayInit,\n  OnGatewayConnection,\n  OnGatewayDisconnect,\n  SubscribeMessage,\n  MessageBody,\n  ConnectedSocket,\n} from '@nestjs/websockets';\nimport { Server, Socket } from 'socket.io';\nimport { Logger } from '@nestjs/common';\nimport { setupRedisAdapter } from './redis-adapter.config';\n\n@WebSocketGateway({\n  cors: {\n    origin: process.env.CORS_ORIGIN?.split(',') || ['http://localhost:3000'],\n    credentials: true,\n  },\n})\nexport class EventsGateway implements OnGatewayInit, OnGatewayConnection, OnGatewayDisconnect {\n  @WebSocketServer()\n  server: Server;\n\n  private logger = new Logger('EventsGateway');\n\n  async afterInit(server: Server) {\n    // Setup Redis adapter for multi-instance support\n    await setupRedisAdapter(server);\n\n    this.logger.log('WebSocket Gateway initialized with Redis adapter');\n  }\n\n  handleConnection(client: Socket) {\n    const userId = client.handshake.query.userId as string;\n    this.logger.log(`Client connected: ${client.id}, User: ${userId}`);\n\n    // Join user to personal room\n    client.join(`user:${userId}`);\n\n    // Broadcast to other servers that user is online\n    this.server.emit('user:online', { userId, socketId: client.id });\n  }\n\n  handleDisconnect(client: Socket) {\n    const userId = client.handshake.query.userId as string;\n    this.logger.log(`Client disconnected: ${client.id}, User: ${userId}`);\n\n    // Broadcast to other servers that user is offline\n    this.server.emit('user:offline', { userId, socketId: client.id });\n  }\n\n  /**\n   * Broadcast message to all connected clients across all servers\n   */\n  @SubscribeMessage('broadcast')\n  async handleBroadcast(@ConnectedSocket() client: Socket, @MessageBody() data: { message: string }) {\n    // This will be broadcast to all servers via Redis\n    this.server.emit('broadcast:message', {\n      from: client.id,\n      message: data.message,\n      timestamp: new Date(),\n    });\n  }\n\n  /**\n   * Send message to specific user (may be on different server)\n   */\n  @SubscribeMessage('send:message')\n  async handleSendMessage(@ConnectedSocket() client: Socket, @MessageBody() data: { userId: string; message: string }) {\n    // Redis adapter will route this to the correct server\n    this.server.to(`user:${data.userId}`).emit('receive:message', {\n      from: client.handshake.query.userId,\n      message: data.message,\n      timestamp: new Date(),\n    });\n  }\n\n  /**\n   * Broadcast to specific room across all servers\n   */\n  broadcastToRoom(room: string, event: string, data: any) {\n    this.server.to(room).emit(event, data);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"example-2-native-redis-pubsub-for-cache-invalidation",children:"Example 2: Native Redis Pub/Sub for Cache Invalidation"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// redis-pubsub.service.ts\nimport { Injectable, Logger, OnModuleInit, OnModuleDestroy } from '@nestjs/common';\nimport { InjectRedis } from '@liaoliaots/nestjs-redis';\nimport Redis from 'ioredis';\n\nexport interface CacheInvalidationMessage {\n  key?: string;\n  pattern?: string;\n  namespace?: string;\n  operation: 'delete' | 'clear' | 'invalidate';\n  timestamp: Date;\n  source: string; // server ID that triggered invalidation\n}\n\n@Injectable()\nexport class RedisPubSubService implements OnModuleInit, OnModuleDestroy {\n  private readonly logger = new Logger('RedisPubSubService');\n  private subscriber: Redis;\n  private publisher: Redis;\n  private readonly serverId = `server-${process.pid}-${Date.now()}`;\n\n  // Channels\n  private readonly CACHE_INVALIDATION_CHANNEL = 'cache:invalidation';\n  private readonly USER_ACTIVITY_CHANNEL = 'user:activity';\n  private readonly SYSTEM_EVENTS_CHANNEL = 'system:events';\n\n  constructor(@InjectRedis() private readonly redis: Redis) {}\n\n  async onModuleInit() {\n    // Create separate connections for pub and sub\n    this.publisher = this.redis.duplicate();\n    this.subscriber = this.redis.duplicate();\n\n    await Promise.all([this.publisher.connect(), this.subscriber.connect()]);\n\n    // Subscribe to channels\n    await this.subscriber.subscribe(\n      this.CACHE_INVALIDATION_CHANNEL,\n      this.USER_ACTIVITY_CHANNEL,\n      this.SYSTEM_EVENTS_CHANNEL,\n    );\n\n    // Setup message handlers\n    this.subscriber.on('message', this.handleMessage.bind(this));\n\n    this.logger.log(`Redis Pub/Sub initialized for server: ${this.serverId}`);\n  }\n\n  async onModuleDestroy() {\n    await this.subscriber.quit();\n    await this.publisher.quit();\n  }\n\n  /**\n   * Handle incoming messages from subscribed channels\n   */\n  private async handleMessage(channel: string, message: string) {\n    try {\n      const data = JSON.parse(message);\n\n      // Ignore messages from self\n      if (data.source === this.serverId) {\n        return;\n      }\n\n      this.logger.log(`Received message on channel ${channel}:`, data);\n\n      switch (channel) {\n        case this.CACHE_INVALIDATION_CHANNEL:\n          await this.handleCacheInvalidation(data);\n          break;\n        case this.USER_ACTIVITY_CHANNEL:\n          await this.handleUserActivity(data);\n          break;\n        case this.SYSTEM_EVENTS_CHANNEL:\n          await this.handleSystemEvent(data);\n          break;\n        default:\n          this.logger.warn(`Unknown channel: ${channel}`);\n      }\n    } catch (error) {\n      this.logger.error(`Error handling message from ${channel}:`, error);\n    }\n  }\n\n  /**\n   * Publish cache invalidation message\n   */\n  async publishCacheInvalidation(message: Omit<CacheInvalidationMessage, 'timestamp' | 'source'>) {\n    const payload: CacheInvalidationMessage = {\n      ...message,\n      timestamp: new Date(),\n      source: this.serverId,\n    };\n\n    await this.publisher.publish(this.CACHE_INVALIDATION_CHANNEL, JSON.stringify(payload));\n\n    this.logger.log('Published cache invalidation:', payload);\n  }\n\n  /**\n   * Handle cache invalidation from other servers\n   */\n  private async handleCacheInvalidation(message: CacheInvalidationMessage) {\n    switch (message.operation) {\n      case 'delete':\n        if (message.key) {\n          await this.redis.del(message.key);\n          this.logger.log(`Cache key deleted: ${message.key}`);\n        }\n        break;\n\n      case 'clear':\n        if (message.namespace) {\n          const keys = await this.redis.keys(`${message.namespace}:*`);\n          if (keys.length > 0) {\n            await this.redis.del(...keys);\n            this.logger.log(`Cache namespace cleared: ${message.namespace}`);\n          }\n        }\n        break;\n\n      case 'invalidate':\n        if (message.pattern) {\n          const keys = await this.redis.keys(message.pattern);\n          if (keys.length > 0) {\n            await this.redis.del(...keys);\n            this.logger.log(`Cache pattern invalidated: ${message.pattern}`);\n          }\n        }\n        break;\n    }\n  }\n\n  /**\n   * Publish user activity event\n   */\n  async publishUserActivity(userId: string, action: string, data?: any) {\n    const payload = {\n      userId,\n      action,\n      data,\n      timestamp: new Date(),\n      source: this.serverId,\n    };\n\n    await this.publisher.publish(this.USER_ACTIVITY_CHANNEL, JSON.stringify(payload));\n  }\n\n  /**\n   * Handle user activity from other servers\n   */\n  private async handleUserActivity(data: any) {\n    this.logger.log(`User activity: ${data.userId} - ${data.action}`);\n    // Update local cache, metrics, etc.\n  }\n\n  /**\n   * Publish system event\n   */\n  async publishSystemEvent(event: string, data: any) {\n    const payload = {\n      event,\n      data,\n      timestamp: new Date(),\n      source: this.serverId,\n    };\n\n    await this.publisher.publish(this.SYSTEM_EVENTS_CHANNEL, JSON.stringify(payload));\n  }\n\n  /**\n   * Handle system events\n   */\n  private async handleSystemEvent(data: any) {\n    this.logger.log(`System event: ${data.event}`, data.data);\n    // Handle deployment, config changes, etc.\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"example-3-distributed-cache-service-with-pubsub",children:"Example 3: Distributed Cache Service with Pub/Sub"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// distributed-cache.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRedis } from '@liaoliaots/nestjs-redis';\nimport Redis from 'ioredis';\nimport { RedisPubSubService } from './redis-pubsub.service';\n\n@Injectable()\nexport class DistributedCacheService {\n  private readonly logger = new Logger('DistributedCacheService');\n  private localCache = new Map<string, { value: any; expiry: number }>();\n\n  constructor(@InjectRedis() private readonly redis: Redis, private readonly pubSubService: RedisPubSubService) {}\n\n  /**\n   * Get value with two-level caching (local + Redis)\n   */\n  async get<T>(key: string): Promise<T | null> {\n    // Check local cache first\n    const localValue = this.getFromLocalCache(key);\n    if (localValue !== null) {\n      this.logger.debug(`Cache hit (local): ${key}`);\n      return localValue;\n    }\n\n    // Check Redis\n    const redisValue = await this.redis.get(key);\n    if (redisValue) {\n      this.logger.debug(`Cache hit (Redis): ${key}`);\n      const parsed = JSON.parse(redisValue);\n\n      // Store in local cache\n      this.setLocalCache(key, parsed, 60); // 60 seconds local TTL\n\n      return parsed;\n    }\n\n    this.logger.debug(`Cache miss: ${key}`);\n    return null;\n  }\n\n  /**\n   * Set value in both caches and invalidate on other servers\n   */\n  async set(key: string, value: any, ttl = 3600): Promise<void> {\n    // Set in Redis\n    await this.redis.setex(key, ttl, JSON.stringify(value));\n\n    // Set in local cache\n    this.setLocalCache(key, value, Math.min(ttl, 60));\n\n    this.logger.debug(`Cache set: ${key}`);\n  }\n\n  /**\n   * Delete key and notify other servers\n   */\n  async delete(key: string): Promise<void> {\n    // Delete from Redis\n    await this.redis.del(key);\n\n    // Delete from local cache\n    this.localCache.delete(key);\n\n    // Notify other servers to invalidate their local cache\n    await this.pubSubService.publishCacheInvalidation({\n      key,\n      operation: 'delete',\n    });\n\n    this.logger.log(`Cache deleted: ${key}`);\n  }\n\n  /**\n   * Clear entire namespace\n   */\n  async clearNamespace(namespace: string): Promise<void> {\n    // Clear from Redis\n    const keys = await this.redis.keys(`${namespace}:*`);\n    if (keys.length > 0) {\n      await this.redis.del(...keys);\n    }\n\n    // Clear from local cache\n    for (const [key] of this.localCache) {\n      if (key.startsWith(`${namespace}:`)) {\n        this.localCache.delete(key);\n      }\n    }\n\n    // Notify other servers\n    await this.pubSubService.publishCacheInvalidation({\n      namespace,\n      operation: 'clear',\n    });\n\n    this.logger.log(`Cache namespace cleared: ${namespace}`);\n  }\n\n  /**\n   * Get or set pattern (cache-aside)\n   */\n  async getOrSet<T>(key: string, factory: () => Promise<T>, ttl = 3600): Promise<T> {\n    // Try to get from cache\n    const cached = await this.get<T>(key);\n    if (cached !== null) {\n      return cached;\n    }\n\n    // Generate value\n    const value = await factory();\n\n    // Store in cache\n    await this.set(key, value, ttl);\n\n    return value;\n  }\n\n  /**\n   * Local cache helpers\n   */\n  private getFromLocalCache(key: string): any | null {\n    const entry = this.localCache.get(key);\n    if (!entry) return null;\n\n    // Check expiry\n    if (Date.now() > entry.expiry) {\n      this.localCache.delete(key);\n      return null;\n    }\n\n    return entry.value;\n  }\n\n  private setLocalCache(key: string, value: any, ttlSeconds: number): void {\n    this.localCache.set(key, {\n      value,\n      expiry: Date.now() + ttlSeconds * 1000,\n    });\n  }\n\n  /**\n   * Cleanup expired local cache entries\n   */\n  startCleanupInterval(intervalMs = 60000) {\n    setInterval(() => {\n      const now = Date.now();\n      let cleaned = 0;\n\n      for (const [key, entry] of this.localCache) {\n        if (now > entry.expiry) {\n          this.localCache.delete(key);\n          cleaned++;\n        }\n      }\n\n      if (cleaned > 0) {\n        this.logger.debug(`Cleaned ${cleaned} expired local cache entries`);\n      }\n    }, intervalMs);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"example-4-real-time-chat-with-pubsub",children:"Example 4: Real-Time Chat with Pub/Sub"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// chat.gateway.ts\nimport { WebSocketGateway, WebSocketServer, SubscribeMessage, MessageBody, ConnectedSocket } from '@nestjs/websockets';\nimport { Server, Socket } from 'socket.io';\nimport { Logger } from '@nestjs/common';\nimport { RedisPubSubService } from './redis-pubsub.service';\n\ninterface ChatMessage {\n  chatId: string;\n  userId: string;\n  message: string;\n  timestamp: Date;\n}\n\n@WebSocketGateway({ namespace: '/chat' })\nexport class ChatGateway {\n  @WebSocketServer()\n  server: Server;\n\n  private readonly logger = new Logger('ChatGateway');\n  private readonly CHAT_MESSAGE_CHANNEL = 'chat:messages';\n\n  constructor(private readonly pubSubService: RedisPubSubService, private readonly chatService: ChatService) {\n    // Subscribe to chat messages from other servers\n    this.subscribeToMessages();\n  }\n\n  private async subscribeToMessages() {\n    const subscriber = await this.pubSubService.createSubscriber();\n\n    await subscriber.subscribe(this.CHAT_MESSAGE_CHANNEL);\n\n    subscriber.on('message', (channel, message) => {\n      if (channel === this.CHAT_MESSAGE_CHANNEL) {\n        const chatMessage: ChatMessage = JSON.parse(message);\n\n        // Emit to connected clients in this server\n        this.server.to(`chat:${chatMessage.chatId}`).emit('message', chatMessage);\n\n        this.logger.log(`Relayed message to chat ${chatMessage.chatId}`);\n      }\n    });\n  }\n\n  @SubscribeMessage('join:chat')\n  async handleJoinChat(@ConnectedSocket() client: Socket, @MessageBody() data: { chatId: string; userId: string }) {\n    await client.join(`chat:${data.chatId}`);\n    this.logger.log(`User ${data.userId} joined chat ${data.chatId}`);\n  }\n\n  @SubscribeMessage('send:message')\n  async handleSendMessage(\n    @ConnectedSocket() client: Socket,\n    @MessageBody() data: { chatId: string; userId: string; message: string },\n  ) {\n    const chatMessage: ChatMessage = {\n      chatId: data.chatId,\n      userId: data.userId,\n      message: data.message,\n      timestamp: new Date(),\n    };\n\n    // Persist message\n    await this.chatService.saveMessage(chatMessage);\n\n    // Publish to Redis for other servers\n    await this.pubSubService.publish(this.CHAT_MESSAGE_CHANNEL, JSON.stringify(chatMessage));\n\n    // Also emit locally (will be received by all servers including this one)\n    this.logger.log(`Message published to Redis: ${chatMessage.chatId}`);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"example-5-real-time-analytics-dashboard",children:"Example 5: Real-Time Analytics Dashboard"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// analytics-pubsub.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRedis } from '@liaoliaots/nestjs-redis';\nimport Redis from 'ioredis';\n\ninterface AnalyticsEvent {\n  type: 'pageview' | 'click' | 'conversion' | 'error';\n  userId?: string;\n  sessionId: string;\n  page?: string;\n  data: any;\n  timestamp: Date;\n}\n\ninterface AggregatedMetrics {\n  pageviews: number;\n  uniqueUsers: Set<string>;\n  conversions: number;\n  errors: number;\n  lastUpdated: Date;\n}\n\n@Injectable()\nexport class AnalyticsPubSubService {\n  private readonly logger = new Logger('AnalyticsPubSubService');\n  private readonly ANALYTICS_CHANNEL = 'analytics:events';\n  private subscriber: Redis;\n  private publisher: Redis;\n\n  // In-memory aggregation (per server instance)\n  private metrics: AggregatedMetrics = {\n    pageviews: 0,\n    uniqueUsers: new Set(),\n    conversions: 0,\n    errors: 0,\n    lastUpdated: new Date(),\n  };\n\n  constructor(@InjectRedis() private readonly redis: Redis) {\n    this.initializePubSub();\n    this.startMetricsFlush();\n  }\n\n  private async initializePubSub() {\n    this.publisher = this.redis.duplicate();\n    this.subscriber = this.redis.duplicate();\n\n    await Promise.all([this.publisher.connect(), this.subscriber.connect()]);\n\n    await this.subscriber.subscribe(this.ANALYTICS_CHANNEL);\n\n    this.subscriber.on('message', (channel, message) => {\n      if (channel === this.ANALYTICS_CHANNEL) {\n        this.handleAnalyticsEvent(JSON.parse(message));\n      }\n    });\n\n    this.logger.log('Analytics Pub/Sub initialized');\n  }\n\n  /**\n   * Publish analytics event to all servers\n   */\n  async trackEvent(event: Omit<AnalyticsEvent, 'timestamp'>) {\n    const analyticsEvent: AnalyticsEvent = {\n      ...event,\n      timestamp: new Date(),\n    };\n\n    // Publish to Redis for real-time aggregation across servers\n    await this.publisher.publish(this.ANALYTICS_CHANNEL, JSON.stringify(analyticsEvent));\n\n    // Also handle locally\n    this.handleAnalyticsEvent(analyticsEvent);\n  }\n\n  /**\n   * Handle incoming analytics events\n   */\n  private handleAnalyticsEvent(event: AnalyticsEvent) {\n    switch (event.type) {\n      case 'pageview':\n        this.metrics.pageviews++;\n        if (event.userId) {\n          this.metrics.uniqueUsers.add(event.userId);\n        }\n        break;\n\n      case 'conversion':\n        this.metrics.conversions++;\n        break;\n\n      case 'error':\n        this.metrics.errors++;\n        break;\n    }\n\n    this.metrics.lastUpdated = new Date();\n  }\n\n  /**\n   * Flush aggregated metrics to Redis periodically\n   */\n  private startMetricsFlush() {\n    setInterval(async () => {\n      const metricsSnapshot = {\n        pageviews: this.metrics.pageviews,\n        uniqueUsers: this.metrics.uniqueUsers.size,\n        conversions: this.metrics.conversions,\n        errors: this.metrics.errors,\n        timestamp: new Date(),\n        serverId: process.pid,\n      };\n\n      // Store in Redis sorted set for time-series data\n      const key = `analytics:metrics:${Date.now()}`;\n      await this.redis.setex(key, 3600, JSON.stringify(metricsSnapshot));\n\n      this.logger.log('Metrics flushed to Redis:', metricsSnapshot);\n\n      // Reset local counters\n      this.metrics = {\n        pageviews: 0,\n        uniqueUsers: new Set(),\n        conversions: 0,\n        errors: 0,\n        lastUpdated: new Date(),\n      };\n    }, 10000); // Flush every 10 seconds\n  }\n\n  /**\n   * Get aggregated metrics from all servers\n   */\n  async getAggregatedMetrics(startTime: Date, endTime: Date): Promise<any> {\n    const keys = await this.redis.keys('analytics:metrics:*');\n    const metrics = await Promise.all(keys.map((key) => this.redis.get(key)));\n\n    const aggregated = metrics\n      .filter(Boolean)\n      .map((m) => JSON.parse(m!))\n      .filter((m) => {\n        const timestamp = new Date(m.timestamp);\n        return timestamp >= startTime && timestamp <= endTime;\n      })\n      .reduce(\n        (acc, m) => ({\n          pageviews: acc.pageviews + m.pageviews,\n          uniqueUsers: acc.uniqueUsers + m.uniqueUsers,\n          conversions: acc.conversions + m.conversions,\n          errors: acc.errors + m.errors,\n        }),\n        { pageviews: 0, uniqueUsers: 0, conversions: 0, errors: 0 },\n      );\n\n    return aggregated;\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"example-6-product-inventory-updates",children:"Example 6: Product Inventory Updates"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// inventory-pubsub.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRedis } from '@liaoliaots/nestjs-redis';\nimport Redis from 'ioredis';\nimport { EventEmitter2 } from '@nestjs/event-emitter';\n\ninterface InventoryUpdate {\n  productId: string;\n  warehouseId: string;\n  quantity: number;\n  operation: 'reserve' | 'release' | 'adjust';\n  orderId?: string;\n  timestamp: Date;\n  source: string;\n}\n\n@Injectable()\nexport class InventoryPubSubService {\n  private readonly logger = new Logger('InventoryPubSubService');\n  private readonly INVENTORY_CHANNEL = 'inventory:updates';\n  private subscriber: Redis;\n  private publisher: Redis;\n  private readonly serverId = `inventory-${process.pid}`;\n\n  constructor(@InjectRedis() private readonly redis: Redis, private eventEmitter: EventEmitter2) {\n    this.initializePubSub();\n  }\n\n  private async initializePubSub() {\n    this.publisher = this.redis.duplicate();\n    this.subscriber = this.redis.duplicate();\n\n    await Promise.all([this.publisher.connect(), this.subscriber.connect()]);\n\n    await this.subscriber.subscribe(this.INVENTORY_CHANNEL);\n\n    this.subscriber.on('message', (channel, message) => {\n      if (channel === this.INVENTORY_CHANNEL) {\n        this.handleInventoryUpdate(JSON.parse(message));\n      }\n    });\n\n    this.logger.log('Inventory Pub/Sub initialized');\n  }\n\n  /**\n   * Publish inventory update\n   */\n  async publishInventoryUpdate(update: Omit<InventoryUpdate, 'timestamp' | 'source'>) {\n    const inventoryUpdate: InventoryUpdate = {\n      ...update,\n      timestamp: new Date(),\n      source: this.serverId,\n    };\n\n    // Update in Redis\n    const key = `inventory:${update.productId}:${update.warehouseId}`;\n\n    const multi = this.redis.multi();\n\n    switch (update.operation) {\n      case 'reserve':\n        multi.decrby(key, update.quantity);\n        break;\n      case 'release':\n        multi.incrby(key, update.quantity);\n        break;\n      case 'adjust':\n        multi.set(key, update.quantity);\n        break;\n    }\n\n    // Store update history\n    multi.lpush(`inventory:history:${update.productId}`, JSON.stringify(inventoryUpdate));\n    multi.ltrim(`inventory:history:${update.productId}`, 0, 99); // Keep last 100\n\n    await multi.exec();\n\n    // Publish to other servers\n    await this.publisher.publish(this.INVENTORY_CHANNEL, JSON.stringify(inventoryUpdate));\n\n    this.logger.log('Inventory update published:', inventoryUpdate);\n  }\n\n  /**\n   * Handle inventory updates from other servers\n   */\n  private async handleInventoryUpdate(update: InventoryUpdate) {\n    // Ignore updates from self\n    if (update.source === this.serverId) {\n      return;\n    }\n\n    this.logger.log('Received inventory update:', update);\n\n    // Emit event for local subscribers (e.g., WebSocket to notify clients)\n    this.eventEmitter.emit('inventory.updated', update);\n\n    // Check if inventory is low\n    const currentQty = await this.redis.get(`inventory:${update.productId}:${update.warehouseId}`);\n\n    if (currentQty && parseInt(currentQty) < 10) {\n      this.eventEmitter.emit('inventory.low', {\n        productId: update.productId,\n        warehouseId: update.warehouseId,\n        quantity: parseInt(currentQty),\n      });\n    }\n  }\n\n  /**\n   * Get current inventory\n   */\n  async getInventory(productId: string, warehouseId: string): Promise<number> {\n    const key = `inventory:${productId}:${warehouseId}`;\n    const qty = await this.redis.get(key);\n    return qty ? parseInt(qty) : 0;\n  }\n\n  /**\n   * Get inventory history\n   */\n  async getInventoryHistory(productId: string, limit = 20): Promise<InventoryUpdate[]> {\n    const history = await this.redis.lrange(`inventory:history:${productId}`, 0, limit - 1);\n\n    return history.map((h) => JSON.parse(h));\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"-performance-optimization",children:"\ud83d\udcca Performance Optimization"}),"\n",(0,i.jsx)(n.h3,{id:"1-message-batching",children:"1. Message Batching"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// batch-publisher.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRedis } from '@liaoliaots/nestjs-redis';\nimport Redis from 'ioredis';\n\n@Injectable()\nexport class BatchPublisherService {\n  private readonly logger = new Logger('BatchPublisherService');\n  private messageQueue: Map<string, any[]> = new Map();\n  private batchSize = 100;\n  private batchInterval = 1000; // 1 second\n\n  constructor(@InjectRedis() private readonly redis: Redis) {\n    this.startBatchProcessing();\n  }\n\n  /**\n   * Queue message for batched publishing\n   */\n  async queueMessage(channel: string, message: any) {\n    if (!this.messageQueue.has(channel)) {\n      this.messageQueue.set(channel, []);\n    }\n\n    const queue = this.messageQueue.get(channel)!;\n    queue.push(message);\n\n    // Flush if batch size reached\n    if (queue.length >= this.batchSize) {\n      await this.flushChannel(channel);\n    }\n  }\n\n  /**\n   * Process batches periodically\n   */\n  private startBatchProcessing() {\n    setInterval(async () => {\n      for (const channel of this.messageQueue.keys()) {\n        await this.flushChannel(channel);\n      }\n    }, this.batchInterval);\n  }\n\n  /**\n   * Flush messages for a channel\n   */\n  private async flushChannel(channel: string) {\n    const queue = this.messageQueue.get(channel);\n    if (!queue || queue.length === 0) return;\n\n    const batch = {\n      messages: queue.splice(0, this.batchSize),\n      timestamp: new Date(),\n      count: queue.length,\n    };\n\n    await this.redis.publish(channel, JSON.stringify(batch));\n\n    this.logger.log(`Flushed ${batch.count} messages to ${channel}`);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"2-message-compression",children:"2. Message Compression"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"import { gzip, gunzip } from 'zlib';\nimport { promisify } from 'util';\n\nconst gzipAsync = promisify(gzip);\nconst gunzipAsync = promisify(gunzip);\n\nexport class CompressedPublisher {\n  async publishCompressed(channel: string, data: any) {\n    const json = JSON.stringify(data);\n    const compressed = await gzipAsync(Buffer.from(json));\n\n    await this.redis.publish(channel, compressed.toString('base64'));\n  }\n\n  async handleCompressedMessage(message: string) {\n    const buffer = Buffer.from(message, 'base64');\n    const decompressed = await gunzipAsync(buffer);\n    return JSON.parse(decompressed.toString());\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"3-pattern-based-subscriptions",children:"3. Pattern-Based Subscriptions"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// pattern-subscriber.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRedis } from '@liaoliaots/nestjs-redis';\nimport Redis from 'ioredis';\n\n@Injectable()\nexport class PatternSubscriberService {\n  private readonly logger = new Logger('PatternSubscriberService');\n  private subscriber: Redis;\n\n  constructor(@InjectRedis() private readonly redis: Redis) {\n    this.initializePatternSubscriptions();\n  }\n\n  private async initializePatternSubscriptions() {\n    this.subscriber = this.redis.duplicate();\n    await this.subscriber.connect();\n\n    // Subscribe to patterns\n    await this.subscriber.psubscribe('user:*:notifications', 'order:*:updates', 'product:*:inventory');\n\n    this.subscriber.on('pmessage', (pattern, channel, message) => {\n      this.handlePatternMessage(pattern, channel, message);\n    });\n\n    this.logger.log('Pattern subscriptions initialized');\n  }\n\n  private handlePatternMessage(pattern: string, channel: string, message: string) {\n    this.logger.log(`Pattern: ${pattern}, Channel: ${channel}`);\n\n    // Extract ID from channel\n    const parts = channel.split(':');\n    const id = parts[1];\n\n    switch (pattern) {\n      case 'user:*:notifications':\n        this.handleUserNotification(id, JSON.parse(message));\n        break;\n      case 'order:*:updates':\n        this.handleOrderUpdate(id, JSON.parse(message));\n        break;\n      case 'product:*:inventory':\n        this.handleInventoryUpdate(id, JSON.parse(message));\n        break;\n    }\n  }\n\n  private handleUserNotification(userId: string, data: any) {\n    // Handle user notification\n  }\n\n  private handleOrderUpdate(orderId: string, data: any) {\n    // Handle order update\n  }\n\n  private handleInventoryUpdate(productId: string, data: any) {\n    // Handle inventory update\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"-monitoring--debugging",children:"\ud83d\udd0d Monitoring & Debugging"}),"\n",(0,i.jsx)(n.h3,{id:"pubsub-metrics",children:"Pub/Sub Metrics"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// pubsub-metrics.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { Counter, Histogram, Gauge } from 'prom-client';\n\n@Injectable()\nexport class PubSubMetricsService {\n  private readonly logger = new Logger('PubSubMetricsService');\n\n  private readonly messagesPublished = new Counter({\n    name: 'redis_pubsub_messages_published_total',\n    help: 'Total number of messages published',\n    labelNames: ['channel'],\n  });\n\n  private readonly messagesReceived = new Counter({\n    name: 'redis_pubsub_messages_received_total',\n    help: 'Total number of messages received',\n    labelNames: ['channel'],\n  });\n\n  private readonly publishLatency = new Histogram({\n    name: 'redis_pubsub_publish_latency_seconds',\n    help: 'Publish latency in seconds',\n    labelNames: ['channel'],\n    buckets: [0.001, 0.005, 0.01, 0.05, 0.1, 0.5, 1],\n  });\n\n  private readonly subscriberCount = new Gauge({\n    name: 'redis_pubsub_subscribers_count',\n    help: 'Number of active subscribers',\n    labelNames: ['channel'],\n  });\n\n  recordPublish(channel: string, latency: number) {\n    this.messagesPublished.inc({ channel });\n    this.publishLatency.observe({ channel }, latency);\n  }\n\n  recordReceive(channel: string) {\n    this.messagesReceived.inc({ channel });\n  }\n\n  setSubscriberCount(channel: string, count: number) {\n    this.subscriberCount.set({ channel }, count);\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"-error-handling--resilience",children:"\ud83d\udea8 Error Handling & Resilience"}),"\n",(0,i.jsx)(n.h3,{id:"retry-logic",children:"Retry Logic"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// resilient-publisher.service.ts\nimport { Injectable, Logger } from '@nestjs/common';\nimport { InjectRedis } from '@liaoliaots/nestjs-redis';\nimport Redis from 'ioredis';\n\n@Injectable()\nexport class ResilientPublisherService {\n  private readonly logger = new Logger('ResilientPublisherService');\n  private failedMessages: Map<string, any[]> = new Map();\n\n  constructor(@InjectRedis() private readonly redis: Redis) {\n    this.startRetryProcessor();\n  }\n\n  /**\n   * Publish with retry logic\n   */\n  async publishWithRetry(channel: string, message: any, maxRetries = 3): Promise<boolean> {\n    for (let attempt = 1; attempt <= maxRetries; attempt++) {\n      try {\n        await this.redis.publish(channel, JSON.stringify(message));\n        return true;\n      } catch (error) {\n        this.logger.error(`Publish failed (attempt ${attempt}/${maxRetries}):`, error);\n\n        if (attempt === maxRetries) {\n          // Store for later retry\n          this.storeFailedMessage(channel, message);\n          return false;\n        }\n\n        // Exponential backoff\n        await this.delay(Math.pow(2, attempt) * 100);\n      }\n    }\n\n    return false;\n  }\n\n  /**\n   * Store failed message for retry\n   */\n  private storeFailedMessage(channel: string, message: any) {\n    if (!this.failedMessages.has(channel)) {\n      this.failedMessages.set(channel, []);\n    }\n\n    this.failedMessages.get(channel)!.push({\n      message,\n      timestamp: new Date(),\n      retries: 0,\n    });\n\n    this.logger.warn(`Failed message stored for channel: ${channel}`);\n  }\n\n  /**\n   * Retry failed messages periodically\n   */\n  private startRetryProcessor() {\n    setInterval(async () => {\n      for (const [channel, messages] of this.failedMessages) {\n        const remaining = [];\n\n        for (const item of messages) {\n          try {\n            await this.redis.publish(channel, JSON.stringify(item.message));\n            this.logger.log(`Retry successful for channel: ${channel}`);\n          } catch (error) {\n            item.retries++;\n\n            // Keep trying for 24 hours\n            if (item.retries < 288) {\n              remaining.push(item);\n            } else {\n              this.logger.error(`Message abandoned after 24 hours: ${channel}`);\n            }\n          }\n        }\n\n        if (remaining.length > 0) {\n          this.failedMessages.set(channel, remaining);\n        } else {\n          this.failedMessages.delete(channel);\n        }\n      }\n    }, 300000); // Retry every 5 minutes\n  }\n\n  private delay(ms: number): Promise<void> {\n    return new Promise((resolve) => setTimeout(resolve, ms));\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"-best-practices",children:"\ud83d\udcda Best Practices"}),"\n",(0,i.jsx)(n.h3,{id:"1-channel-naming-convention",children:"1. Channel Naming Convention"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// Use hierarchical naming\nconst channels = {\n  // user:{userId}:{eventType}\n  userNotification: (userId: string) => `user:${userId}:notifications`,\n  userActivity: (userId: string) => `user:${userId}:activity`,\n\n  // order:{orderId}:{eventType}\n  orderUpdates: (orderId: string) => `order:${orderId}:updates`,\n  orderStatus: (orderId: string) => `order:${orderId}:status`,\n\n  // product:{productId}:{eventType}\n  productInventory: (productId: string) => `product:${productId}:inventory`,\n  productPricing: (productId: string) => `product:${productId}:pricing`,\n\n  // system:{service}:{eventType}\n  systemHealth: (service: string) => `system:${service}:health`,\n  systemMetrics: (service: string) => `system:${service}:metrics`,\n};\n"})}),"\n",(0,i.jsx)(n.h3,{id:"2-message-schema-validation",children:"2. Message Schema Validation"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"import { IsString, IsDate, validateOrReject } from 'class-validator';\n\nclass PubSubMessage {\n  @IsString()\n  type: string;\n\n  @IsDate()\n  timestamp: Date;\n\n  data: any;\n}\n\nasync function publishValidatedMessage(channel: string, message: any) {\n  const validated = Object.assign(new PubSubMessage(), message);\n  await validateOrReject(validated);\n\n  await redis.publish(channel, JSON.stringify(validated));\n}\n"})}),"\n",(0,i.jsx)(n.h3,{id:"3-graceful-shutdown",children:"3. Graceful Shutdown"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-typescript",children:"// app.service.ts\nimport { Injectable, OnModuleDestroy } from '@nestjs/common';\n\n@Injectable()\nexport class AppService implements OnModuleDestroy {\n  constructor(private pubSubService: RedisPubSubService) {}\n\n  async onModuleDestroy() {\n    // Unsubscribe from all channels\n    await this.pubSubService.unsubscribeAll();\n\n    // Close connections\n    await this.pubSubService.disconnect();\n  }\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"-summary",children:"\ud83c\udfaf Summary"}),"\n",(0,i.jsx)(n.p,{children:"Redis Pub/Sub is an excellent choice for:"}),"\n",(0,i.jsxs)(n.p,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Real-time broadcasting"})," across multiple server instances",(0,i.jsx)(n.br,{}),"\n","\u2705 ",(0,i.jsx)(n.strong,{children:"Cache invalidation"})," in distributed systems",(0,i.jsx)(n.br,{}),"\n","\u2705 ",(0,i.jsx)(n.strong,{children:"Event-driven microservices"})," communication",(0,i.jsx)(n.br,{}),"\n","\u2705 ",(0,i.jsx)(n.strong,{children:"Live notifications"})," and updates",(0,i.jsx)(n.br,{}),"\n","\u2705 ",(0,i.jsx)(n.strong,{children:"Horizontal scaling"})," of WebSocket servers"]}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Key Considerations:"})}),"\n",(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:["\u26a0\ufe0f ",(0,i.jsx)(n.strong,{children:"No message persistence"})," - messages are lost if no subscribers"]}),"\n",(0,i.jsxs)(n.li,{children:["\u26a0\ufe0f ",(0,i.jsx)(n.strong,{children:"No delivery guarantees"})," - fire-and-forget pattern"]}),"\n",(0,i.jsxs)(n.li,{children:["\u26a0\ufe0f ",(0,i.jsx)(n.strong,{children:"Memory bound"})," - all messages held in memory"]}),"\n",(0,i.jsxs)(n.li,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Very fast"})," - microsecond latency"]}),"\n",(0,i.jsxs)(n.li,{children:["\u2705 ",(0,i.jsx)(n.strong,{children:"Simple"})," - easy to implement and scale"]}),"\n"]}),"\n",(0,i.jsx)(n.p,{children:"For critical messages requiring guaranteed delivery, combine with message queues (Kafka, RabbitMQ) or use Redis Streams instead of Pub/Sub."})]})}function h(e={}){const{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);